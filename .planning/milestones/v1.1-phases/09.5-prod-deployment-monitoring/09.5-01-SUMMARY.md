---
phase: "09.5"
plan: "01"
subsystem: deployment
tags: [github, release, repo-xml, install-xml, gitignore]
dependency_graph:
  requires: []
  provides:
    - "Public GitHub repo with LMS custom repository XML"
    - "GitHub releases for plugin distribution"
  affects:
    - ".gitignore"
    - "repo.xml"
    - "Plugins/SpotOn/install.xml"
tech_stack:
  added: []
  patterns:
    - "GitHub release zip as LMS plugin distribution mechanism"
    - "repo.xml with SHA1 integrity check for LMS plugin manager"
key_files:
  created: []
  modified:
    - ".gitignore"
    - "repo.xml"
    - "Plugins/SpotOn/install.xml"
decisions:
  - "Internal files excluded via .gitignore + git rm --cached (history stays, current state clean)"
  - "Version 1.1.x series for v1.1 milestone releases"
  - "Release zip contains Plugins/SpotOn/ at top level (LMS expected layout)"
metrics:
  completed: "2026-06-04"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 3
---

# Phase 09.5 Plan 01: Repo Preparation Summary

Made GitHub repo public with clean working tree, LMS custom repository XML, and versioned releases.

## What Was Done

### Task 1: Exclude internal files, bump version
- Added 7 patterns to .gitignore (.planning/, .claude/, CLAUDE.md, REQUIREMENTS.md, etc.)
- Ran git rm --cached on all tracked internal files (stay on disk, removed from index)
- Bumped version to 1.1.0 in install.xml and repo.xml

### Task 2: Release, repo.xml, make public
- Built release zip (44MB, all 8 platform binaries)
- Created GitHub release v1.1.0 with zip asset
- Finalized repo.xml with real SHA1 and download URL
- Made repo stiefenm/spoton public
- Verified raw repo.xml accessible over HTTPS

## UAT Fixes

### Fix 1: German umlauts in strings.txt (v1.1.1)
- Replaced ASCII substitutes (Oeffne→Öffne, fuer→für, Geraet→Gerät, etc.)
- Added Redirect URI hint to setup guide (http://127.0.0.1:9000/plugins/SpotOn/callback)

### Fix 2: Crash-loop recovery race condition (v1.1.2)
- discoveryDisabledByCrashLoop reset ran in init() when no players connected yet
- Moved reset to initHelpers() where players are present
- Fixed permanent discovery-disabled state after fresh install

## Self-Check: PASSED
