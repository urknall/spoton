---
phase: 11-track-history-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/DontStopTheMusic.pm
  - Plugins/SpotOn/Connect.pm
  - t/11_track_history.t
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
must_haves:
  truths:
    - "D-02: All spoton_meta_ cache entries use 604800s TTL (7 days) instead of 3600s"
    - "D-01, D-04: Connect _fetchTrackMetadata persists metadata to spoton_meta_ cache with spotifyUri field"
    - "t/11_track_history.t exists and validates TTL, cache persistence, and spotifyUri presence"
  artifacts:
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "7-day TTL for Browse metadata cache"
      contains: "604800"
    - path: "Plugins/SpotOn/DontStopTheMusic.pm"
      provides: "7-day TTL for DSTM metadata cache"
      contains: "604800"
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "Connect metadata cache persistence with spotifyUri"
      contains: "spotifyUri"
    - path: "t/11_track_history.t"
      provides: "Unit tests for Phase 11 history metadata"
  key_links:
    - from: "Plugins/SpotOn/Connect.pm"
      to: "Slim::Utils::Cache"
      via: "cache->set with spoton_meta_ key and spotifyUri field"
      pattern: "spoton_meta_.*spotifyUri"
---

<objective>
Extend metadata cache persistence for track history: bump TTL from 3600s to 604800s (7 days) across all cache-set call sites, and add persistent cache entries for Connect-mode tracks with a `spotifyUri` field enabling future Connect-to-Browse URL translation.

Purpose: Browse-mode tracks survive in history for 7 days instead of 1 hour (HIST-01/HIST-02), and Connect-mode tracks persist their Spotify URI for replay translation (HIST-03 data foundation).
Output: Modified Plugin.pm, DontStopTheMusic.pm, Connect.pm, new t/11_track_history.t
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/11-track-history-metadata/11-CONTEXT.md
@/home/sti/spoton/.planning/phases/11-track-history-metadata/11-RESEARCH.md
@/home/sti/spoton/.planning/phases/11-track-history-metadata/11-PATTERNS.md

<interfaces>
<!-- Key code locations the executor needs -->

From Plugins/SpotOn/Plugin.pm (lines 397-407) — _trackItem cache set:
  $cache->set('spoton_meta_' . md5_hex($spotify_url), { ... }, 3600);
  ^^^^ 3600 on line 407 is the TTL to change to 604800

From Plugins/SpotOn/Plugin.pm (lines 1136-1145) — _albumTrackItem cache set:
  $cache->set('spoton_meta_' . md5_hex($spotify_url), { ... }, 3600);
  ^^^^ 3600 on line 1145 is the TTL to change to 604800

From Plugins/SpotOn/DontStopTheMusic.pm (lines 265-274) — cache set:
  $cache->set('spoton_meta_' . md5_hex($uri), { ... }, 3600);
  ^^^^ 3600 on line 274 is the TTL to change to 604800

From Plugins/SpotOn/Connect.pm (lines 847-857) — _fetchTrackMetadata pluginData write:
  $song->pluginData(info => { title, artist, album, duration, cover, url, bitrate, originalType, type });
  ^^^^ Add persistent cache write AFTER this block (before line 859)

From Plugins/SpotOn/Connect.pm (line 1) — imports:
  Does NOT import Digest::MD5 — must add: use Digest::MD5 qw(md5_hex);
  Has: use Slim::Utils::Cache; but NO $cache package var — use Slim::Utils::Cache->new()

From t/10_stream_metadata.t — test stub pattern (lines 21-248):
  write_stub() helper, LMS module stubs (Cache with ttl() method), MockClient, BEGIN constants
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create t/11_track_history.t test scaffold with TTL and cache assertions</name>
  <files>t/11_track_history.t</files>
  <read_first>
    t/10_stream_metadata.t
    Plugins/SpotOn/ProtocolHandler.pm
    Plugins/SpotOn/Connect.pm
    Plugins/SpotOn/Plugin.pm
    Plugins/SpotOn/DontStopTheMusic.pm
  </read_first>
  <action>
    Create t/11_track_history.t following the exact structural pattern from t/10_stream_metadata.t:
    1. Same preamble: strict/warnings/Test::More, File::Basename/Temp/Path/Cwd, project root resolution, stub_dir/cache_dir tempdir.
    2. Same write_stub() helper.
    3. Same LMS module stubs: Log::Log4perl::Logger, Log::Log4perl, Slim::Utils::Log, Slim::Utils::Prefs (with client() and get/set), Slim::Utils::Cache (with ttl() method per lines 120-131 of the analog), Slim::Utils::Timers, Slim::Utils::Strings, Slim::Utils::Unicode, JSON::XS::VersionOneAndTwo, Time::HiRes, Slim::Plugin::OPMLBased, Slim::Player::ProtocolHandlers, Slim::Player::TranscodingHelper, Slim::Formats::RemoteStream.
    4. Add stubs NOT in 10_stream_metadata.t but needed for ProtocolHandler.pm: Slim::Utils::Network (sub serverAddr returns '127.0.0.1'), Slim::Utils::Versions (sub compareVersions returns 0), Slim::Utils::Misc (sub crackURL returns ('127.0.0.1', 80, '/stream')), Slim::Control::Request (sub notifyFromArray increments a package counter $Slim::Control::Request::notify_count), Slim::Music::Info (sub setCurrentTitle does nothing), Slim::Player::Protocols::HTTP (sub new returns bless {}, shift).
    5. Add stub for Plugins::SpotOn::API::Client with package var $mock_track and sub getTrack that calls the callback with $mock_track.
    6. Add stub for Plugins::SpotOn::Helper (same as t/10_stream_metadata.t pattern).
    7. Same BEGIN constants block and @INC unshift.
    8. Pre-require Plugins::SpotOn::Helper.
    9. require_ok for Plugins::SpotOn::ProtocolHandler.

    Test groups (subtest blocks):

    A. "TTL grep gate: Plugin.pm" — open Plugin.pm, scan non-comment lines for pattern /},\s*3600\s*\)/, assert 0 matches. Per D-02.
    B. "TTL grep gate: DontStopTheMusic.pm" — same grep gate for DontStopTheMusic.pm, assert 0 matches of 3600 TTL.
    C. "TTL grep gate: Connect.pm cache set" — scan Connect.pm for 'spoton_meta_' cache sets, verify all use 604800.
    D. "Connect cache persistence has spotifyUri" — grep Connect.pm for 'spotifyUri' in the cache set block, assert >= 1 match.
    E. "getMetadataFor returns cached Browse metadata" — set a spoton_meta_ cache entry via Slim::Utils::Cache->new->set with a Browse URL md5, call Plugins::SpotOn::ProtocolHandler->getMetadataFor(MockClient->new, 'spotify://track:ABC123'), verify returned hash contains title, cover, type fields.
    F. "getMetadataFor returns placeholder on cache miss" — call getMetadataFor for an uncached URL, verify returned hash has cover field (not empty hashref {}). This test will initially fail until Plan 02 implements async re-fetch — mark with TODO block.
    G. "Connect URL with spotifyUri in cache returns metadata" — set a spoton_meta_ cache entry keyed on a connect URL md5 with spotifyUri => 'spotify:track:XYZ789', call getMetadataFor with the connect URL, verify result contains data. Initially a TODO until Plan 02.

    End with done_testing().
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/11_track_history.t 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - t/11_track_history.t exists and is valid Perl (perl -c t/11_track_history.t exits 0)
    - Running perl t/11_track_history.t produces TAP output with require_ok passing
    - TTL grep gate tests for Plugin.pm and DontStopTheMusic.pm initially FAIL (3600 still present — expected, fixed in Task 2)
    - Cache lookup test for Browse metadata passes (getMetadataFor returns cached data)
    - TODO-marked tests for placeholder and Connect translation are recognized as TODO by Test::More
  </acceptance_criteria>
  <done>Test file runs, grep gates detect the old 3600 TTL values, cache lookup for existing Browse entries works</done>
</task>

<task type="auto">
  <name>Task 2: TTL bump 3600 to 604800 in Plugin.pm, DontStopTheMusic.pm + Connect.pm cache persistence with spotifyUri</name>
  <files>Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/DontStopTheMusic.pm, Plugins/SpotOn/Connect.pm</files>
  <read_first>
    Plugins/SpotOn/Plugin.pm
    Plugins/SpotOn/DontStopTheMusic.pm
    Plugins/SpotOn/Connect.pm
    .planning/phases/11-track-history-metadata/11-CONTEXT.md
    .planning/phases/11-track-history-metadata/11-PATTERNS.md
  </read_first>
  <action>
    Three changes across three files. Per D-02 (unified 7-day TTL) and D-01 (Connect cache persistence).

    A. Plugin.pm — two TTL literals:
       - Line 407 (_trackItem): change `3600` to `604800` in the $cache->set call's third argument.
       - Line 1145 (_albumTrackItem): change `3600` to `604800` in the $cache->set call's third argument.
       No other changes to Plugin.pm.

    B. DontStopTheMusic.pm — one TTL literal:
       - Line 274: change `3600` to `604800` in the $cache->set call's third argument.
       No other changes to DontStopTheMusic.pm.

    C. Connect.pm — add cache persistence after pluginData write:
       - Add `use Digest::MD5 qw(md5_hex);` to the imports section (after line 9, alongside other use statements).
       - In _fetchTrackMetadata, AFTER the $song->pluginData(info => {...}) block (after line 857) and BEFORE the duration update block (line 859), insert a persistent cache write.
       - The cache write uses `Slim::Utils::Cache->new()->set(...)` (Connect.pm has no $cache package var).
       - Cache key: `'spoton_meta_' . md5_hex($song->streamUrl)` where $song->streamUrl is the connect-timestamp URL.
       - Cache value hash: title, artist, album, duration, cover, icon (same as cover), bitrate ($bitrate . 'k'), type ($type_str), and the critical `spotifyUri => $trackInfo->{uri}` field per D-01.
       - TTL: 604800 per D-02.
       - Guard: only write if $song->streamUrl is truthy.
       - Add a comment: "# D-01/D-02: Persist Connect metadata to cache for history replay"

    IMPORTANT: In Connect.pm, $trackInfo->{uri} contains the full Spotify URI like "spotify:track:ABC123". This is what gets stored as spotifyUri. Do NOT strip the prefix here — Plan 02 will parse it.

    Pitfall avoidance: The `delete $_pendingRefetch{$url}` pattern is NOT relevant here — that belongs in Plan 02 (ProtocolHandler.pm).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/05_perl_syntax.t && perl t/11_track_history.t 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - perl -c Plugins/SpotOn/Plugin.pm exits 0 (valid syntax)
    - perl -c Plugins/SpotOn/DontStopTheMusic.pm exits 0
    - perl -c Plugins/SpotOn/Connect.pm exits 0
    - grep -c '604800' Plugins/SpotOn/Plugin.pm returns 2 (two cache-set sites)
    - grep -c '604800' Plugins/SpotOn/DontStopTheMusic.pm returns 1
    - grep -c 'spotifyUri' Plugins/SpotOn/Connect.pm returns >= 1
    - grep -v '^\s*#' Plugins/SpotOn/Plugin.pm | grep -c '3600' returns 0 (no remaining old TTL in non-comment lines)
    - grep -v '^\s*#' Plugins/SpotOn/DontStopTheMusic.pm | grep -c '3600' returns 0
    - TTL grep gate tests in t/11_track_history.t now pass
    - Connect.pm spotifyUri grep gate test passes
    - prove t/ full suite green
  </acceptance_criteria>
  <done>All three files have 604800 TTL, Connect.pm persists to cache with spotifyUri, all grep gates and syntax checks pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cache key input | URL strings from LMS history fed to md5_hex for cache key construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Connect.pm cache set | accept | Cache key is md5_hex(URL) — not executable, no injection vector. URL originates from LMS internal state (song->streamUrl), not user input |
| T-11-02 | Information Disclosure | spotifyUri in cache | accept | Spotify track URIs are public identifiers (not secrets). Cache is local to LMS server process |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure Perl code changes to existing files |
</threat_model>

<verification>
1. `perl t/05_perl_syntax.t` — all modified files pass syntax check
2. `perl t/11_track_history.t` — TTL grep gates pass, cache tests pass
3. `prove t/` — full suite green, no regressions
4. `grep -c '604800' Plugins/SpotOn/Plugin.pm` returns 2
5. `grep -c '604800' Plugins/SpotOn/DontStopTheMusic.pm` returns 1
6. `grep -c 'spotifyUri' Plugins/SpotOn/Connect.pm` returns >= 1
</verification>

<success_criteria>
- All spoton_meta_ cache-set call sites across Plugin.pm, DontStopTheMusic.pm, and Connect.pm use 604800s TTL
- Connect.pm _fetchTrackMetadata writes a persistent cache entry with spotifyUri field containing the Spotify URI
- t/11_track_history.t test file validates all of the above via grep gates and cache assertions
- Full test suite (prove t/) passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/11-track-history-metadata/11-01-SUMMARY.md` when done
</output>
