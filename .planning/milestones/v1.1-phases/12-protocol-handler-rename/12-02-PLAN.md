---
phase: 12-protocol-handler-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - Plugins/SpotOn/Bin/x86_64-linux/spoton
  - Plugins/SpotOn/Bin/aarch64-linux/spoton
  - Plugins/SpotOn/Bin/armhf-linux/spoton
  - Plugins/SpotOn/Bin/arm-linux/spoton
  - Plugins/SpotOn/Bin/i386-linux/spoton
  - Plugins/SpotOn/Bin/x86_64-win64/spoton.exe
autonomous: false
requirements:
  - PROTO-01
  - PROTO-05

must_haves:
  truths:
    - "All 6 platform binaries contain the spoton:// normalization, not spotify://"
    - "Browse playback works after binary replacement (spoton://track:ID routed correctly)"
    - "D-07: Spotty and SpotOn can be enabled simultaneously in LMS without handler conflict — verified on raspi"
    - "D-03: No automatic history migration — old spotify:// entries ignored, manual DB cleanup"
    - "D-09: No Connect device name collision — Spotty has no Connect mode"
  artifacts:
    - path: "Plugins/SpotOn/Bin/x86_64-linux/spoton"
      provides: "x86_64 Linux binary with spoton:// normalization"
    - path: "Plugins/SpotOn/Bin/aarch64-linux/spoton"
      provides: "aarch64 Linux binary with spoton:// normalization"
    - path: "Plugins/SpotOn/Bin/x86_64-win64/spoton.exe"
      provides: "Windows binary with spoton:// normalization"
  key_links:
    - from: "Plugins/SpotOn/Bin/*/spoton"
      to: "librespot-spoton/src/main.rs"
      via: "compiled from source with spoton:// normalization"
      pattern: "spoton://"
---

<objective>
Rebuild all 6 platform binaries from the updated Rust source (spoton:// normalization) and verify Spotty + SpotOn coexistence on real hardware.

Purpose: Without rebuilt binaries, Browse playback is broken (binary receives spoton://track:ID but still normalizes spotify://). Coexistence test confirms the entire rename chain works end-to-end.
Output: 6 updated platform binaries in Bin/ directories, coexistence verified on raspi.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/12-protocol-handler-rename/12-CONTEXT.md
@/home/sti/spoton/.planning/phases/12-protocol-handler-rename/12-RESEARCH.md
@/home/sti/spoton/.planning/phases/12-protocol-handler-rename/12-01-SUMMARY.md

<interfaces>
<!-- CI workflow and binary layout -->

From .github/workflows/build-librespot.yml:
- Trigger: workflow_dispatch (manual) or push tag v*
- Matrix: 5 Linux targets (x86_64-musl, aarch64-musl, armv7-musleabihf, arm-musleabi, i686-musl)
- Output: Binary artifacts per target in Bin/{bin_dir}/spoton
- Windows (x86_64-pc-windows-gnu) is NOT in CI — requires local cross build

From Plugins/SpotOn/Bin/ directory layout:
- x86_64-linux/spoton
- aarch64-linux/spoton
- armhf-linux/spoton
- arm-linux/spoton
- i386-linux/spoton
- x86_64-win64/spoton.exe

From librespot-spoton/src/main.rs (after Plan 01):
- Line 667: let normalized_uri = track_uri.replace("spoton://", "spotify:");
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trigger CI binary rebuild + local Windows cross-build</name>
  <files>
    Plugins/SpotOn/Bin/x86_64-linux/spoton
    Plugins/SpotOn/Bin/aarch64-linux/spoton
    Plugins/SpotOn/Bin/armhf-linux/spoton
    Plugins/SpotOn/Bin/arm-linux/spoton
    Plugins/SpotOn/Bin/i386-linux/spoton
    Plugins/SpotOn/Bin/x86_64-win64/spoton.exe
  </files>
  <read_first>
    .github/workflows/build-librespot.yml
    librespot-spoton/src/main.rs
  </read_first>
  <action>
Two parallel build tracks:

**Track A — Linux binaries via GitHub CI:**
Use `gh workflow run build-librespot.yml` to trigger the CI workflow dispatch. This builds all 5 Linux targets (x86_64-musl, aarch64-musl, armv7-musleabihf, arm-musleabi, i686-musl). Wait for the workflow run to complete (check with `gh run list --workflow=build-librespot.yml --limit=1`). Download artifacts with `gh run download` and copy each binary to the corresponding `Plugins/SpotOn/Bin/{bin_dir}/spoton` path.

**Track B — Windows binary via local cross-build:**
Run `cd /home/sti/spoton/librespot-spoton && cross build --release --target x86_64-pc-windows-gnu --features with-libmdns,passthrough-decoder`. Copy the resulting binary from `target/x86_64-pc-windows-gnu/release/spoton.exe` to `Plugins/SpotOn/Bin/x86_64-win64/spoton.exe`.

**Verification after both tracks:**
Run `strings Plugins/SpotOn/Bin/x86_64-linux/spoton | grep -c 'spoton://'` to confirm the new normalization string is present in at least the x86_64 binary. Run `strings Plugins/SpotOn/Bin/x86_64-linux/spoton | grep -c 'spotify://'` to confirm the old normalization string is absent. Repeat for one ARM binary as a spot-check.

Mark all 6 binaries as executable (`chmod +x`).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && strings Plugins/SpotOn/Bin/x86_64-linux/spoton | grep -c 'spoton://' && echo "---" && strings Plugins/SpotOn/Bin/x86_64-linux/spoton | grep -c 'spotify://' | xargs -I{} test {} -eq 0 && echo "old-string-absent" || echo "WARNING-old-string-present"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 binary files exist under Plugins/SpotOn/Bin/ in their respective directories
    - `strings Plugins/SpotOn/Bin/x86_64-linux/spoton | grep -c 'spoton://'` returns >= 1
    - `strings Plugins/SpotOn/Bin/x86_64-linux/spoton | grep -c 'spotify://'` returns 0 (old normalization gone)
    - `file Plugins/SpotOn/Bin/x86_64-linux/spoton` reports ELF 64-bit executable
    - `file Plugins/SpotOn/Bin/x86_64-win64/spoton.exe` reports PE32+ executable
    - All Linux binaries are executable (`-x` flag set)
  </acceptance_criteria>
  <done>All 6 platform binaries rebuilt with spoton:// normalization. Old spotify:// normalization string absent from binaries.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Spotty + SpotOn coexistence on raspi</name>
  <what-built>
All source files renamed from spotify:// to spoton://, all 6 platform binaries rebuilt, cache namespace migrated to named 'spoton' with version 2. The plugin is ready for coexistence testing.
  </what-built>
  <how-to-verify>
1. Deploy the updated SpotOn plugin to raspi (192.168.13.5) — zip and install via LMS plugin manager or direct copy
2. Activate BOTH SpotOn AND Spotty in LMS Plugin Settings
3. Restart LMS
4. Verify SpotOn Browse: Navigate SpotOn menus, search for a track, play it — audio should work
5. Verify Spotty Browse: Navigate Spotty menus, search for a track, play it — audio should work (independent of SpotOn)
6. Check LMS log for errors: `grep -i 'error\|conflict\|handler' /var/log/squeezeboxserver/server.log | tail -20`
7. Verify SpotOn Connect: Open Spotify app, select the SpotOn Connect device, play a track
8. Check that LMS history shows `spoton://` URLs (not `spotify://`) for newly played SpotOn tracks
9. Confirm old `spotify://` history entries are inert (expected per D-03 — they won't play, that's by design)

**Also on dev machine:**
10. Run the manual DB cleanup per D-03 if desired: `sqlite3 /path/to/squeezebox.db "UPDATE tracks SET url = REPLACE(url, 'spotify://', 'spoton://') WHERE url LIKE 'spotify://%';"`
  </how-to-verify>
  <resume-signal>Type "approved" if coexistence works, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI artifacts -> local Bin/ | Downloaded binaries from GitHub CI placed into plugin directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | CI binary artifacts | mitigate | Verify binary strings contain spoton:// normalization; confirm ELF/PE format with `file` command |
| T-12-05 | Denial of Service | Stale binary on one platform | mitigate | Strings check on at least 2 binaries (x86_64 + one ARM); all 6 must be present |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages; cross-rs is already installed |
</threat_model>

<verification>
After Task 1:
1. All 6 binaries exist and are the correct file type (ELF for Linux, PE for Windows)
2. At least x86_64-linux binary contains "spoton://" string and does not contain "spotify://" string

After Task 2 (human):
3. SpotOn Browse playback works on raspi
4. Spotty Browse playback works simultaneously on raspi
5. No handler conflict errors in LMS log
6. Connect playback works with spoton://connect- URLs
</verification>

<success_criteria>
- All 6 platform binaries rebuilt and deployed to Bin/ directories
- Human verified: SpotOn and Spotty coexist without conflict on raspi
- Browse and Connect playback both work with the new spoton:// scheme
</success_criteria>

<output>
Create `.planning/phases/12-protocol-handler-rename/12-02-SUMMARY.md` when done
</output>
