---
phase: 01-plugin-skeleton-binary-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/ProtocolHandler.pm
  - Plugins/SpotOn/install.xml
  - Plugins/SpotOn/strings.txt
  - Plugins/SpotOn/custom-types.conf
  - Plugins/SpotOn/custom-convert.conf
autonomous: true
requirements:
  - LMS-01
  - LMS-03
  - LMS-04
  - LMS-05

must_haves:
  truths:
    - "LMS erkennt das SpotOn-Plugin beim Neustart als gueltige Extension"
    - "spotify:// URIs sind als Protokoll registriert und fuehren nicht zum Absturz"
    - "Alle UI-Strings haben EN- und DE-Uebersetzungen ohne fehlende Platzhalter"
    - "Die vier Transcoding-Pipelines son->pcm/flc/mp3/ogg sind in custom-convert.conf deklariert"
  artifacts:
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "Plugin-Registrierung, Prefs-Init, OPMLBased-Feed"
      contains: "Plugins::SpotOn::Plugin"
    - path: "Plugins/SpotOn/ProtocolHandler.pm"
      provides: "spotify:// URI-Routing, contentType son"
      contains: "Plugins::SpotOn::ProtocolHandler"
    - path: "Plugins/SpotOn/install.xml"
      provides: "Plugin-Manifest mit GUID, minVersion 8.0"
      contains: "Plugins::SpotOn::Plugin"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "i18n-Strings EN + DE"
      contains: "PLUGIN_SPOTON"
    - path: "Plugins/SpotOn/custom-types.conf"
      provides: "son Format-Deklaration"
      contains: "son"
    - path: "Plugins/SpotOn/custom-convert.conf"
      provides: "Transcoding-Pipelines son -> pcm/flc/mp3/ogg"
      contains: "son flc"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Plugins/SpotOn/ProtocolHandler.pm"
      via: "registerHandler('spotify', ...)"
      pattern: "registerHandler.*spotify.*ProtocolHandler"
    - from: "Plugins/SpotOn/install.xml"
      to: "Plugins/SpotOn/Plugin.pm"
      via: "module element"
      pattern: "Plugins::SpotOn::Plugin"
---

<objective>
Plugin-Skelett und Konfigurationsdateien erstellen: install.xml, Plugin.pm, ProtocolHandler.pm, strings.txt, custom-types.conf, custom-convert.conf.

Purpose: Das Plugin muss von LMS als gueltige Extension erkannt werden. Alle Basis-Vertraege (Manifest, Protokoll-Handler, Format-Deklaration, i18n, Transcoding-Pipelines) muessen stehen, bevor Helper.pm oder Settings.pm aufgebaut werden.

Output: 6 Dateien die zusammen ein ladbares (wenn auch noch nicht funktionales) LMS-Plugin ergeben.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/REQUIREMENTS.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/01-plugin-skeleton-binary-foundation/01-RESEARCH.md
@/home/sti/spoton/.planning/phases/01-plugin-skeleton-binary-foundation/01-PATTERNS.md

<interfaces>
<!-- Keine existierenden Interfaces — Greenfield. Dieses Plan ERSTELLT die Kern-Interfaces. -->
<!-- Downstream-Plans (01-02, 01-03) importieren aus diesen Dateien. -->

Plugin.pm exports (wird von Helper.pm und Settings.pm benoetigt):
- package Plugins::SpotOn::Plugin (OPMLBased-Basis)
- _pluginDataFor('basedir') (fuer Bin/-Pfadkonstruktion in Helper.pm)
- $prefs = preferences('plugin.spoton') (Prefs-Namespace)

ProtocolHandler.pm exports (wird von LMS Transcoding-Pipeline benoetigt):
- contentType() returns 'son'
- formatOverride() returns 'son'
- canDirectStream() returns 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: install.xml + strings.txt + custom-types.conf + custom-convert.conf</name>
  <files>
    Plugins/SpotOn/install.xml,
    Plugins/SpotOn/strings.txt,
    Plugins/SpotOn/custom-types.conf,
    Plugins/SpotOn/custom-convert.conf
  </files>
  <read_first>
    /usr/share/squeezeboxserver/Plugins/Spotty/install.xml,
    /usr/share/squeezeboxserver/Plugins/Spotty/strings.txt,
    /usr/share/squeezeboxserver/Plugins/Spotty/custom-types.conf,
    /usr/share/squeezeboxserver/Plugins/Spotty/custom-convert.conf,
    /home/sti/spoton/.planning/phases/01-plugin-skeleton-binary-foundation/01-PATTERNS.md
  </read_first>
  <action>
    Erstelle die vier Konfigurationsdateien im Verzeichnis Plugins/SpotOn/:

    install.xml (per D-04, LMS-04):
    - UUID: 7fdb8daa-2ff3-4725-8464-753478541deb (frisch generiert, NICHT Spotty's GUID)
    - module: Plugins::SpotOn::Plugin
    - name: PLUGIN_SPOTON_NAME (String-Referenz, kein Literal)
    - description: PLUGIN_SPOTON (String-Referenz)
    - category: musicservices
    - minVersion: 8.0, maxVersion: * (NIEMALS eine konkrete Version — P-35)
    - optionsURL: plugins/SpotOn/settings/basic.html
    - icon: plugins/SpotOn/html/images/icon.png
    - defaultState: enabled
    - type: 2
    - version: 0.1.0
    - creator: Marek Stiefenhofer
    - email: sti@posteo.de

    strings.txt (per LMS-03):
    - Format: Schluessel auf eigener Zeile, Sprach-Zeilen mit Tab + Sprachcode + Tab + Wert
    - Pflicht-Schluessel (alle mit EN + DE): PLUGIN_SPOTON, PLUGIN_SPOTON_NAME, PLUGIN_SPOTON_BINARY_MISSING, PLUGIN_SPOTON_BINARY_STATUS, PLUGIN_SPOTON_ACCOUNT_SETTINGS, PLUGIN_SPOTON_ACCOUNT_PLACEHOLDER, PLUGIN_SPOTON_BITRATE, PLUGIN_SPOTON_NO_BINARY, SON
    - SON Schluessel: nur EN (Formatname fuer LMS)
    - Einrueckung der Sprachzeilen: genau ein Tab-Zeichen, nicht Leerzeichen

    custom-types.conf (per D-08):
    - Eine Zeile: son son audio/x-sb-spoton audio
    - Header-Kommentar wie im Spotty-Analog

    custom-convert.conf (per D-08, D-09, LMS-05):
    - Vier Pipelines: son pcm, son flc, son mp3, son ogg
    - Binary-Platzhalter: [spoton] (NICHT [spotty])
    - son ogg Pipeline mit --passthrough Flag
    - son mp3 Pipeline mit RB:{BITRATE=--abr %B} und Pipe zu [lame]
    - son flc Pipeline mit Pipe zu [flac] und Parametern: -cs --channels=2 --sample-rate=44100 --bps=16 --endian=little --sign=signed --fast --totally-silent --ignore-chunk-sizes -
    - Alle Pipelines mit RT:{START=--start-position %s} Kommentar
    - Einrueckung der Befehlszeilen: genau ein Tab-Zeichen
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -MXML::Simple -e 'XMLin("Plugins/SpotOn/install.xml") or die' && echo "install.xml valid" && grep -c 'PLUGIN_SPOTON' Plugins/SpotOn/strings.txt | grep -q '[7-9]\|[1-9][0-9]' && echo "strings.txt has keys" && grep -q 'son.*audio/x-sb-spoton' Plugins/SpotOn/custom-types.conf && echo "types.conf valid" && grep -c 'son .* \* \*' Plugins/SpotOn/custom-convert.conf | grep -q '4' && echo "convert.conf has 4 pipelines"</automated>
  </verify>
  <acceptance_criteria>
    - install.xml parst als gueltiges XML und enthaelt id=7fdb8daa-2ff3-4725-8464-753478541deb
    - install.xml enthaelt minVersion=8.0 und maxVersion=*
    - install.xml enthaelt module=Plugins::SpotOn::Plugin
    - strings.txt enthaelt mindestens 7 PLUGIN_SPOTON_* Schluessel mit je EN und DE Zeile
    - strings.txt enthaelt SON Schluessel mit EN Zeile
    - custom-types.conf enthaelt genau eine Zeile mit "son son audio/x-sb-spoton audio"
    - custom-convert.conf enthaelt genau 4 Pipeline-Bloecke: son pcm, son flc, son mp3, son ogg
    - custom-convert.conf referenziert [spoton] (nicht [spotty]) in allen Pipelines
    - custom-convert.conf son ogg Pipeline enthaelt --passthrough
  </acceptance_criteria>
  <done>Alle vier Konfigurationsdateien existieren, parsen korrekt, und enthalten die per D-08/D-09 spezifizierten Werte.</done>
</task>

<task type="auto">
  <name>Task 2: Plugin.pm + ProtocolHandler.pm</name>
  <files>
    Plugins/SpotOn/Plugin.pm,
    Plugins/SpotOn/ProtocolHandler.pm
  </files>
  <read_first>
    /usr/share/squeezeboxserver/Plugins/Spotty/Plugin.pm,
    /usr/share/squeezeboxserver/Plugins/Spotty/ProtocolHandler.pm,
    /home/sti/spoton/.planning/phases/01-plugin-skeleton-binary-foundation/01-PATTERNS.md
  </read_first>
  <action>
    Erstelle Plugin.pm als Hauptmodul des Plugins:

    Package: Plugins::SpotOn::Plugin
    Base: Slim::Plugin::OPMLBased
    Imports: File::Basename, Slim::Utils::Log, Slim::Utils::Prefs, Slim::Utils::Strings (qw(string cstring))

    Prefs-Init in initPlugin():
    - Namespace: plugin.spoton
    - Defaults: bitrate => 320, binary => '' (LMS-10 Vorbereitung Phase 6)

    Log-Kategorie:
    - category: plugin.spoton, defaultLevel: WARN, description: PLUGIN_SPOTON

    Transcoding-Guard am Anfang von initPlugin():
    - Pruefe main::TRANSCODING; wenn false, logge Error und return

    Protocol-Handler-Registrierung:
    - Slim::Player::ProtocolHandlers->registerHandler('spotify', 'Plugins::SpotOn::ProtocolHandler')

    Helper-Init (per D-07):
    - require Plugins::SpotOn::Helper; Plugins::SpotOn::Helper->init();
    - VERSION aus _pluginDataFor('version') setzen

    Settings-Init (conditional auf main::WEBUI):
    - require Plugins::SpotOn::Settings; Plugins::SpotOn::Settings->new();

    OPMLBased-Registrierung via SUPER::initPlugin:
    - feed => \&handleFeed (lokale Sub — kein separates OPML.pm in Phase 1)
    - tag => 'spoton', menu => 'radios', is_app => 1, weight => 100

    handleFeed Sub (direkt in Plugin.pm):
    - Signatur: ($client, $callback, $args)
    - Wenn Helper->get() undef: Callback mit type => 'textarea', name => cstring($client, 'PLUGIN_SPOTON_BINARY_MISSING') und return
    - Sonst: Placeholder-Callback mit type => 'textarea', name => cstring($client, 'PLUGIN_SPOTON_NAME')

    Erstelle ProtocolHandler.pm als Stub:

    Package: Plugins::SpotOn::ProtocolHandler
    Base: Slim::Formats::RemoteStream
    Imports: Slim::Utils::Log, Slim::Utils::Prefs

    Pflicht-Methoden:
    - contentType returns 'son' (per D-08)
    - isRemote returns 1
    - canDirectStream returns 0 (KRITISCH: erzwingt Transcoding-Pipeline)
    - getFormatForURL returns 'flc' (Default-Pipeline)
    - formatOverride($class, $song) returns 'son'
    - canSeek: returns Slim::Utils::Versions->compareVersions($::VERSION, '7.9.1') >= 0
    - canTranscodeSeek: identisch wie canSeek
    - getSeekData($class, $client, $song, $newtime): returns { timeOffset => $newtime }
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -I. -c Plugins/SpotOn/Plugin.pm 2>&1 && perl -I. -c Plugins/SpotOn/ProtocolHandler.pm 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm kompiliert ohne Fehler mit perl -c (Exit 0)
    - Plugin.pm enthaelt package Plugins::SpotOn::Plugin
    - Plugin.pm erbt von Slim::Plugin::OPMLBased
    - Plugin.pm registriert spotify:// Protokoll-Handler via registerHandler
    - Plugin.pm ruft Helper->init() auf
    - Plugin.pm hat handleFeed Sub mit textarea-Fallback bei fehlendem Binary
    - Plugin.pm setzt prefs('plugin.spoton') mit bitrate => 320
    - ProtocolHandler.pm kompiliert ohne Fehler mit perl -c (Exit 0)
    - ProtocolHandler.pm contentType() gibt 'son' zurueck
    - ProtocolHandler.pm canDirectStream() gibt 0 zurueck
    - ProtocolHandler.pm erbt von Slim::Formats::RemoteStream
  </acceptance_criteria>
  <done>Plugin.pm und ProtocolHandler.pm kompilieren fehlerfrei, registrieren das spotify:// Protokoll, und implementieren den Phase-1 OPML-Feed mit Binary-Missing-Hint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install.xml -> LMS PluginManager | LMS parst die XML-Datei; boeswilliges XML koennte LMS beeinflussen |
| custom-convert.conf -> LMS Transcoder | LMS fuehrt Shell-Befehle aus convert.conf aus; Pipeline-Injection theoretisch moeglich |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | install.xml | accept | XML wird nur vom Plugin-Autor geschrieben, nicht von Nutzereingaben. LMS parst es beim Startup. Kein dynamischer Input. |
| T-01-02 | Tampering | custom-convert.conf | accept | Pipeline-Befehle sind statisch. $URL$ wird von LMS sanitized (spotify:// URI). Kein Nutzereingabe-Injection-Vektor in Phase 1. |
| T-01-03 | Information Disclosure | strings.txt | accept | Enthaelt nur UI-Texte, keine Geheimnisse. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 installiert keine Packages. Alle Perl-Module sind LMS-bundled. Binary wird in Phase 1 nicht gebaut, sondern als Placeholder angelegt. |
</threat_model>

<verification>
- perl -c kompiliert Plugin.pm und ProtocolHandler.pm fehlerfrei
- install.xml parst als gueltiges XML
- strings.txt enthaelt alle Pflicht-Schluessel mit EN + DE
- custom-convert.conf hat 4 Pipeline-Bloecke mit [spoton] Binary-Referenz
- custom-types.conf deklariert son Format
</verification>

<success_criteria>
- Alle 6 Dateien existieren unter Plugins/SpotOn/
- perl -I. -c auf beide .pm-Dateien gibt "syntax OK" zurueck
- install.xml hat die korrekte UUID und minVersion=8.0
- strings.txt hat 7+ Schluessel mit EN und DE Uebersetzungen
- custom-convert.conf hat 4 Pipelines die [spoton] referenzieren
</success_criteria>

<output>
Create `.planning/phases/01-plugin-skeleton-binary-foundation/01-01-SUMMARY.md` when done
</output>
