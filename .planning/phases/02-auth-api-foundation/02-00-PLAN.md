---
phase: 02-auth-api-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - t/07_token_manager.t
  - t/08_api_client.t
  - t/09_settings.t
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, API-01, API-02, API-03, API-04, API-06]

must_haves:
  truths:
    - "Test stub files exist and run via prove without errors (all tests skip or TODO)"
    - "Mock binary infrastructure is established for token manager tests"
    - "LMS module stubs (Cache, Prefs, Timers, Log) are defined for reuse across all test files"
  artifacts:
    - path: "t/07_token_manager.t"
      provides: "Test stubs for AUTH-01 through AUTH-05 with mock binary"
      min_lines: 40
    - path: "t/08_api_client.t"
      provides: "Test stubs for API-01 through API-04, API-06 with HTTP mocks"
      min_lines: 40
    - path: "t/09_settings.t"
      provides: "Test stubs for AUTH-04 (chmod), AUTH-05 (multi-account), AUTH-06 (switch)"
      min_lines: 30
  key_links: []
---

<objective>
Create test stub files with mock infrastructure before any production code is written in Phase 2.

Purpose: Nyquist compliance requires test infrastructure to exist before implementation starts. These stubs define the behavioral contracts that Plans 02-02, 02-03, and 02-04 will implement against. All tests should initially skip (modules not yet created) but define the expected test structure, mock objects, and assertion patterns.

Output: Three test files with LMS stubs, mock binary, and skip-guarded test blocks that will be activated as production code lands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-api-foundation/02-RESEARCH.md
@.planning/phases/02-auth-api-foundation/02-PATTERNS.md

<interfaces>
<!-- Existing test infrastructure from Phase 1 -->

From t/05_perl_syntax.t (stub pattern):
```perl
# write_stub helper:
sub write_stub {
    my ($dir, $pkg, $code) = @_;
    my @parts = split /::/, $pkg;
    my $file  = pop @parts;
    my $path  = $dir . '/' . join('/', @parts);
    make_path($path) unless -d $path;
    open(my $fh, '>', "$path/$file.pm") or die "Cannot write stub $pkg: $!";
    print $fh $code;
    close($fh);
}

# LMS module stubs already established: Slim::Utils::Log, Slim::Utils::Prefs, etc.
# Main constants: TRANSCODING, WEBUI, SCANNER, INFOLOG, ISWINDOWS, ISMAC, PERFMON
```

From t/06_binary_check.t:
```perl
# Binary discovery pattern:
my $test_dir    = dirname(abs_path($0));
my $project_dir = dirname($test_dir);
# Mock binary: shell script that prints expected output
```

Expected module contracts (defined by RESEARCH.md, implemented in Plans 02-02..04):

TokenManager.pm will provide:
- refreshToken($class, $accountId, $cb) -- spawns binary, parses token
- getToken($class, $accountId, $cb) -- cache-first, refresh on miss
- addAccount($class, $username, $password, $cb) -- spawns binary with --authenticate
- removeAccount($class, $accountId) -- cleanup
- getAccountIds($class) -- list configured accounts
- refreshAllTokens($class) -- refresh all, re-arm timer

Client.pm will provide:
- getMe($class, $accountId, $cb) -- GET /me
- reset($class) -- reset inflight counter
- RATE_LIMIT_CACHE_KEY constant

Settings.pm will provide:
- handler() with addAccount, removeAccount, switchAccount handling
- prefs() returning ($prefs, 'bitrate', 'binary', 'activeAccount')
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub files with mock infrastructure and skip-guarded tests</name>
  <files>t/07_token_manager.t, t/08_api_client.t, t/09_settings.t</files>
  <read_first>
    - t/05_perl_syntax.t (full file: stub infrastructure, write_stub helper, main constants pattern)
    - t/06_binary_check.t (mock binary pattern: shell script creation, binary discovery)
    - .planning/phases/02-auth-api-foundation/02-RESEARCH.md (Validation Architecture section: test map, Wave 0 gaps)
    - .planning/phases/02-auth-api-foundation/02-PATTERNS.md (t/07, t/08, t/09 sections: test structure, mock binary, cache stub patterns)
  </read_first>
  <action>
    Create three test stub files. Each file must:
    - Be runnable via prove immediately (exit cleanly with skip or pass)
    - Contain all LMS module stubs needed for the production module
    - Define test blocks for each requirement, guarded by skip_all or SKIP blocks until the production module exists
    - Establish the mock objects that Plan executors will use (not re-invent)

    t/07_token_manager.t:
    - Shebang, strict/warnings, Test::More, File::Basename, File::Temp, File::Path, Cwd, Digest::MD5.
    - $test_dir, $project_dir resolution (same pattern as t/06_binary_check.t).
    - tempdir for stubs (CLEANUP => 1).
    - write_stub helper (copy from t/05_perl_syntax.t pattern).
    - Write all LMS stubs needed by TokenManager.pm:
      * Slim::Utils::Log (logger sub returning object with info/warn/error subs)
      * Slim::Utils::Prefs (preferences sub returning object with get/set/init/client methods; must support preferences('server')->get('cachedir') returning the test tempdir)
      * Slim::Utils::Cache (new/get/set/remove with in-memory hash; set records TTL for inspection)
      * Slim::Utils::Timers (killTimers/setTimer that record calls in a package-level array for inspection)
      * Slim::Utils::Strings (cstring sub returning its args joined)
      * JSON::XS::VersionOneAndTwo (from_json delegating to JSON::PP::decode_json)
      * Slim::Utils::Unicode (utf8toLatin1Transliterate returns $_[1])
      * Time::HiRes (pass through to real Time::HiRes)
      * File::Spec::Functions (pass through)
      * Slim::Plugin::OPMLBased (empty base class)
      * Slim::Player::ProtocolHandlers (registerHandler sub)
      * Slim::Web::Settings (base class with handler method)
      * Slim::Web::HTTP::CSRF (protectCommand/protectName subs)
      * Slim::Formats::RemoteStream (empty)
      * Log::Log4perl (empty), Log::Log4perl::Logger (empty)
    - Write main:: constants via BEGIN block (TRANSCODING, WEBUI, SCANNER, INFOLOG => 0, ISWINDOWS, ISMAC, PERFMON).
    - Create mock binary (shell script) in tempdir that handles --get-token (prints {"accessToken":"mock_token_abc123","expiresIn":3600}), --authenticate (prints "authorized" and creates credentials.json in --cache dir), and --check (prints existing format). chmod 755 the mock binary.
    - Write Plugins::SpotOn::Helper stub that returns mock binary path from get().
    - Define test blocks, each wrapped in SKIP: { skip "TokenManager.pm not yet created", N unless -f "$project_dir/Plugins/SpotOn/API/TokenManager.pm"; ... }
    - Test blocks for:
      AUTH-01: refreshToken with mock binary produces token
      AUTH-01: getToken calls refreshToken on cache miss
      AUTH-02: Token cached with key "spoton_token_$accountId"
      AUTH-02: Token TTL is expiresIn - 300
      AUTH-03: refreshAllTokens re-arms timer (check Timers stub)
      AUTH-04: addAccount sets chmod 0700 on dir, 0600 on credentials.json
      AUTH-05: Two addAccount calls create separate subdirectories
      AUTH-05: getAccountIds returns both IDs
      AUTH-05: removeAccount removes from prefs and cache
    - done_testing at end.

    t/08_api_client.t:
    - Same stub infrastructure as t/07 (write_stub, tempdir, LMS stubs).
    - Write Slim::Networking::SimpleAsyncHTTP stub: new() returns object, get/post methods capture request args in package-level array, allow controllable success/error callback invocation.
    - Write Plugins::SpotOn::API::TokenManager stub: getToken invokes $cb->('mock_token') immediately.
    - Define SKIP-guarded test blocks for:
      API-01: getMe routes through _request which uses SimpleAsyncHTTP
      API-02: MAX_CONCURRENT_REQUESTS limits to 3 concurrent dispatches
      API-03: _cacheTTL returns correct values (0 for me/player, 60 for me/tracks, 3600 for tracks/x, 300 for playlists/x)
      API-04: 429 response sets RATE_LIMIT_CACHE_KEY with Retry-After TTL
      API-04: Retry-After > 300 is capped at 300
      API-05: No batch methods exist (grep source file)
      API-06: No LWP or SimpleSyncHTTP in API/ modules (grep)
    - done_testing at end.

    t/09_settings.t:
    - Same stub infrastructure.
    - Define test blocks for:
      AUTH-04: chmod 0700 on directory, chmod 0600 on credentials.json (filesystem test using tempdir -- this test runs immediately, no SKIP guard needed as it tests filesystem behavior, not module behavior)
      AUTH-05: Two separate account subdirectories exist (filesystem test, immediate)
      AUTH-06: Account switch updates preference (uses Prefs stub, SKIP-guarded until Settings.pm updated)
      i18n: All PLUGIN_SPOTON_ACCOUNT_* string keys exist in strings.txt (immediate, reads file)
      i18n: PLUGIN_SPOTON_ACTIVE_ACCOUNT contains %s in both DE and EN (immediate)
    - done_testing at end.

    All three files must pass prove -v immediately (skip-guarded tests skip cleanly, filesystem/grep tests pass against existing codebase).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -v t/07_token_manager.t t/08_api_client.t t/09_settings.t 2>&1 | tail -20</automated>
  </verify>
  <done>Three test stub files exist, run cleanly via prove (skipping module-dependent tests), and define the behavioral contracts for Plans 02-02, 02-03, and 02-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs -> production code | Stubs must not be loaded in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00 | Tampering | Test stubs loaded in production | mitigate | Stubs live in t/ directory tempdir; never in lib path; write_stub uses File::Temp with CLEANUP |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed; all Perl core modules |
</threat_model>

<verification>
- prove -v t/07_token_manager.t passes (with skips for unimplemented modules)
- prove -v t/08_api_client.t passes (with skips for unimplemented modules)
- prove -v t/09_settings.t passes (filesystem tests pass, module tests skip)
- Full suite: prove -v t/ passes with no failures
</verification>

<success_criteria>
Test stub files define behavioral contracts for all Phase 2 requirements. Mock binary and LMS stubs are established. All test files run cleanly via prove. Subsequent plans (02-02, 02-03, 02-04) can activate skip-guarded tests by creating the production modules.
</success_criteria>

<output>
Create `.planning/phases/02-auth-api-foundation/02-00-SUMMARY.md` when done
</output>
