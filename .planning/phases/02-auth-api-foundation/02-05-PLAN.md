---
phase: 02-auth-api-foundation
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-03, 02-04]
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - t/05_perl_syntax.t
autonomous: false
requirements: [AUTH-02, AUTH-03, AUTH-06, API-01]

must_haves:
  truths:
    - "Plugin.pm starts a 45-minute token refresh timer on initPlugin"
    - "Plugin.pm resets API Client inflight counter on startup"
    - "OPML menu shows account switcher as first real item when an account is configured"
    - "OPML menu shows rate-limit hint when Spotify API is throttled"
    - "Account switcher submenu lists all configured accounts with selection triggering menu refresh"
    - "Full test suite passes including syntax checks for new API modules"
    - "User confirms account add/token flow works with real Spotify credentials"
  artifacts:
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "Timer setup, account switcher OPML menu, rate-limit hint, API client reset"
      contains: "refreshAllTokens"
    - path: "t/05_perl_syntax.t"
      provides: "Syntax checks extended to cover new API/ modules"
      contains: "API/TokenManager.pm"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Plugins/SpotOn/API/TokenManager.pm"
      via: "refreshAllTokens timer callback"
      pattern: "TokenManager->refreshAllTokens"
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Plugins/SpotOn/API/Client.pm"
      via: "Client->reset in initPlugin, RATE_LIMIT_CACHE_KEY in handleFeed"
      pattern: "Client->reset"
---

<objective>
Wire all Phase 2 modules together in Plugin.pm: token refresh timer, API client initialization, OPML account switcher, and rate-limit hint. Then verify the full integration with a human checkpoint.

Purpose: Plugin.pm is the central entry point. It must initialize the token refresh timer (AUTH-02, AUTH-03), reset the API client (Pitfall 2 prevention), show the account switcher in the OPML menu (AUTH-06, D-05), and display the rate-limit hint when throttled (D-12). After automated verification, a human checkpoint confirms the full auth flow works with real Spotify credentials.

Output: Fully integrated Plugin.pm connecting TokenManager, Client, and Settings. Extended syntax tests covering all new modules. Human verification of the live auth flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-auth-api-foundation/02-RESEARCH.md
@.planning/phases/02-auth-api-foundation/02-PATTERNS.md
@.planning/phases/02-auth-api-foundation/02-02-SUMMARY.md
@.planning/phases/02-auth-api-foundation/02-03-SUMMARY.md
@.planning/phases/02-auth-api-foundation/02-04-SUMMARY.md

<interfaces>
From Plugins/SpotOn/Plugin.pm (current Phase 1):
```perl
sub initPlugin {
    # L26-28: transcoding check
    # L31-34: prefs init (bitrate, binary)
    # L36-37: require Helper, Helper->init()
    # L39: $VERSION
    # L41-44: protocol handler registration
    # L46-49: WEBUI -> Settings->new()
    # L51-58: SUPER::initPlugin(feed => \&handleFeed, ...)
}
sub handleFeed {
    # L64: binary check
    # L74-80: Phase 1 placeholder
}
```

From Plugins/SpotOn/API/TokenManager.pm (Plan 02):
```perl
sub refreshAllTokens { ... }  # refreshes all accounts, re-arms timer
sub getActiveAccountName {
    my ($class, $client) = @_;
    # Returns display name string or undef
}
use constant TOKEN_REFRESH_TIMER => 45 * 60;
```

From Plugins/SpotOn/API/Client.pm (Plan 03):
```perl
sub reset { $inflightCount = 0; }
use constant RATE_LIMIT_CACHE_KEY => 'spoton_rate_limit_exceeded';
```

OPML item structure (from CLAUDE.md):
```perl
{
    name => 'Item Label',
    url  => \&submenu_handler,  # for navigable items
    type => 'link',             # or 'textarea' for non-navigable
}
# nextWindow => 'refreshOrigin' triggers parent menu refresh after selection
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate timer, client reset, and OPML menu in Plugin.pm</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (current Phase 1: initPlugin, handleFeed, imports)
    - Plugins/SpotOn/API/TokenManager.pm (refreshAllTokens, getActiveAccountName, TOKEN_REFRESH_TIMER)
    - Plugins/SpotOn/API/Client.pm (reset, RATE_LIMIT_CACHE_KEY)
    - .planning/phases/02-auth-api-foundation/02-PATTERNS.md (Plugin.pm section: timer init, prefs init extension, handleFeed modification with rate-limit hint and account switcher)
    - .planning/phases/02-auth-api-foundation/02-RESEARCH.md (Pattern 5: OPML rate-limit hint)
    - .planning/phases/02-auth-api-foundation/02-CONTEXT.md (D-05: account switcher as first line; D-12: rate-limit hint; Pitfall 5: nextWindow timing)
  </read_first>
  <action>
    Modify Plugin.pm:

    1. Add imports at the top (after existing use statements):
       - use Slim::Utils::Timers;
       - use Slim::Utils::Cache;
       - use Time::HiRes;
       Add module-level cache variable: my $cache = Slim::Utils::Cache->new();

    2. Extend initPlugin (after Helper->init() and before SUPER::initPlugin):
       - Extend prefs->init to add accounts => {} and activeAccount => '' keys.
       - Add: require Plugins::SpotOn::API::TokenManager;
       - Add: require Plugins::SpotOn::API::Client;
       - Add: Plugins::SpotOn::API::Client->reset(); (Pitfall 2 prevention per RESEARCH.md)
       - Start token refresh timer: only if not SCANNER context (guard with if !main::SCANNER).
         Slim::Utils::Timers::killTimers($class, \&_refreshAllTokens);
         Slim::Utils::Timers::setTimer($class, Time::HiRes::time() + 10, \&_refreshAllTokens);
         (Initial delay of 10 seconds to let plugin finish loading, then refreshAllTokens re-arms at 45 min intervals.)

    3. Add _refreshAllTokens class method:
       sub _refreshAllTokens { Plugins::SpotOn::API::TokenManager->refreshAllTokens(); }
       (Thin wrapper needed because Slim::Utils::Timers passes the object as first arg.)

    4. Rewrite handleFeed to add rate-limit hint and account switcher:
       - Keep existing binary check at the top (lines 64-72).
       - After binary check, build @items array:
         a. Rate-limit hint (D-12): if $cache->get(Plugins::SpotOn::API::Client::RATE_LIMIT_CACHE_KEY), push a textarea item with PLUGIN_SPOTON_RATE_LIMIT_HINT string.
         b. Account switcher (D-05, AUTH-06): get active account name via TokenManager->getActiveAccountName($client). If present, push a link item with name = cstring($client, 'PLUGIN_SPOTON_ACTIVE_ACCOUNT', $activeName), url => \&_accountSwitcherFeed, type => 'link'.
         c. If no account configured: push a textarea item suggesting account setup in settings.
       - Invoke $callback with the items array.

    5. Add _accountSwitcherFeed handler:
       sub _accountSwitcherFeed {
           my ($client, $callback, $args) = @_;
           my $accounts = $prefs->get('accounts') || {};
           my $activeId = $prefs->client($client)->get('activeAccount') || $prefs->get('activeAccount');
           my @items;
           for my $id (sort keys %$accounts) {
               my $name = $accounts->{$id}{displayName} || $accounts->{$id}{username} || $id;
               my $isActive = ($id eq ($activeId || ''));
               push @items, {
                   name => $name . ($isActive ? ' *' : ''),
                   url  => \&_switchAccount,
                   passthrough => [{ accountId => $id }],
                   type => 'link',
                   nextWindow => 'refreshOrigin',
               };
           }
           $callback->({ items => \@items });
       }

    6. Add _switchAccount handler:
       sub _switchAccount {
           my ($client, $callback, $args, $passthrough) = @_;
           my $accountId = $passthrough->[0]{accountId} if $passthrough;
           if ($accountId && $client) {
               $prefs->client($client)->set('activeAccount', $accountId);
               # Also update global default if no global is set
               $prefs->set('activeAccount', $accountId) unless $prefs->get('activeAccount');
           }
           $callback->({ items => [{ name => 'OK', type => 'textarea', showBriefly => 1 }], nextWindow => 'refreshOrigin' });
       }

    Preserve the existing SUPER::initPlugin call with all its parameters unchanged. Preserve the $VERSION assignment. Preserve the ProtocolHandler registration.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c 'refreshAllTokens\|Client->reset\|RATE_LIMIT_CACHE_KEY\|_accountSwitcherFeed' Plugins/SpotOn/Plugin.pm</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm imports Slim::Utils::Timers, Slim::Utils::Cache, Time::HiRes
    - initPlugin calls Client->reset()
    - initPlugin requires TokenManager and Client modules
    - initPlugin starts a timer with _refreshAllTokens callback (guarded by !main::SCANNER)
    - prefs->init includes accounts and activeAccount keys
    - handleFeed checks RATE_LIMIT_CACHE_KEY and shows hint when set
    - handleFeed shows account switcher with active account name when account is configured
    - _accountSwitcherFeed lists all accounts with nextWindow => 'refreshOrigin'
    - _switchAccount updates per-client activeAccount preference
    - Existing Phase 1 code (binary check, ProtocolHandler, SUPER::initPlugin) unchanged
  </acceptance_criteria>
  <done>Plugin.pm fully integrated with TokenManager, Client, and Settings; timer, account switcher, and rate-limit hint operational</done>
</task>

<task type="auto">
  <name>Task 2: Extend t/05_perl_syntax.t for new API modules and run full suite</name>
  <files>t/05_perl_syntax.t</files>
  <read_first>
    - t/05_perl_syntax.t (current: @pm_files list, stub infrastructure)
    - Plugins/SpotOn/API/TokenManager.pm (verify package name)
    - Plugins/SpotOn/API/Client.pm (verify package name)
  </read_first>
  <action>
    Extend t/05_perl_syntax.t:

    1. Add the new module paths to the @pm_files array:
       - "$project_dir/Plugins/SpotOn/API/TokenManager.pm"
       - "$project_dir/Plugins/SpotOn/API/Client.pm"

    2. Add any additional stubs needed for the new modules to compile under perl -c:
       - Slim::Utils::Timers stub (if not already present): package with killTimers and setTimer subs.
       - Time::HiRes stub (if needed): pass through to real Time::HiRes or provide time() sub.
       - Slim::Utils::Cache stub: new, get, set, remove methods.
       - Slim::Utils::Unicode stub: utf8toLatin1Transliterate returns $_[1].
       - Digest::MD5 stub: pass through to real Digest::MD5 (it is Perl core).
       - File::Path stub: pass through to real File::Path (Perl core).
       - File::Copy stub: pass through to real File::Copy (Perl core).
       Check which of these are already stubbed. Only add what is missing.

    3. Run the full test suite: prove -v t/
       Verify all test files pass including the new t/07_token_manager.t, t/08_api_client.t, t/09_settings.t.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove -v t/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - t/05_perl_syntax.t @pm_files array includes API/TokenManager.pm and API/Client.pm
    - All required stubs are present for perl -c to pass on all 6 module files
    - prove -v t/ reports 0 failures across all test files (01-09)
    - Each new test file (07, 08, 09) reports "All tests successful" or equivalent
  </acceptance_criteria>
  <done>Full test suite passes for all Phase 1 + Phase 2 modules; syntax validation covers all .pm files</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live auth flow with real Spotify account</name>
  <what-built>
    Complete Phase 2 authentication and API foundation:
    - Rust binary with --authenticate and --get-token (Plan 01)
    - TokenManager.pm with token lifecycle management (Plan 02)
    - API/Client.pm with rate limiting, caching, getMe (Plan 03)
    - Settings.pm with account CRUD and dynamic UI (Plan 04)
    - Plugin.pm integration with timer, account switcher, rate-limit hint (Plan 05)
  </what-built>
  <how-to-verify>
    1. Install plugin into LMS:
       ```bash
       sudo ln -sf /home/sti/spoton/Plugins/SpotOn /usr/share/squeezeboxserver/Plugins/SpotOn
       sudo systemctl restart lyrionmusicserver
       ```

    2. Check server.log for clean startup:
       ```bash
       tail -50 /var/log/squeezeboxserver/server.log | grep -i spoton
       ```
       Expected: No errors. Should see plugin load, binary detection, timer start.

    3. Open LMS Settings -> SpotOn:
       - Verify binary status shows v1.0.0 (green)
       - Verify "Account Settings" section shows "No account configured"
       - Verify username and password input fields are present

    4. Add a Spotify account:
       - Enter your Spotify username and password
       - Click "Add"
       - Expected: Account appears in the list with your username

    5. If login5 authentication fails (Spotify disabled password auth):
       - Report the error message
       - This triggers the fallback discussion (OAuth redirect)
       - Type "login5-failed" to signal this outcome

    6. If authentication succeeds:
       - Check server.log for token refresh:
         ```bash
         grep -i "token cached\|token refresh" /var/log/squeezeboxserver/server.log
         ```
       - Navigate to SpotOn in the LMS menu
       - Expected: "Active: [your display name] [switch]" appears as first item
       - Verify no error messages in the menu

    7. Check credential permissions:
       ```bash
       ls -la /var/lib/squeezeboxserver/cache/spoton/*/credentials.json
       ```
       Expected: -rw------- (0600) on credentials.json, drwx------ (0700) on parent dir

    8. If everything works: type "approved"
    9. If issues found: describe them
  </how-to-verify>
  <resume-signal>Type "approved", "login5-failed", or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin -> TokenManager -> binary | Full auth chain traverses multiple trust boundaries |
| OPML menu -> client prefs | Account switch modifies per-player state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Elevation of Privilege | Account switch without auth | accept | LMS Settings access already requires admin; OPML menu is per-player preference, not privilege escalation |
| T-02-15 | Denial of Service | Timer leak on plugin reload | mitigate | killTimers called before setTimer in initPlugin; _refreshAllTokens re-arms with killTimers first |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed |
</threat_model>

<verification>
- prove -v t/ passes all tests (01-09)
- Plugin loads in LMS without errors
- Account add/switch/remove works in Settings UI
- Token refresh timer visible in debug log
- Credential file permissions are 0600/0700
</verification>

<success_criteria>
All Phase 2 modules are integrated and working end-to-end. A real Spotify account can be added, tokens are cached and refreshed, the account switcher appears in the OPML menu, and credential permissions are enforced.
</success_criteria>

<output>
Create `.planning/phases/02-auth-api-foundation/02-05-SUMMARY.md` when done
</output>
