---
phase: 02.1-oauth-pkce-browser-auth
plan: "03"
subsystem: auth, ui
tags: [lms, perl, oauth-pkce, settings-page, template-toolkit, i18n]

requires:
  - phase: 02.1-01
    provides: TokenManager.pm with startOAuthFlow, _buildRedirectUri, exchangeCode
  - phase: 02.1-02
    provides: Settings/Callback.pm with init() route registration

provides:
  - Settings.pm with startOAuth handler (HTTP 302 redirect to Spotify, IC-02)
  - Plugin.pm with clientId pref default and Callback.pm route initialization
  - basic.html two-step Setup Wizard UI (UI-SPEC Screen 1 + Screen 2)
  - strings.txt with 17 new i18n keys in EN+DE, 4 obsolete keys removed

affects:
  - human-verify checkpoint (Task 3) — visual inspection of Settings page required

tech-stack:
  added: []
  patterns:
    - "HTTP 302 server-side redirect from Perl handler via $response->code(302) + Location header"
    - "Prototype.js observe('input') for client-side button enable/disable gating"
    - "WRAPPER setting title= pattern for LMS TT2 settings blocks"
    - "Setup Wizard hidden via [% IF NOT accounts.keys.size %] per IC-04"

key-files:
  created: []
  modified:
    - Plugins/SpotOn/Settings.pm
    - Plugins/SpotOn/Plugin.pm
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
    - Plugins/SpotOn/strings.txt
    - t/02_strings.t
    - t/09_settings.t

key-decisions:
  - "HTTP 302 server-side redirect for OAuth initiation per IC-02 (not JS window.location)"
  - "startOAuth button name reused for both initial connect and Add Another Account"
  - "Setup Wizard block hidden when accounts exist (IC-04) not when clientId set — matching UI-SPEC"
  - "Test stubs updated to reflect new Settings.pm API (_buildRedirectUri, startOAuthFlow)"

patterns-established:
  - "prefs() method in LMS Settings subclass controls which prefs auto-save from form POST"
  - "Template vars passed via $paramRef before SUPER::handler call"

requirements-completed:
  - AUTH-06
  - D-04
  - D-05
  - D-07
  - D-09
  - D-12

duration: 5min
completed: 2026-05-27
---

# Phase 02.1 Plan 03: Settings UI and i18n Summary

**Setup Wizard UI with PKCE OAuth initiation wired into Settings.pm via HTTP 302 redirect, username/password fields removed, 17 new i18n strings added in EN and DE**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-27T19:31:05Z
- **Completed:** 2026-05-27T19:35:30Z
- **Tasks:** 2 of 3 (Task 3 is human-verify checkpoint)
- **Files modified:** 6

## Accomplishments

- Settings.pm: startOAuth action handler issues HTTP 302 redirect to Spotify auth URL (IC-02); addAccount block with username/password completely removed
- Plugin.pm: clientId pref added to defaults; Settings::Callback route initialized in WEBUI block (D-08); username fallback removed from _accountSwitcherFeed (D-02)
- basic.html: two-step Setup Wizard block per UI-SPEC Screen 1; disabled connect button with Prototype.js observer (IC-01); Setup Wizard hidden when accounts exist (IC-04); confirm() dialog on Remove Account (UI-SPEC Destructive Action)
- strings.txt: 17 new i18n keys added (SETUP_WIZARD through ACCOUNT_REMOVE_CONFIRM) in both EN and DE; 4 obsolete keys removed (ACCOUNT_USERNAME, ACCOUNT_PASSWORD, ACCOUNT_ADD, ACCOUNT_ADD_BTN)
- All 160 tests pass

## Task Commits

1. **Task 1: Update Settings.pm and Plugin.pm** - `c360382` (feat)
2. **Task 2: Update basic.html and strings.txt** - `2a311af` (feat)
3. **Task 3: Visual verification** - awaiting human-verify checkpoint

## Files Created/Modified

- `Plugins/SpotOn/Settings.pm` - Added startOAuth handler with HTTP 302; clientId in prefs(); redirectUri/clientId/oauthError template vars; removed addAccount block
- `Plugins/SpotOn/Plugin.pm` - clientId in prefs->init; Settings::Callback->init() in WEBUI block; removed username fallback
- `Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` - Complete rewrite of account section: Setup Wizard (Step 1 + Step 2), account table without username column, Add Another Account button
- `Plugins/SpotOn/strings.txt` - 17 new keys + 4 removed obsolete keys
- `t/02_strings.t` - Updated bilingual_keys list; added removed_keys verification
- `t/09_settings.t` - Updated required_keys; added _buildRedirectUri and startOAuthFlow to TokenManager stub

## Decisions Made

- Used the same `startOAuth` button name for both "Connect with Spotify" and "Add Another Account" — both trigger the same OAuth flow with whatever clientId is currently stored
- Setup Wizard hidden when `accounts.keys.size > 0` (not when `clientId` is set) — matches UI-SPEC IC-04 which says "when at least one account is configured"
- Prototype.js observer wrapped in `document.observe("dom:loaded", ...)` for safe DOM access

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated t/02_strings.t to reflect removed and added strings**
- **Found during:** Task 2 (strings.txt update)
- **Issue:** Test checked for ACCOUNT_USERNAME, ACCOUNT_PASSWORD, ACCOUNT_ADD, ACCOUNT_ADD_BTN which were intentionally removed; did not check for any of the 17 new keys
- **Fix:** Replaced obsolete keys in bilingual_keys list with new keys; added @removed_keys array with negative assertions
- **Files modified:** t/02_strings.t
- **Verification:** prove -v t/02_strings.t passes (67 tests)
- **Committed in:** 2a311af (Task 2 commit)

**2. [Rule 1 - Bug] Updated t/09_settings.t TokenManager stub and required_keys list**
- **Found during:** Task 2 verification
- **Issue:** t/09_settings.t crashed with "Can't locate object method _buildRedirectUri" because Settings.pm now calls TokenManager->_buildRedirectUri() but stub lacked that method; required_keys list still referenced removed strings
- **Fix:** Added _buildRedirectUri and startOAuthFlow to stub; updated required_keys to include new PKCE strings, removed old username/password/add strings
- **Files modified:** t/09_settings.t
- **Verification:** prove -v t/09_settings.t passes (18 tests); full prove -v t/ passes (160 tests)
- **Committed in:** 2a311af (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (both Rule 1 - test breakage after intentional code changes)
**Impact on plan:** Both fixes necessary to maintain green test suite. No scope creep — tests reflect the new intended behavior.

## Issues Encountered

None beyond the test deviations documented above.

## Known Stubs

None — all template variables (clientId, redirectUri, accounts, activeAccount, oauthError) are wired to real prefs data in Settings.pm.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes beyond those in the plan's threat_model (T-02.1-11 through T-02.1-15). startOAuth action is protected by protectURI CSRF guard (T-02.1-12).

## Next Phase Readiness

- Task 3 (human-verify) awaits user visual inspection of the Settings page
- After approval: Phase 02.1 Plan 04 (token refresh + API client wiring) can proceed
- Callback.pm route is initialized; Settings page redirect works end-to-end pending verification

---
*Phase: 02.1-oauth-pkce-browser-auth*
*Completed: 2026-05-27*
