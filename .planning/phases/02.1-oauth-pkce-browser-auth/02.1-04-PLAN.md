---
phase: 02.1-oauth-pkce-browser-auth
plan: 04
type: execute
wave: 1
depends_on: ["02.1-02"]
files_modified:
  - Plugins/SpotOn/Settings/Callback.pm
  - t/10_pkce_callback.t
  - .planning/REQUIREMENTS.md
autonomous: true
gap_closure: true
requirements: [AUTH-01, AUTH-05, AUTH-06]

must_haves:
  truths:
    - "Success callback page shows 'Erfolgreich verbunden!' AND the user's display name"
    - ".planning/REQUIREMENTS.md AUTH-01 references OAuth 2.0 PKCE (not Keymaster/login5)"
    - ".planning/REQUIREMENTS.md Out-of-Scope table does NOT list PKCE/OAuth or Spotify Developer App"
    - ".planning/REQUIREMENTS.md traceability rows for AUTH-01 through AUTH-06 reference Phase 02.1"
  artifacts:
    - path: "Plugins/SpotOn/Settings/Callback.pm"
      provides: "Display name in success callback page"
      contains: "displayName"
    - path: "t/10_pkce_callback.t"
      provides: "Test asserting display name appears in success HTML"
      contains: "displayName"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability and scope reflecting PKCE auth"
      contains: "OAuth 2.0 PKCE"
  key_links:
    - from: "Plugins/SpotOn/Settings/Callback.pm"
      to: "Slim::Utils::Prefs accounts hash"
      via: "$prefs->get('accounts')->{$accountId}{displayName}"
      pattern: "accounts.*displayName"
---

<objective>
Close two verification gaps from Phase 02.1 verification:

1. **SC-2 gap (functional):** The success callback page shows "Erfolgreich verbunden!" but omits the user's display name, which the ROADMAP success criterion requires.

2. **Documentation gap:** `.planning/REQUIREMENTS.md` still references Keymaster/login5 and lists PKCE as out-of-scope, contradicting the actual implementation.

Purpose: Achieve 5/5 must-have verification score for Phase 02.1.
Output: Fixed Callback.pm with display name, updated test, corrected REQUIREMENTS.md.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/02.1-oauth-pkce-browser-auth/02.1-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add display name to callback success page and update test</name>
  <files>Plugins/SpotOn/Settings/Callback.pm, t/10_pkce_callback.t</files>

  <read_first>
    Plugins/SpotOn/Settings/Callback.pm — current _renderResult signature (L104-110), success HTML (L120-138), handler success path (L81-93)
    Plugins/SpotOn/API/TokenManager.pm — _storeTokens (L370-399) shows accounts hash structure: {displayName, refreshToken}; exchangeCode callback (L195-211) calls $cb->($accountId)
    t/10_pkce_callback.t — Test 4 at L440-474 validates success HTML with `like($$html_ref, qr/Erfolgreich|verbunden/i)`; TokenManager stub at L293-305 returns 'testacct' as accountId
  </read_first>

  <action>
    Callback.pm changes:

    1. Add `use Slim::Utils::Prefs;` and `my $prefs = preferences('plugin.spoton');` at the top alongside the existing use/my declarations (after L17, before L20).

    2. In the `handler` sub, success branch (L85-88): after confirming `defined $accountId`, look up the display name from prefs before calling _renderResult. Retrieve display name via `my $accounts = $prefs->get('accounts') || {}; my $displayName = $accounts->{$accountId}{displayName};`. Pass it as a new parameter to _renderResult — insert `$displayName` between the `$response` and `$success` arguments.

    3. Update `_renderResult` signature (L110) from `($client, $paramRef, $callback, $httpClient, $response, $success, $errorMsg)` to `($client, $paramRef, $callback, $httpClient, $response, $displayName, $success, $errorMsg)`.

    4. In the success HTML block inside _renderResult (L121-138): after the `<h2>` line, add a paragraph showing the display name: `<p>Verbunden als: <strong>` followed by the HTML-escaped displayName and `</strong></p>`. Use `_html_escape($displayName)` if displayName is defined; omit the paragraph entirely if displayName is undef/empty.

    5. Update the two error-path calls to _renderResult in handler (L53 and L64) to pass `undef` as the new $displayName argument (between $response and the success flag 0).

    6. Update the error-path call in the exchangeCode failure branch (L90-91) similarly — pass `undef` as $displayName.

    t/10_pkce_callback.t changes:

    7. Update the TokenManager stub (L293-305) so that `exchangeCode` also seeds the accounts hash with a displayName before calling $cb. After the `$cb->('testacct', undef)` line, add: `Slim::Utils::Prefs->preferences('plugin.spoton')->set('accounts', { testacct => { displayName => 'Test User' } })`. Actually, do this BEFORE calling $cb so the prefs are set when handler reads them. The stub's success branch should first set the accounts pref, then call `$cb->('testacct', undef)`.

    8. In Test 4 (L440-474), update the success HTML assertion to also check for the display name. Add a new assertion: `like($$html_ref, qr/Verbunden als:.*Test User/, 'SC-2: success page shows display name')`.

    9. Add a new Test 7 after Test 6 (after L525): test that when displayName is empty/undef in accounts hash, the success page still renders without the "Verbunden als" paragraph. Seed PKCE cache, set accounts hash with `{ testacct => { displayName => '' } }`, call handler, assert `$$html_ref` matches `Erfolgreich` but does NOT match `Verbunden als`.
  </action>

  <verify>
    <automated>cd /home/sti/spoton && prove -v t/10_pkce_callback.t</automated>
  </verify>

  <acceptance_criteria>
    - Callback.pm _renderResult signature has 8 parameters: $client, $paramRef, $callback, $httpClient, $response, $displayName, $success, $errorMsg
    - Success HTML contains `Verbunden als:` and `<strong>` tag with the escaped displayName when displayName is defined and non-empty
    - Success HTML does NOT contain `Verbunden als:` when displayName is undef or empty string
    - Error HTML path is unchanged (still shows "Verbindung fehlgeschlagen" with error message)
    - `prove -v t/10_pkce_callback.t` passes all tests including the new display name assertion
    - grep -c 'displayName' Plugins/SpotOn/Settings/Callback.pm returns >= 3
    - grep -c 'Verbunden als' Plugins/SpotOn/Settings/Callback.pm returns 1
  </acceptance_criteria>

  <done>Success callback page renders display name when available; test confirms SC-2 truth "shows Erfolgreich verbunden! with the user's display name"</done>
</task>

<task type="auto">
  <name>Task 2: Update .planning/REQUIREMENTS.md to reflect PKCE auth</name>
  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    .planning/REQUIREMENTS.md — current AUTH-01 at L10, Out-of-Scope table at L109-119, Traceability table at L123-186
    REQUIREMENTS.md (root) — reference for the correct AUTH-01 wording (already updated to OAuth 2.0 PKCE)
  </read_first>

  <action>
    1. Update AUTH-01 description (L10) from "Plugin obtains Spotify access token via Keymaster/login5 through librespot binary" to "Plugin obtains Spotify access token via OAuth 2.0 Authorization Code + PKCE browser flow through per-user Spotify Developer App".

    2. Update AUTH-04 description (L14) to reflect that credential file permissions are no longer relevant for PKCE (tokens stored in LMS Prefs, not filesystem). Change to "Credential storage uses LMS Prefs with restricted plugin namespace (PKCE replaced filesystem credential files)". Or mark as REMOVED/N-A if root REQUIREMENTS.md did so — check root file first.

    3. Remove row "PKCE/OAuth browser flow | Keymaster-only decision..." from Out-of-Scope table (L114).

    4. Remove row "Spotify Developer App registration | Consequence of Keymaster-only auth" from Out-of-Scope table (L115).

    5. Update Traceability table rows for AUTH-01 through AUTH-06 (L125-130): change "Phase 2" to "Phase 02.1" in the Phase column, and change "Pending" to "Complete" in the Status column.

    6. Update the "Last updated" line at the bottom to: "Last updated: 2026-05-27 after Phase 02.1 gap closure — AUTH requirements reflect OAuth PKCE".
  </action>

  <verify>
    <automated>grep -c 'Keymaster' /home/sti/spoton/.planning/REQUIREMENTS.md | grep -q '^0$' && grep -c 'OAuth 2.0' /home/sti/spoton/.planning/REQUIREMENTS.md | grep -q '^[1-9]' && grep -v '^|' /home/sti/spoton/.planning/REQUIREMENTS.md | grep -c 'PKCE/OAuth browser flow' | grep -q '^0$' && echo "PASS" || echo "FAIL"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c 'Keymaster' .planning/REQUIREMENTS.md returns 0
    - grep -c 'login5' .planning/REQUIREMENTS.md returns 0
    - grep 'AUTH-01' .planning/REQUIREMENTS.md contains "OAuth 2.0" and "PKCE"
    - grep 'PKCE/OAuth browser flow' .planning/REQUIREMENTS.md returns no matches (removed from Out-of-Scope)
    - grep 'Spotify Developer App registration' .planning/REQUIREMENTS.md returns no matches in the Out-of-Scope table
    - grep 'AUTH-01.*Phase 02.1.*Complete' .planning/REQUIREMENTS.md returns a match in the Traceability table
    - grep 'AUTH-06.*Phase 02.1.*Complete' .planning/REQUIREMENTS.md returns a match in the Traceability table
  </acceptance_criteria>

  <done>.planning/REQUIREMENTS.md accurately reflects OAuth 2.0 PKCE as auth mechanism; no Keymaster/login5 references remain; PKCE and Developer App removed from Out-of-Scope; traceability rows point to Phase 02.1 with Complete status</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prefs → HTML | displayName from Spotify profile rendered in callback page — XSS vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-GC-01 | Injection | Callback.pm _renderResult | mitigate | displayName passed through existing _html_escape() before embedding in HTML |
| T-02.1-GC-02 | Information Disclosure | .planning/REQUIREMENTS.md | accept | Planning artifact, not deployed code; no sensitive data |
</threat_model>

<verification>
1. `prove -v t/10_pkce_callback.t` — all tests pass including new display name assertions
2. `prove -v t/` — full test suite still passes (no regressions)
3. `grep -c 'Keymaster\|login5' .planning/REQUIREMENTS.md` returns 0
4. `grep 'OAuth 2.0' .planning/REQUIREMENTS.md` returns AUTH-01 line
5. Success HTML in Callback.pm contains "Verbunden als:" with escaped displayName
</verification>

<success_criteria>
- SC-2 from ROADMAP verified: callback page shows "Erfolgreich verbunden!" with user's display name
- .planning/REQUIREMENTS.md is consistent with implemented PKCE auth (no Keymaster references, no PKCE in Out-of-Scope, traceability points to Phase 02.1)
- Full test suite passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/02.1-oauth-pkce-browser-auth/02.1-04-SUMMARY.md` when done
</output>
