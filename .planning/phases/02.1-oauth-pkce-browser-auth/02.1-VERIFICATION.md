---
phase: 02.1-oauth-pkce-browser-auth
verified: 2026-05-27T20:58:07Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 4/5
  gaps_closed:
    - "After authenticating on Spotify, the browser returns to LMS and shows 'Erfolgreich verbunden!' with the user's display name — Callback.pm now reads displayName from prefs accounts hash and renders it in success HTML"
    - ".planning/REQUIREMENTS.md traceability document updated to reflect PKCE as auth mechanism — AUTH-01 now references OAuth 2.0 PKCE; PKCE/OAuth browser flow and Spotify Developer App registration removed from Out-of-Scope table; AUTH-01 through AUTH-06 traceability rows reference Phase 02.1 with Complete status"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Visual inspection of Settings page Setup Wizard"
    expected: "Setup Wizard section visible with Step 1 (Client-ID input, redirect URI in code element) and Step 2 (disabled 'Mit Spotify verbinden' button). Typing in Client-ID field enables the button. No username or password fields visible. Binary Status and Bitrate sections unchanged at top."
    why_human: "Requires browser rendering of TT2 template with LMS running; template conditionals and Prototype.js observer behavior cannot be verified by static analysis alone."
  - test: "Full OAuth round-trip: enter Client-ID, click connect, authenticate on Spotify, verify callback page shows display name"
    expected: "Browser redirected to accounts.spotify.com; after auth, LMS shows callback page with 'Erfolgreich verbunden!' and the Spotify display name (e.g. 'Verbunden als: John Doe'), then auto-redirects to Settings after 2 seconds."
    why_human: "Requires live Spotify Developer App credentials and a running LMS instance to execute the full browser-based OAuth flow."
  - test: "English language display"
    expected: "All 17 new i18n keys display in English when browser language is set to English; all strings are readable and correctly worded."
    why_human: "LMS string rendering depends on browser locale detection; cannot be verified without a running LMS instance."
---

# Phase 02.1: OAuth-PKCE Browser Auth Verification Report

**Phase Goal:** Replace non-functional Keymaster/login5 authentication with OAuth 2.0 Authorization Code + PKCE browser flow; users authenticate via their own Spotify Developer App through a guided Setup Wizard in the LMS Settings page
**Verified:** 2026-05-27T20:58:07Z
**Status:** human_needed
**Re-verification:** Yes — after Plan 04 gap closure

## Goal Achievement

### Observable Truths (from ROADMAP.md Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | Entering Client-ID and clicking "Mit Spotify verbinden" redirects browser to Spotify auth page | VERIFIED | Settings.pm L54-65: `if ($paramRef->{startOAuth})` reads `$prefs->get('clientId')`, calls `TokenManager->startOAuthFlow($clientId)`, then `$response->code(302)` + `$response->header('Location' => $authUrl)`. basic.html L41-43: `<input type="submit" name="startOAuth" ... id="startOAuthBtn">`. |
| SC-2 | After authenticating on Spotify, the browser returns to LMS and shows "Erfolgreich verbunden!" with the user's display name | VERIFIED | Callback.pm L90-92: `my $accounts = $prefs->get('accounts') \|\| {}; my $displayName = $accounts->{$accountId}{displayName}; _renderResult(..., $displayName, 1, undef)`. L126-128: conditional `<p>Verbunden als: <strong>` + `_html_escape($displayName)` + `</strong></p>`. Test t/10 ok 11: "SC-2: success page shows display name" — 15/15 tests pass. |
| SC-3 | The access token is cached with TTL and automatically refreshed via refresh_token before expiry | VERIFIED | TokenManager.pm L412: `$cache->set("spoton_token_$accountId", $accessToken, $ttl)`. L65-130: async `refreshToken` reads `$account->{refreshToken}` from prefs, POSTs to `SPOTIFY_TOKEN_URL` with `grant_type=refresh_token`. t/07 tests 20-22 verify caching and refresh. 28/28 unit tests pass. |
| SC-4 | No username/password fields in Settings page; old Keymaster/login5 code completely removed | VERIFIED | `grep -c "addAccount\|--get-token\|--authenticate\|_cacheDir\|CLIENT_ID" TokenManager.pm` = 0. `grep "addAccount\|username\|password" Settings.pm` = 0 matches. basic.html has no username/password inputs. Plugin.pm L147: `displayName \|\| $id` (username fallback removed). |
| SC-5 | All new UI strings display correctly in both English and German | VERIFIED (automated) | strings.txt contains PLUGIN_SPOTON_SETUP_WIZARD (present, count=1). `grep -c "PLUGIN_SPOTON_ACCOUNT_USERNAME" strings.txt` = 0. t/02_strings.t: 67/67 tests pass including negative assertions for 4 removed keys. Runtime rendering requires human verification. |

**Score: 5/5 truths verified**

### Re-verification: Gaps Closed

The previous verification (2026-05-27T20:00:00Z, score 4/5) identified two gaps. Both are now closed:

**Gap 1 (SC-2) — CLOSED:** `Callback.pm` now reads `$prefs->get('accounts')->{$accountId}{displayName}` after successful `exchangeCode`, passes it to `_renderResult` as the new `$displayName` parameter (8-arg signature), and conditionally renders `<p>Verbunden als: <strong>[name]</strong></p>` with XSS escaping. Test ok 11 and ok 15 in t/10_pkce_callback.t confirm both success-with-name and success-without-name paths.

**Gap 2 (.planning/REQUIREMENTS.md) — CLOSED:** `.planning/REQUIREMENTS.md` now shows AUTH-01 as "OAuth 2.0 Authorization Code + PKCE browser flow through per-user Spotify Developer App". `grep "Keymaster\|login5" .planning/REQUIREMENTS.md` returns 0 matches. `grep "PKCE/OAuth browser flow\|Spotify Developer App registration" .planning/REQUIREMENTS.md` returns 0 matches. Traceability table shows AUTH-01 through AUTH-06 at "Phase 02.1 | Complete".

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Plugins/SpotOn/API/TokenManager.pm` | PKCE auth engine | VERIFIED | All 8 required PKCE methods present: `_generatePkce`, `_generateState`, `startOAuthFlow`, `exchangeCode`, `refreshToken`, `_fetchUserProfile`, `_storeTokens`, `_buildRedirectUri`. Public API `getToken`, `getAccountIds`, `getActiveAccountName`, `removeAccount`, `refreshAllTokens` preserved. |
| `Plugins/SpotOn/Settings/Callback.pm` | OAuth callback handler with display name | VERIFIED | 180 lines. `sub init` calls `addPageFunction`. `sub handler` validates PKCE state, exchanges code, reads `displayName` from prefs, calls 8-arg `_renderResult`. `_html_escape` on displayName (XSS prevention). |
| `Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/callback.html` | Callback HTML template | VERIFIED | Static reference file with TT2 success/error paths. |
| `Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` | Setup Wizard template | VERIFIED | Two-step wizard; `pref_clientId` input; `redirectUri` in `<code>`; `startOAuth` button with `disabled` attribute + Prototype.js observer; account management table without username column. |
| `Plugins/SpotOn/Settings.pm` | Settings handler with startOAuth | VERIFIED | `startOAuth` handler issues HTTP 302; `prefs()` returns `clientId`; passes `clientId`, `redirectUri`, `oauthError`, `accounts`, `activeAccount` to template; no `addAccount`/username/password. |
| `Plugins/SpotOn/Plugin.pm` | Plugin bootstrap with Callback init | VERIFIED | L38: `clientId => ''` in prefs init. L74-75: `require Plugins::SpotOn::Settings::Callback; Plugins::SpotOn::Settings::Callback->init()` inside WEBUI block. Username fallback removed from `_accountSwitcherFeed`. |
| `Plugins/SpotOn/strings.txt` | 17 new i18n strings EN+DE | VERIFIED | `PLUGIN_SPOTON_SETUP_WIZARD` present (count=1). 4 obsolete keys absent: `ACCOUNT_USERNAME`=0, `ACCOUNT_PASSWORD`=0, `ACCOUNT_ADD`=0, `ACCOUNT_ADD_BTN`=0. t/02_strings.t 67/67 pass. |
| `t/07_token_manager.t` | PKCE TokenManager unit tests | VERIFIED | 28 tests, all passing. Covers AUTH-01 through AUTH-05, D-12. No Keymaster remnants. |
| `t/10_pkce_callback.t` | Callback unit tests with display name | VERIFIED | 15 tests (up from 12), all passing. Added SC-2 display name assertions at ok 11 and ok 15. Covers D-06, D-08, T-02.1-02 anti-replay. |
| `REQUIREMENTS.md` (root) | Updated with PKCE as auth mechanism | VERIFIED | AUT-01: OAuth 2.0 PKCE sole auth. AUT-04: REMOVED (login5 deactivated). AUT-06: per-user Developer App model. No PKCE/Developer App in Out-of-Scope. |
| `.planning/REQUIREMENTS.md` | GSD planning requirements tracking — PREVIOUSLY FAILED, NOW FIXED | VERIFIED | AUTH-01: "OAuth 2.0 Authorization Code + PKCE browser flow". `Keymaster\|login5` count = 0. `PKCE/OAuth browser flow` in Out-of-Scope = 0. AUTH-01 through AUTH-06 traceability = "Phase 02.1 | Complete". |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `Settings.pm` | `TokenManager.pm` | `TokenManager->startOAuthFlow()` | WIRED | L60 in Settings.pm: `require` + call |
| `Settings.pm` | `TokenManager.pm` | `TokenManager->_buildRedirectUri()` | WIRED | L121 in Settings.pm |
| `Plugin.pm` | `Settings/Callback.pm` | `Settings::Callback->init()` in WEBUI block | WIRED | L74-75 in Plugin.pm |
| `Callback.pm` | `TokenManager.pm` | `TokenManager->exchangeCode()` | WIRED | L78 in Callback.pm |
| `Callback.pm` | `Slim::Utils::Prefs` | `$prefs->get('accounts')->{$accountId}{displayName}` | WIRED | L90-91 in Callback.pm (gap closure) |
| `Callback.pm` | `Slim::Web::Pages` | `addPageFunction(CALLBACK_PATH, ...)` | WIRED | L35 in Callback.pm |
| `Callback.pm` | `Slim::Utils::Cache` | `cache->get/remove("spoton_pkce_$state")` | WIRED | L61, L71 in Callback.pm |
| `TokenManager.pm` | `https://accounts.spotify.com/api/token` | `SimpleAsyncHTTP->post()` | WIRED | L90-127 (refresh), L178-223 (exchange) |
| `TokenManager.pm` | `Slim::Utils::Cache` | `cache->set("spoton_token_$accountId")` | WIRED | L412 in `_cacheToken` |
| `TokenManager.pm` | `Slim::Utils::Prefs` | `$prefs->set('accounts', ...)` | WIRED | L109, L390 |
| `basic.html` | `Settings.pm` | Template vars: `accounts`, `clientId`, `redirectUri`, `oauthError` | WIRED | L23-58 in basic.html; L118-123 in Settings.pm |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `basic.html` | `redirectUri` | `Settings.pm` L121: `TokenManager->_buildRedirectUri()` — reads `preferences('server')->get('httpport')` | Yes | FLOWING |
| `basic.html` | `clientId` | `Settings.pm` L120: `$prefs->get('clientId')` from LMS Prefs | Yes | FLOWING |
| `basic.html` | `accounts` | `Settings.pm` L118: `$prefs->get('accounts')` from LMS Prefs | Yes | FLOWING |
| `Callback.pm` success page | `displayName` | L90-91: `$prefs->get('accounts')->{$accountId}{displayName}` — reads from the same prefs hash populated by `TokenManager::_storeTokens` | Yes — populated by `_storeTokens` after `_fetchUserProfile` | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TokenManager unit tests pass | `prove -v t/07_token_manager.t` | 28/28 tests pass | PASS |
| Callback unit tests pass (incl. display name) | `prove -v t/10_pkce_callback.t` | 15/15 tests pass | PASS |
| Full test suite passes | `prove -v t/` | 176/176 tests, 10 files | PASS |
| Strings format validation | `prove -v t/02_strings.t` | 67/67 tests pass | PASS |
| No Keymaster/login5 remnants in TokenManager | `grep -c "addAccount\|--get-token\|_cacheDir\|CLIENT_ID" TokenManager.pm` | 0 | PASS |
| No username/password in Settings.pm | `grep "addAccount\|username\|password" Settings.pm` | 0 matches | PASS |
| displayName wired in Callback.pm | `grep -c "displayName" Callback.pm` | 8 (>= 3 required) | PASS |
| .planning/REQUIREMENTS.md no Keymaster | `grep "Keymaster\|login5" .planning/REQUIREMENTS.md` | 0 matches | PASS |
| .planning/REQUIREMENTS.md PKCE not Out-of-Scope | `grep "PKCE/OAuth browser flow" .planning/REQUIREMENTS.md` | 0 matches | PASS |

### Probe Execution

No probe scripts found in `scripts/` directory. Step 7c: SKIPPED (no probe-*.sh files).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| AUTH-01 | 02.1-01 | OAuth 2.0 PKCE as sole auth mechanism | SATISFIED | `_generatePkce`, `startOAuthFlow`, `exchangeCode` in TokenManager.pm; Settings.pm startOAuth handler |
| AUTH-02 | 02.1-01 | Access token cached + auto-refreshed | SATISFIED | `_cacheToken` with TTL, async `refreshToken` via SimpleAsyncHTTP |
| AUTH-03 | 02.1-01 | Proactive refresh timer | SATISFIED | `refreshAllTokens` + `TOKEN_REFRESH_TIMER` re-arm in TokenManager.pm |
| AUTH-04 | 02.1-01 | Keymaster credential storage — REMOVED | N/A | AUT-04 marked REMOVED in both REQUIREMENTS.md files; replaced by LMS Prefs storage |
| AUTH-05 | 02.1-01 | Multiple accounts per LMS instance | SATISFIED | `getAccountIds`, `_storeTokens` creates new accounts by MD5 of Spotify user_id |
| AUTH-06 | 02.1-03 | Account switching in plugin menu | SATISFIED | Plugin.pm `_accountSwitcherFeed`, Settings.pm `switchAccount` handler |
| D-01 | 02.1-01 | login5 confirmed broken — PKCE required | SATISFIED | `addAccount`, `--authenticate`, `--get-token` all removed from TokenManager.pm |
| D-02 | 02.1-01 | No fallback auth — PKCE only | SATISFIED | TokenManager.pm contains no Keymaster code; Settings.pm has no `addAccount` block |
| D-03 | 02.1-01 | Binary role reduced to playback only | NEEDS HUMAN | `--get-token` removed from TokenManager; cannot verify runtime binary behavior without running instance |
| D-04 | 02.1-03 | Per-user Spotify Developer App registration | SATISFIED | `clientId` pref in Plugin.pm, Setup Wizard Step 1 in basic.html, AUT-06 updated in REQUIREMENTS.md |
| D-05 | 02.1-03 | Guided Setup Wizard | SATISFIED | basic.html two-step wizard per UI-SPEC Screen 1 |
| D-06 | 02.1-02 | Redirect URI = `http://127.0.0.1:{port}/plugins/SpotOn/settings/callback` | SATISFIED | `_buildRedirectUri()` uses `127.0.0.1` (not `serverURL()`); `CALLBACK_PATH` constant |
| D-07 | 02.1-03 | "Mit Spotify verbinden" button starts OAuth | SATISFIED | basic.html `startOAuth` submit button; Settings.pm handler issues HTTP 302 |
| D-08 | 02.1-02 | Dedicated `addPageFunction` route for callback | SATISFIED | Callback.pm `init()` calls `addPageFunction(CALLBACK_PATH, \&handler)` |
| D-09 | 02.1-03 | No headless support in this phase | SATISFIED | Not implemented (intentionally deferred) |
| D-10 | 02.1-01 | TokenManager completely rewritten (not refactored) | SATISFIED | All Keymaster code deleted; PKCE methods added |
| D-11 | 02.1-01 | Same OAuth scopes retained | SATISFIED | `REQUIRED_SCOPES` constant unchanged |
| D-12 | 02.1-01 | Refresh token in Prefs (persistent), access token in Cache (TTL) | SATISFIED | `_storeTokens` stores refresh in prefs accounts hash; `_cacheToken` stores access in Cache with TTL |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None | — | No TBD/FIXME/XXX markers found in any phase-modified file | — | — |

`grep -c "TBD\|FIXME\|XXX"` returns 0 for TokenManager.pm, Callback.pm, Settings.pm, and Plugin.pm.

### Human Verification Required

#### 1. Settings Page Visual Inspection (Plan 03, Task 3 — blocking checkpoint)

**Test:** Start LMS, open `http://127.0.0.1:9000/settings/plugin/spoton.html` in browser.
**Expected:**
- Setup Wizard section visible with "Schritt 1: Spotify-App registrieren" and "Schritt 2: Mit Spotify verbinden" headings
- Redirect URI displayed as `http://127.0.0.1:9000/plugins/SpotOn/settings/callback` in `<code>` element
- Client-ID text input is present
- "Mit Spotify verbinden" button is **disabled** while Client-ID field is empty
- Typing text into Client-ID field enables the button
- No username or password fields anywhere on the page
- Binary Status and Bitrate sections unchanged at top
**Why human:** Template rendering, DOM events, and Prototype.js observer behavior cannot be verified without a running LMS instance.

#### 2. Full OAuth Round-Trip with Display Name

**Test:** Enter a real Spotify Developer App Client-ID, click "Mit Spotify verbinden", complete Spotify auth, check callback page.
**Expected:** Browser redirects to `accounts.spotify.com`; returns to LMS callback page showing "Erfolgreich verbunden!" and a line "Verbunden als: [your Spotify display name]"; then auto-redirects to Settings after 2 seconds. Settings page then shows the connected account in the Account Management section.
**Why human:** Requires live Spotify credentials and a running LMS instance to execute the full browser-based OAuth flow.

#### 3. English Language Display

**Test:** Set browser/LMS language to English, reload Settings page.
**Expected:** All 17 new string keys render in English with correct phrasing (Setup Wizard, Connect with Spotify, etc.).
**Why human:** LMS i18n rendering depends on runtime browser locale detection.

### Gaps Summary

No gaps remaining. Both previously identified gaps were closed by Plan 04:

- **SC-2 (display name):** Closed. `Callback.pm` now reads `displayName` from the prefs accounts hash and renders "Verbunden als: [name]" in the success page. Test ok 11 in t/10_pkce_callback.t confirms this path.
- **(.planning/REQUIREMENTS.md):** Closed. AUTH-01 description updated, PKCE/OAuth removed from Out-of-Scope, traceability rows reference Phase 02.1 with Complete status. Zero Keymaster/login5 references remain.

Three items require human verification before this phase can be fully signed off: visual inspection of the Settings page template rendering, a live OAuth round-trip with display name confirmation, and English i18n display. Automated evidence for all 5 ROADMAP success criteria is complete.

---

_Verified: 2026-05-27T20:58:07Z_
_Verifier: Claude (gsd-verifier)_
