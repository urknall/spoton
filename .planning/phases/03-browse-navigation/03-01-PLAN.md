---
phase: 03-browse-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/API/TokenManager.pm
  - Plugins/SpotOn/strings.txt
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - NAV-05
  - NAV-06
  - NAV-07
  - NAV-08
  - NAV-09
  - NAV-10
  - NAV-11

must_haves:
  truths:
    - "Client.pm exposes all 11 Spotify API methods needed for Browse, Search, and Library"
    - "TokenManager.pm includes user-follow-read and playlist-read-collaborative scopes"
    - "Cache TTL rules cover all new API paths (search, me/following, me/top)"
    - "All i18n strings for Phase 3 menus exist in EN and DE"
  artifacts:
    - path: "Plugins/SpotOn/API/Client.pm"
      provides: "11 new endpoint methods: search, getRecentlyPlayed, getTopTracks, getSavedTracks, getSavedAlbums, getFollowedArtists, getUserPlaylists, getArtist, getArtistAlbums, getAlbum, getAlbumTracks, getPlaylistItems"
      exports: ["search", "getRecentlyPlayed", "getTopTracks", "getSavedTracks", "getSavedAlbums", "getFollowedArtists", "getUserPlaylists", "getArtist", "getArtistAlbums", "getAlbum", "getAlbumTracks", "getPlaylistItems"]
    - path: "Plugins/SpotOn/API/TokenManager.pm"
      provides: "Extended REQUIRED_SCOPES with user-follow-read and playlist-read-collaborative"
      contains: "user-follow-read"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "16 new i18n string keys for Phase 3 menus"
      contains: "PLUGIN_SPOTON_HOME"
  key_links:
    - from: "Plugins/SpotOn/API/Client.pm"
      to: "Spotify Web API v1"
      via: "_request() pipeline"
      pattern: "_request\\('get'"
---

<objective>
API Foundation + i18n Strings for Browse/Search/Library

Purpose: All 11 Spotify API endpoint methods must exist in Client.pm before any feed handler can call them. The TokenManager needs two additional OAuth scopes (user-follow-read, playlist-read-collaborative) and strings.txt needs all Phase 3 i18n keys. This plan creates the complete data layer that Plans 02 and 03 consume.

Output: Extended Client.pm with 11 new methods, TokenManager.pm with broadened scopes, strings.txt with 16 new keys, updated cache TTL rules.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/REQUIREMENTS.md
@/home/sti/spoton/.planning/phases/03-browse-navigation/03-CONTEXT.md
@/home/sti/spoton/.planning/phases/03-browse-navigation/03-RESEARCH.md
@/home/sti/spoton/.planning/phases/03-browse-navigation/03-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/API/Client.pm (existing pattern — getMe):
  sub getMe {
      my ($class, $accountId, $cb) = @_;
      $class->_request('get', 'me', { _accountId => $accountId, _noCache => 1 }, $cb);
  }

_request signature: $class->_request($method, $path, $params_hashref, $cb)
  - $params keys prefixed with _ are internal (_accountId, _noCache, _cacheKey)
  - all other keys become query string parameters
  - $cb->($result) on success; $cb->(undef, $err_hashref) on failure

_cacheTTL dispatch (existing regexes):
  me/player  -> 0
  me         -> 0
  me/(tracks|albums) -> 60
  (tracks|albums|artists)/ -> 3600
  (playlists|browse)/ -> 300
  default -> 0

From Plugins/SpotOn/API/TokenManager.pm (line 26-36):
  use constant REQUIRED_SCOPES => join(' ', qw(
      user-read-playback-state  user-modify-playback-state
      user-read-currently-playing  user-read-recently-played
      user-read-private  playlist-read-private
      user-library-read  user-top-read  streaming
  ));

From Plugins/SpotOn/strings.txt (entry pattern — always Tab-indented, EN+DE):
  PLUGIN_SPOTON_SOME_KEY
  	DE	Deutscher Text
  	EN	English Text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 11 API endpoint methods to Client.pm + extend cache TTL</name>
  <files>Plugins/SpotOn/API/Client.pm</files>
  <read_first>
    - Plugins/SpotOn/API/Client.pm (full file — understand _request pipeline, getMe pattern, _cacheTTL regexes)
    - .planning/phases/03-browse-navigation/03-RESEARCH.md (Standard Stack table: endpoint paths, parameters, scopes)
    - .planning/phases/03-browse-navigation/03-PATTERNS.md (Pattern 1-6: endpoint method pattern, pagination patterns)
  </read_first>
  <action>
    Add 11 public class methods to Client.pm below the existing getMe method. Each follows the getMe pattern: ($class, $accountId, ..., $cb) calling $class->_request(). Per D-09 and Pitfall 1 from RESEARCH.md, getArtistAlbums takes a single include_groups value per call (not combined).

    Methods to add (all use _request('get', ...)):

    1. search($class, $accountId, $params, $cb) — path: "search", params: q, type (default "track,album,artist,playlist"), limit (default 10 per D-10/NAV-11 Dev Mode), offset. The q parameter value must be passed through uri_escape is NOT needed here because _request already appends params via uri_escape in the URL builder.

    2. getRecentlyPlayed($class, $accountId, $params, $cb) — path: "me/player/recently-played", params: limit (default 50). No offset — cursor-based (Pitfall 4).

    3. getTopTracks($class, $accountId, $params, $cb) — path: "me/top/tracks", params: time_range (default "medium_term" per D-05), limit (default 50).

    4. getSavedTracks($class, $accountId, $params, $cb) — path: "me/tracks", params: offset, limit (max 50).

    5. getSavedAlbums($class, $accountId, $params, $cb) — path: "me/albums", params: offset, limit (max 50).

    6. getFollowedArtists($class, $accountId, $params, $cb) — path: "me/following", params: type => "artist" (hardcoded), limit (max 50), after (cursor). No offset (Pitfall 4).

    7. getUserPlaylists($class, $accountId, $params, $cb) — path: "me/playlists", params: offset, limit (max 50).

    8. getArtist($class, $accountId, $artistId, $cb) — path: "artists/$artistId", no extra params.

    9. getArtistAlbums($class, $accountId, $artistId, $params, $cb) — path: "artists/$artistId/albums", params: include_groups (SINGLE value per call — album OR single OR compilation OR appears_on), offset, limit (max 50). Per D-09 and RESEARCH Pitfall 1: combined include_groups breaks pagination.

    10. getAlbum($class, $accountId, $albumId, $cb) — path: "albums/$albumId", no extra params. Returns album metadata including tracks (first page).

    11. getAlbumTracks($class, $accountId, $albumId, $params, $cb) — path: "albums/$albumId/tracks", params: offset, limit (max 50).

    12. getPlaylistItems($class, $accountId, $playlistId, $params, $cb) — path: "playlists/$playlistId/items" (NOT /tracks — Pitfall 3, Feb 2026 rename), params: offset, limit (max 100).

    Then extend _cacheTTL to cover new paths. Add these regex rules BEFORE the existing rules (order matters for regex matching):
    - me/player/recently-played -> 0 (already matched by me/player, but explicit is safer)
    - me/top -> 60 (Library tier, same as me/tracks)
    - me/following -> 60 (Library tier)
    - me/playlists -> 60 (Library tier)
    - search -> 300 (Browse tier)

    Update the me/(tracks|albums) regex to: me/(?:tracks|albums|top|following|playlists) for 60s TTL.

    Do NOT implement any methods for removed Dev Mode endpoints (Artist Top Tracks, Related Artists, Browse Categories, New Releases) per D-11/NAV-10.

    Comment style: match existing getMe — one-line doc comment above each method.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/API/Client.pm 2>&amp;1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - Client.pm contains sub search, sub getRecentlyPlayed, sub getTopTracks, sub getSavedTracks, sub getSavedAlbums, sub getFollowedArtists, sub getUserPlaylists, sub getArtist, sub getArtistAlbums, sub getAlbum, sub getAlbumTracks, sub getPlaylistItems (12 new subs total)
    - getFollowedArtists hardcodes type => 'artist' in params
    - getArtistAlbums accepts include_groups as single value (no comma-separated combining)
    - getPlaylistItems uses path "playlists/$playlistId/items" (not /tracks)
    - _cacheTTL returns 60 for path "me/following"
    - _cacheTTL returns 60 for path "me/top/tracks"
    - _cacheTTL returns 60 for path "me/playlists"
    - _cacheTTL returns 300 for path "search"
    - _cacheTTL returns 0 for path "me/player/recently-played" (matched by me/player rule)
    - perl -c Plugins/SpotOn/API/Client.pm exits 0
    - No methods for top-tracks (artist), related-artists, browse/categories, browse/new-releases exist
  </acceptance_criteria>
  <done>All 12 new Client.pm methods compile, follow _request() pattern, cache TTLs cover all new paths</done>
</task>

<task type="auto">
  <name>Task 2: Extend TokenManager scopes + add all Phase 3 i18n strings</name>
  <files>Plugins/SpotOn/API/TokenManager.pm, Plugins/SpotOn/strings.txt</files>
  <read_first>
    - Plugins/SpotOn/API/TokenManager.pm (lines 26-36 — REQUIRED_SCOPES constant)
    - Plugins/SpotOn/strings.txt (full file — existing entry pattern with Tab indentation)
    - .planning/phases/03-browse-navigation/03-PATTERNS.md (strings.txt pattern, full new strings list)
  </read_first>
  <action>
    Two changes in this task:

    1. TokenManager.pm REQUIRED_SCOPES: Add two scopes to the existing qw() list:
       - user-follow-read (required for GET /me/following?type=artist — NAV-03, Pitfall 2)
       - playlist-read-collaborative (required for collaborative playlist access)
       Place them alphabetically among the existing scopes. This is a BREAKING CHANGE for existing sessions — users must re-authenticate. No code change needed to trigger re-auth because TokenManager already checks scope mismatch on token refresh.

    2. strings.txt: Append 16 new string keys at the end of the file (before the SON entry, which must remain last). Each key follows the existing Tab-indentation pattern (one Tab before DE/EN). The complete list of keys and values:

       PLUGIN_SPOTON_HOME — DE: Home, EN: Home
       PLUGIN_SPOTON_SEARCH — DE: Suche, EN: Search
       PLUGIN_SPOTON_LIBRARY — DE: Bibliothek, EN: Library
       PLUGIN_SPOTON_RECENTLY_PLAYED — DE: Kuerzlich gehoert, EN: Recently Played
       PLUGIN_SPOTON_MADE_FOR_YOU — DE: Fuer dich gemacht, EN: Made For You
       PLUGIN_SPOTON_TOP_TRACKS — DE: Top Tracks, EN: Top Tracks
       PLUGIN_SPOTON_LIKED_SONGS — DE: Lieblingssongs, EN: Liked Songs
       PLUGIN_SPOTON_ALBUMS — DE: Alben, EN: Albums
       PLUGIN_SPOTON_ARTISTS — DE: Kuenstler, EN: Artists
       PLUGIN_SPOTON_PLAYLISTS — DE: Playlists, EN: Playlists
       PLUGIN_SPOTON_SINGLES — DE: Singles, EN: Singles
       PLUGIN_SPOTON_COMPILATIONS — DE: Compilations, EN: Compilations
       PLUGIN_SPOTON_APPEARS_ON — DE: Erscheint auf, EN: Appears On
       PLUGIN_SPOTON_TOP_RESULT — DE: Bestes Ergebnis, EN: Top Result
       PLUGIN_SPOTON_TRACKS — DE: Tracks, EN: Tracks
       PLUGIN_SPOTON_NO_RESULTS — DE: Keine Ergebnisse, EN: No results

       IMPORTANT: Use proper German umlauts in the actual file (ue->ue is wrong; use the actual UTF-8 characters): Kuerzlich -> Kurzlich gehort with umlaut u and o. Specifically:
       - Kurzlich gehoert -> use the actual umlaut characters from PATTERNS.md: "Kuerzlich gehoert" should be the real umlaut form from the PATTERNS.md reference which shows them as unicode.
       - Read the PATTERNS.md strings list for the exact DE values with correct umlauts.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/API/TokenManager.pm 2>&amp;1 | tail -1 && grep -c 'user-follow-read' Plugins/SpotOn/API/TokenManager.pm && grep -c 'PLUGIN_SPOTON_HOME' Plugins/SpotOn/strings.txt && grep -c 'PLUGIN_SPOTON_NO_RESULTS' Plugins/SpotOn/strings.txt</automated>
  </verify>
  <acceptance_criteria>
    - TokenManager.pm REQUIRED_SCOPES contains "user-follow-read"
    - TokenManager.pm REQUIRED_SCOPES contains "playlist-read-collaborative"
    - perl -c Plugins/SpotOn/API/TokenManager.pm exits 0
    - strings.txt contains all 16 new keys: PLUGIN_SPOTON_HOME through PLUGIN_SPOTON_NO_RESULTS
    - Each new key has both DE and EN translations with Tab indentation
    - German translations use proper UTF-8 umlauts (not ASCII approximations)
    - SON entry remains as the last entry in strings.txt
    - TokenManager.pm contains scope mismatch detection logic (grep for REQUIRED_SCOPES comparison or scope validation in token refresh path)
  </acceptance_criteria>
  <done>TokenManager includes both new scopes, strings.txt has all 16 Phase 3 keys with correct DE/EN values</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client.pm -> Spotify API | Outbound HTTP with Bearer token; untrusted response JSON |
| LMS User Input -> search query | User-provided search text flows to Spotify API query parameter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Client.pm search() q parameter | mitigate | uri_escape already applied in _request() URL builder (line 124); no double-escaping needed |
| T-03-02 | Information Disclosure | Client.pm new methods | mitigate | All methods follow existing pattern: no token logging (T-02-10 from Phase 2); only path+status logged |
| T-03-03 | Elevation of Privilege | TokenManager.pm scope expansion | accept | Adding scopes is privilege expansion but user-consented via PKCE re-auth; no server-side risk |
| T-03-04 | Spoofing | Spotify API response JSON | mitigate | JSON::XS parse with eval protection already in _onSuccess(); malformed JSON returns parse_error |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all modules are LMS-bundled |
</threat_model>

<verification>
- perl -c Plugins/SpotOn/API/Client.pm exits 0
- perl -c Plugins/SpotOn/API/TokenManager.pm exits 0
- grep -c 'sub search\|sub get' Plugins/SpotOn/API/Client.pm shows 13 (1 existing + 12 new)
- grep 'user-follow-read' Plugins/SpotOn/API/TokenManager.pm returns a match
- grep -c 'PLUGIN_SPOTON_' Plugins/SpotOn/strings.txt shows at least 46 (30 existing + 16 new)
</verification>

<success_criteria>
Client.pm compiles with 12 new endpoint methods following the established _request() pattern. TokenManager.pm includes user-follow-read and playlist-read-collaborative scopes. strings.txt contains all 16 Phase 3 i18n keys with correct DE/EN translations. Cache TTL rules correctly categorize all new API paths.
</success_criteria>

<output>
Create `.planning/phases/03-browse-navigation/03-01-SUMMARY.md` when done
</output>
