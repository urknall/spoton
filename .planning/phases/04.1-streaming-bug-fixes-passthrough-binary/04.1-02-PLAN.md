---
phase: 04.1-streaming-bug-fixes-passthrough-binary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - librespot-spoton/Cargo.toml
  - librespot-spoton/src/main.rs
autonomous: true
requirements:
  - STR-01
  - STR-05

must_haves:
  truths:
    - "librespot-spoton binary supports --single-track flag for decoding a Spotify track to stdout"
    - "librespot-spoton binary supports --passthrough flag for raw OGG Vorbis output"
    - "librespot-spoton binary supports --start-position, --bitrate, --disable-discovery, --disable-audio-cache flags"
    - "--check output reports passthrough:true only when compiled with passthrough-decoder feature"
    - "cargo build --release succeeds without errors"
  artifacts:
    - path: "librespot-spoton/Cargo.toml"
      provides: "librespot-playback dependency with passthrough-decoder feature"
      contains: "librespot-playback"
    - path: "librespot-spoton/src/main.rs"
      provides: "SingleTrack mode with Player, pipe backend, event loop"
      contains: "SingleTrack"
  key_links:
    - from: "librespot-spoton/src/main.rs"
      to: "librespot-playback crate"
      via: "Player::new with pipe backend, PlayerEvent::EndOfTrack"
      pattern: "Player::new"
    - from: "Plugins/SpotOn/custom-convert.conf"
      to: "librespot-spoton binary"
      via: "[spoton] --single-track $URL$ invocation"
      pattern: "single-track"
---

<objective>
Implement the --single-track playback mode in the librespot-spoton binary, adding librespot-playback dependency with passthrough-decoder feature. The binary must decode a single Spotify track to stdout (pipe backend) for LMS's transcoding pipeline to consume.

Purpose: Without --single-track, no audio can flow from Spotify through LMS. This is the binary counterpart to Plan 01's Perl fixes. Together they complete the end-to-end streaming pipeline.

Output: Modified Cargo.toml with librespot-playback dependency, rewritten main.rs with SingleTrack mode implementation. Binary that compiles with `cargo build --release`.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/04.1-streaming-bug-fixes-passthrough-binary/04.1-RESEARCH.md
@/home/sti/spoton/.planning/phases/04-single-track-streaming/04-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from librespot crates and existing binary -->

From librespot-spoton/src/main.rs (current modes):
  enum Mode { Check, Authenticate, GetToken, Connect }
  # MUST ADD: SingleTrack

From librespot-core (already in Cargo.toml):
  librespot_core::authentication::Credentials
  librespot_core::cache::Cache
  librespot_core::config::SessionConfig
  librespot_core::Session

From librespot-playback (to be added):
  librespot_playback::audio_backend  (find backend by name, SinkBuilder)
  librespot_playback::config::{AudioFormat, Bitrate, PlayerConfig}
  librespot_playback::player::{Player, PlayerEvent}

From librespot-core::spotify_id (already available):
  SpotifyId::from_uri(&str) -> Result<SpotifyId>

From custom-convert.conf (consumer of binary output):
  [spoton] -n Squeezebox -c "$CACHE$" --single-track $URL$ --bitrate 320
           --disable-discovery --disable-audio-cache $START$
  [spoton] ... --single-track $URL$ --bitrate 320 --passthrough
           --disable-discovery --disable-audio-cache $START$

Expected binary invocation examples:
  spoton -n Squeezebox -c /path/to/cache --single-track spotify://track:75PWv --bitrate 320 --disable-discovery --disable-audio-cache
  spoton -n Squeezebox -c /path/to/cache --single-track spotify://track:75PWv --bitrate 320 --passthrough --disable-discovery --disable-audio-cache --start-position 30
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add librespot-playback dependency to Cargo.toml</name>
  <files>librespot-spoton/Cargo.toml</files>
  <action>
  Add the librespot-playback crate dependency with passthrough-decoder feature to Cargo.toml.

  In the [dependencies] section, add after the librespot-core line:
    librespot-playback = { version = "0.8", default-features = false, features = ["passthrough-decoder", "rustls-tls-native-roots"] }

  The features are:
  - passthrough-decoder: enables raw OGG Vorbis passthrough output (uses ogg crate internally)
  - rustls-tls-native-roots: matches librespot-core's TLS backend to avoid linking two different TLS implementations

  default-features = false prevents pulling in audio backends we do not need (rodio, alsa, pulseaudio). The pipe backend is always available (not feature-gated).

  After the edit, run `cargo check` in the librespot-spoton directory to verify dependency resolution succeeds. If cargo check reports API differences from what is described below (Task 2), note them — the executor should adapt Task 2 implementation to the actual 0.8 API.
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && grep -q "librespot-playback" Cargo.toml && grep -q "passthrough-decoder" Cargo.toml && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>
  - Cargo.toml contains librespot-playback with passthrough-decoder and rustls-tls-native-roots features
  - cargo check resolves all dependencies without errors (compile errors in main.rs are expected until Task 2)
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement --single-track mode in main.rs</name>
  <files>librespot-spoton/src/main.rs</files>
  <action>
  Extend main.rs to implement the SingleTrack playback mode. This is the core of librespot-spoton's streaming functionality.

  **New imports to add at the top of main.rs:**

  Add after the existing librespot_core imports:
    use librespot_core::spotify_id::SpotifyId;
    use librespot_playback::audio_backend;
    use librespot_playback::config::{AudioFormat, Bitrate, PlayerConfig};
    use librespot_playback::player::{Player, PlayerEvent};

  **Extend the Mode enum:**

  Add `SingleTrack` variant:
    enum Mode { Check, Authenticate, GetToken, Connect, SingleTrack }

  **Add new CLI argument parsing in the argument parser loop:**

  Add these new match arms inside the `while i < args.len()` loop:

    "--single-track" => {
        mode = Mode::SingleTrack;
        if i + 1 < args.len() && !args[i + 1].starts_with("--") {
            // Next arg is the track URI
            // Store it — add `let mut track_uri = String::new();` at top with other vars
            track_uri = args[i + 1].clone();
            i += 1;
        }
    }
    "--bitrate" | "-b" => {
        if i + 1 < args.len() {
            bitrate_str = args[i + 1].clone();
            i += 1;
        }
    }
    "--passthrough" => {
        passthrough = true;
    }
    "--start-position" => {
        if i + 1 < args.len() {
            start_position_str = args[i + 1].clone();
            i += 1;
        }
    }
    "--disable-discovery" => {
        // Accepted and ignored (not relevant for single-track mode)
    }
    "--disable-audio-cache" => {
        disable_audio_cache = true;
    }
    "--enable-volume-normalisation" => {
        normalisation = true;
    }

  Add these new variable declarations at the top of main() alongside existing ones:
    let mut track_uri = String::new();
    let mut bitrate_str = String::new();
    let mut passthrough = false;
    let mut start_position_str = String::new();
    let mut disable_audio_cache = false;
    let mut normalisation = false;

  **Add SingleTrack mode handler in the match block:**

  After the Mode::Connect arm, add:

    Mode::SingleTrack => {
        if track_uri.is_empty() {
            eprintln!("Error: track URI is required for --single-track");
            eprintln!("Usage: spoton -n 'SpotOn' -c <dir> --single-track <spotify:track:ID> --bitrate 320");
            process::exit(1);
        }
        if cache_dir.is_empty() {
            eprintln!("Error: --cache is required for --single-track");
            process::exit(1);
        }

        let bitrate: u32 = bitrate_str.parse().unwrap_or(320);
        let start_position: f64 = start_position_str.parse().unwrap_or(0.0);

        match run_single_track(&cache_dir, &track_uri, bitrate, passthrough, start_position, disable_audio_cache, normalisation).await {
            Ok(_) => process::exit(0),
            Err(e) => {
                eprintln!("Single-track playback failed: {}", e);
                process::exit(1);
            }
        }
    }

  **Implement the run_single_track async function:**

  Add this function after `run_get_token`:

    async fn run_single_track(
        cache_dir: &str,
        track_uri: &str,
        bitrate: u32,
        passthrough: bool,
        start_position_secs: f64,
        disable_audio_cache: bool,
        normalisation: bool,
    ) -> Result<(), Box<dyn std::error::Error>> {
        // Set up cache — audio cache path only if not disabled
        let audio_cache_path = if disable_audio_cache {
            None
        } else {
            Some(cache_dir)
        };
        let cache = Cache::new(Some(cache_dir), None::<&str>, audio_cache_path, None)?;

        let credentials = match cache.credentials() {
            Some(c) => c,
            None => {
                return Err(format!(
                    "No cached credentials found in '{}'. Run --authenticate first.",
                    cache_dir
                ).into());
            }
        };

        let session_config = SessionConfig::default();
        let session = Session::new(session_config, Some(cache));
        session.connect(credentials, false).await?;

        // Configure player
        let player_config = PlayerConfig {
            bitrate: match bitrate {
                96  => Bitrate::Bitrate96,
                160 => Bitrate::Bitrate160,
                _   => Bitrate::Bitrate320,
            },
            passthrough,
            normalisation,
            ..PlayerConfig::default()
        };

        // Use pipe backend (stdout) — always available, not feature-gated
        let backend = audio_backend::find(Some("pipe".to_string()))
            .expect("Pipe audio backend not found");

        let audio_format = AudioFormat::default();

        let (player, mut event_channel) = Player::new(
            player_config,
            session,
            None,  // no mixer
            move || backend(None, audio_format),
        );

        // Normalize URI: LMS passes spotify://track:ID, librespot needs spotify:track:ID
        let normalized_uri = track_uri.replace("spotify://", "spotify:");

        let track_id = SpotifyId::from_uri(&normalized_uri)?;

        // Start playback; position in milliseconds
        let start_position_ms = (start_position_secs * 1000.0) as u32;
        player.load(track_id, true, start_position_ms);

        // Wait for track to end or playback to stop
        while let Some(event) = event_channel.recv().await {
            match event {
                PlayerEvent::EndOfTrack { .. } => break,
                PlayerEvent::Stopped { .. } => break,
                _ => {}
            }
        }

        Ok(())
    }

  **Fix --check capabilities output:**

  In the Mode::Check arm, change the `passthrough` and `ogg-direct` values to use compile-time feature detection:

  Replace the hardcoded JSON string with:
    let has_passthrough = cfg!(feature = "passthrough-decoder");
    let json = serde_json::json!({
        "version": VERSION,
        "lms-auth": false,
        "ogg-direct": has_passthrough,
        "passthrough": has_passthrough,
    });
    println!("{}", json);

  This ensures --check reports capabilities truthfully based on the compiled features.

  **IMPORTANT API ADAPTATION NOTES:**
  The librespot 0.8.0 API may differ from the code shown above in these areas:
  - `PlayerConfig` field names (e.g., `normalisation` might be `normalisation_type` or have a different structure)
  - `Player::new` signature (number of arguments, mixer handling)
  - `PlayerEvent` variant names
  - `audio_backend::find` return type
  - `SpotifyId::from_uri` might be `SpotifyId::from_base62` or similar

  If `cargo check` or `cargo build` fails after writing the code, read the actual compiler errors and adapt the implementation to the real 0.8.0 API. Use `cargo doc --open` or read the source in the cargo registry cache (`~/.cargo/registry/src/`) to find the correct API signatures. The INTENT of each piece of code is clear — adapt the syntax to what the compiler accepts.
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo build --release 2>&1 | tail -20</automated>
  </verify>
  <done>
  - main.rs contains SingleTrack mode with full Player integration
  - --single-track accepts a Spotify URI and streams decoded audio to stdout via pipe backend
  - --passthrough flag enables raw OGG Vorbis output (passthrough-decoder feature)
  - --start-position, --bitrate, --disable-discovery, --disable-audio-cache, --enable-volume-normalisation all accepted
  - --check reports correct passthrough capability based on compiled features
  - cargo build --release succeeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS commandTable -> binary CLI args | Track URI from Plugin.pm flows through $URL$ substitution into --single-track argument |
| Binary -> Spotify servers | Cached credentials used to authenticate, track data streamed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-05 | Tampering | --single-track URI argument | mitigate | SpotifyId::from_uri validates URI format; malformed URIs return Err and exit(1). URI comes from LMS commandTable, not user input directly. |
| T-04.1-06 | Information Disclosure | stdout audio stream | accept | Audio data is the intended output; sent to LMS's transcoding pipeline (FLAC encoder or direct to player). No credentials in audio stream. |
| T-04.1-07 | Denial of Service | --single-track hangs if no EndOfTrack | mitigate | LMS transcoding pipeline has its own timeout; if librespot hangs, LMS kills the process via transcoding timeout. |
| T-04.1-SC | Tampering | cargo dependency resolution | mitigate | librespot-playback is official librespot-org crate; version pinned to 0.8. No unknown or suspicious crates added. |
</threat_model>

<verification>
After both tasks complete:

1. Build succeeds:
   cd librespot-spoton && cargo build --release 2>&1 | grep -q "Finished"

2. --check reports correct capabilities:
   ./target/release/spoton --check | head -2
   # Line 1: "ok spoton v1.0.0"
   # Line 2: JSON with "passthrough":true,"ogg-direct":true

3. --single-track shows usage when no URI provided:
   ./target/release/spoton --single-track 2>&1 | grep -q "track URI is required"

4. Binary contains passthrough-decoder code:
   strings target/release/spoton | grep -qi "passthrough\|ogg" | head -5
</verification>

<success_criteria>
- cargo build --release compiles successfully with librespot-playback + passthrough-decoder
- Binary supports --single-track with pipe backend (stdout output)
- --passthrough flag enables OGG Vorbis passthrough
- --check accurately reports passthrough:true and ogg-direct:true
- All custom-convert.conf pipeline patterns work with the binary's CLI interface
</success_criteria>

<output>
Create `.planning/phases/04.1-streaming-bug-fixes-passthrough-binary/04.1-02-SUMMARY.md` when done
</output>
