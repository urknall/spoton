---
phase: 04.2-credentials-made-for-you-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - librespot-spoton/src/main.rs
  - Plugins/SpotOn/API/TokenManager.pm
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02

must_haves:
  truths:
    - "D-01: SpotOn provisions its own librespot credentials via --token-login mode after OAuth callback"
    - "D-02: ZeroConf fallback not needed — Research confirmed Credentials::with_access_token() works in librespot 0.8"
    - "D-03: Credential provisioning triggers automatically after OAuth callback — no separate user step"
    - "D-04: credentials.json is written to the SpotOn cache directory (not Spotty's) — Spotty-Credentials-Hack replaced"
    - "Streaming works after a fresh SpotOn setup without Spotty installed"
    - "Access token is never logged in any log output"
  artifacts:
    - path: "librespot-spoton/src/main.rs"
      provides: "Mode::TokenLogin enum variant and run_token_login() async function"
      contains: "Mode::TokenLogin"
    - path: "Plugins/SpotOn/API/TokenManager.pm"
      provides: "_provisionCredentials method called after OAuth _storeTokens"
      contains: "_provisionCredentials"
  key_links:
    - from: "Plugins/SpotOn/API/TokenManager.pm"
      to: "librespot-spoton binary"
      via: "system() call with --token-login --token --cache flags"
      pattern: "token-login.*--token.*--cache"
    - from: "Plugins/SpotOn/API/TokenManager.pm::_storeTokens"
      to: "Plugins/SpotOn/API/TokenManager.pm::_provisionCredentials"
      via: "method call before $cb->($accountId)"
      pattern: "_provisionCredentials"
---

<objective>
Implement the --token-login binary mode in librespot-spoton and wire credential provisioning into TokenManager.pm's OAuth callback flow, so SpotOn creates its own credentials.json independently from Spotty.

Purpose: SpotOn currently requires manually copying Spotty's credentials.json. After this plan, SpotOn provisions its own librespot credentials automatically after OAuth PKCE login (per D-01, D-03, D-04).

Output: Modified main.rs with Mode::TokenLogin + run_token_login(). Modified TokenManager.pm with _provisionCredentials() called after _storeTokens().
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/04.2-credentials-made-for-you-fix/04.2-CONTEXT.md
@/home/sti/spoton/.planning/phases/04.2-credentials-made-for-you-fix/04.2-RESEARCH.md
@/home/sti/spoton/.planning/phases/04.2-credentials-made-for-you-fix/04.2-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From librespot-spoton/src/main.rs (current Mode enum, line 48-55):
```rust
#[derive(Debug, PartialEq)]
enum Mode {
    Check,
    Authenticate,
    GetToken,
    Connect,
    SingleTrack,
}
```

From librespot-spoton/src/main.rs (run_authenticate pattern, lines 274-291):
```rust
async fn run_authenticate(
    username: &str,
    password: &str,
    cache_dir: &str,
) -> Result<(), Box<dyn std::error::Error>> {
    let credentials = Credentials::with_password(username, password);
    let cache = Cache::new(Some(cache_dir), None::<&str>, None::<&str>, None)?;
    let session_config = SessionConfig::default();
    let session = Session::new(session_config, Some(cache));
    session.connect(credentials, true).await?;
    Ok(())
}
```

From librespot-core-0.8.0/src/authentication.rs (VERIFIED in cargo cache):
```rust
pub fn with_access_token(token: impl Into<String>) -> Self {
    Self {
        username: None,
        auth_type: AuthenticationType::AUTHENTICATION_SPOTIFY_TOKEN,
        auth_data: token.into().into_bytes(),
    }
}
```

From Plugins/SpotOn/API/TokenManager.pm (_storeTokens, lines 382-408):
```perl
sub _storeTokens {
    my ($class, $accessToken, $refreshToken, $expiresIn, $userProfile, $cb) = @_;
    # ... stores tokens ...
    main::INFOLOG && $log->info("TokenManager: stored tokens for account $accountId ($displayName)");
    $cb->($accountId);
}
```

From Plugins/SpotOn/Helper.pm (get method, line 34-59):
```perl
sub get {
    # Returns ($helper, $helperVersion) in list context, or $helper in scalar
    return wantarray ? ($helper, $helperVersion) : $helper;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Mode::TokenLogin to librespot-spoton binary</name>
  <files>librespot-spoton/src/main.rs</files>
  <read_first>
    - librespot-spoton/src/main.rs (current state: Mode enum line 48-55, CLI parsing loop lines 79-165, Mode::Authenticate match arm lines 183-210, run_authenticate() lines 274-291, Mode::Check JSON manifest lines 168-181)
    - .planning/phases/04.2-credentials-made-for-you-fix/04.2-PATTERNS.md (Pattern assignment for main.rs — enum extension, CLI parsing, match arm, core function, --check capability flag)
    - .planning/phases/04.2-credentials-made-for-you-fix/04.2-RESEARCH.md (Pattern 1: Token-Login Mode, Pitfall 1: no manual reconnect needed, Pitfall 3: token in ps aux accepted)
  </read_first>
  <action>
    Add Mode::TokenLogin to the enum Mode (after SingleTrack, line 54). Per D-01, this mode uses Credentials::with_access_token() to obtain reusable credentials from Spotify's AP.

    Add a `let mut token_str = String::new();` variable declaration alongside the existing username/password variables (after line 64).

    Add two CLI parsing match arms in the while-loop (after the --single-track block, around line 99):
    - "--token-login" sets mode = Mode::TokenLogin
    - "--token" captures the next argument into token_str (same pattern as "--username")

    Add a Mode::TokenLogin match arm in the main match block (before Mode::SingleTrack, after Mode::Connect). Validate that token_str and cache_dir are both non-empty, printing usage on error and calling process::exit(1). On success, call run_token_login(&token_str, &cache_dir).await. Print "credentials_saved" on Ok and process::exit(0). Print error message on Err and process::exit(1). Follow the exact same structure as Mode::Authenticate (lines 183-210).

    Add the async function run_token_login(access_token: &str, cache_dir: &str) -> Result<(), Box<dyn std::error::Error>> after run_authenticate (after line 291). Implementation: call Credentials::with_access_token(access_token), create Cache::new(Some(cache_dir), None::<&str>, None::<&str>, None)?, create SessionConfig::default() and Session::new(session_config, Some(cache)), call session.connect(credentials, true).await? (store_credentials=true triggers the automatic token-to-reusable-credentials exchange per session.rs:184-201). Return Ok(()). Per Pitfall 1 from RESEARCH.md: no manual reconnect code needed — librespot handles the double-connect internally.

    Update the --check JSON manifest (line 173-178) to include "token-login": true in the serde_json::json! block.

    Update the file header comment block (lines 5-10) to add a new line documenting Mode::TokenLogin:
    "  --token-login   : Acquire reusable credentials from OAuth access token, write credentials.json"

    Add a contract comment block for --token-login (after the --get-token contract comment, around line 23):
    "// --token-login contract (for TokenManager.pm):
    //   Command: spoton -n 'SpotOn' --token-login --token <access_token> --cache <dir>
    //   stdout: "credentials_saved" on success
    //   exit 0 on success, non-zero on failure"
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo build --release 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - main.rs contains `Mode::TokenLogin` in the enum
    - main.rs contains `async fn run_token_login(` function definition
    - main.rs contains `Credentials::with_access_token(` call inside run_token_login
    - main.rs contains `session.connect(credentials, true).await` inside run_token_login
    - main.rs contains `"credentials_saved"` println on success path
    - main.rs Mode::Check json block contains `"token-login": true`
    - main.rs contains `"--token-login"` and `"--token"` match arms in CLI parsing
    - `cargo build --release` completes without errors
    - Built binary at target/release/spoton responds to `--token-login` (exits 1 with usage when --token missing)
  </acceptance_criteria>
  <done>librespot-spoton binary compiles with Mode::TokenLogin. Running `spoton --token-login` without --token prints usage and exits 1. The --check JSON output includes "token-login": true.</done>
</task>

<task type="auto">
  <name>Task 2: Wire credential provisioning into TokenManager.pm OAuth callback</name>
  <files>Plugins/SpotOn/API/TokenManager.pm</files>
  <read_first>
    - Plugins/SpotOn/API/TokenManager.pm (current state: _storeTokens lines 382-408, refreshToken lines 67-129, use statements lines 1-18, _cacheToken lines 413-423)
    - Plugins/SpotOn/Helper.pm (get() method lines 34-59 — returns binary path)
    - Plugins/SpotOn/Plugin.pm (cache dir pattern lines 17-18: `use File::Spec::Functions qw(catdir);` and line 1089 analog: `catdir($serverPrefs->get('cachedir'), 'spoton')`)
    - .planning/phases/04.2-credentials-made-for-you-fix/04.2-PATTERNS.md (TokenManager pattern — integration point, _provisionCredentials implementation, error handling)
    - .planning/phases/04.2-credentials-made-for-you-fix/04.2-RESEARCH.md (Pattern 2, Anti-Patterns: token never logged T-02.1-03, Pitfall 5: provisioning only at OAuth callback)
  </read_first>
  <action>
    Add `use File::Spec::Functions qw(catdir);` to the use-statement block (after line 10, alongside existing use statements). Per P6 from PATTERNS.md.

    Add a new private method _provisionCredentials($class, $accountId, $accessToken) after the existing _cacheToken method (after line 423, before the final `1;`). Per D-01, D-03.

    Implementation of _provisionCredentials:
    1. require Plugins::SpotOn::Helper and call Plugins::SpotOn::Helper->get() to obtain the binary path. Return early with $log->warn if binary not found.
    2. Build cache dir using catdir(preferences('server')->get('cachedir'), 'spoton') — identical to Plugin.pm updateTranscodingTable pattern.
    3. Shell-escape the three values (helper path, token, cache dir) using the s/'/'\\''/g pattern for single-quote safety.
    4. Build the command string: sprintf("'%s' -n 'SpotOn' --token-login --token '%s' --cache '%s' 2>&1", $safeHelper, $safeToken, $safeDir)
    5. SECURITY (T-02.1-03): Log the command with [REDACTED] replacing the token value. Never pass $accessToken to any $log-> call.
    6. Execute via backticks: my $output = `$cmd`; my $exit = $? >> 8;
    7. Check exit code 0 AND output contains "credentials_saved". Log success at INFOLOG level. Log failure at error level (include $output for diagnostics, which does NOT contain the token — only librespot error messages).
    8. No hard failure — streaming continues if credentials.json already exists from prior provisioning.

    Modify _storeTokens (line 406-407): Insert $class->_provisionCredentials($accountId, $accessToken) call BEFORE the existing $cb->($accountId) call and BEFORE the existing INFOLOG line. The provisioning is synchronous and short-lived (< 2s per A3 from RESEARCH.md).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/API/TokenManager.pm 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - TokenManager.pm contains `use File::Spec::Functions qw(catdir);`
    - TokenManager.pm contains `sub _provisionCredentials {`
    - TokenManager.pm _provisionCredentials contains `--token-login` in the sprintf command string
    - TokenManager.pm _provisionCredentials contains `[REDACTED]` in the log output (never the raw token)
    - TokenManager.pm _provisionCredentials contains `credentials_saved` check on $output
    - TokenManager.pm _storeTokens calls _provisionCredentials before $cb->($accountId)
    - TokenManager.pm _provisionCredentials calls Plugins::SpotOn::Helper->get() for binary path
    - TokenManager.pm _provisionCredentials uses catdir(preferences('server')->get('cachedir'), 'spoton') for cache dir
    - `perl -c Plugins/SpotOn/API/TokenManager.pm` reports syntax OK
  </acceptance_criteria>
  <done>TokenManager.pm provisions librespot credentials automatically after each OAuth callback. Token is never logged. Binary path comes from Helper.pm. Cache dir is the SpotOn-owned directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin -> Binary (system call) | Access token crosses from Perl process to Rust binary via CLI argument |
| Binary -> Spotify AP | Token sent to Spotify's AP server via librespot-core TLS connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-01 | Information Disclosure | TokenManager.pm _provisionCredentials | mitigate | Token replaced with [REDACTED] in all $log-> calls. sprintf for shell command uses token only at runtime, never logged. |
| T-04.2-02 | Information Disclosure | system() call (ps aux visibility) | accept | Token visible in process listing for < 2s on local system only. Short-lived, local-only exposure. Document for future hardening (env var in Phase 5). |
| T-04.2-03 | Tampering | --cache argument path traversal | mitigate | cacheDir built internally via catdir($serverPrefs->get('cachedir'), 'spoton') — not user-controllable. Binary receives plugin-constructed path only. |
| T-04.2-04 | Tampering | --token argument injection | mitigate | Shell escaping via s/'/'\\''/g on all three interpolated values (helper path, token, cache dir) prevents command injection. |
| T-04.2-05 | Denial of Service | Blocking system() in async callback | accept | Provisioning runs once after OAuth callback only, < 2s duration. Not triggered by normal API flow. Acceptable for Phase 04.2. |
| T-04.2-SC | Tampering | npm/pip/cargo installs | mitigate | No new external dependencies added. All crates already in Cargo.lock. |
</threat_model>

<verification>
1. Binary compiles: `cd librespot-spoton && cargo build --release` exits 0
2. Binary --check includes token-login: `./target/release/spoton --check` output contains "token-login"
3. Binary --token-login without token: exits 1 with usage message
4. Perl syntax: `perl -c Plugins/SpotOn/API/TokenManager.pm` reports OK
5. No token in log: `grep -c 'accessToken\|access_token' Plugins/SpotOn/API/TokenManager.pm` minus the parameter names equals grep for REDACTED patterns
</verification>

<success_criteria>
- librespot-spoton binary has Mode::TokenLogin that converts an OAuth access token to persistent credentials.json
- TokenManager.pm triggers credential provisioning automatically after OAuth PKCE callback
- Access token never appears in any log output
- No dependency on Spotty's cache directory
</success_criteria>

<output>
Create `.planning/phases/04.2-credentials-made-for-you-fix/04.2-01-SUMMARY.md` when done
</output>
