---
phase: 04.2-credentials-made-for-you-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/Plugin.pm
autonomous: true
requirements:
  - NAV-02

must_haves:
  truths:
    - "Made For You feed shows Daily Mix, Discover Weekly, Release Radar and other personal playlists"
    - "Category endpoint is tried first; fallback to me/playlists + name-matching on 404/403"
    - "Library playlists feed correctly excludes Made For You playlists using the same regex"
    - "Spotify's response ordering is preserved (no custom sorting)"
  artifacts:
    - path: "Plugins/SpotOn/API/Client.pm"
      provides: "getPersonalMixes method using browse/categories endpoint"
      contains: "sub getPersonalMixes"
    - path: "Plugins/SpotOn/API/Client.pm"
      provides: "PERSONAL_MIX_CATEGORY constant"
      contains: "PERSONAL_MIX_CATEGORY"
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "Regex-based _isMadeForYou replacing owner.id filter"
      contains: "PERSONAL_MIX_REGEX"
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "Rewritten _madeForYouFeed with category endpoint + fallback"
      contains: "getPersonalMixes"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm::_madeForYouFeed"
      to: "Plugins/SpotOn/API/Client.pm::getPersonalMixes"
      via: "API method call with accountId"
      pattern: "Client->getPersonalMixes"
    - from: "Plugins/SpotOn/Plugin.pm::_madeForYouFeed"
      to: "Plugins/SpotOn/API/Client.pm::getUserPlaylists"
      via: "Fallback on 404/403 error"
      pattern: "getUserPlaylists.*fallback"
    - from: "Plugins/SpotOn/Plugin.pm::_libraryPlaylistsFeed"
      to: "Plugins/SpotOn/Plugin.pm::_isMadeForYou"
      via: "grep filter (excludes MFY from library)"
      pattern: "grep.*_isMadeForYou"
---

<objective>
Fix the Made For You feed to use Spotify's category endpoint instead of the broken owner.id filter, and add a name-matching fallback for Dev Mode where the category endpoint returns 404/403.

Purpose: The current _isMadeForYou filter uses owner.id eq 'spotify' which fails in Dev Mode (Feb 2026: owner fields restricted). This plan switches to the Spotty-proven category endpoint (D-05) with regex-based name-matching fallback (D-06), fixing NAV-02.

Output: New getPersonalMixes() method in Client.pm. Rewritten _madeForYouFeed() and _isMadeForYou() in Plugin.pm.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/04.2-credentials-made-for-you-fix/04.2-CONTEXT.md
@/home/sti/spoton/.planning/phases/04.2-credentials-made-for-you-fix/04.2-RESEARCH.md
@/home/sti/spoton/.planning/phases/04.2-credentials-made-for-you-fix/04.2-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/API/Client.pm (getUserPlaylists pattern, lines 136-143):
```perl
sub getUserPlaylists {
    my ($class, $accountId, $params, $cb) = @_;
    $class->_request('get', 'me/playlists', {
        _accountId => $accountId,
        offset     => $params->{offset} // 0,
        limit      => $params->{limit}  // 50,
    }, $cb);
}
```

From Plugins/SpotOn/API/Client.pm (_cacheTTL, lines 395-396):
```perl
# Playlists and browse data: 300 seconds (5 minutes)
return 300 if $path =~ /^(?:playlists|browse)\//;
```

From Plugins/SpotOn/API/Client.pm (_onError callback contract, line 371):
```perl
$cb->(undef, { error => $error, code => $code });
```

From Plugins/SpotOn/Plugin.pm (_isMadeForYou current, lines 299-306):
```perl
sub _isMadeForYou {
    my ($playlist) = @_;
    return ($playlist->{owner}{id} // '') eq 'spotify';
}
```

From Plugins/SpotOn/Plugin.pm (_madeForYouFeed current, lines 482-497):
```perl
sub _madeForYouFeed {
    my ($client, $callback, $args) = @_;
    my $accountId = _getAccountId($client);
    Plugins::SpotOn::API::Client->getUserPlaylists($accountId, { offset => 0, limit => 50 }, sub {
        my $data = shift;
        # ...
        my @mfy   = grep { _isMadeForYou($_) } @{ $data->{items} || [] };
        my @items = map  { _playlistItem($client, $_) } @mfy;
        $callback->({ items => \@items });
    });
}
```

From Plugins/SpotOn/Plugin.pm (_libraryPlaylistsFeed, line 675):
```perl
my @user  = grep { !_isMadeForYou($_) } @{ $data->{items} || [] };
```

From Spotty API.pm (verified reference):
```perl
use constant PERSONAL_MIX_CATEGORY => '0JQ5DAt0tbjZptfcdMSKl3';
```

From Spotty OPML.pm (verified regex patterns):
```perl
my $dailyMixRegex   = qr/Daily Mix|MixTape/i;
my $weeklyMixRegex  = qr/Discover Weekly|Mix der Woche/i;
my $releaseRadarRegex = qr/Release Radar/i;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getPersonalMixes to Client.pm and rewrite Plugin.pm Made For You logic</name>
  <files>Plugins/SpotOn/API/Client.pm, Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/API/Client.pm (full file: getUserPlaylists lines 136-143 as pattern, _cacheTTL lines 377-400 to verify browse/ path cached at 300s, use constant block lines 17-21)
    - Plugins/SpotOn/Plugin.pm (lines 299-306 _isMadeForYou, lines 476-497 _madeForYouFeed, line 675 _libraryPlaylistsFeed grep, lines 411-430 _playlistItem, lines 278-286 _getAccountId)
    - .planning/phases/04.2-credentials-made-for-you-fix/04.2-PATTERNS.md (Client.pm pattern for getPersonalMixes, Plugin.pm patterns for _isMadeForYou regex, _madeForYouFeed rewrite, _libraryPlaylistsFeed note)
    - .planning/phases/04.2-credentials-made-for-you-fix/04.2-RESEARCH.md (Pattern 3: getPersonalMixes, Pattern 4: _isMadeForYou regex, Pattern 5: _madeForYouFeed umbau, Pitfall 2: response structure {playlists:{items:[]}}, Pitfall 4: _isMadeForYou at two call sites, Pitfall 6: Category endpoint in Dev Mode)
  </read_first>
  <action>
    **Client.pm changes:**

    Add a constant after the existing use-constant block (after line 21):
    use constant PERSONAL_MIX_CATEGORY => '0JQ5DAt0tbjZptfcdMSKl3';
    Source: Spotty API.pm line 18, verified.

    Add getPersonalMixes method after getUserPlaylists (after line 143). Signature: ($class, $accountId, $params, $cb). Calls $class->_request('get', 'browse/categories/' . PERSONAL_MIX_CATEGORY . '/playlists', { _accountId => $accountId, limit => $params->{limit} // 50 }, $cb). Per D-05. Cache TTL is already 300s via the browse/ path match in _cacheTTL line 396.

    **Plugin.pm changes:**

    1. Replace _isMadeForYou (lines 299-306) with a regex-based implementation per D-06. Define a lexical regex variable $PERSONAL_MIX_REGEX on the line before the sub (package scope my-variable). Pattern: qr/Daily\s+Mix|MixTape|Discover\s+Weekly|Mix\s+der\s+Woche|Release\s+Radar/i — uses \s+ instead of literal spaces to handle whitespace variations. The function body becomes: return ($playlist->{name} // '') =~ $PERSONAL_MIX_REGEX. Update the comment block above to reference D-06 (name-matching replaces broken owner.id filter). Per Pitfall 4: both call sites (line 493 in _madeForYouFeed and line 675 in _libraryPlaylistsFeed) use this function, so updating the function itself fixes both.

    2. Rewrite _madeForYouFeed (lines 476-497). New implementation per D-05, D-06, D-07, D-08:
       - Primary path: Call Plugins::SpotOn::API::Client->getPersonalMixes($accountId, {}, sub { ... })
       - Success path: Extract items from $data->{playlists}{items} (NOT $data->{items} — Pitfall 2). Map through _playlistItem. Pass to $callback.
       - Fallback path: If !$data or ($err and $err->{code} is 404 or 403), log at INFOLOG level and call getUserPlaylists as fallback. Filter results through _isMadeForYou (grep), map through _playlistItem.
       - Empty fallback: If fallback also returns no data, show PLUGIN_SPOTON_NO_RESULTS textarea.
       - Update the comment block above the function to document the category endpoint primary path and name-matching fallback.
       - The callback receives two arguments ($data, $err) from _request pipeline's _onError path (see Client.pm _onError line 371: $cb->(undef, { error => ..., code => $code })).

    3. _libraryPlaylistsFeed (line 675): No change needed. It already calls _isMadeForYou which will now use regex. Verify the grep { !_isMadeForYou($_) } line is unchanged after edits.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/API/Client.pm 2>&1 && perl -c Plugins/SpotOn/Plugin.pm 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Client.pm contains `use constant PERSONAL_MIX_CATEGORY => '0JQ5DAt0tbjZptfcdMSKl3';`
    - Client.pm contains `sub getPersonalMixes {`
    - Client.pm getPersonalMixes calls _request with path 'browse/categories/' . PERSONAL_MIX_CATEGORY . '/playlists'
    - Plugin.pm contains `my $PERSONAL_MIX_REGEX = qr/` (package-level lexical)
    - Plugin.pm _isMadeForYou uses $PERSONAL_MIX_REGEX on $playlist->{name}, NOT owner.id
    - Plugin.pm _isMadeForYou does NOT contain `owner` anywhere
    - Plugin.pm _madeForYouFeed calls Client->getPersonalMixes as primary path
    - Plugin.pm _madeForYouFeed extracts items from $data->{playlists}{items} (NOT $data->{items})
    - Plugin.pm _madeForYouFeed has fallback to getUserPlaylists on 404/403 error codes
    - Plugin.pm _madeForYouFeed fallback uses grep { _isMadeForYou($_) } for filtering
    - Plugin.pm _libraryPlaylistsFeed line 675 still contains grep { !_isMadeForYou($_) } (unchanged)
    - `perl -c Plugins/SpotOn/API/Client.pm` reports syntax OK
    - `perl -c Plugins/SpotOn/Plugin.pm` reports syntax OK
  </acceptance_criteria>
  <done>Made For You feed uses category endpoint with name-matching fallback. _isMadeForYou uses regex instead of broken owner.id filter. Both call sites (_madeForYouFeed and _libraryPlaylistsFeed) benefit from the updated function. No custom sorting applied (D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin -> Spotify API | HTTP requests to browse/categories endpoint and me/playlists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-06 | Information Disclosure | getPersonalMixes response | accept | Category endpoint returns public playlist metadata only. No PII exposure beyond what user already has access to. |
| T-04.2-07 | Spoofing | Hardcoded category ID | accept | Category ID 0JQ5DAt0tbjZptfcdMSKl3 is Spotty-proven. If Spotify changes it, fallback to name-matching activates. No security impact. |
| T-04.2-08 | Denial of Service | Category endpoint 404 -> fallback chain | mitigate | Fallback makes exactly one additional API call (getUserPlaylists). No retry loop. Rate limiting via Client.pm central throttle (API-02). |
| T-04.2-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in this plan. |
</threat_model>

<verification>
1. Perl syntax: `perl -c Plugins/SpotOn/API/Client.pm && perl -c Plugins/SpotOn/Plugin.pm` both OK
2. No owner.id filter: `grep -c 'owner.*id' Plugins/SpotOn/Plugin.pm` in _isMadeForYou context should be 0
3. Category constant: `grep 'PERSONAL_MIX_CATEGORY' Plugins/SpotOn/API/Client.pm` returns the constant definition
4. Response structure: `grep 'playlists.*items' Plugins/SpotOn/Plugin.pm` confirms correct nested access
5. Fallback: `grep '404\|403' Plugins/SpotOn/Plugin.pm` confirms error code handling
</verification>

<success_criteria>
- Made For You feed tries category endpoint first, falls back to name-matching on 404/403
- _isMadeForYou uses regex pattern matching (Daily Mix, Discover Weekly, Release Radar, etc.)
- Library playlists feed correctly excludes Made For You playlists using the same updated function
- No custom sorting applied — Spotify's response ordering preserved (D-07)
</success_criteria>

<output>
Create `.planning/phases/04.2-credentials-made-for-you-fix/04.2-02-SUMMARY.md` when done
</output>
