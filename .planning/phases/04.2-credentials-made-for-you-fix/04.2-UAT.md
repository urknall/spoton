---
status: partial
phase: 04.2-credentials-made-for-you-fix
source: [04.2-01-SUMMARY.md, 04.2-02-SUMMARY.md]
started: 2026-05-29T05:45:00Z
updated: 2026-05-29T09:00:00Z
---

## Current Test

[paused — credential provisioning requires architectural fix, see Phase 04.3]

## Tests

### 1. Credential-Provisioning nach OAuth
expected: Nach OAuth PKCE Login: credentials.json in /var/lib/squeezeboxserver/cache/spoton/
result: issue
reported: "_provisionCredentials triggered correctly (D-03 works), but Spotify AP rejects the PKCE Web API token with 'Bad credentials'. Credentials::with_access_token() expects an AP token, not a Web API OAuth token. Same issue as Spotty-NG. Requires architectural fix: ZeroConf + Keymaster instead of PKCE token bootstrap."
severity: blocker

### 2. Token nie im Log sichtbar
expected: Kein roher Access-Token-Wert in Log-Ausgaben
result: pass

### 3. Made For You Feed zeigt Personal Mixes
expected: LMS UI -> Home -> Für dich gemacht zeigt Daily Mix, Discover Weekly, Release Radar
result: [pending]

### 4. Made For You Fallback bei Category-Fehler
expected: Fallback auf me/playlists bei Category-Endpoint 404/403
result: [pending]

### 5. Library Playlists schließen MFY-Mixes aus
expected: Library -> Playlists zeigt nur eigene Playlists, keine MFY-Mixes
result: [pending]

### 6. Streaming ohne Spotty
expected: Track abspielen mit eigenen Credentials ohne Spotty
result: blocked
blocked_by: prior-phase
reason: "Credentials provisioning failed — requires Phase 04.3 (ZeroConf + Keymaster auth rework)"

## Summary

total: 6
passed: 1
issues: 1
pending: 3
skipped: 0
blocked: 1

## Gaps

- truth: "SpotOn provisions its own librespot credentials via --token-login mode after OAuth callback"
  status: failed
  reason: "PKCE Web API token incompatible with Spotify AP AUTHENTICATION_SPOTIFY_TOKEN. Credentials::with_access_token() needs AP-native token, not Web API OAuth token."
  severity: blocker
  test: 1
  artifacts: [librespot-spoton/src/main.rs, Plugins/SpotOn/API/TokenManager.pm]
  missing: [ZeroConf discovery mode for credential provisioning, Keymaster token provider for Web API access]
  fix: "New Phase 04.3: Replace PKCE credential bootstrap with ZeroConf discovery + Keymaster --get-token"
