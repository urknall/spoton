---
phase: 04.3-zeroconf-keymaster-auth
plan: "03"
type: execute
wave: 2
depends_on:
  - "04.3-01"
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/connect-hint.png
  - t/09_settings.t
autonomous: true
requirements:
  - AUTH-01
  - AUTH-05
must_haves:
  truths:
    - "Settings page shows ZeroConf discovery instructions with visual hint image (D-02)"
    - "AJAX timer polls discoveryStatus endpoint every 3s and auto-reloads on 'connected' (D-04)"
    - "Account list shows display names with switch/remove buttons (D-05)"
    - "Start Discovery / Stop Discovery buttons work via form submit (D-01)"
    - "No Client-ID field, no OAuth button, no redirect URI visible"
    - "All strings available in EN and DE"
  artifacts:
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "ZeroConf discovery UI handler, AJAX discoveryStatus endpoint"
      exports: ["handler", "_discoveryStatusHandler"]
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      provides: "ZeroConf discovery UI template"
      contains: "discoveryStatus"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "All ZeroConf-related i18n strings in EN and DE"
      contains: "PLUGIN_SPOTON_ZEROCONF"
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/connect-hint.png"
      provides: "Visual hint image for Spotify Connect pairing (D-02)"
  key_links:
    - from: "basic.html"
      to: "/plugins/SpotOn/settings/discoveryStatus"
      via: "XHR AJAX poll"
      pattern: "discoveryStatus"
    - from: "Settings.pm handler"
      to: "TokenManager->startDiscovery"
      via: "form submit startDiscovery param"
      pattern: "startDiscovery"
    - from: "Settings.pm"
      to: "TokenManager->_setupAccountFromCredentials"
      via: "discoveryStatus connected -> setupAccount"
      pattern: "_setupAccountFromCredentials"
---

<objective>
Rebuild the Settings UI for ZeroConf discovery: replace PKCE Setup Wizard with visual ZeroConf instructions, AJAX status polling endpoint, and multi-account management. Add all i18n strings.

Purpose: This is the user-facing auth experience. After this plan, users see a clear visual instruction to connect via Spotify app, get live status updates, and manage multiple accounts — all without browser redirects or Client IDs.
Output: Rewritten Settings.pm with AJAX endpoint, new basic.html template, updated strings.txt, connect-hint image asset, updated tests.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-RESEARCH.md
@/home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-PATTERNS.md
@/home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-01-SUMMARY.md

<interfaces>
From Plugins/SpotOn/Settings.pm (current — will be rewritten):
- sub new() — calls SUPER::new
- sub name() — returns CSRF-protected name
- sub page() — returns CSRF-protected URI
- sub prefs() — returns ($prefs, 'bitrate', 'binary', 'clientId', 'normalization')
- sub handler() — loads Template Toolkit params, processes form actions

From Plugins/SpotOn/API/TokenManager.pm (after Plan 02 rewrite):
- sub startDiscovery($class) — spawns --discover-once process
- sub stopDiscovery($class) — kills discovery process
- sub _setupAccountFromCredentials($class, $cb) — processes ZeroConf result
- sub removeAccount($class, $accountId) — removes account from prefs
- my $discoveryProc package variable — Proc::Background ref or undef

From Slim::Web::Pages (LMS API):
- Slim::Web::Pages->addRawFunction($path, \&handler) — registers JSON endpoint

From Spotty/Settings/Auth.pm (analog for addRawFunction pattern):
- Slim::Web::HTTP::addHTTPResponse($httpClient, $response, \$content) for raw responses
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Settings.pm — remove OAuth, add discovery actions + AJAX endpoint</name>
  <files>Plugins/SpotOn/Settings.pm, t/09_settings.t</files>
  <read_first>
    /home/sti/spoton/Plugins/SpotOn/Settings.pm
    /home/sti/spoton/t/09_settings.t
    /home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-PATTERNS.md (Settings.pm section)
    /home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-RESEARCH.md (Pattern 3)
    /home/sti/spotty-ng/Spotty-Plugin/Settings/Auth.pm
  </read_first>
  <action>
    Settings.pm rewrite:

    1. IMPORTS: Add File::Spec::Functions qw(catdir catfile), JSON::XS::VersionOneAndTwo (for to_json in AJAX handler). Keep existing imports.

    2. prefs() method: Remove 'clientId' from the return list. New: return ($prefs, 'bitrate', 'binary', 'normalization').

    3. new() method: After SUPER::new, register the AJAX endpoint:
       Slim::Web::Pages->addRawFunction('plugins/SpotOn/settings/discoveryStatus', \&_discoveryStatusHandler).
       This must be inside new() to ensure it runs when Settings is instantiated. Add "require Slim::Web::Pages;" before the call if not already available.

    4. handler() method — remove OAuth block: Delete the entire "if ($paramRef->{startOAuth})" block (lines 60-74 in current file). Delete the template params for clientId and redirectUri (lines 123-124).

    5. handler() method — add discovery actions: Inside the saveSettings block, add:
       - if ($paramRef->{startDiscovery}): require TokenManager, call TokenManager->startDiscovery()
       - if ($paramRef->{stopDiscovery}): require TokenManager, call TokenManager->stopDiscovery()
       - if ($paramRef->{setupAccount}): require TokenManager, call TokenManager->_setupAccountFromCredentials(sub { ... }). This is triggered when the AJAX poll detects 'connected' and reloads the page — the page reload causes handler to run with setupAccount=1 (set by JS before reload or by checking for pending credentials in __DISCOVER__).

    6. handler() template params: Replace clientId/redirectUri with:
       - $paramRef->{discoveryRunning} = _isDiscoveryRunning() ? 1 : 0
       - Keep accounts and activeAccount params unchanged.

    7. handler() — auto-setup on page load: When handler runs and __DISCOVER__/credentials.json exists (discovery completed but page hasn't processed it yet), automatically call _setupAccountFromCredentials. Check: my $discoverCredsFile = catfile($serverPrefs->get('cachedir'), 'spoton', '__DISCOVER__', 'credentials.json'); if (-f $discoverCredsFile) { trigger setup }. Since _setupAccountFromCredentials is async (has /me callback), return from handler is tricky. Alternative: do the setup synchronously in handler by reading credentials.json, creating the account in prefs, and deferring the /me display_name fetch.

    8. NEW: _discoveryStatusHandler($httpClient, $response) — raw function handler:
       - Build discoverDir from server prefs
       - Check credentials.json exists: status = 'connected'
       - Check _isDiscoveryRunning(): status = 'waiting'
       - Otherwise: status = 'idle'
       - Also include accounts info when connected: accountCount
       - Serialize as JSON, set Content-Length, Content-Type application/json, Connection close
       - Call Slim::Web::HTTP::addHTTPResponse

    9. NEW: _isDiscoveryRunning() — checks if TokenManager's discovery proc is alive:
       require Plugins::SpotOn::API::TokenManager and call a status method, or check directly if the package var $discoveryProc exists and is alive.
       Simplest approach: TokenManager provides isDiscoveryRunning() class method that returns $discoveryProc && $discoveryProc->alive().

    10. Keep account remove and switch logic unchanged (lines 85-117).

    t/09_settings.t changes:
    - Remove tests referencing startOAuth, clientId, authUrl, oauthError
    - Remove tests referencing redirectUri, buildRedirectUri
    - Keep tests for: account remove, account switch, bitrate validation, normalization save
    - Add test for: prefs() return list does NOT include 'clientId'
    - Add test for: discoveryRunning template param is set
    - Add mock for _discoveryStatusHandler returning JSON with status key
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/05_perl_syntax.t t/09_settings.t 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Settings.pm prefs() does NOT return 'clientId'
    - Settings.pm handler() does NOT contain startOAuth, clientId, redirectUri, buildRedirectUri
    - Settings.pm contains addRawFunction('plugins/SpotOn/settings/discoveryStatus')
    - Settings.pm contains sub _discoveryStatusHandler with to_json and addHTTPResponse
    - Settings.pm handler() contains $paramRef->{startDiscovery} and $paramRef->{stopDiscovery} checks
    - Settings.pm handler() sets $paramRef->{discoveryRunning}
    - t/09_settings.t does NOT reference startOAuth, clientId, redirectUri
    - prove t/05_perl_syntax.t exits 0
    - prove t/09_settings.t exits 0
  </acceptance_criteria>
  <done>Settings.pm rewritten: OAuth removed, ZeroConf discovery form actions, AJAX status endpoint registered, multi-account management preserved. Tests updated.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild basic.html template + strings.txt + connect-hint image</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/strings.txt, Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/connect-hint.png</files>
  <read_first>
    /home/sti/spoton/Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
    /home/sti/spoton/Plugins/SpotOn/strings.txt
    /home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-PATTERNS.md (basic.html section, AJAX poll pattern)
    /home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-RESEARCH.md (Pattern 3)
  </read_first>
  <action>
    basic.html complete rewrite. Keep the header/footer PROCESS tags and binary status section (lines 1-10). Keep bitrate select and normalization checkbox sections (lines 12-24). Replace everything from line 26 onwards (Setup Wizard + Account sections).

    NEW SECTION 1: ZeroConf Discovery (replaces PKCE Setup Wizard, shown when no accounts):
    - Wrap in [% IF NOT accounts.keys.size %] ... [% END %]
    - WRAPPER setting with title="PLUGIN_SPOTON_ZEROCONF_SETUP"
    - D-02: Display connect-hint.png image centered, max-width 300px, border-radius 8px
    - Step title: PLUGIN_SPOTON_ZEROCONF_STEP1
    - Instructions text: PLUGIN_SPOTON_ZEROCONF_INSTRUCTIONS (explains: open Spotify app, look for devices, select LMS server name)
    - Conditional on discoveryRunning:
      IF discoveryRunning: show "waiting" status div with id="discovery-status", AJAX poll script (3s interval per D-04), and Stop Discovery button (name="stopDiscovery")
      ELSE: show Start Discovery button (name="startDiscovery")
    - AJAX poll script: XHR GET to /plugins/SpotOn/settings/discoveryStatus every 3000ms. On status 'connected': clearInterval, location.reload(). Script only runs inside the discoveryRunning branch.

    NEW SECTION 2: Account list (always shown):
    - WRAPPER setting with title="PLUGIN_SPOTON_ACCOUNT_SETTINGS"
    - IF accounts.keys.size > 0: Show table with displayName, Active/Switch/Remove buttons (keep existing pattern from lines 70-90, it works well)
    - "Add another account" button: change name from startOAuth to startDiscovery
    - IF no accounts AND discoveryRunning is false: show PLUGIN_SPOTON_ACCOUNT_NONE message

    Remove from basic.html:
    - Entire Setup Wizard section (Client-ID input, pref_clientId, Developer Dashboard link, OAuth button, startOAuth, authUrl link, sessionStorage spoton_auth_pending logic)
    - Remove sessionStorage clearing script in account section

    strings.txt changes:
    REMOVE these obsolete PKCE strings: PLUGIN_SPOTON_SETUP_WIZARD, PLUGIN_SPOTON_STEP1_BODY, PLUGIN_SPOTON_DASHBOARD_LINK, PLUGIN_SPOTON_CLIENT_ID_LABEL, PLUGIN_SPOTON_CLIENT_ID_HINT, PLUGIN_SPOTON_CONNECT_BTN, PLUGIN_SPOTON_OPEN_SPOTIFY, PLUGIN_SPOTON_OPEN_SPOTIFY_HINT, PLUGIN_SPOTON_CLOSE_TAB, PLUGIN_SPOTON_SETUP_HINT, PLUGIN_SPOTON_AUTH_STATE_ERROR, PLUGIN_SPOTON_AUTH_DENIED, PLUGIN_SPOTON_AUTH_REDIRECT, PLUGIN_SPOTON_CLIENT_ID_REQUIRED, PLUGIN_SPOTON_AUTH_SUCCESS, PLUGIN_SPOTON_AUTH_FAILED, PLUGIN_SPOTON_BACK_TO_SETTINGS, PLUGIN_SPOTON_CONNECTED_AS.

    Keep: PLUGIN_SPOTON_STEP1_TITLE (rewrite content), PLUGIN_SPOTON_STEP2_TITLE (rewrite content), PLUGIN_SPOTON_ADD_ANOTHER (keep as-is), PLUGIN_SPOTON_ACCOUNT_* strings (keep as-is), all non-auth strings.

    ADD new strings (both EN and DE):
    - PLUGIN_SPOTON_ZEROCONF_SETUP: DE="Spotify verbinden" EN="Connect Spotify"
    - PLUGIN_SPOTON_ZEROCONF_STEP1: DE="Mit der Spotify App verbinden" EN="Connect with the Spotify App"
    - PLUGIN_SPOTON_ZEROCONF_INSTRUCTIONS: DE="Oeffne die Spotify App auf deinem Handy. Tippe auf das Geraete-Symbol (unten rechts) und waehle \"%s\" aus der Geraetliste." EN="Open the Spotify app on your phone. Tap the device icon (bottom right) and select \"%s\" from the device list."
      Note: The %s will be filled by LMS server name in the template via cstring().
    - PLUGIN_SPOTON_WAITING_FOR_CONNECTION: DE="Warte auf Verbindung ueber Spotify App..." EN="Waiting for connection via Spotify app..."
    - PLUGIN_SPOTON_START_DISCOVERY: DE="Erkennung starten" EN="Start Discovery"
    - PLUGIN_SPOTON_STOP_DISCOVERY: DE="Erkennung stoppen" EN="Stop Discovery"
    - PLUGIN_SPOTON_CONNECT_HINT_ALT: DE="Spotify App Geraeteauswahl" EN="Spotify app device picker"
    - PLUGIN_SPOTON_CONNECTED_AS: DE="Verbunden als" EN="Connected as"
      (This string was in the old list but keep it with new phrasing for ZeroConf context)

    Rewrite PLUGIN_SPOTON_STEP1_TITLE: DE="Schritt 1: Spotify App oeffnen" EN="Step 1: Open Spotify App"
    Rewrite PLUGIN_SPOTON_STEP2_TITLE: DE="Schritt 2: Geraet auswaehlen" EN="Step 2: Select Device"

    connect-hint.png: Create a simple SVG-based placeholder image (since we cannot source actual Spotify screenshots). Generate a 300x200 PNG with a simple graphic: phone outline with a speaker/device icon and an arrow. Use a CLI tool (convert from ImageMagick if available, or create minimal PNG programmatically). If no image tool is available, create a minimal 1x1 transparent PNG as placeholder and add a comment in the HTML that the image should be replaced with a real screenshot. The D-02 decision requires the image, but the actual Spotify app screenshot must come from the user.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c 'clientId\|startOAuth\|pref_clientId\|SPOTIFY_AUTH_URL' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html; grep -c 'discoveryStatus' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</automated>
  </verify>
  <acceptance_criteria>
    - basic.html contains NO references to clientId, startOAuth, pref_clientId, authUrl, developer.spotify.com, spoton_auth_pending
    - basic.html contains discoveryStatus AJAX poll with setInterval(function(), 3000)
    - basic.html contains startDiscovery and stopDiscovery submit buttons
    - basic.html contains connect-hint.png image reference
    - basic.html contains [% IF discoveryRunning %] conditional
    - strings.txt contains PLUGIN_SPOTON_ZEROCONF_SETUP with both DE and EN
    - strings.txt contains PLUGIN_SPOTON_WAITING_FOR_CONNECTION with both DE and EN
    - strings.txt contains PLUGIN_SPOTON_START_DISCOVERY with both DE and EN
    - strings.txt does NOT contain PLUGIN_SPOTON_CLIENT_ID_LABEL
    - strings.txt does NOT contain PLUGIN_SPOTON_CLIENT_ID_HINT
    - connect-hint.png file exists in Plugins/SpotOn/HTML/EN/plugins/SpotOn/html/images/
    - t/02_strings.t passes (prove t/02_strings.t)
    - grep -c 'clientId' basic.html returns 0
  </acceptance_criteria>
  <done>Settings template rebuilt for ZeroConf: visual instructions, AJAX polling, discovery buttons. All PKCE UI elements removed. Strings updated in EN+DE. Placeholder image created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> LMS Settings | Form submits + AJAX polls via HTTP within LMS web UI |
| LMS Settings -> discoveryStatus | Raw function endpoint returns JSON status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-10 | CSRF | startDiscovery/stopDiscovery form actions | mitigate | LMS Settings pages use built-in CSRF protection via protectURI (already in Settings.pm::page()) |
| T-04.3-11 | Info Disclosure | discoveryStatus endpoint | accept | Returns only status string ('idle'/'waiting'/'connected') and account count. No tokens, no credentials. Accessible only within LMS web UI. |
| T-04.3-12 | Tampering | removeAccount form param | mitigate | Existing WR-03 validation preserved: 8-char hex regex + exists check before acting |
| T-04.3-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
prove t/05_perl_syntax.t t/09_settings.t t/02_strings.t all pass. basic.html has zero PKCE references. AJAX endpoint URL matches Settings.pm registration.
</verification>

<success_criteria>
Settings UI completely rebuilt for ZeroConf flow. Visual hint image present. AJAX status polling functional. All strings in EN+DE. No PKCE artifacts remain in UI layer.
</success_criteria>

<output>
Create `.planning/phases/04.3-zeroconf-keymaster-auth/04.3-03-SUMMARY.md` when done
</output>
