---
phase: 04.3-zeroconf-keymaster-auth
plan: "04"
type: execute
wave: 3
depends_on:
  - "04.3-02"
  - "04.3-03"
files_modified:
  - Plugins/SpotOn/Settings/Callback.pm
  - t/10_pkce_callback.t
autonomous: false
requirements:
  - AUTH-01
must_haves:
  truths:
    - "Callback.pm is deleted — no OAuth callback route exists"
    - "PKCE callback test file is deleted"
    - "Full test suite passes without Callback.pm"
    - "UAT confirms Library, Playlists, Search, Browse all work via Keymaster tokens"
    - "D-06: PKCE deletion happens only after UAT confirmation — no premature removal"
  artifacts: []
  key_links: []
---

<objective>
Remove PKCE Callback.pm and its test file after UAT confirms all API endpoints work with Keymaster tokens (per D-06). This is the final cleanup step.

Purpose: D-06 specifies PKCE code removal happens only after UAT confirms Library, Playlists, Search, Browse all work via Keymaster tokens. This plan gates the deletion on human verification.
Output: Callback.pm and t/10_pkce_callback.t deleted. Clean test suite.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-02-SUMMARY.md
@/home/sti/spoton/.planning/phases/04.3-zeroconf-keymaster-auth/04.3-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT — Keymaster tokens work for all API endpoints</name>
  <what-built>Plans 01-03 replaced PKCE OAuth with ZeroConf Discovery + Keymaster --get-token. All token acquisition now uses the binary's Keymaster/Mercury protocol instead of PKCE HTTP refresh.</what-built>
  <how-to-verify>
    1. Open LMS Settings -> SpotOn
    2. Verify ZeroConf discovery UI is shown (visual instructions, no Client-ID field)
    3. Click "Start Discovery" (or verify auto-start worked)
    4. Open Spotify app on phone, go to device picker, select the LMS server name
    5. Wait for Settings page to auto-refresh to "Connected as [Display Name]"
    6. Navigate SpotOn menus and verify each of these works:
       - Home -> Recently Played (lists items)
       - Home -> Made For You (shows playlists)
       - Library -> Liked Songs (lists tracks)
       - Library -> Playlists (lists user playlists)
       - Search -> type "Radiohead" -> results appear in Tracks/Albums/Artists
       - Browse into an album -> tracks listed with durations
       - Play a track -> audio plays through LMS
    7. Check LMS log for any "no_token" or "401" errors
    8. Verify: ls -la {cachedir}/spoton/{accountId}/credentials.json exists with correct permissions
    9. Verify connect-hint.png on Settings page shows a real Spotify app screenshot (not a placeholder) per D-02
  </how-to-verify>
  <resume-signal>Type "approved" if all API endpoints work with Keymaster tokens, or describe any failures</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Delete Callback.pm and PKCE callback test</name>
  <files>Plugins/SpotOn/Settings/Callback.pm, t/10_pkce_callback.t</files>
  <read_first>
    /home/sti/spoton/Plugins/SpotOn/Settings/Callback.pm
    /home/sti/spoton/t/10_pkce_callback.t
  </read_first>
  <action>
    Per D-06 (UAT approved in Task 1):
    1. Delete Plugins/SpotOn/Settings/Callback.pm entirely (git rm)
    2. Delete t/10_pkce_callback.t entirely (git rm)
    3. Verify no other file imports or references Callback.pm (grep -r 'Callback' Plugins/SpotOn/ — should return 0 hits after Plan 02 removed the require from Plugin.pm)
    4. Run full test suite to confirm nothing depends on the deleted files
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/ 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File Plugins/SpotOn/Settings/Callback.pm does NOT exist
    - File t/10_pkce_callback.t does NOT exist
    - grep -r "Settings::Callback" Plugins/SpotOn/ returns 0 results
    - prove t/ (full suite) exits 0
  </acceptance_criteria>
  <done>PKCE Callback.pm and its test file deleted. No references remain. Full test suite passes. D-06 fulfilled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan only deletes code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.3-13 | Spoofing | Residual OAuth callback route | mitigate | Callback.pm deleted; Plugin.pm no longer registers the route (done in Plan 02); grep confirms no references |
| T-04.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Full test suite passes. No PKCE-related files remain. No imports reference Callback.pm.
</verification>

<success_criteria>
Complete PKCE removal after human-verified UAT. No OAuth callback route, no PKCE test file. Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/04.3-zeroconf-keymaster-auth/04.3-04-SUMMARY.md` when done
</output>
