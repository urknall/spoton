---
phase: 04.3
slug: zeroconf-keymaster-auth
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-29
---

# Phase 04.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual UAT + LMS log inspection (Perl plugin — no unit test framework) |
| **Config file** | none |
| **Quick run command** | `grep -i "spoton\|error\|warn" /path/to/lms/server.log \| tail -50` |
| **Full suite command** | Manual UAT checklist (see Per-Task Verification Map) |
| **Estimated runtime** | ~120 seconds per manual verification |

---

## Sampling Rate

- **After every task commit:** Verify LMS starts without errors, plugin loads
- **After every plan wave:** Run full UAT verification checklist
- **Before `/gsd:verify-work`:** All manual verifications must pass
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | AUTH-01 | — | Credentials stored securely | manual | ls -la cache/credentials.json | N/A | pending |
| TBD | TBD | TBD | AUTH-02 | — | Token refresh without user action | manual | LMS log inspection | N/A | pending |
| TBD | TBD | TBD | AUTH-03 | — | Multi-account support | manual | Settings page verification | N/A | pending |
| TBD | TBD | TBD | AUTH-05 | — | Secure credential storage | manual | File permission check | N/A | pending |

*Status: pending*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — LMS plugin load verification + manual UAT.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| ZeroConf discovery via Spotify app | AUTH-01 | Requires real Spotify app interaction | 1. Open Settings, 2. Start discovery, 3. Connect via Spotify app, 4. Verify credentials.json created |
| Keymaster token retrieval | AUTH-02 | Requires valid credentials + Spotify backend | 1. Run --get-token, 2. Verify JSON output with accessToken, 3. Test API call with token |
| Token auto-refresh | AUTH-02 | Time-dependent behavior | 1. Wait for token near expiry, 2. Verify automatic refresh in logs |
| Multi-account switching | AUTH-03 | Requires multiple Spotify accounts | 1. Add second account via ZeroConf, 2. Switch in Settings, 3. Verify correct tokens used |
| PKCE removal completeness | AUTH-05 | Code audit required | 1. grep for PKCE/OAuth references, 2. Verify no browser redirect code remains |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
