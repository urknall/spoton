---
phase: 04.4-dual-token-api-routing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
autonomous: true
requirements:
  - API-01
  - API-02

must_haves:
  truths:
    - "D-02: Settings page shows a Client-ID input field for custom Spotify Developer App (eigene Client-ID)"
    - "D-03: Degraded-mode warning appears when no custom Client-ID is configured — mit Link zur Spotify Developer App-Registrierung"
    - "D-03: Warning includes a link to developer.spotify.com/dashboard for app registration"
    - "D-02: Client-ID is sanitized to alphanumeric, max 32 chars before persistence"
    - "All new strings display correctly in English and German"
  artifacts:
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "clientId pref save/load, _isDegradedMode helper, customClientId + degradedMode template vars"
      contains: "_isDegradedMode"
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      provides: "Client-ID input field with degraded-mode warning banner"
      contains: "pref_clientId"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "EN+DE strings for Client-ID settings, degraded-mode warning, register app link"
      contains: "PLUGIN_SPOTON_CLIENT_ID_SETTINGS"
  key_links:
    - from: "Plugins/SpotOn/Settings.pm"
      to: "Slim::Utils::Prefs"
      via: "$prefs->set('clientId', $id) / $prefs->get('clientId')"
      pattern: "clientId"
    - from: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      to: "Plugins/SpotOn/Settings.pm"
      via: "template vars customClientId, degradedMode"
      pattern: "customClientId|degradedMode"
---

<objective>
Add Client-ID settings field and degraded-mode warning to the SpotOn Settings page. Users can optionally enter their own Spotify Developer App Client-ID for higher rate limits. When no custom ID is configured, a visible warning with registration link guides the user.

Purpose: Implements D-02 (custom Client-ID field) and D-03 (degraded-mode warning) from CONTEXT.md. Provides the UI-side of the dual-token architecture -- the clientId pref is consumed by TokenManager.pm and Client.pm (Plan 01) for own-token routing.
Output: Modified Settings.pm with clientId pref handling, modified basic.html with Client-ID input and warning banner, extended strings.txt with EN+DE strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.4-dual-token-api-routing/04.4-CONTEXT.md
@.planning/phases/04.4-dual-token-api-routing/04.4-RESEARCH.md
@.planning/phases/04.4-dual-token-api-routing/04.4-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Settings.pm (current interface -- will be modified):
  sub handler -- lines 47-143: saves prefs, passes template vars, calls SUPER::handler
  Existing prefs saved: bitrate, normalization, startDiscovery, stopDiscovery, removeAccount, switchAccount
  Existing template vars: helperMissing, binaryVersion, binaryPath, accounts, activeAccount, discoveryRunning

From Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html:
  Structure: header.html -> setting blocks -> footer.html
  Existing blocks: BINARY_STATUS, BITRATE, NORMALIZATION, ZEROCONF_SETUP (conditional), ACCOUNT_SETTINGS

From Plugins/SpotOn/strings.txt:
  Format: KEY\n\tDE\tvalue\n\tEN\tvalue\n (tab-indented, DE/EN per key)
  Last key: PLUGIN_SPOTON_CONNECT_HINT_ALT (line 189-191)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings.pm -- clientId pref handling and degraded-mode detection</name>
  <files>Plugins/SpotOn/Settings.pm</files>
  <read_first>
    - Plugins/SpotOn/Settings.pm
    - .planning/phases/04.4-dual-token-api-routing/04.4-PATTERNS.md (section: Settings.pm)
    - .planning/phases/04.4-dual-token-api-routing/04.4-RESEARCH.md (Pattern 7, Security Domain)
  </read_first>
  <action>
    Extend Settings.pm handler() with Client-ID pref save/load and degraded-mode template variable.

    **In the saveSettings block** (after the normalization save on line 66, before the startDiscovery check on line 70): Add clientId save logic. Read `$paramRef->{pref_clientId}`, default to empty string. Sanitize: strip all non-alphanumeric chars with `$id =~ s/[^a-zA-Z0-9]//g` (T-04.4-01 input validation from RESEARCH Security Domain). Truncate to 32 chars with `substr($id, 0, 32)`. Save via `$prefs->set('clientId', $id)`.

    **In the template data block** (after discoveryRunning on line 141): Add two new template variables:
    - `$paramRef->{customClientId} = $prefs->get('clientId') || ''`
    - `$paramRef->{degradedMode} = _isDegradedMode()`

    **New helper sub** `_isDegradedMode` (after `_isDiscoveryRunning` on line 237): Returns 1 if no custom clientId is configured (`$prefs->get('clientId')` is falsy), 0 otherwise. This mirrors the pattern of `_isDiscoveryRunning`.

    **prefs() method** (line 43): Add 'clientId' to the prefs list so LMS can handle the pref lifecycle: `return ($prefs, 'bitrate', 'binary', 'normalization', 'clientId');`

    **Security:** T-04.4-01 -- clientId input is sanitized to [a-zA-Z0-9]{0,32} before persistence. No shell metacharacters can reach _fetchKeymasterToken's --client-id flag.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/Settings.pm 2>&1 | grep -v "^$" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Settings.pm compiles without errors (perl -c exits 0)
    - grep -c 'clientId' Plugins/SpotOn/Settings.pm returns >= 4 (prefs list, save, template var, degradedMode)
    - grep -c '_isDegradedMode' Plugins/SpotOn/Settings.pm returns >= 2 (declaration + usage)
    - grep -c 'customClientId' Plugins/SpotOn/Settings.pm returns >= 1 (template var)
    - grep -c 'degradedMode' Plugins/SpotOn/Settings.pm returns >= 1 (template var)
    - Input sanitization regex present: s/[^a-zA-Z0-9]//g
    - Length truncation present: substr($id, 0, 32)
    - prefs() method returns list including 'clientId'
  </acceptance_criteria>
  <done>Settings.pm saves/loads clientId pref with input sanitization, exposes customClientId and degradedMode template variables, includes _isDegradedMode helper that returns true when no custom Client-ID is configured.</done>
</task>

<task type="auto">
  <name>Task 2: basic.html + strings.txt -- Client-ID UI block with degraded-mode warning</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/strings.txt</files>
  <read_first>
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
    - Plugins/SpotOn/strings.txt
    - .planning/phases/04.4-dual-token-api-routing/04.4-PATTERNS.md (sections: basic.html, strings.txt)
    - .planning/phases/04.4-dual-token-api-routing/04.4-RESEARCH.md (Pattern 7)
  </read_first>
  <action>
    **basic.html:** Add a new WRAPPER setting block for the Client-ID field. Insert it AFTER the normalization block (line 24, after the [% END %]) and BEFORE the [% IF NOT accounts.keys.size %] ZeroConf block (line 26).

    The block uses WRAPPER setting title="PLUGIN_SPOTON_CLIENT_ID_SETTINGS" desc="PLUGIN_SPOTON_CLIENT_ID_DESC". Inside:
    - Conditional degraded-mode warning: [% IF degradedMode %] shows a div with style="color: orange; margin-bottom:10px" containing the warning string PLUGIN_SPOTON_DEGRADED_MODE_WARNING followed by an anchor link to https://developer.spotify.com/dashboard (target=_blank) using string PLUGIN_SPOTON_REGISTER_APP_LINK. Close with [% END %].
    - Text input: input type="text" class="stdedit" name="pref_clientId" id="pref_clientId" with value="[% customClientId | html %]" and placeholder using PLUGIN_SPOTON_CLIENT_ID_PLACEHOLDER string filter.
    - Close the WRAPPER with [% END %].

    Follow the existing WRAPPER pattern from the bitrate/normalization blocks exactly. Use | string filter for all string references and | html for value escaping.

    **strings.txt:** Append 5 new string keys at the end of the file (after PLUGIN_SPOTON_CONNECT_HINT_ALT). Each key has DE and EN translations with tab indentation per the existing format:

    - PLUGIN_SPOTON_CLIENT_ID_SETTINGS -- DE: "Eigene Spotify App", EN: "Custom Spotify App"
    - PLUGIN_SPOTON_CLIENT_ID_DESC -- DE: "Optionale eigene Spotify Developer App-ID fuer hoehere Rate-Limits", EN: "Optional custom Spotify Developer App ID for higher rate limits"
    - PLUGIN_SPOTON_CLIENT_ID_PLACEHOLDER -- DE: "Spotify Client-ID (32 Zeichen, alphanumerisch)", EN: "Spotify Client ID (32 characters, alphanumeric)"
    - PLUGIN_SPOTON_DEGRADED_MODE_WARNING -- DE: "Keine eigene Spotify App-ID konfiguriert. Fuer volle Funktionalitaet bitte eigene App registrieren:", EN: "No custom Spotify App ID configured. For full functionality, please register your own app:"
    - PLUGIN_SPOTON_REGISTER_APP_LINK -- DE: "Eigene App registrieren", EN: "Register your own app"

    NOTE: Use proper German umlauts in the actual strings (the action above uses ASCII for clarity). Actual German strings use: "fuer" -> "fuer" is wrong, use the actual UTF-8 umlaut characters: ue = u with umlaut, oe = o with umlaut. So: "fuer hoehere" becomes the proper German with umlauts, "Fuer volle Funktionalitaet" likewise with umlauts.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c 'PLUGIN_SPOTON_CLIENT_ID' Plugins/SpotOn/strings.txt && grep -c 'pref_clientId' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'PLUGIN_SPOTON_CLIENT_ID' Plugins/SpotOn/strings.txt returns >= 3 (SETTINGS, DESC, PLACEHOLDER)
    - grep -c 'PLUGIN_SPOTON_DEGRADED_MODE_WARNING' Plugins/SpotOn/strings.txt returns >= 1
    - grep -c 'PLUGIN_SPOTON_REGISTER_APP_LINK' Plugins/SpotOn/strings.txt returns >= 1
    - Each new string key has both DE and EN translations
    - grep -c 'pref_clientId' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html returns >= 1
    - grep -c 'degradedMode' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html returns >= 1
    - grep -c 'developer.spotify.com/dashboard' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html returns >= 1
    - grep -c 'customClientId' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html returns >= 1
    - The Client-ID block appears between the NORMALIZATION block and the ZEROCONF block in basic.html
    - strings.txt has no parse errors (no tabs-vs-spaces issues, consistent format with existing entries)
  </acceptance_criteria>
  <done>basic.html contains Client-ID input field with conditional degraded-mode warning banner linking to developer.spotify.com/dashboard. strings.txt contains all 5 new string keys (CLIENT_ID_SETTINGS, CLIENT_ID_DESC, CLIENT_ID_PLACEHOLDER, DEGRADED_MODE_WARNING, REGISTER_APP_LINK) with EN and DE translations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Settings.pm | User-supplied clientId from HTML form POST |
| Settings.pm -> Prefs | Sanitized clientId stored in LMS preferences |
| Prefs -> TokenManager.pm | clientId read by _fetchKeymasterToken for --client-id flag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.4-01 | Tampering | Settings.pm clientId input | mitigate | Strip all non-alphanumeric chars, truncate to 32 chars before $prefs->set. Spotify Client-IDs are exactly 32 hex chars -- regex s/[^a-zA-Z0-9]//g + substr(0,32) eliminates injection vectors. |
| T-04.4-06 | Information Disclosure | basic.html degraded-mode | accept | Warning text reveals that no custom Client-ID is set. This is intentional UI feedback, not sensitive information. |
| T-04.4-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase -- all modules are LMS-bundled |
</threat_model>

<verification>
1. perl -c Plugins/SpotOn/Settings.pm exits 0
2. grep -c 'PLUGIN_SPOTON_CLIENT_ID' Plugins/SpotOn/strings.txt returns >= 3
3. grep -c 'pref_clientId' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html returns >= 1
4. grep -c '_isDegradedMode' Plugins/SpotOn/Settings.pm returns >= 2
</verification>

<success_criteria>
- Settings page has Client-ID input field
- Degraded-mode warning visible when no custom ID configured
- Warning links to developer.spotify.com/dashboard
- clientId pref sanitized to [a-zA-Z0-9]{0,32}
- All 5 new strings present in EN and DE
- Settings.pm compiles cleanly
</success_criteria>

<output>
Create `.planning/phases/04.4-dual-token-api-routing/04.4-02-SUMMARY.md` when done
</output>
