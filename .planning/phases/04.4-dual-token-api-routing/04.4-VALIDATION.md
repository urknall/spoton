---
phase: 04.4
slug: dual-token-api-routing
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-29
---

# Phase 04.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual / Live-Test gegen Spotify Web API + LMS Log-Analyse |
| **Config file** | none — Perl LMS-Plugin, kein Testharness |
| **Quick run command** | `grep -i "spoton" /path/to/lms/server.log \| grep -E "429\|403\|bundled\|own"` |
| **Full suite command** | Manuelle Prüfung via LMS Web-UI + Log-Analyse |
| **Estimated runtime** | ~60 seconds (manuelle Verifizierung) |

---

## Sampling Rate

- **After every task commit:** Run quick log grep
- **After every plan wave:** Full manual verification via LMS UI
- **Before `/gsd:verify-work`:** All verification steps must pass
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | API-01 | — | All HTTP egress through Client.pm | structural | `grep -r "SimpleAsyncHTTP" Plugins/SpotOn/ \| grep -v Client.pm` | ⬜ | ⬜ pending |
| TBD | TBD | TBD | API-02 | — | Per-flavor rate-limit keys | integration | LMS log: 429 on bundled does not block me/* | ⬜ | ⬜ pending |
| TBD | TBD | TBD | API-03 | — | Cache-key without flavor scope for browse/* | integration | LMS log: cache hit for browse/* | ⬜ | ⬜ pending |
| TBD | TBD | TBD | D-05 | T-04.4-01 | me/* never uses bundled token | integration | LMS log: me/* calls always show flavor=own | ⬜ | ⬜ pending |
| TBD | TBD | TBD | D-02 | T-04.4-02 | Client-ID input validated (alphanumeric, max 32) | manual | Settings page: enter invalid ID, verify rejection | ⬜ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — no test framework to install.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| me/* endpoints always use own-token | D-05 | Requires live Spotify API call + log analysis | 1. Browse Library → 2. Check log for `flavor=own` on me/* calls |
| browse/categories uses bundled-token | SC-2 | Requires live Spotify API call | 1. Browse categories → 2. Check log for `flavor=bundled` |
| 403/410 fallback triggers bundled retry | D-06 | Requires simulated/live 403 response | 1. Force own-token 403 → 2. Verify bundled retry in log |
| Hint-cache persists 24h | D-06 | Requires cache inspection | 1. Trigger bundled hint → 2. Check cache key exists |
| Settings shows Client-ID field | D-02 | Visual verification | 1. Open Settings → 2. Verify field present |
| Degraded-mode warning visible | D-03 | Visual verification | 1. Remove custom Client-ID → 2. Verify warning banner |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
