---
phase: 04.4-dual-token-api-routing
verified: 2026-05-29T13:00:00Z
status: human_needed
score: 10/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Dual-Token-Routing im LMS-Kontext testen: me/tracks-Request löst own-Token aus, browse/categories löst bundled-Token aus"
    expected: "Log-Einträge zeigen [flavor=own] für me/*, [flavor=bundled] für browse/categories nach erstem 403/410-Fallback"
    why_human: "main::INFOLOG-Konstante und Slim::*-Module nur im laufenden LMS verfügbar — perl -c außerhalb LMS schlägt fehl. Routing-Logik ist nicht automatisch prüfbar ohne LMS-Instanz."
  - test: "Settings-UI im Browser öffnen: Client-ID-Feld und Degraded-Mode-Banner sichtbar"
    expected: "Eingabefeld mit Placeholder 'Spotify Client ID (32 characters, alphanumeric)'; orange Banner mit developer.spotify.com/dashboard-Link wenn kein clientId konfiguriert"
    why_human: "LMS-Template-Rendering (Template Toolkit WRAPPER-Blocks) ist nicht ohne laufenden LMS-Server testbar"
  - test: "Client-ID eingeben, speichern, erneut öffnen: Wert bleibt, Banner verschwindet"
    expected: "Alphanumerische 32-Zeichen-ID wird gespeichert; degradedMode=0 → kein oranges Banner mehr"
    why_human: "Prefs-Lifecycle und SUPER::handler-Interaction (CR-01-Fix) nur im LMS-Kontext verifizierbar"
  - test: "429 auf me/tracks auslösen, dann browse/categories aufrufen: bundle-Token-Requests laufen weiter"
    expected: "spoton_rate_limit_own ist gesetzt, aber spoton_rate_limit_bundled bleibt frei; browse/categories-Request läuft durch"
    why_human: "Per-Flavor-Rate-Limit-Isolation (D-01 / CR-02-Fix) erfordert echte HTTP-Antworten vom Spotify API"
---

# Phase 04.4: Dual-Token API Routing Verifikationsbericht

**Phase-Ziel:** Dual-Token API Routing — own-token (eigene Client-ID) für me/* und search-Endpunkte, bundled-token (librespot-Default-ID) für deprecated browse-Endpunkte und kuratierte Playlists. 403/410-Fallback mit Hint-Cache, per-Flavor-Rate-Limiting, me/*-Hard-Guard. Settings-UI für Client-ID.
**Verifiziert:** 2026-05-29T13:00:00Z
**Status:** human_needed
**Re-Verifikation:** Nein — initiale Verifikation

---

## Ergebnis: Ziel erreicht (10/10 Must-Haves verifiziert — Human-UAT ausstehend)

Alle automatisch prüfbaren Must-Haves sind in der tatsächlichen Codebasis vollständig implementiert und verdrahtet. Der Code-Review (04.4-REVIEW.md) hat drei kritische Probleme (CR-01, CR-02, CR-03) identifiziert — **alle drei wurden im tatsächlichen Code behoben**, nicht nur dokumentiert. Human-UAT ist erforderlich für LMS-spezifisches Verhalten (perl -c außerhalb LMS schlägt aufgrund von main::INFOLOG und Slim::*-Stubs fehl).

---

## Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-05: me/* API-Calls verwenden immer own-Token — harter Guard, niemals bundled-Fallback | VERIFIED | `_resolveStartFlavor` gibt sofort 'own' zurück wenn `$isMeFamily=1` (Client.pm:316). Fallback-Bedingung in `_doFlavouredRequest` enthält `!$isMeFamily` (Client.pm:453) — me/* kann strukturell nicht auf bundled fallen |
| 2 | D-04: browse/categories und kuratierte Playlists (37i9*) verwenden bundled-Token | VERIFIED | `@KNOWN_DEPRECATED_FAMILIES` enthält `qr{^browse/categories\b}` und `qr{^playlists/37i9[A-Za-z0-9]+\b}` (Client.pm:52-59). Nach erstem Erfolg via bundled-Retry schreibt `_rememberBundledHint` 24h-Hint → alle Folgerequests direkt zu bundled |
| 3 | D-06: 403/410 auf own-Token für non-me/*-Pfade löst automatischen bundled-Retry aus, Hint 24h gecacht | VERIFIED | Fallback-Bedingung in `_doFlavouredRequest` (Client.pm:453-461): `!$isRetry && $flavor eq 'own' && !$isMeFamily && ($code == 403 || $code == 410 || ...)`. Hint-Schreiben nur auf confirmed 2xx bundled-Erfolg (Client.pm:405-406) |
| 4 | D-01: 429 auf own-Token blockiert nur own-Paths, 429 auf bundled blockiert nur bundled-Paths — CR-02-Fix verifiziert | VERIFIED | In `_doFlavouredRequest` 429-Handler (Client.pm:432-435): nur `spoton_rate_limit_bundled` oder `spoton_rate_limit_own` gesetzt — `RATE_LIMIT_CACHE_KEY` wird NICHT mehr gesetzt. Rate-Limit-Check in `_request` (Client.pm:276-279) testet nur den flavor-spezifischen Key |
| 5 | D-02: SPOTON_DEFAULT_CLIENT_ID-Konstante als Default, User-Override via Prefs clientId | VERIFIED | `SPOTON_DEFAULT_CLIENT_ID => ''` in TokenManager.pm:25 und Client.pm:32. `_fetchKeymasterToken` liest `$prefs->get('clientId') \|\| SPOTON_DEFAULT_CLIENT_ID` (TokenManager.pm:360). `_resolveStartFlavor` prüft identisch (Client.pm:323) |
| 6 | Beide Token-Flavors refreshen unabhängig via separate _fetchKeymasterToken-Calls | VERIFIED | `refreshAllTokens` ruft `_fetchKeymasterToken($id, 'own', ...)` auf (TokenManager.pm:123). Bundled-Tokens sind lazy-only (on-demand). `_fetchKeymasterToken` dispatcht `--client-id`-Flag nur für `$flavor eq 'own'` mit truthy Client-ID (TokenManager.pm:355-368) |
| 7 | D-02: Settings-Seite zeigt Client-ID-Eingabefeld | VERIFIED | `basic.html:26-38`: WRAPPER-Block mit `pref_clientId`-Eingabe, `[% customClientId \| html %]`-Value und Placeholder-String |
| 8 | D-03: Degraded-Mode-Warnung erscheint wenn keine Client-ID konfiguriert — mit Link zu developer.spotify.com/dashboard | VERIFIED | `basic.html:27-33`: `[% IF degradedMode %]` zeigt orange div mit `PLUGIN_SPOTON_DEGRADED_MODE_WARNING` und `<a href="https://developer.spotify.com/dashboard">`. `_isDegradedMode()` in Settings.pm:256-258 gibt 1 zurück wenn `clientId` falsy |
| 9 | D-02: Client-ID wird zu alphanumerisch max 32 Zeichen sanitized vor Persistenz | VERIFIED | Settings.pm:76-77: `$id =~ s/[^a-zA-Z0-9]//g` + `substr($id, 0, 32)`. CR-01-Fix verifiziert: `prefs()` (Settings.pm:46) gibt `('bitrate', 'binary', 'normalization')` zurück — 'clientId' FEHLT, verhindert SUPER::handler-Overwrite mit Raw-Input |
| 10 | Alle neuen Strings korrekt in EN und DE | VERIFIED | strings.txt:193-211: PLUGIN_SPOTON_CLIENT_ID_SETTINGS, CLIENT_ID_DESC, CLIENT_ID_PLACEHOLDER, DEGRADED_MODE_WARNING, REGISTER_APP_LINK — alle mit DE und EN Übersetzungen |

**Score: 10/10 Truths verifiziert**

---

## Deferred Items

Keine — alle Must-Haves sind in Phase 04.4 implementiert.

---

## Required Artifacts

| Artifact | Erwartet | Status | Details |
|----------|----------|--------|---------|
| `Plugins/SpotOn/API/TokenManager.pm` | Flavor-aware getToken/cacheToken/removeAccount | VERIFIED | `SPOTON_DEFAULT_CLIENT_ID` >= 3x, `spoton_token_.*_(own\|bundled)` 4x, getToken backward-compat via `ref $flavorOrCb eq 'CODE'` |
| `Plugins/SpotOn/API/Client.pm` | Dual-Token-Routing-Pipeline mit `_meFamilyRegex`, Hint-Cache, per-Flavor-RL | VERIFIED | `_meFamilyRegex` 2x, `KNOWN_DEPRECATED_FAMILIES` 4x, `_doFlavouredRequest` 7x, `userCbCalled` 2x, `flavor` 47x, Bearer 1x (nur HTTP-Header) |
| `Plugins/SpotOn/Settings.pm` | clientId-Pref save/load mit Sanitization, `_isDegradedMode`, Template-Vars | VERIFIED | `clientId` 6x, `_isDegradedMode` 2x, `customClientId` 1x, `degradedMode` 1x, Sanitization-Regex + substr vorhanden |
| `Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` | Client-ID-Eingabefeld mit Degraded-Mode-Banner | VERIFIED | `pref_clientId` 1x, `degradedMode` 1x, `developer.spotify.com/dashboard` 1x, `customClientId` 1x |
| `Plugins/SpotOn/strings.txt` | 5 neue String-Keys EN+DE | VERIFIED | CLIENT_ID_SETTINGS, CLIENT_ID_DESC, CLIENT_ID_PLACEHOLDER, DEGRADED_MODE_WARNING, REGISTER_APP_LINK — alle mit DE+EN |

---

## Key Link Verification

| Von | Nach | Via | Status | Details |
|-----|------|-----|--------|---------|
| `Client.pm` | `TokenManager.pm` | `getToken($accountId, $flavor, $cb)` | VERIFIED | Client.pm:337: `TokenManager->getToken($accountId, $flavor, sub {...})` |
| `Client.pm` | `Slim::Utils::Cache` | `spoton_rate_limit_own / spoton_rate_limit_bundled` | VERIFIED | Client.pm:276-277 (check), 435 (set in 429-Handler) — kein globaler RATE_LIMIT_CACHE_KEY mehr gesetzt (CR-02-Fix) |
| `Settings.pm` | `Slim::Utils::Prefs` | `$prefs->set('clientId', $id)` | VERIFIED | Settings.pm:78: `$prefs->set('clientId', $id)` nach Sanitization |
| `basic.html` | `Settings.pm` | Template-Vars `customClientId`, `degradedMode` | VERIFIED | Settings.pm:157-158 setzt beide Vars; basic.html:27,36 konsumiert sie |

---

## Data-Flow Trace (Level 4)

| Artifact | Datenvariable | Quelle | Produziert echte Daten | Status |
|----------|--------------|--------|----------------------|--------|
| `basic.html: pref_clientId.value` | `customClientId` | `$prefs->get('clientId')` | Ja — live prefs read | FLOWING |
| `basic.html: [% IF degradedMode %]` | `degradedMode` | `_isDegradedMode()` → `$prefs->get('clientId')` | Ja — live prefs read | FLOWING |
| `_doFlavouredRequest: token` | `$token` | `TokenManager->getToken($accountId, $flavor, sub{...})` | Ja — librespot --get-token subprocess | FLOWING |
| `_request: $startFlavor` | `$startFlavor` | `_resolveStartFlavor` → prefs + hint cache | Ja — live prefs + LMS-Cache | FLOWING |

---

## Behavioral Spot-Checks

Step 7b: TEILWEISE AUSGEFÜHRT — `perl -c` außerhalb LMS schlägt fehl wegen `main::INFOLOG` (LMS-Konstante, nicht im System-Perl) und fehlender `Exporter::Lite`-Abhängigkeit in `Slim::Networking::SimpleHTTP::Base`. Dies ist eine bekannte LMS-Plugin-Einschränkung.

| Verhalten | Befehl | Ergebnis | Status |
|-----------|--------|----------|--------|
| Settings.pm kompiliert | `perl -I/tmp/ubAOWLo41W -c Settings.pm` | `syntax OK` | PASS |
| TokenManager.pm strukturell korrekt | `perl -I/tmp/ubAOWLo41W -c TokenManager.pm` | Nur `main::INFOLOG`-Bareword-Fehler (LMS-Muster) — keine strukturellen Fehler | UNCERTAIN (LMS-Kontext fehlt) |
| Client.pm strukturell korrekt | `perl -I/tmp/ubAOWLo41W -c Client.pm` | `Exporter::Lite` fehlt (LMS-intern) — keine strukturellen Fehler | UNCERTAIN (LMS-Kontext fehlt) |
| Commit-Hashes vorhanden | `git log --oneline d33d4fa 2911686 e79b93a a1f725c` | Alle 4 Commits gefunden | PASS |
| Bearer nur einmal (nicht geloggt) | `grep -v '#' Client.pm | grep -c 'Bearer'` | 1 — nur HTTP-Header | PASS |
| $inflightCount-- nicht in _onSuccess/_onError | awk-Suche | Nicht gefunden — nur in $userCb-Guard | PASS |
| RATE_LIMIT_CACHE_KEY nicht in cache->set() | grep | Nur als Konstante definiert, nie gesetzt (CR-02-Fix) | PASS |

---

## Probe Execution

Step 7c: ÜBERSPRUNGEN — keine Probe-Skripte in `scripts/*/tests/probe-*.sh` für Phase 04.4 definiert oder konventionell vorhanden.

---

## Requirements Coverage

| Requirement | PLAN | Beschreibung | Status | Evidence |
|-------------|------|-------------|--------|---------|
| API-01 | 04.4-01, 04.4-02 | Central HTTP client (`API/Client.pm`) als einziger HTTP-Egress-Punkt | SATISFIED | `grep -r 'SimpleAsyncHTTP' Plugins/SpotOn/` zeigt nur Client.pm, TokenManager.pm (/me-Fetch), Settings.pm (AJAX) — kein weiterer Egress |
| API-02 | 04.4-01, 04.4-02 | Rate-Limiting mit per-Flavor-Isolation | SATISFIED | Per-Flavor-Keys `spoton_rate_limit_own/bundled` implementiert; Concurrency-Cap `MAX_CONCURRENT_REQUESTS=3` bleibt aktiv |
| API-03 | 04.4-01 | Response-Caching mit domain-spezifischen TTLs | SATISFIED | Cache-Key enthält NICHT den Flavor (Client.pm:353) — gleiche Response unabhängig vom Token. `_cacheTTL` unverändert |

**Hinweis zu REQUIREMENTS.md-Traceability:** API-01/02/03 sind in REQUIREMENTS.md als "Phase 2: Pending" markiert. Dies spiegelt den ursprünglichen Traceability-Status wider — die Implementierung ist in Phase 04.4 (nicht Phase 2) erfolgt. Die REQUIREMENTS.md-Traceability-Tabelle sollte aktualisiert werden, ist aber kein Blocker für diese Phase.

---

## Anti-Pattern-Scan

| Datei | Zeile | Muster | Schwere | Auswirkung |
|-------|-------|--------|---------|------------|
| Client.pm | 532, 561 | Dead code: `_onSuccess` und `_onError` (WR-02 aus REVIEW) | WARNUNG | Zwei benannte Subs (150 Zeilen) sind nie aufgerufen — nur via Comment referenziert. Kein funktionaler Fehler, aber Wartungsrisiko. Kein TBD/FIXME-Marker — kein Blocker |
| TokenManager.pm, Client.pm | 25, 32 | `SPOTON_DEFAULT_CLIENT_ID => ''` doppelt definiert (WR-03 aus REVIEW) | WARNUNG | Duplikat-Konstante — bei Release-Befüllung muss an zwei Stellen geändert werden. Explizit als Known Stub in Summary dokumentiert — kein Blocker |
| TokenManager.pm | 25 | `SPOTON_DEFAULT_CLIENT_ID => ''` — Placeholder für SpotOn App-ID | INFO | Explizit dokumentierter Stub; D-03-Degraded-Mode greift korrekt wenn leer. Kein TBD/FIXME |

Keine TBD/FIXME/XXX-Marker in einem der phasenmodifizierten Dateien gefunden.

---

## Code-Review-Befund-Abgleich (CR-01, CR-02, CR-03)

Der Code-Review hatte drei kritische Probleme gefunden. Verifizierungsergebnis:

| CR-ID | Problem | Status im Code | Evidence |
|-------|---------|----------------|---------|
| CR-01 | SUPER::handler überschreibt sanitizertes clientId | **BEHOBEN** | `prefs()` (Settings.pm:46) gibt nur `('bitrate', 'binary', 'normalization')` zurück — 'clientId' fehlt bewusst (Kommentar erklärt Grund) |
| CR-02 | RATE_LIMIT_CACHE_KEY kontaminiert Flavor-isolierte Rate-Limits | **BEHOBEN** | In 429-Handler: nur `spoton_rate_limit_own` oder `spoton_rate_limit_bundled` gesetzt. `RATE_LIMIT_CACHE_KEY` nur noch als Konstante (niemals `$cache->set`) |
| CR-03 | Non-me-Pfade prüfen nicht own-Token-Rate-Limit | **BEHOBEN** | `_resolveStartFlavor` wird VOR Rate-Limit-Check ausgeführt (Client.pm:272 vor 277). Rate-Limit-Check (Client.pm:276-279) verwendet `$startFlavor` vom Resolver — no pre-resolution check nötig |

---

## Human-UAT Erforderlich

### 1. LMS-Kompilierung verifizieren

**Test:** SpotOn-Plugin in laufendem LMS laden (Plugin-Neu-Scan oder LMS-Neustart)
**Erwartet:** Plugin lädt ohne Fehler im LMS-Log; keine `Can't locate`-Fehler
**Warum Human:** `perl -c` schlägt außerhalb LMS fehl wegen `main::INFOLOG`-Konstante und LMS-internen Modulen (`Slim::*`)

### 2. Dual-Token-Routing in LMS-Logs beobachten

**Test:** Nach LMS-Laden mit konfigurierter clientId: SpotOn öffnen → Liked Songs → irgendeine Kategorie öffnen
**Erwartet:** `[flavor=own]` in Log-Einträgen für `me/tracks`, `[flavor=bundled]` nach erstem 403/410-Fallback für `browse/categories/*/playlists`
**Warum Human:** Routing-Logik und Logging nur im LMS-Kontext mit echten Spotify-API-Antworten beobachtbar

### 3. Settings-UI: Degraded-Mode-Banner und Client-ID-Feld

**Test:** LMS Settings → SpotOn öffnen ohne konfigurierte clientId
**Erwartet:** Orange Banner "No custom Spotify App ID configured..." mit Link zu developer.spotify.com/dashboard; Eingabefeld mit korrektem Placeholder
**Warum Human:** LMS Template Toolkit Rendering nur im Browser testbar

### 4. Client-ID speichern und CR-01-Fix bestätigen

**Test:** 32-stellige hex-ID eingeben, speichern, neu laden
**Erwartet:** ID bleibt erhalten, Banner verschwindet; raw Sonderzeichen werden herausgefiltert
**Warum Human:** SUPER::handler-Interaction und prefs-Lifecycle nur im LMS-Kontext verifizierbar

---

## Gaps-Zusammenfassung

Keine blockierenden Gaps. Alle Must-Haves sind implementiert und verifiziert. Die Human-Verifikation ist für LMS-spezifisches Laufzeitverhalten erforderlich, nicht für Codedefekte.

**Warnungen (nicht blockierend):**
- WR-02: `_onSuccess` und `_onError` sind Dead Code — 150 Zeilen nicht erreichbarer Code. Empfehlung: in einer Aufräum-Phase entfernen.
- WR-03: `SPOTON_DEFAULT_CLIENT_ID` doppelt definiert — vor Release-Befüllung zusammenführen.

---

_Verifiziert: 2026-05-29T13:00:00Z_
_Verifier: Claude (gsd-verifier)_
