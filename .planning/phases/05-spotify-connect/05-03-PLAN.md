---
phase: 05-spotify-connect
plan: 03
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - Plugins/SpotOn/Connect/DaemonManager.pm
  - Plugins/SpotOn/Plugin.pm
autonomous: true
requirements:
  - CON-01
  - CON-02
  - CON-06
  - CON-09
  - CON-10
  - CON-15

must_haves:
  truths:
    - "One Connect daemon runs per connected LMS player with Connect enabled"
    - "Sync groups run a single daemon on the master player with concatenated device name"
    - "Watchdog timer restarts crashed daemons every 60 seconds"
    - "Sync-group changes trigger differential restart with 0.1s micro-delay"
    - "Connect daemon PIDs are excluded from _killOrphanedProcesses cleanup"
    - "Plugin.pm initPlugin starts Connect daemon manager at LMS boot"
  artifacts:
    - path: "Plugins/SpotOn/Connect/DaemonManager.pm"
      provides: "Daemon lifecycle, sync groups, watchdog"
      min_lines: 120
      exports: ["init", "initHelpers", "helperForClient", "streamPortForClient", "helperPids"]
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "Connect init in initPlugin, CON-09 PID exclusion"
      contains: "Connect"
  key_links:
    - from: "DaemonManager.pm initHelpers()"
      to: "Daemon.pm new()"
      via: "startHelper creates Daemon instance"
      pattern: "Plugins::SpotOn::Connect::Daemon->new"
    - from: "Plugin.pm _killOrphanedProcesses()"
      to: "DaemonManager.pm helperPids()"
      via: "PID exclusion before pgrep kill"
      pattern: "helperPids"
    - from: "Plugin.pm initPlugin()"
      to: "Connect.pm init()"
      via: "deferred timer startup"
      pattern: "Connect->init"
---

<objective>
Create DaemonManager.pm for daemon lifecycle management and wire Connect startup + PID protection into Plugin.pm.

Purpose: DaemonManager manages one Connect daemon per player (or sync-group master), with watchdog restart, sync-group awareness, and per-player enable/disable. Plugin.pm starts Connect at LMS boot and protects daemon PIDs from orphaned-process cleanup.
Output: DaemonManager.pm, updated Plugin.pm with Connect init and CON-09 fix.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/05-spotify-connect/05-CONTEXT.md
@/home/sti/spoton/.planning/phases/05-spotify-connect/05-PATTERNS.md

@/home/sti/spoton/.planning/phases/05-spotify-connect/05-01-SUMMARY.md
@/home/sti/spoton/.planning/phases/05-spotify-connect/05-02-SUMMARY.md

<interfaces>
<!-- From Daemon.pm (Plan 02 output): -->
<!-- Plugins::SpotOn::Connect::Daemon->new($clientId) — creates and starts daemon -->
<!-- $daemon->alive() — returns 1 if process running -->
<!-- $daemon->pid() — returns process PID -->
<!-- $daemon->stop() — kills process -->
<!-- $daemon->stopForSync() — kills process and resets counters -->
<!-- $daemon->_streamPort() — returns HTTP stream port -->
<!-- $daemon->_streamMode() — returns 1 if streaming active -->
<!-- $daemon->uptime() — seconds since last start -->

<!-- From Plugin.pm (existing, will be modified): -->
<!-- sub initPlugin — line 33, currently starts token refresh + discovery auto-start -->
<!-- sub _killOrphanedProcesses — line 120, has PHASE-5-NOTE at line 156 -->
<!-- sub updateTranscodingTable — line 1186, iterates son-* entries -->
<!-- $prefs = preferences('plugin.spoton') -->
<!-- $prefs->client($client)->get('activeAccount') — per-player account -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DaemonManager.pm — daemon lifecycle and sync groups</name>
  <files>Plugins/SpotOn/Connect/DaemonManager.pm</files>
  <read_first>
    - /home/sti/spotty-ng/Spotty-Plugin/Connect/DaemonManager.pm (full 337-line reference — init, initHelpers, startHelper, stopHelper, shutdown, helperForClient, streamPortForClient, helperPids)
    - /home/sti/spoton/.planning/phases/05-spotify-connect/05-PATTERNS.md (DaemonManager.pm section — all patterns with SpotOn deviations)
    - /home/sti/spoton/.planning/phases/05-spotify-connect/05-RESEARCH.md (Architecture Patterns, Pattern 5)
    - Plugins/SpotOn/Connect/Daemon.pm (Task 2 from Plan 02 — accessor methods, constructor signature)
  </read_first>
  <action>
    Create Plugins/SpotOn/Connect/DaemonManager.pm adapting Spotty-NG DaemonManager.pm.

    **Package:** Plugins::SpotOn::Connect::DaemonManager

    **Constants:** DAEMON_INIT_DELAY = 2, DAEMON_WATCHDOG_INTERVAL = 60, STREAM_WATCHDOG_INTERVAL = 5.

    **Imports:** strict, Scalar::Util blessed, Slim::Utils::Log, Slim::Utils::Prefs, Slim::Utils::Timers, Plugins::SpotOn::Plugin, Plugins::SpotOn::Connect::Daemon.

    **State:** my %helperInstances (hash of clientId => Daemon instances), $prefs = preferences('plugin.spoton'), $log = logger('plugin.spoton').

    **init():** Class method. Subscribe to [['client'], ['new', 'disconnect']] events with debounced initHelpers (DAEMON_INIT_DELAY = 2s). Subscribe to [['sync']] events: on sync change, call stopForSync() on all existing helpers, then debounced initHelpers with 0.1s delay (CON-15 differential restart). React to enableSpotifyConnect pref changes via $prefs->setChange (D-10, CON-10).

    **initHelpers():** Class method. Kill existing timers. Call shutdown('inactive-only'). Iterate Slim::Player::Client::clients(). For each client: determine if it's a sync slave with a master that has Connect enabled. If client is sync master with Connect enabled, startHelper. If client is sync slave, stopHelper. If client is standalone with Connect enabled, startHelper. Otherwise stopHelper. Set 60s watchdog timer for next initHelpers call.

    **startHelper($class, $client):** Get clientId. If helper exists and is alive, return. If helper exists but dead, call start() to restart. Otherwise create new Daemon instance: Plugins::SpotOn::Connect::Daemon->new($client->id). Store in %helperInstances.

    **stopHelper($class, $client):** Get clientId. If helper exists, call stop(). Delete from %helperInstances.

    **shutdown($class, $mode):** If mode is 'inactive-only', stop only dead helpers. Otherwise stop all and clear hash.

    **helperForClient($class, $clientId):** Normalize $clientId via blessed check (accept object or string). Return $helperInstances{$clientId}.

    **streamPortForClient($class, $clientId):** Normalize clientId. Return helper->_streamPort if helper exists.

    **helperPids($class):** Return list of PIDs from all alive helper instances (CON-09). map { $_->pid } grep { $_->alive } values %helperInstances.

    **uptime($class, $clientId):** Return helper->uptime if helper exists for clientId, otherwise 0.

    SpotOn deviations per PATTERNS.md: No checkDaemonConnected (429 source), no forceFallbackAP, prefs under plugin.spoton.
  </action>
  <verify>
    <automated>perl -e 'do "Plugins/SpotOn/Connect/DaemonManager.pm"; print "OK\n" unless $@; print "ERROR: $@\n" if $@' 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Plugins/SpotOn/Connect/DaemonManager.pm exists with package Plugins::SpotOn::Connect::DaemonManager
    - DaemonManager::init() subscribes to [['client'], ['new', 'disconnect']] events
    - DaemonManager::init() subscribes to [['sync']] events with 0.1s debounce (CON-15)
    - DaemonManager::init() calls $prefs->setChange for enableSpotifyConnect (CON-10)
    - DaemonManager::initHelpers() iterates Slim::Player::Client::clients() and starts/stops based on sync status and Connect pref
    - DaemonManager::initHelpers() sets DAEMON_WATCHDOG_INTERVAL (60s) watchdog timer
    - DaemonManager::helperForClient() normalizes clientId via blessed check
    - DaemonManager::helperPids() returns list of alive daemon PIDs (CON-09)
    - DaemonManager::uptime() returns daemon uptime for given clientId
    - No checkDaemonConnected method exists (explicitly NOT ported from Spotty-NG)
    - Uses preferences('plugin.spoton') for prefs namespace
  </acceptance_criteria>
  <done>DaemonManager.pm manages daemon lifecycle with watchdog, sync-group master selection, differential restart on sync changes, and helperPids() for CON-09 exclusion.</done>
</task>

<task type="auto">
  <name>Task 2: Plugin.pm — Connect init at boot + CON-09 PID exclusion + updateTranscodingTable soc awareness</name>
  <files>Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (full file — focus on initPlugin() lines 33-100, _killOrphanedProcesses() lines 120-178, updateTranscodingTable() lines 1186-1249)
    - /home/sti/spoton/.planning/phases/05-spotify-connect/05-PATTERNS.md (Plugin.pm section — initPlugin extension, _killOrphanedProcesses fix, updateTranscodingTable soc)
    - /home/sti/spoton/.planning/phases/05-spotify-connect/05-CONTEXT.md (D-07 daemon start at init, D-10 per-player toggle)
  </read_first>
  <action>
    **initPlugin() extension:** After the existing _autoStartDiscovery timer setup (near line 95), add Connect daemon startup. Use deferred timer pattern (same as _autoStartDiscovery): Slim::Utils::Timers::setTimer with 3s delay to allow player list to populate after LMS start. The callback requires Plugins::SpotOn::Connect and calls Plugins::SpotOn::Connect->init(). Guard with if (main::WEBUI) to match existing pattern. Name the thin wrapper sub _startConnectDaemons (same pattern as _autoStartDiscovery and _refreshAllTokens).

    **_killOrphanedProcesses() CON-09 fix:** Replace the PHASE-5-NOTE comment block (line 156) with actual Connect daemon PID exclusion. Check if DaemonManager module is loaded via $INC{'Plugins/SpotOn/Connect/DaemonManager.pm'}. If loaded, call Plugins::SpotOn::Connect::DaemonManager->helperPids() to get list of active Connect PIDs. Build a hash %connectPids. In the existing kill loop, add "next if $connectPids{$pid}" guard before the kill statement.

    **updateTranscodingTable() soc awareness:** The existing loop guard `next if $key =~ /^son-/ && ...` correctly skips non-son entries. soc-* entries in the commandTable use "# I" (passthrough input) and contain NO "single-track" string, so they are already safe from the son-specific regex substitution. However, add an explicit guard for OGG-passthrough: after the existing guard that deletes son-ogg when binary lacks passthrough capability, add the same deletion for soc-ogg-*-*. Also add per-player connectOggOverride pref support: if player pref says 'pcm', delete soc-ogg-*-* for that player.

    **Add pref defaults:** In the $prefs->init({}) block (near line 23), add enableSpotifyConnect => 1 (default on for all players, CON-10) and connectOggOverride => 'auto' (D-05).
  </action>
  <verify>
    <automated>grep -c 'Connect' /home/sti/spoton/Plugins/SpotOn/Plugin.pm && grep -c 'connectPids\|helperPids' /home/sti/spoton/Plugins/SpotOn/Plugin.pm</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm initPlugin() contains a deferred timer that calls Plugins::SpotOn::Connect->init() (or equivalent thin wrapper)
    - Plugin.pm has sub _startConnectDaemons (or similar thin wrapper)
    - Plugin.pm _killOrphanedProcesses() checks $INC for DaemonManager.pm before calling helperPids
    - Plugin.pm _killOrphanedProcesses() has "next if $connectPids{$pid}" in the kill loop
    - Plugin.pm PHASE-5-NOTE comment is replaced with actual implementation
    - Plugin.pm updateTranscodingTable() deletes soc-ogg-*-* when binary lacks passthrough capability
    - Plugin.pm $prefs->init includes enableSpotifyConnect => 1 and connectOggOverride => 'auto'
    - No PHASE-5-NOTE comment remaining in the file
    - Existing _killOrphanedProcesses timer and son-* transcoding logic remain intact
  </acceptance_criteria>
  <done>Plugin.pm starts Connect daemons at LMS boot (D-07), protects Connect daemon PIDs from orphan cleanup (CON-09), and handles soc-ogg transcoding table entries for OGG-passthrough control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin.pm initPlugin | Auto-starts daemon processes — must be guarded by !main::SCANNER |
| _killOrphanedProcesses | Must NOT kill Connect daemons — PID exclusion is security-critical for availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | DoS | _killOrphanedProcesses kills Connect daemons | mitigate | helperPids() exclusion list checked before every kill (CON-09) |
| T-05-11 | DoS | Watchdog timer creates too many processes | mitigate | initHelpers checks alive() before starting new daemon; dead helpers get restarted, not duplicated |
| T-05-12 | Elevation | Connect init in SCANNER context | mitigate | Guarded by if (main::WEBUI) — SCANNER context never starts daemons |
| T-05-SC | Tampering | Module require | mitigate | All modules are project-local (Plugins::SpotOn::Connect::*) or LMS-bundled |
</threat_model>

<verification>
- DaemonManager.pm exists with correct package
- Plugin.pm contains Connect->init() or equivalent
- Plugin.pm _killOrphanedProcesses has helperPids integration
- PHASE-5-NOTE comment no longer present
- `grep -c 'enableSpotifyConnect' Plugin.pm` >= 1
</verification>

<success_criteria>
DaemonManager.pm manages per-player Connect daemons with sync-group awareness. Plugin.pm starts Connect at boot, protects daemon PIDs, and handles soc transcoding table entries. All 6 CON requirements addressed in this plan are implemented.
</success_criteria>

<output>
Create `.planning/phases/05-spotify-connect/05-03-SUMMARY.md` when done
</output>
