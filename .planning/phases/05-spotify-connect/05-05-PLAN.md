---
phase: 05-spotify-connect
plan: 05
type: execute
wave: 4
depends_on:
  - "05-04"
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
autonomous: false
requirements:
  - CON-10

must_haves:
  truths:
    - "Per-player Connect toggle checkbox in Settings UI (default: enabled) (D-10)"
    - "Per-player OGG-passthrough override select in Settings UI (Auto/OGG/PCM) (D-05)"
    - "All new UI strings display in English and German"
    - "Toggling Connect off for a player stops its daemon within one watchdog cycle"
  artifacts:
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "Per-player Connect toggle and OGG override persistence"
      contains: "enableSpotifyConnect"
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      provides: "Connect settings UI block with checkbox and select"
      contains: "pref_enableSpotifyConnect"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "i18n strings for Connect settings (EN + DE)"
      contains: "PLUGIN_SPOTON_CONNECT_ENABLED"
  key_links:
    - from: "Settings.pm handler()"
      to: "$prefs->client($client)->set('enableSpotifyConnect')"
      via: "per-player pref save"
      pattern: "enableSpotifyConnect"
---

<objective>
Add per-player Connect toggle and OGG-passthrough override to Settings UI with i18n strings.

Purpose: Users need to enable/disable Spotify Connect per player (CON-10, D-10) and control OGG-passthrough behavior (D-05). The DaemonManager already reacts to pref changes; this plan provides the UI.
Output: Updated Settings.pm, basic.html, strings.txt.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/05-spotify-connect/05-CONTEXT.md
@/home/sti/spoton/.planning/phases/05-spotify-connect/05-PATTERNS.md

@/home/sti/spoton/.planning/phases/05-spotify-connect/05-03-SUMMARY.md

<interfaces>
<!-- From Settings.pm (existing): -->
<!-- sub prefs { return ($prefs, 'bitrate', 'binary', 'clientId', 'normalization') } -->
<!-- handler() processes saveSettings: reads pref_* from $paramRef, saves via $prefs->set() -->
<!-- handler() passes template vars via $paramRef->{key} -->
<!-- Per-player pattern: $prefs->client($client)->get/set for player-specific values -->
<!-- Existing per-player example: activeAccount pref in handler() -->

<!-- From DaemonManager.pm (Plan 03): -->
<!-- DaemonManager::init() calls $prefs->setChange(\&initHelpers, 'enableSpotifyConnect') -->
<!-- Changing pref triggers daemon start/stop automatically -->

<!-- From basic.html (existing structure): -->
<!-- Bitrate select block -> Normalization checkbox -> Client-ID block -> ZeroConf section -> Accounts section -->
<!-- Checkbox pattern: <input type="checkbox" name="pref_X" value="1" [% IF Y %]checked[% END %]/> -->
<!-- Select pattern: <select name="pref_X"><option value="V" [% IF X == 'V' %]selected[% END %]>Label</option></select> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings.pm per-player Connect toggle and OGG override + basic.html UI + i18n strings</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/strings.txt</files>
  <read_first>
    - Plugins/SpotOn/Settings.pm (full file — handler() saveSettings block, template variable passing, existing per-player pref pattern with activeAccount)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (full file — existing checkbox and select patterns, block ordering)
    - Plugins/SpotOn/strings.txt (full file — existing string key format, EN+DE pattern, string ordering)
    - /home/sti/spoton/.planning/phases/05-spotify-connect/05-PATTERNS.md (Settings.pm and basic.html sections)
    - /home/sti/spoton/.planning/phases/05-spotify-connect/05-CONTEXT.md (D-05 OGG override, D-10 per-player toggle)
  </read_first>
  <action>
    **Settings.pm handler() extension:** In the saveSettings block, after existing pref saves (bitrate, normalization, clientId), add per-player Connect prefs when $client is available:

    1. Read pref_enableSpotifyConnect from $paramRef. Save as 0 or 1 via $prefs->client($client)->set('enableSpotifyConnect', $val). Checkbox unchecked = not in params = 0.

    2. Read pref_connectOggOverride from $paramRef. Validate against whitelist (auto|ogg|pcm), default to 'auto'. Save via $prefs->client($client)->set('connectOggOverride', $val).

    In the template variable block, pass to template:
    - connectEnabled => $prefs->client($client)->get('enableSpotifyConnect') // 1 (default on)
    - connectOggOverride => $prefs->client($client)->get('connectOggOverride') || 'auto'

    Guard the per-player block with "if ($client)" to avoid errors on global settings page (no player selected).

    **basic.html — Add Connect settings block:** Insert a new settings section between the Client-ID block and the ZeroConf section. Use the WRAPPER setting pattern matching existing blocks:

    1. Connect enabled checkbox: WRAPPER setting with title PLUGIN_SPOTON_CONNECT_ENABLED, desc PLUGIN_SPOTON_CONNECT_ENABLED_DESC. Input type=checkbox, name=pref_enableSpotifyConnect, value=1, checked if connectEnabled. Label with PLUGIN_SPOTON_CONNECT_ENABLED_LABEL.

    2. OGG-passthrough override select: WRAPPER setting with title PLUGIN_SPOTON_CONNECT_OGG_OVERRIDE. Select with 3 options: Auto (value=auto), OGG Passthrough (value=ogg), PCM Force (value=pcm). Selected based on connectOggOverride.

    Wrap both in a [% IF playercount %] guard so they only show when a player is selected (per-player settings).

    **strings.txt — Add 6 new string keys** before the existing SON entry (which must remain last):
    - PLUGIN_SPOTON_CONNECT_ENABLED: DE "Spotify Connect", EN "Spotify Connect"
    - PLUGIN_SPOTON_CONNECT_ENABLED_DESC: DE "Spotify Connect fuer diesen Player aktivieren", EN "Enable Spotify Connect for this player"
    - PLUGIN_SPOTON_CONNECT_ENABLED_LABEL: DE "Spotify Connect aktivieren", EN "Enable Spotify Connect"
    - PLUGIN_SPOTON_CONNECT_OGG_OVERRIDE: DE "Audio-Format (Connect)", EN "Audio Format (Connect)"
    - PLUGIN_SPOTON_CONNECT_OGG_OVERRIDE_DESC: DE "Audio-Format fuer Spotify Connect Wiedergabe", EN "Audio format for Spotify Connect playback"
    - PLUGIN_SPOTON_CONNECT_OGG_AUTO: DE "Automatisch (Player-abhaengig)", EN "Auto (player dependent)"

    Follow existing string key format: indented DE and EN blocks with tab indentation.
  </action>
  <verify>
    <automated>grep -c 'enableSpotifyConnect' /home/sti/spoton/Plugins/SpotOn/Settings.pm && grep -c 'pref_enableSpotifyConnect' /home/sti/spoton/Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html && grep -c 'PLUGIN_SPOTON_CONNECT' /home/sti/spoton/Plugins/SpotOn/strings.txt</automated>
  </verify>
  <acceptance_criteria>
    - Settings.pm handler() saves enableSpotifyConnect as per-player pref via $prefs->client($client)->set
    - Settings.pm handler() saves connectOggOverride as per-player pref with whitelist validation (auto|ogg|pcm)
    - Settings.pm passes connectEnabled and connectOggOverride template variables
    - Settings.pm per-player block guarded by "if ($client)"
    - basic.html has checkbox named pref_enableSpotifyConnect with checked condition on connectEnabled
    - basic.html has select named pref_connectOggOverride with 3 options (auto, ogg, pcm)
    - basic.html Connect section wrapped in [% IF playercount %] guard
    - basic.html Connect section positioned between Client-ID and ZeroConf blocks
    - strings.txt has all 6 new PLUGIN_SPOTON_CONNECT_* keys with both DE and EN translations
    - strings.txt SON entry remains the last entry in the file
    - No duplicate string keys in strings.txt
  </acceptance_criteria>
  <done>Settings UI shows per-player Connect toggle and OGG-passthrough override with full i18n support.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — End-to-end Spotify Connect verification</name>
  <action>Human verifies complete Spotify Connect integration across all plans.</action>
  <what-built>
    Complete Spotify Connect integration:
    - Binary --connect mode with Spirc, HTTP streaming, control endpoints
    - Daemon.pm process wrapper, DaemonManager.pm lifecycle manager
    - Connect.pm event dispatch with bidirectional control
    - ProtocolHandler.pm soc routing with canDirectStream
    - Settings UI per-player Connect toggle and OGG override
    - Config files with soc type and transcoding profiles
  </what-built>
  <how-to-verify>
    1. LMS neustarten und im LMS-Log nach "SpotOn" suchen — Connect-Daemons sollten starten
    2. `ps aux | grep spoton` — pro verbundenem Player ein spoton-Prozess mit --connect Flag sichtbar
    3. Spotify App oeffnen, Geraete-Liste pruefen — LMS-Player sollten als Geraete erscheinen
    4. Playback von Spotify App auf einen LMS-Player uebertragen — Audio sollte innerhalb von 3 Sekunden starten
    5. Spotify App: Play/Pause/Skip Next/Skip Previous testen — Befehle sollten am LMS-Player ankommen
    6. Spotify App: Lautstaerke aendern — LMS-Lautstaerke aendert sich (kein Springen/Oszillieren)
    7. LMS: Lautstaerke aendern — Spotify App zeigt neue Lautstaerke
    8. Waehrend Connect aktiv: einen Track aus SpotOn Browse-Menue abspielen — Connect sollte gestoppt werden
    9. Settings-Seite: "Spotify Connect" Checkbox sichtbar, abwaehlen, Daemon wird gestoppt
    10. Settings-Seite: Audio-Format-Select zeigt Auto/OGG/PCM Optionen
  </how-to-verify>
  <verify>
    <automated>echo "checkpoint:human-verify — manual verification required"</automated>
  </verify>
  <done>All 10 UAT items verified by user.</done>
  <resume-signal>"approved" oder Problembeschreibung</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings form input | Per-player prefs from user form — must validate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | enableSpotifyConnect pref | mitigate | Coerce to 0/1 via ternary (checkbox unchecked = absent = 0) |
| T-05-19 | Tampering | connectOggOverride pref | mitigate | Whitelist validation against (auto|ogg|pcm), default to 'auto' on invalid input |
| T-05-SC | Tampering | No new deps | accept | Settings uses only existing LMS framework modules |
</threat_model>

<verification>
- Settings.pm contains enableSpotifyConnect handling
- basic.html contains Connect settings UI block
- strings.txt has all new CONNECT_* keys with DE+EN
- `grep -c 'PLUGIN_SPOTON_CONNECT' Plugins/SpotOn/strings.txt` >= 6
</verification>

<success_criteria>
Settings UI shows per-player Connect toggle (default on) and OGG-passthrough override (Auto/OGG/PCM). DaemonManager reacts to pref changes. All strings display in EN and DE. Human verification confirms end-to-end Connect functionality.
</success_criteria>

<output>
Create `.planning/phases/05-spotify-connect/05-05-SUMMARY.md` when done
</output>
