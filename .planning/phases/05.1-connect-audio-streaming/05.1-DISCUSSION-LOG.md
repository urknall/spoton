# Phase 05.1: Connect Audio Streaming Bugfix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-01
**Phase:** 05.1-connect-audio-streaming
**Areas discussed:** Debug-Methodik, Spirc Stop-Event Ursache, Spotty-NG Referenz-Vergleich, Binary vs. Perl Verantwortung

---

## Debug-Methodik

| Option | Description | Selected |
|--------|-------------|----------|
| squeezelite -d all | Voller SLIMproto-Debug-Output, zeigt strm-Befehle und HTTP-Verbindungen | ✓ |
| SLIMproto Tap-Proxy | Python-Proxy zwischen squeezelite und LMS, logged alle Frames | |
| Binary stderr + LMS verbose | Binary eprintln + LMS player.source/slimproto auf DEBUG | |

**User's choice:** squeezelite -d all
**Notes:** Einfachste Methode, zeigt genau was auf SLIMproto-Ebene passiert

### Binary-Logging

| Option | Description | Selected |
|--------|-------------|----------|
| --verbose Flag | Persistentes CLI-Flag mit Log-File | |
| stderr eprintln | Events als eprintln, Daemon.pm captured stderr | |
| Nur temporär | Debug-Output nur für Session, dann entfernen | |

**User's choice:** Persistent für dieses Projekt, aber in einer späteren Phase rückbauen
**Notes:** Als Memory/Ticket merken für späteren Rückbau

---

## Spirc Stop-Event Ursache

| Option | Description | Selected |
|--------|-------------|----------|
| Binary-seitig unterdrücken | Grace-Timer nach TrackChanged, Stopped/Paused unterdrückt | ✓ |
| Perl-seitig robuster filtern | CONNECT_START_GRACE ohne isPlaying-Check | |
| Beides — Defense in Depth | Zwei Schutzschichten | |

**User's choice:** Binary-seitig unterdrücken
**Notes:** Problem an der Quelle lösen statt in Perl maskieren

---

## Spotty-NG Referenz-Vergleich

| Option | Description | Selected |
|--------|-------------|----------|
| 1:1 Abgleich des spc-Pfads | Zeile für Zeile vergleichen | |
| Gezielter Diff an Schmerzpunkten | Nur 3 bekannte Problemstellen vergleichen | ✓ |
| Eigene Lösung, Spotty-NG nur bei Sackgasse | Erst eigene Diagnose | |

**User's choice:** Gezielter Diff an Schmerzpunkten
**Notes:** strm Format-Byte, requestString, Event-Sequenz

---

## Binary vs. Perl Verantwortung

| Option | Description | Selected |
|--------|-------------|----------|
| DirectStream beibehalten | canDirectStream + direkte squeezelite-Verbindung | ✓ |
| LMS-Proxy als Fallback | ProtocolHandler::new() proxied Stream | |
| Hybrid — DirectStream mit Proxy-Fallback | Automatischer Umschaltung | |

**User's choice:** DirectStream beibehalten
**Notes:** Spotty-NG-Ansatz, Bug liegt im Setup nicht im Konzept

---

## Claude's Discretion

- Debug-Schritt-Reihenfolge
- stderr-Capture-Methode in Daemon.pm
- Grace-Timer-Wert in Binary
- Perl Grace-Period Vereinfachung

## Deferred Ideas

- Debug-Logging Rückbau in späterer Phase
- LMS-Proxy Fallback nur wenn DirectStream nach Fix weiterhin scheitert
