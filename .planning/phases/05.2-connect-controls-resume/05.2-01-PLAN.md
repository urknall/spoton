---
phase: 05.2-connect-controls-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - librespot-spoton/src/connect.rs
  - librespot-spoton/Cargo.toml
  - librespot-spoton/src/main.rs
autonomous: true
requirements:
  - CON-05
  - CON-11
  - CON-13
must_haves:
  truths:
    - "D-01: Binary sends notify('resume', track_id, position_secs) when Playing fires for the same track after Paused/Stopped"
    - "D-01: Binary does NOT send false resume after grace-timer-suppressed Paused events"
    - "D-03: /control/volume with valid JSON body returns 204, not 404"
    - "D-06: eprintln! debug lines are replaced by log::debug!/log::trace! controlled via RUST_LOG env var"
    - "D-07: Reconnect/crash eprintln! lines remain on stderr (always visible) — Daemon.pm stderr capture beibehalten"
  artifacts:
    - path: "librespot-spoton/src/connect.rs"
      provides: "was_paused AtomicBool, resume notify, volume 404 fix, log migration"
      contains: "was_paused"
    - path: "librespot-spoton/Cargo.toml"
      provides: "log + env_logger dependencies"
      contains: "env_logger"
    - path: "librespot-spoton/src/main.rs"
      provides: "env_logger::init() at process start"
      contains: "env_logger::init"
  key_links:
    - from: "connect.rs handle_player_event Playing"
      to: "LMS JSON-RPC"
      via: "notify('resume', track_id, position_secs)"
      pattern: 'notify.*resume'
    - from: "connect.rs Paused/Stopped handler"
      to: "was_paused AtomicBool"
      via: "store(true) after grace-timer check"
      pattern: "was_paused.*store.*true"
---

<objective>
Fix the binary-side bugs for Connect Controls and clean up debug logging.

Three changes in connect.rs: (1) Add was_paused AtomicBool to LMS struct and implement resume detection — when Playing fires for the same track after a Paused/Stopped event, send notify("resume", track_id, position_secs) to LMS. Remove current_track.take() in Paused/Stopped handler so track context is preserved. (2) Fix /control/volume returning 404 on valid endpoint by adding "volume" and "seek" to the 204-status-code condition. (3) Replace all 13 debug eprintln! lines with log::debug!/log::trace! using env_logger, keeping only the 3 fatal-error eprintln! lines. Add log + env_logger to Cargo.toml and env_logger::init() to main.rs. Rebuild binary.

Purpose: Binary must send resume notifications so Perl-side can unpause squeezelite (Plan 02). Volume 404 fix unblocks the binary /control/volume endpoint. Logging cleanup fulfills D-06.
Output: Rebuilt spoton binary with resume support, correct volume status codes, and env-var-gated logging.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/05.2-connect-controls-resume/05.2-CONTEXT.md
@/home/sti/spoton/.planning/phases/05.2-connect-controls-resume/05.2-RESEARCH.md
@/home/sti/spoton/.planning/phases/05.2-connect-controls-resume/05.2-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From librespot-spoton/src/connect.rs (LMS struct, lines 66-80):
```rust
pub struct LMS {
    pub host_port: Option<String>,
    pub player_mac: Option<String>,
    pub auth: Option<String>,
    pub suppress_next_volume: Arc<AtomicBool>,
    pub flush_tx: Option<watch::Sender<u64>>,
    pub seek_gen: Arc<AtomicU64>,
    pub needs_position_sync: Arc<AtomicBool>,
    pub last_session_start_ns: Arc<AtomicU64>,
}
```

From librespot-spoton/src/connect.rs (Clone impl, lines 110-125):
```rust
impl Clone for LMS {
    fn clone(&self) -> Self {
        Self {
            host_port: self.host_port.clone(),
            player_mac: self.player_mac.clone(),
            auth: self.auth.clone(),
            suppress_next_volume: Arc::clone(&self.suppress_next_volume),
            flush_tx: None,
            seek_gen: Arc::clone(&self.seek_gen),
            needs_position_sync: Arc::clone(&self.needs_position_sync),
            last_session_start_ns: Arc::clone(&self.last_session_start_ns),
        }
    }
}
```

From librespot-spoton/src/connect.rs (handle_player_event signature, lines 139-143):
```rust
pub async fn handle_player_event(
    &self,
    event: &PlayerEvent,
    current_track: &mut Option<String>,
)
```

From librespot-spoton/src/connect.rs (notify signature, ~line 279):
```rust
async fn notify(&self, cmd: &str, p1: &str, p2: &str)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add log/env_logger deps, was_paused field, eprintln! migration</name>
  <files>librespot-spoton/Cargo.toml, librespot-spoton/src/main.rs, librespot-spoton/src/connect.rs</files>

  <read_first>
    - librespot-spoton/Cargo.toml (current dependencies)
    - librespot-spoton/src/main.rs (main() entry point, lines 82-95)
    - librespot-spoton/src/connect.rs (full file — LMS struct lines 66-80, Clone impl lines 110-125, new() lines 82-101, handle_player_event lines 139-215, sink write line 426, HTTP server lines 529/571/623, fatal errors lines 1014/1043/1050)
  </read_first>

  <action>
  Per D-06: Replace eprintln! debug logging with log crate + env_logger.

  **Cargo.toml:** Add two dependencies after the existing `tokio-stream` line:
  - `log = "0.4"`
  - `env_logger = "0.11"`

  **main.rs:** Add `env_logger::init();` as the very first line inside `async fn main()` (line 84, before `let args`). No `use` import needed — fully qualified call.

  **connect.rs — LMS struct (line 66-80):**
  Add new field `pub was_paused: Arc<AtomicBool>` after `last_session_start_ns`. Update the docstring at line 61 from "5 command vocabulary" to "6 command vocabulary: start, change, stop, volume, seek, resume".

  **connect.rs — LMS::new() (lines 82-101):**
  Add `was_paused: Arc::new(AtomicBool::new(false))` to the Self initializer.

  **connect.rs — Clone impl (lines 110-125):**
  Add `was_paused: Arc::clone(&self.was_paused)` to the clone fields.

  **connect.rs — handle_player_event docstring (line 137):**
  Update from "Wire vocabulary (5 commands): start, change, stop, volume, seek" to "Wire vocabulary (6 commands): start, change, stop, volume, seek, resume".

  **connect.rs — eprintln! migration (13 lines):**
  Replace each debug eprintln! with the appropriate log macro:
  - Line 156: `eprintln!("[spoton] Playing:...")` -> `log::debug!("[spoton] Playing: track_id={new_id}, position_ms={position_ms}")`
  - Line 182-183: `eprintln!("[spoton] Paused/Stopped...")` -> `log::debug!(...)`
  - Line 196-197: `eprintln!("[spoton] Paused/Stopped suppressed...")` -> `log::debug!(...)`
  - Line 210: `eprintln!("[spoton] VolumeChanged...")` -> `log::debug!(...)`
  - Line 222: `eprintln!("[spoton] Seeked...")` -> `log::debug!(...)`
  - Line 236: `eprintln!("[spoton] SessionConnected")` -> `log::debug!(...)`
  - Line 244: `eprintln!("[spoton] TrackChanged...")` -> `log::debug!(...)`
  - Line 261: `eprintln!("[spoton] TrackChanged (session start)...")` -> `log::debug!(...)`
  - Line 426 (sink write): `eprintln!("[spoton] sink write...")` -> `log::trace!(...)` (too verbose for debug)
  - Line 529: `eprintln!("[spoton] /stream: GET...")` -> `log::debug!(...)`
  - Line 571: `eprintln!("[spoton] /stream: relay starting...")` -> `log::debug!(...)`
  - Line 623: `eprintln!("[spoton] /stream: relay client disconnected")` -> `log::debug!(...)`

  DO NOT touch the three fatal-error eprintln! at lines ~1014, ~1043, ~1050 — these must remain as stderr output for crash diagnostics.
  </action>

  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo check 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Cargo.toml contains `log = "0.4"` and `env_logger = "0.11"` in [dependencies]
    - main.rs line 84 (first line in main()) is `env_logger::init();`
    - connect.rs LMS struct contains field `pub was_paused: Arc<AtomicBool>`
    - connect.rs LMS::new() initializes `was_paused: Arc::new(AtomicBool::new(false))`
    - connect.rs Clone impl includes `was_paused: Arc::clone(&self.was_paused)`
    - `grep -c "eprintln!" librespot-spoton/src/connect.rs` returns exactly 3 (the fatal error lines)
    - `grep -c "log::debug!\|log::trace!" librespot-spoton/src/connect.rs` returns at least 13
    - `cargo check` succeeds with no errors
  </acceptance_criteria>

  <done>LMS struct has was_paused field, all debug eprintln! replaced with log macros, env_logger initialized in main, cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Resume detection in Playing handler + Paused/Stopped fix + volume 404 fix + binary rebuild</name>
  <files>librespot-spoton/src/connect.rs</files>

  <read_first>
    - librespot-spoton/src/connect.rs (after Task 1 edits — re-read lines 148-215 for handle_player_event Playing/Paused handlers, lines 700-720 for status code logic)
  </read_first>

  <action>
  Per D-01: Implement resume detection using was_paused flag.

  **Paused/Stopped handler (currently lines 181-204):**
  Replace `current_track.take().is_some()` (line 201) with `current_track.is_some()`. This preserves the track_id in current_track for resume detection. After the grace-timer check passes AND current_track.is_some() is true, add `self.was_paused.store(true, Ordering::Release)` BEFORE the `self.notify("stop", "", "").await` call. CRITICAL: the `was_paused.store(true)` must come AFTER the grace-timer `return` at line 198 — a grace-suppressed Paused must NOT set the flag (Pitfall 2 from RESEARCH.md).

  **Playing handler same-track branch (currently lines 158-166):**
  Replace the entire `Some(prev) if prev == new_id.as_str()` block. The new logic: first check `self.was_paused.swap(false, Ordering::AcqRel)` — if true, this is a resume: compute `let secs = f64::from(*position_ms) / 1000.0` and call `self.notify("resume", &new_id, &format!("{secs:.3}")).await`. If was_paused was false, fall through to the existing needs_position_sync check (unchanged).

  **Volume/Seek 404 fix (status code logic, ~line 710-716):**
  The current condition for 204 is: `result.is_some() || cmd == "pause" || cmd == "play" || cmd == "next" || cmd == "prev"`. Add `|| cmd == "volume" || cmd == "seek"` to this condition. This makes known endpoints always return 204 instead of misleading 404 on body parse failures.

  **Binary rebuild:**
  After all connect.rs changes, rebuild the binary:
  `cd /home/sti/spoton/librespot-spoton && cargo build --release`
  Then copy the binary to the plugin directory:
  `cp target/release/spoton /home/sti/spoton/Plugins/SpotOn/Bin/x86_64-linux/spoton`
  </action>

  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo build --release 2>&1 | tail -5 && test -f target/release/spoton && echo "BUILD OK"</automated>
  </verify>

  <acceptance_criteria>
    - connect.rs Paused/Stopped handler does NOT contain `current_track.take()` — uses `current_track.is_some()` instead
    - connect.rs Paused/Stopped handler contains `self.was_paused.store(true, Ordering::Release)` AFTER the grace-timer return and BEFORE the notify("stop") call
    - connect.rs Playing handler same-track branch contains `self.was_paused.swap(false, Ordering::AcqRel)` as the first check
    - connect.rs Playing handler sends `self.notify("resume", &new_id, &format!("{secs:.3}")).await` when was_paused is true
    - `grep 'cmd == "volume" || cmd == "seek"' librespot-spoton/src/connect.rs` returns a match in the status code condition
    - `cargo build --release` succeeds
    - `Plugins/SpotOn/Bin/x86_64-linux/spoton` is updated (newer timestamp than connect.rs source)
  </acceptance_criteria>

  <done>Binary sends resume notify on Playing-after-Pause, preserves current_track on Paused/Stopped, returns 204 for volume/seek endpoints regardless of body parse result. Rebuilt binary deployed to plugin directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spotify Spirc -> Binary | PlayerEvent data from librespot internal — trusted (same process) |
| Binary -> LMS JSON-RPC | notify() sends cmd/p1/p2 over HTTP to localhost LMS — CRLF-sanitized (line 93-94, unchanged) |
| HTTP /control/* -> Binary | POST body from Perl Connect.pm to binary — localhost only, JSON parsed with serde_json |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Tampering | notify() p1/p2 params for resume | accept | CRLF sanitization at line 93-94 connect.rs already covers all notify params including new resume. Position is f64 format — no injection vector. |
| T-05.2-02 | Denial of Service | was_paused flag manipulation | accept | AtomicBool is lock-free, single-threaded event loop — no amplification. False resume at worst sends one extra unpause command. |
| T-05.2-03 | Information Disclosure | log::debug! output | mitigate | env_logger only outputs when RUST_LOG is explicitly set. Default (production) is silent. Track IDs and positions in debug logs are not secrets. |
| T-05.2-SC | Tampering | Cargo dependency install (log, env_logger) | accept | log (0.4) and env_logger (0.11) are top-10 Rust ecosystem crates with billions of downloads combined. Both maintained by the rust-lang-nursery. No npm/pip packages — slopcheck not applicable. |
</threat_model>

<verification>
1. `cd /home/sti/spoton/librespot-spoton && cargo check` passes
2. `cargo build --release` produces updated binary
3. `grep -c "eprintln!" librespot-spoton/src/connect.rs` returns 3 (fatal errors only)
4. `grep "was_paused" librespot-spoton/src/connect.rs | wc -l` returns at least 5 (field, new, clone, store, swap)
5. `grep 'notify.*resume' librespot-spoton/src/connect.rs` returns a match
6. `grep 'cmd == "volume"' librespot-spoton/src/connect.rs` returns a match in the status code block
</verification>

<success_criteria>
Binary compiles and deploys. was_paused flag tracks Paused->Playing transitions. Resume notify fires for same-track Playing after Paused/Stopped. Volume/seek endpoints return 204 on known commands. Debug logging is env-var-gated via RUST_LOG. Fatal error logging remains on stderr.
</success_criteria>

<output>
Create `.planning/phases/05.2-connect-controls-resume/05.2-01-SUMMARY.md` when done
</output>
