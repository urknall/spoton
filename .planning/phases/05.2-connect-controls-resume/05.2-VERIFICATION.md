---
phase: 05.2-connect-controls-resume
verified: 2026-06-01T20:09:53Z
status: human_needed
score: 9/9 must-haves verified (automated)
overrides_applied: 0
human_verification:
  - test: "Resume nach Pause (CON-05, D-01, D-02)"
    expected: "Pause from Spotify app, then Resume from Spotify app — audio resumes, progress bar shows correct position (~where you paused), LMS log shows 'Resume event: trackId=... position=...'"
    why_human: "Requires live Spotify Connect session and a physical LMS player; cannot be verified via grep or static analysis"
  - test: "Volume App->LMS (CON-05, CON-11)"
    expected: "During active Connect playback, changing volume in Spotify app causes LMS player volume to change within 2 seconds. No 404 errors in LMS log for /control/volume."
    why_human: "Requires live bidirectional event flow between Spotify app and LMS"
  - test: "Volume LMS->App (CON-05, D-04)"
    expected: "During active Connect playback, changing volume in LMS web UI or remote causes Spotify app to reflect the change. No 411 errors in LMS log."
    why_human: "Requires live bidirectional Spotify Web API PUT calls and observable Spotify app state"
  - test: "Pause/Resume bidirectional (CON-05)"
    expected: "Pause from LMS pauses Spotify app. Resume from LMS plays Spotify app. Pause from Spotify pauses LMS. Resume from Spotify unpauses LMS (see Resume test above)."
    why_human: "Requires live bidirectional control between two UIs"
  - test: "Skip semi-bidirectional (CON-05)"
    expected: "Skip next from Spotify app advances track on LMS. Skip from LMS sends skip to Spotify and advances track."
    why_human: "Requires playlist with multiple tracks in active Connect session"
  - test: "Seek unidirectional (CON-13, D-05)"
    expected: "Seek in Spotify app updates LMS progress bar. Attempting seek in LMS has no effect (seekbar disabled or ignored). No stream restart."
    why_human: "Requires observing LMS seekbar state and verifying no TCP reconnect to /stream"
  - test: "Gapless next song"
    expected: "Letting a track end naturally in a playlist causes the next track to start without a gap. Metadata updates in LMS."
    why_human: "Requires real-time playback observation — end-of-track event timing not testable statically"
  - test: "Volume Grace (CON-11)"
    expected: "Transfer playback from Spotify app to LMS — first volume event after transfer is suppressed (no volume jump)"
    why_human: "Requires observing LMS volume during Connect transfer initiation"
  - test: "Debug Logging (D-06)"
    expected: "Without RUST_LOG set: binary produces no debug output to stderr. With RUST_LOG=spoton=debug: binary produces structured log lines."
    why_human: "Requires running the daemon with and without RUST_LOG to observe stderr"
---

# Phase 05.2: Connect Controls & Resume — Verification Report

**Phase Goal:** Fix Connect Resume after Pause and verify/fix all Connect control paths bidirectionally. All controls tested from both Spotify app->LMS and LMS->Spotify directions.
**Verified:** 2026-06-01T20:09:53Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Phase Success Criteria (from ROADMAP.md)

| # | Success Criterion | Status | Evidence |
|---|-------------------|--------|----------|
| SC-1 | Resume after Pause works from Spotify app (binary sends resume event to LMS, squeezelite unpauses) | ? NEEDS HUMAN | Code path wired end-to-end; live behavior not verifiable statically |
| SC-2 | Volume sync is bidirectional: Spotify app->LMS and LMS->Spotify | ? NEEDS HUMAN | Code path wired; live behavior requires UAT |
| SC-3 | Pause/Resume is bidirectional | ? NEEDS HUMAN | Code path wired; live behavior requires UAT |
| SC-4 | Skip (next/prev) from Spotify app advances track on LMS; Skip from LMS sends skip to Spotify | ? NEEDS HUMAN | Code path wired; live behavior requires UAT |
| SC-5 | Seek from Spotify app updates LMS playback position (unidirectional) | ? NEEDS HUMAN | canSeek returns 0 in Connect mode — verified; live seek from Spotify requires UAT |
| SC-6 | Debug eprintln! logging from Phase 05.1 cleaned up or gated behind a flag | VERIFIED | eprintln! count = 3 (fatal only), 12 log::debug!/log::trace! calls; RUST_LOG gates output |

### Observable Truths (from Plan must_haves)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01: Binary sends notify('resume', track_id, position_secs) when Playing fires for the same track after Paused/Stopped | VERIFIED | Line 168 connect.rs: `self.notify("resume", &new_id, &format!("{secs:.3}")).await` inside `if self.was_paused.swap(false, Ordering::AcqRel)` |
| 2 | D-01: Binary does NOT send false resume after grace-timer-suppressed Paused events | VERIFIED | Lines 207-210 connect.rs: grace-timer `return` fires BEFORE lines 213-218 (was_paused.store); grace-suppressed events exit before setting the flag |
| 3 | D-03: /control/volume with valid JSON body returns 204, not 404 | VERIFIED | Line 726 connect.rs: `|| cmd == "volume" || cmd == "seek"` added to 204-status condition |
| 4 | D-06: eprintln! debug lines are replaced by log::debug!/log::trace! controlled via RUST_LOG env var | VERIFIED | `grep -c "eprintln!" connect.rs` = 3 (fatal errors only); `grep -c "log::debug!\|log::trace!" connect.rs` = 12; env_logger::init() at main.rs:84 |
| 5 | D-07: Reconnect/crash eprintln! lines remain on stderr (always visible) | VERIFIED | Lines 1032, 1061, 1068 connect.rs: exactly 3 fatal eprintln! ("Spirc reconnect failed", "shut down too often", "Player shut down") |
| 6 | D-02: Perl resume handler in _connectEvent unpauses squeezelite via ['pause', 0] when binary sends resume notify | VERIFIED | Lines 559-561 Connect.pm: `Slim::Control::Request->new($client->id, ['pause', 0])` with `->source(__PACKAGE__)` and `->execute()` inside `if ($cmd eq 'resume')` |
| 7 | D-02: Resume handler adjusts startOffset per CON-13 — never uses ['time', N] in stream mode | VERIFIED | Lines 565-573 Connect.pm: `$song->startOffset(int($position) - $elapsed)` inside resume handler; no `['time', N]` present |
| 8 | D-04: All PUT requests to Spotify Web API include Content-Length: 0 header — no more 411 responses | VERIFIED | Lines 535-541 Client.pm: `if (uc($method) eq 'PUT' \|\| uc($method) eq 'POST') { push @headers, 'Content-Length' => 0; }` |
| 9 | D-01: Wire vocabulary docstring in Connect.pm lists 6 commands including resume | VERIFIED | Line 448 Connect.pm: `#   resume — Playing after Pause (same track); p1=track_id(base62), p2=position (seconds, 3 decimals)` |

**Score:** 9/9 automated truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `librespot-spoton/src/connect.rs` | was_paused AtomicBool, resume notify, volume 204 fix, log migration | VERIFIED | Field at line 82, store at line 217, notify("resume") at line 168, volume/seek condition at line 726, 12 log macros |
| `librespot-spoton/Cargo.toml` | log + env_logger dependencies | VERIFIED | `log = "0.4"` and `env_logger = "0.11"` present in [dependencies] |
| `librespot-spoton/src/main.rs` | env_logger::init() at process start | VERIFIED | Line 84: `env_logger::init();` — first statement in `async fn main()` |
| `Plugins/SpotOn/Connect.pm` | resume branch in _connectEvent, updated wire docstring | VERIFIED | `if ($cmd eq 'resume')` block at line 545; resume in wire vocab at line 448 |
| `Plugins/SpotOn/API/Client.pm` | Content-Length: 0 on all PUT/POST requests | VERIFIED | Lines 535-541: PUT/POST guard with `push @headers, 'Content-Length' => 0` |
| `Plugins/SpotOn/Bin/x86_64-linux/spoton` | Rebuilt binary deployed | VERIFIED | 19,540,856 bytes, timestamp Jun 1 19:33, same minute as source files |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| connect.rs handle_player_event Playing | LMS JSON-RPC | notify('resume', track_id, position_secs) | WIRED | Line 168: `self.notify("resume", &new_id, &format!("{secs:.3}")).await` inside was_paused check |
| connect.rs Paused/Stopped handler | was_paused AtomicBool | store(true) after grace-timer check | WIRED | Lines 207-218: grace `return` at line 210 precedes `was_paused.store(true, Ordering::Release)` at line 217 |
| Connect.pm _connectEvent resume branch | LMS ['pause', 0] command | Slim::Control::Request with source(__PACKAGE__) | WIRED | Lines 559-561: source-marked ['pause', 0] request with execute() |
| Connect.pm _connectEvent resume branch | song->startOffset() | CON-13 position sync | WIRED | Lines 565-573: `$song->startOffset(int($position) - $elapsed)` |
| Client.pm _doFlavouredRequest | Spotify Web API PUT endpoints | Content-Length => 0 header | WIRED | Lines 539-541: push to @headers for PUT/POST inside guard |

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies event dispatch logic and control flow, not data-rendering components. All artifacts are event handlers and HTTP transport code, not UI components rendering state.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Binary --check capability response | `timeout 5s ./spoton --check` | `ok spoton v1.0.0` + JSON manifest | PASS |
| eprintln! count in connect.rs | `grep -c "eprintln!" connect.rs` | 3 | PASS |
| log macro count in connect.rs | `grep -c "log::debug!\|log::trace!" connect.rs` | 12 | PASS |
| notify("resume") call exists | `grep 'notify.*resume' connect.rs` | Line 168 match | PASS |
| volume/seek 204 condition | `grep 'cmd == "volume"' connect.rs` | Line 726 match in status block | PASS |
| resume handler in Connect.pm | `grep "cmd eq 'resume'" Connect.pm` | Line 545 match | PASS |
| Content-Length guard in Client.pm | `grep "Content-Length" Client.pm` | Lines 535-540 match with `=> 0` | PASS |

### Probe Execution

No probes declared in PLAN files. No conventional `scripts/*/tests/probe-*.sh` files exist for this phase. Step 7c: SKIPPED (no probes).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CON-05 | 05.2-01, 05.2-02 | Play/Pause/Skip/Volume controllable from Spotify app | PARTIAL — code verified, live behavior NEEDS HUMAN | Binary sends correct events; Perl handles all control commands; UAT required for live verification |
| CON-11 | 05.2-01 | Volume suppression window after Connect start | PARTIAL — binary side verified, live suppression NEEDS HUMAN | suppress_next_volume AtomicBool unchanged from Phase 5; volume 404 fix present; live UAT needed |
| CON-13 | 05.2-01, 05.2-02 | Position sync via startOffset (never ['time', N]) | CODE VERIFIED, live NEEDS HUMAN | Both seek handler (line 530) and resume handler (line 569) use startOffset; canSeek returns 0 in Connect mode (ProtocolHandler.pm lines 163, 174); live UAT needed |

Note: REQUIREMENTS.md maps CON-05, CON-11, CON-13 to "Phase 5" (not Phase 05.2). Phase 05.2 is a sub-phase that addresses specific bugs within Phase 5's scope. The requirements remain marked "Pending" in REQUIREMENTS.md — this reflects their overall Phase 5 status, not a verification gap for this sub-phase.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `librespot-spoton/src/connect.rs` | 6 | File header says "5 commands: start/change/stop/volume/seek" — stale after resume addition | INFO | Struct docstring (line 61) and handle_player_event docstring (line 142) both correctly say "6 commands"; file-level comment is cosmetic only |

No BLOCKER or WARNING anti-patterns found. No TBD, FIXME, or XXX markers in any modified file. No unreachable code or empty stubs detected.

### Human Verification Required

The following 9 items are deferred UAT tests from Plan 05.2-02, Task 2 (`checkpoint:human-verify`, gate=blocking). The SUMMARY explicitly states "awaiting human verification." All code paths are wired correctly — these tests require a live LMS player with Spotify Connect active.

#### 1. Resume nach Pause (CON-05, D-01, D-02)

**Test:** Transfer Spotify playback to LMS player. Wait for audio. Pause from Spotify app. Wait 3-5 seconds. Resume from Spotify app.
**Expected:** Audio resumes; progress bar shows correct position; LMS log shows "Resume event: trackId=... position=..."
**Why human:** Requires live Spotify Connect session + physical LMS player. Cannot verify timing, audio continuity, or position accuracy via static analysis.

#### 2. Volume App->LMS (CON-05, CON-11)

**Test:** During active Connect playback, change volume in Spotify app.
**Expected:** LMS player volume changes within 2 seconds. No 404 errors in LMS log for /control/volume.
**Why human:** Requires live bidirectional event flow.

#### 3. Volume LMS->App (CON-05, D-04)

**Test:** During active Connect playback, change volume in LMS web UI or remote.
**Expected:** Spotify app reflects volume change. No 411 errors in LMS log.
**Why human:** Requires live Spotify Web API PUT calls and observable Spotify app state.

#### 4. Pause/Resume bidirectional (CON-05)

**Test:** Pause from LMS -> verify Spotify app shows paused. Resume from LMS -> verify Spotify app shows playing. Pause from Spotify -> verify LMS pauses. Resume from Spotify -> verify LMS resumes.
**Expected:** All four directions work within 2 seconds.
**Why human:** Requires observing two UIs simultaneously.

#### 5. Skip semi-bidirectional (CON-05)

**Test:** Play a playlist with multiple tracks. Skip next from Spotify app. Skip from LMS.
**Expected:** Both directions advance track; Spotify and LMS stay in sync.
**Why human:** Requires playlist with multiple tracks in active Connect session.

#### 6. Seek unidirectional (CON-13, D-05)

**Test:** Seek in Spotify app to different position. Attempt seek in LMS.
**Expected:** Spotify seek updates LMS progress bar. LMS seek has no effect (seekbar disabled in Connect mode).
**Why human:** Requires observing LMS seekbar state and verifying no stream restart occurs.

#### 7. Gapless next song

**Test:** Let a track end naturally in a playlist.
**Expected:** Next track starts without gap; metadata updates in LMS.
**Why human:** Requires real-time playback observation of end-of-track timing.

#### 8. Volume Grace (CON-11)

**Test:** Transfer playback from Spotify app to LMS.
**Expected:** First volume event after transfer is suppressed — no volume jump on LMS.
**Why human:** Requires observing LMS volume during Connect transfer initiation.

#### 9. Debug Logging (D-06)

**Test:** (a) Run daemon without RUST_LOG set; observe stderr. (b) Run daemon with RUST_LOG=spoton=debug; observe stderr.
**Expected:** (a) No debug output. (b) Structured log lines from connect.rs handlers.
**Why human:** Requires running the daemon in a live LMS environment and observing Daemon.pm stderr capture.

---

## Gaps Summary

No automated gaps found. All 9 must-have truths are VERIFIED in the codebase. All artifacts exist, are substantive, and are wired correctly. The binary is rebuilt and deployed.

The `human_needed` status is solely due to the blocking `checkpoint:human-verify` task (Plan 05.2-02, Task 2) that was not completed before this verification. The SUMMARY explicitly documents this: "awaiting human verification." The 9 UAT tests above are the deferred items from that checkpoint.

---

_Verified: 2026-06-01T20:09:53Z_
_Verifier: Claude (gsd-verifier)_
