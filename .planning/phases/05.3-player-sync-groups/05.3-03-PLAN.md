---
phase: 05.3-player-sync-groups
plan: 03
type: execute
wave: 2
depends_on:
  - 05.3-01
  - 05.3-02
files_modified:
  - Plugins/SpotOn/Connect.pm
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - CON-06
must_haves:
  truths:
    - "D-06: SC #3 removed from ROADMAP — dead code already cleaned in prior phases, _isMadeForYou is active code"
    - "D-07: stderr-capture teardown stays deferred to Phase 6 — not in scope for Phase 05.3"
    - "Binary 'ready' event triggers automatic playlist play resumption in Connect.pm"
    - "Session recovery after source switch works without user pressing Play again in Spotify app"
    - "ROADMAP SC #3 (Dead Code Cleanup) removed from Phase 05.3 success criteria"
    - "Two synced LMS players both receive Connect audio simultaneously"
  artifacts:
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "ready command handler in _connectEvent dispatch"
      contains: "cmd eq 'ready'"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 05.3 success criteria without SC #3"
  key_links:
    - from: "connect.rs lms_for_reconnect.notify('ready')"
      to: "Connect.pm _connectEvent ready handler"
      via: "JSON-RPC spottyconnect dispatch"
      pattern: "cmd eq 'ready'"
    - from: "Connect.pm ready handler"
      to: "ProtocolHandler.pm new() sync proxy"
      via: "playlist play spotify://connect-<ts>"
      pattern: "spotify://connect-"
---

<objective>
Add the Perl-side 'ready' command handler for session-handover recovery, update ROADMAP to
correct Phase 05.3 success criteria per D-06, and verify the full end-to-end sync-group
audio + session handover flow via UAT checkpoint.

Purpose: The binary now sends a 'ready' notify after successful Spirc reconnect (Plan 02).
Connect.pm needs a handler to re-issue playlist play so LMS resumes streaming without the user
having to press Play again in the Spotify app. The ROADMAP must also be corrected to remove
SC #3 (Dead Code Cleanup) which was already completed in prior phases (D-06).

Output: Connect.pm with ready handler, corrected ROADMAP, and UAT verification.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/05.3-player-sync-groups/05.3-RESEARCH.md
@/home/sti/spoton/.planning/phases/05.3-player-sync-groups/05.3-PATTERNS.md
@/home/sti/spoton/.planning/phases/05.3-player-sync-groups/05.3-01-SUMMARY.md
@/home/sti/spoton/.planning/phases/05.3-player-sync-groups/05.3-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Connect.pm (current):
- _connectEvent dispatcher: if-chain at lines 450-718, pattern: if ($cmd eq 'X') { ... return; }
- $_activeConnectPlayer: package-level scalar, tracks MAC of active Connect player
- $playReq->source(__PACKAGE__): source-marking for T-05-13 loop prevention
- $client->pluginData('connectStartTime' => Time::HiRes::time()): grace period timestamp
- Wire vocabulary docstring at lines 441-448: start, change, stop, volume, seek, resume
- Last handler before unhandled-command log is 'stop' at line 691-716
- Unhandled command log at line 718

From Plugins/SpotOn/Connect.pm _connectEvent 'start' handler (line 596-644):
- Pattern for playlist play: $client->execute(['playlist', 'play', sprintf("spotify://connect-%u", $ts)])
- Pattern for source-marking: $playReq->source(__PACKAGE__)
- Pattern for timestamp: my $ts = int(Time::HiRes::time() * 1000)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'ready' command handler to Connect.pm _connectEvent</name>
  <files>Plugins/SpotOn/Connect.pm</files>
  <read_first>
    - Plugins/SpotOn/Connect.pm (full file — 813 lines, current state)
    - .planning/phases/05.3-player-sync-groups/05.3-PATTERNS.md (ready handler pattern with code)
  </read_first>
  <action>
    Add a 'ready' command handler block in the _connectEvent dispatcher. Insert it AFTER the
    'stop' handler (after the closing brace + return at line 716) and BEFORE the unhandled-command
    log line (line 718).

    The handler must:
    1. Check $_activeConnectPlayer matches $client->id (same guard pattern used in _onPause
       lines 320-325). If no match, log and return — do not re-issue play for a non-active player.
    2. Log via INFOLOG: "Spirc reconnected (ready event) for <client_id> — re-issuing Connect play"
    3. Set $client->pluginData(connectStartTime => Time::HiRes::time()) — same pattern as
       'start' handler line 616, provides grace period for the resulting stop/pause events.
    4. Generate timestamp: my $ts = int(Time::HiRes::time() * 1000)
    5. Issue playlist play: my $playReq = $client->execute(['playlist', 'play', sprintf("spotify://connect-%u", $ts)])
    6. Source-mark: $playReq->source(__PACKAGE__) — T-05-13 loop prevention
    7. Return

    Also update the wire vocabulary docstring (lines 441-448) to add the 7th command:
      ready  — Spirc reconnected internally;           p1="", p2=""

    Do NOT modify any other handler or any code outside _connectEvent.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c "cmd eq 'ready'" Plugins/SpotOn/Connect.pm && grep "ready.*Spirc reconnected" Plugins/SpotOn/Connect.pm | head -1 && grep -c "source(__PACKAGE__)" Plugins/SpotOn/Connect.pm</automated>
  </verify>
  <acceptance_criteria>
    - Connect.pm contains exactly one `if ($cmd eq 'ready')` block
    - The ready handler checks $_activeConnectPlayer eq $client->id before acting
    - The ready handler calls $client->execute(['playlist', 'play', sprintf("spotify://connect-%u", $ts)])
    - The ready handler calls $playReq->source(__PACKAGE__) for T-05-13 loop prevention
    - The ready handler sets $client->pluginData(connectStartTime => Time::HiRes::time())
    - Wire vocabulary docstring includes "ready" as the 7th command
    - The ready handler is positioned AFTER the 'stop' handler and BEFORE the unhandled-command log
    - INFOLOG guard pattern (main::INFOLOG && $log->is_info) used for logging
    - The handler ends with `return;`
  </acceptance_criteria>
  <done>
    Connect.pm _connectEvent handles 'ready' command by re-issuing playlist play for the
    active Connect player, enabling transparent session recovery after Spirc reconnect.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md per D-06 — remove SC #3</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 05.3 section around line 320-340)
    - .planning/phases/05.3-player-sync-groups/05.3-CONTEXT.md (D-06 decision)
  </read_first>
  <action>
    In the Phase 05.3 section of ROADMAP.md:

    1. Update the Goal line from:
       "Connect audio works for synced LMS players, Connect session survives LMS source switches, and accumulated dead code is cleaned up."
       To:
       "Connect audio works for synced LMS players and Connect session survives LMS source switches."

    2. Remove SC #3 from the Success Criteria list. The three criteria currently are:
       (1) Two synced LMS players both receive Connect audio simultaneously via LMS proxy path
       (2) After playing a different LMS source and pressing Play in Spotify app, Connect session resumes
       (3) Dead code removed: _isMadeForYou...
       Remove item 3 entirely. Renumber if needed (should just be items 1 and 2).

    3. In the Known Blockers section, remove blocker #4 about Spirc session appearing dead —
       this is addressed by the ready notify mechanism.

    4. Update the Plans line from "0 plans — not yet scoped" to "3 plans" and add the plan list:
       Plans:
       **Wave 1** *(parallel — no file overlap)*
       - [ ] 05.3-01-PLAN.md — ProtocolHandler proxy fix (requestString + canEnhanceHTTP overrides)
       - [ ] 05.3-02-PLAN.md — Binary relay_active safety + ready notify + rebuild

       **Wave 2** *(depends on Wave 1)*
       - [ ] 05.3-03-PLAN.md — Connect.pm ready handler + ROADMAP correction + UAT checkpoint
  </action>
  <verify>
    <automated>cd /home/sti/spoton && awk '/### Phase 05\.3/,/### Phase 0[6-9]/' .planning/ROADMAP.md | grep -vic '^#' | head -1 && awk '/### Phase 05\.3/,/### Phase 0[6-9]/' .planning/ROADMAP.md | grep -ic 'dead code' && grep "Plans.*3" .planning/ROADMAP.md | head -1 && grep -c "05.3-01-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 05.3 Goal line does NOT mention "dead code" or "cleanup"
    - ROADMAP.md Phase 05.3 Success Criteria has exactly 2 items (sync-group audio + session handover)
    - SC #3 (Dead code removed...) is completely absent from Phase 05.3
    - Plans line shows "3 plans" with the three plan filenames listed
    - Known Blockers #4 (Spirc session dead) is removed or resolved
    - The word "dead code" does not appear in the Phase 05.3 section (may still appear in Backlog as strikethrough)
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 05.3 corrected per D-06: goal simplified, SC #3 removed,
    plans listed with wave structure.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — Sync-group audio + session handover + regression</name>
  <files>Plugins/SpotOn/Connect.pm, Plugins/SpotOn/ProtocolHandler.pm, librespot-spoton/src/connect.rs</files>
  <action>
    Restart LMS to pick up all Perl changes. Ensure the rebuilt binary (from Plan 02) is in
    Bin/librespot-spoton. Run the four manual UAT tests below.

    Test 1 — Sync-Group Audio (D-02 hard blocker):
    Create sync group with 2 players, transfer Connect playback from Spotify app, verify both
    players output audio simultaneously for at least 30 seconds without interruption. Check LMS
    log for absence of "Using Persistent service" on the stream URL.

    Test 2 — Session Handover After Source Switch (D-04):
    While Connect is playing, switch to local music or Spotty. Wait 5-10 seconds. Press Play
    in Spotify app. Verify Connect audio resumes within 15 seconds without manual daemon restart.

    Test 3 — relay_active Recovery (Pitfall 1):
    Play Connect audio, stop from Spotify app, wait 5 seconds, start again from Spotify app.
    Verify audio starts successfully without 503 responses blocking stream.

    Test 4 — Single Player Regression:
    Unsync all players. Transfer Connect playback to single player. Verify DirectStream path
    works (canDirectStream returns URL in log, audio plays).
  </action>
  <verify>
    <automated>echo "Manual UAT checkpoint — requires human verification"</automated>
    <human-check>Run all 4 UAT tests described in action. Type "approved" if all pass, or describe failures.</human-check>
  </verify>
  <done>
    All four UAT tests pass: sync-group audio works for 2 synced players, session handover
    recovers after source switch, relay_active is not stuck, single-player regression passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Binary JSON-RPC -> Connect.pm | localhost POST, binary-controlled command name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.3-06 | Elevation of Privilege | ready handler playlist play | mitigate | Guard checks $_activeConnectPlayer eq $client->id before acting — same pattern as all other handlers; prevents non-Connect player from triggering play |
| T-05.3-07 | Repudiation | ready handler source marking | mitigate | $playReq->source(__PACKAGE__) prevents the resulting playlist events from echoing back to binary (T-05-13) |
| T-05.3-08 | Tampering | ROADMAP edit | accept | Planning artifact only — no runtime impact |
</threat_model>

<verification>
After all tasks:
1. grep "cmd eq 'ready'" Connect.pm -> 1 match
2. ROADMAP Phase 05.3 has 2 success criteria (no "dead code")
3. UAT: sync-group audio, session handover, relay recovery, single-player regression
</verification>

<success_criteria>
Connect.pm handles 'ready' command for transparent session recovery. ROADMAP reflects the
corrected Phase 05.3 scope. All four UAT tests pass: sync-group audio works, session handover
works, relay_active recovery works, single-player regression passes.
</success_criteria>

<output>
Create `.planning/phases/05.3-player-sync-groups/05.3-03-SUMMARY.md` when done
</output>
