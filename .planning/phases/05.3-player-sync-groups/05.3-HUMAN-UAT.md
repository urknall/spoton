---
status: passed
phase: 05.3-player-sync-groups
source: [05.3-03-PLAN.md UAT checkpoint]
started: 2026-06-02T14:15:00Z
updated: 2026-06-02T18:08:00Z
---

## Tests

### 1. Sync-Group Audio (SC #1)
expected: Two synced LMS players both receive Connect audio simultaneously for 30s+
result: PASSED — both players (chilly & coffee sync group) received Connect audio >60s stable. Required bugfixes: MAC dedup in DaemonManager (duplicate daemons caused 503), enableSpotifyConnect per-player pref fallback, disableDiscovery reset.

### 2. Session-Handover After Source Switch (SC #2)
expected: Connect resumes after Browse source switch without daemon restart
result: PASSED — resume handler re-enters Connect via playlist play, audio resumes within 2s

### 3. relay_active Recovery
expected: Stop/Play from Spotify starts audio without 503 blocking
result: PASSED — connection takeover in binary works, no 503

### 4. Single Player Regression
expected: DirectStream path works for unsynchronized single player
result: PASSED — after unsyncing, Connect to standalone player (claude) works via DirectStream

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Bugs Fixed During UAT

1. `_isConnectEnabled` per-player pref returned undef for unconfigured players (cf87e82)
2. Duplicate Connect daemons for same MAC — UPnP bridge + squeezelite caused 503 (cf87e82)
3. `disableDiscovery` pref permanently set by crash-loop protection, never auto-reset (manual fix)
4. Sync-change handler killed ALL daemons instead of only affected group (c97e2bf)
5. helperForClient returned dead daemons during sync-change restart window (c97e2bf)

## Deferred to Phase 05.4

- mDNS Connect discovery: devices visible but connection via discovery fails
- Repeated Spirc `ready` events (~30s interval) suggest unstable registration
- `disableDiscovery` needs auto-reset mechanism + per-player Settings UI option
