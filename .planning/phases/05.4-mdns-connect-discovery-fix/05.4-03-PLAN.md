---
phase: 05.4-mdns-connect-discovery-fix
plan: 03
type: execute
wave: 2
depends_on:
  - 05.4-01
  - 05.4-02
files_modified: []
autonomous: false
requirements:
  - CON-01
must_haves:
  truths:
    - "D-06: Parallel approach — code-analysis fixes first (Plans 01/02), UAT diagnosis second (this plan)"
    - "D-07: UAT diagnosis session required before phase completion"
    - "mDNS-discovered device appears in Spotify app within 10 seconds of daemon start"
    - "Connecting to a device via mDNS discovery starts audio playback"
    - "Spirc registration is stable — no repeated ready events at 30-second intervals"
    - "Per-player disableDiscovery checkbox renders in Settings UI"
  artifacts: []
  key_links: []
---

<objective>
UAT diagnosis session to verify the device-id fix resolves mDNS discovery and the Perl-side changes work correctly.

Purpose: D-07 requires a UAT diagnosis session before considering the phase complete. The code-analysis-identified bugs (device-id mismatch, permanent crash-loop flag) have been fixed in Plans 01 and 02, but real-world verification is needed to confirm: (1) mDNS discovery works, (2) connection via discovery starts audio, (3) Spirc registration is stable (no 30s ready-event loops), (4) Settings UI Discovery toggle renders correctly. This is the phase gate.

Output: UAT results confirming all 5 phase success criteria.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/05.4-mdns-connect-discovery-fix/05.4-CONTEXT.md
@/home/sti/spoton/.planning/phases/05.4-mdns-connect-discovery-fix/05.4-01-SUMMARY.md
@/home/sti/spoton/.planning/phases/05.4-mdns-connect-discovery-fix/05.4-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy fixed binary and restart LMS</name>
  <files></files>
  <read_first>
    - .planning/phases/05.4-mdns-connect-discovery-fix/05.4-01-SUMMARY.md (confirm binary build succeeded)
    - .planning/phases/05.4-mdns-connect-discovery-fix/05.4-02-SUMMARY.md (confirm Perl changes in place)
  </read_first>
  <action>
    Verify the fixed binary is at `librespot-spoton/target/release/librespot-spoton` and confirm it responds to `--check`. Then restart LMS to pick up Perl changes and trigger the D-01 LMS-start reset of any stale `discoveryDisabledByCrashLoop` flags.

    After LMS restart, check logs for the DaemonManager init message and confirm Connect daemons start with Discovery enabled (no `--disable-discovery` flag in the daemon start log line).

    Verify the `device_id_shared` log line appears in daemon stderr output, confirming the binary uses the shared device-id.
  </action>
  <verify>
    <automated>/home/sti/spoton/librespot-spoton/target/release/librespot-spoton --check 2>&1 | grep -c "version"</automated>
  </verify>
  <acceptance_criteria>
    - Binary --check outputs JSON with "version" field
    - LMS restarts without errors
    - Daemon start log line does NOT contain "--disable-discovery" (unless user deliberately disabled it)
    - Binary stderr shows "[spoton] Device ID (shared): " log line
  </acceptance_criteria>
  <done>Fixed binary deployed, LMS restarted, daemons running with Discovery enabled.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT — mDNS Discovery + Spirc Stability + Settings UI</name>
  <what-built>
    Plan 01 fixed the Device-ID mismatch between Discovery (mDNS) and Spirc (Spotify Cloud) in the Rust binary.
    Plan 02 implemented per-player crash-loop protection with 30-minute cooldown auto-reset, LMS-start reset, and a per-player Discovery toggle in Settings UI.
  </what-built>
  <how-to-verify>
    **SC-1: mDNS Discovery Visibility (10 seconds)**
    1. Open Spotify app on phone/desktop
    2. Go to device picker (speaker icon)
    3. Verify SpotOn Connect device(s) appear under "This device" or local network section (NOT "In anderen Netzwerken")
    4. Devices should appear within 10 seconds of daemon start

    **SC-2: Discovery Connection Starts Audio**
    1. In Spotify app, select a SpotOn Connect device found via mDNS
    2. Play a track
    3. Verify audio comes through the LMS player within 5 seconds
    4. Check LMS log: look for `ready` event followed by playback start

    **SC-3: Spirc Registration Stability**
    1. After connecting, monitor LMS log for 2+ minutes
    2. Verify there is only ONE `ready` event at connection time
    3. There should NOT be repeated `ready` events every ~30 seconds
    4. If repeated events appear, note the interval and pattern

    **SC-4: disableDiscovery Auto-Reset (optional — can test later)**
    - This requires triggering a crash-loop (not easy to do manually)
    - Lower priority — the timer mechanism is code-verified

    **SC-5: Per-Player Discovery Toggle in Settings**
    1. Open LMS Settings -> SpotOn
    2. Select a player from the player dropdown
    3. Verify "mDNS-Discovery" checkbox appears below "Spotify Connect" checkbox
    4. Verify checkbox is checked by default
    5. Uncheck it, save, verify daemon restarts WITHOUT --disable-discovery becoming active
    6. Wait — actually, unchecking should ADD --disable-discovery. Check logs.
    7. Re-check, save, verify daemon restarts WITH discovery enabled

    **Log commands for diagnosis:**
    - `journalctl -u logitechmediaserver -f` or wherever LMS logs
    - Look for: "[spoton] Device ID (shared): xxxx" in daemon stderr
    - Look for: "Starting SpotOn Connect daemon:" lines (check --disable-discovery presence)
    - Look for: "ready" events and their frequency
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | UAT plan — no new code, only verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.4-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan |
</threat_model>

<verification>
1. Binary responds to --check
2. LMS restarts cleanly
3. mDNS discovery shows devices in Spotify app
4. Connection via discovery starts audio
5. No repeated ready events at 30s intervals
6. Settings UI shows Discovery toggle per player
</verification>

<success_criteria>
All 5 phase success criteria verified via manual UAT. Phase 05.4 is complete when the user approves the checkpoint.
</success_criteria>

<output>
Create `.planning/phases/05.4-mdns-connect-discovery-fix/05.4-03-SUMMARY.md` when done
</output>
