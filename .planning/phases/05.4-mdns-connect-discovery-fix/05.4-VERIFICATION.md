---
phase: 05.4-mdns-connect-discovery-fix
verified: 2026-06-02T22:00:00Z
status: human_needed
score: 9/10 must-haves verified
overrides_applied: 0
human_verification:
  - test: "SC-4: disableDiscovery crash-loop auto-reset after cooldown"
    expected: "After triggering a crash-loop, discoveryDisabledByCrashLoop resets to 0 after 1800s and daemon restarts with discovery enabled"
    why_human: "Requires triggering 3+ daemon crashes within 5 minutes and waiting 30 minutes — not automatable via grep; timer mechanism is code-verified but end-to-end behavior requires live LMS"
---

# Phase 05.4: mDNS Connect Discovery Fix — Verification Report

**Phase Goal:** mDNS/ZeroConf discovery for Spotify Connect works reliably — devices appear in Spotify app and connections succeed via discovery (not just "In anderen Netzwerken").
**Verified:** 2026-06-02T22:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | Connect devices appear in Spotify app via mDNS within 10 seconds of daemon start | VERIFIED | UAT PASS in 05.4-03-SUMMARY.md: "All 3 devices visible in Spotify local network". mDNS IP detection fixed (UDP default-route socket, `zeroconf_ip` passed to `Discovery::builder`). User approved checkpoint. |
| SC-2 | Connecting to a device via mDNS discovery starts audio (not just "In anderen Netzwerken") | VERIFIED | UAT PASS in 05.4-03-SUMMARY.md: "Audio plays within seconds". Device-ID mismatch root cause fixed: `device_id_shared` FNV-1a(cache_dir+player_mac) shared between `SessionConfig` and `Discovery::builder`. |
| SC-3 | Spirc registration is stable — no repeated `ready` events every 30 seconds | VERIFIED | UAT PASS in 05.4-03-SUMMARY.md: "No repeated ready-event loops". `spirc_active` watcher correctly tracks SessionConnected/Playing/TrackChanged events; Stopped no longer sets `was_paused=true` (WR-02 fix). |
| SC-4 | `disableDiscovery` crash-loop protection auto-resets after a cooldown period (not permanent) | UNCERTAIN | Code implementation verified: `DISCOVERY_COOLDOWN_SECONDS = 1800`, `Slim::Utils::Timers::setTimer` call in `_checkStartTimes()`, `_resetDiscoveryCooldown()` sub present and correct. Cannot verify live timer execution without triggering a crash-loop. UAT SKIPPED in 05.4-03-SUMMARY.md: "Not trivially triggerable, code-verified". Needs human confirmation. |
| SC-5 | Per-player `disableDiscovery` option available in Settings UI | VERIFIED | UAT PASS in 05.4-03-SUMMARY.md: "Checkbox renders, saves, restarts daemon". `pref_enableDiscovery` checkbox in basic.html at line 14-25; `discoveryEnabled`/`discoveryByCrashLoop` template vars in Settings.pm lines 191-192; save handler at lines 156-157 before `initHelpers()`. |

**Score:** 9/10 truths verified (SC-4 deferred to human; 4 of 5 ROADMAP SCs + all plan-specific truths verified)

### Plan-Level Must-Have Truths

| # | Truth (from Plan frontmatter) | Status | Evidence |
|---|-------------------------------|--------|----------|
| P01-T1 | D-08: Minimalinvasive architecture fix — Discovery reconnect path stays (status quo), device_id synchronized | VERIFIED | connect.rs: reconnect at line 1053 uses `session_config.clone()` which carries `device_id_shared`. No Discovery architecture changes. |
| P01-T2 | D-09: Small bug (device-id mismatch) = small fix (one-variable hoist) | VERIFIED | connect.rs lines 856-868: `device_id_shared` computed once; assigned to `session_config.device_id`; passed to `Discovery::builder`. Commit 471bf69. |
| P01-T3 | Discovery and Spirc use the same device_id (FNV-1a hash of cache_dir) | VERIFIED | connect.rs line 856: `let device_id_shared = { FNV-1a(cache_dir + player_mac) }`. Line 868: `session_config.device_id = device_id_shared.clone()`. Line 1011: `Discovery::builder(device_id_shared.clone(), ...)`. |
| P01-T4 | Reconnect branch preserves the shared device_id across session recreation | VERIFIED | connect.rs line 1053: `Session::new(session_config.clone(), cache2)` — session_config carries the shared device_id. Comment at line 999 documents this explicitly. |
| P01-T5 | Binary compiles and runs with the device-id fix | VERIFIED | Binary at `Plugins/SpotOn/Bin/x86_64-linux/spoton` (19189432 bytes, 2026-06-02): `--check` returns `{"version":"1.0.0","passthrough":true,...}`. |
| P02-T1 | D-01: disableDiscovery auto-resets via timer-cooldown AND LMS-restart (dual safety) | VERIFIED | DaemonManager.pm lines 79-97: LMS-start reset loop for `discoveryDisabledByCrashLoop` + global `disableDiscovery`. Daemon.pm lines 239-245: 30-min cooldown timer via `Slim::Utils::Timers`. |
| P02-T2 | D-02: 30 minutes cooldown (DISCOVERY_COOLDOWN_SECONDS = 1800) | VERIFIED | Daemon.pm line 21: `use constant DISCOVERY_COOLDOWN_SECONDS => 1800;` |
| P02-T3 | D-03: After cooldown, discoveryDisabledByCrashLoop resets and daemon restarts with discovery enabled | VERIFIED | Daemon.pm lines 255-273: `_resetDiscoveryCooldown()` clears `discoveryDisabledByCrashLoop`, calls `stopHelper` + `startHelper`. |
| P02-T4 | D-04: Per-player disableDiscovery checkbox in Settings under Connect toggle | VERIFIED | basic.html lines 14-25: checkbox inside `[% IF playerid %]` block, positioned after Connect toggle. |
| P02-T5 | D-05: Crash-loop flag overrides user checkbox — UI shows warning when crash-loop is active | VERIFIED | basic.html lines 15-18: `[% IF discoveryByCrashLoop %]` warning div. Line 23: `disabled` when crash-loop active. Daemon.pm lines 116-119: crash-loop flag evaluated at highest priority. |
| P02-T6 | Crash-loop protection sets discoveryDisabledByCrashLoop per-player, not global disableDiscovery | VERIFIED | Daemon.pm lines 229-235: `$prefs->client($client)->set('discoveryDisabledByCrashLoop', 1)` when `$client` defined; global fallback only when `$client` is undef. |
| P02-T7 | LMS restart clears all discoveryDisabledByCrashLoop flags | VERIFIED | DaemonManager.pm lines 81-87: iterates `Slim::Player::Client::clients()`, clears `discoveryDisabledByCrashLoop` for each. |
| P02-T8 | disableDiscovery pref has default 0 in prefs->init | VERIFIED | Plugin.pm line 49: `disableDiscovery => 0` in `$prefs->init` block. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `librespot-spoton/src/connect.rs` | Device-ID synchronization between SessionConfig and Discovery | VERIFIED | `device_id_shared` appears 5 times: declaration, log, session assignment, comment, Discovery::builder call. |
| `librespot-spoton/target/release/spoton` | Rebuilt binary with device-id fix | VERIFIED | File exists (19MB, 2026-06-02 21:36). `--check` returns valid JSON with `"version":"1.0.0"`. Binary renamed from `librespot-spoton` to `spoton`. |
| `Plugins/SpotOn/Bin/x86_64-linux/spoton` | Deployed binary in plugin directory | VERIFIED | File exists, `--check` works. |
| `Plugins/SpotOn/Connect/Daemon.pm` | Per-player crash-loop protection with cooldown timer | VERIFIED | `discoveryDisabledByCrashLoop` appears 6 times (>= 3 required). `DISCOVERY_COOLDOWN_SECONDS`, `_resetDiscoveryCooldown`, three-source priority evaluation all present. |
| `Plugins/SpotOn/Connect/DaemonManager.pm` | LMS-start reset + _isDiscoveryEnabled helper | PARTIAL | LMS-start reset loop present (lines 79-97). `_isDiscoveryEnabled` helper was planned but removed in post-review commit c41f5e8 as dead code (IN-02). Functional discovery evaluation lives in Daemon.pm start() via inline three-source logic — goal is achieved, helper absent. |
| `Plugins/SpotOn/Plugin.pm` | disableDiscovery default in prefs->init | VERIFIED | Line 49: `disableDiscovery => 0`. |
| `Plugins/SpotOn/Settings.pm` | Per-player disableDiscovery save/load handler | VERIFIED | Lines 156-157: pref save; lines 191-192: template vars. Save occurs BEFORE `initHelpers()` call (WR-03 fix). |
| `Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` | Discovery checkbox UI with crash-loop warning | VERIFIED | Lines 14-25: checkbox with `pref_enableDiscovery`, crash-loop warning div, disabled state. |
| `Plugins/SpotOn/strings.txt` | i18n strings for Discovery toggle (DE + EN) | VERIFIED | 4 PLUGIN_SPOTON_DISCOVERY keys present: `_ENABLED`, `_ENABLED_DESC`, `_ENABLED_LABEL`, `_CRASH_LOOP_WARNING`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| connect.rs run_connect() | SessionConfig.device_id | device_id_shared = FNV-1a(cache_dir+player_mac) | VERIFIED | Lines 856-868: computed before SessionConfig; `session_config.device_id = device_id_shared.clone()`. |
| connect.rs Discovery::builder() | device_id_shared | same variable as SessionConfig | VERIFIED | Line 1011: `Discovery::builder(device_id_shared.clone(), ...)` — old local `device_id` computation inside Discovery block removed. |
| Daemon.pm _checkStartTimes() | prefs->client->set('discoveryDisabledByCrashLoop', 1) | crash-loop detection | VERIFIED | Lines 211-231: MAX_FAILURES_BEFORE_DISABLE_DISCOVERY check, per-player pref set. |
| Daemon.pm _resetDiscoveryCooldown() | DaemonManager->stopHelper + startHelper | Slim::Utils::Timers callback after 1800s | VERIFIED | Lines 239-245: timer set; lines 271-272: stopHelper + startHelper called. |
| Daemon.pm start() | --disable-discovery CLI flag | per-player pref evaluation | VERIFIED | Lines 113-119: three-source priority check (discoveryDisabledByCrashLoop > disableDiscovery per-player > disableDiscovery global). |
| DaemonManager.pm init() | discoveryDisabledByCrashLoop reset | LMS-start reset loop | VERIFIED | Lines 81-87: all clients iterated, flag cleared. |

### Data-Flow Trace (Level 4)

Not applicable — this phase modifies a Rust binary and Perl daemon management code; no React/JS components render dynamic data from DB queries.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Binary responds to --check | `/home/sti/spoton/librespot-spoton/target/release/spoton --check` | `{"version":"1.0.0","passthrough":true,...}` | PASS |
| Deployed binary responds to --check | `/home/sti/spoton/Plugins/SpotOn/Bin/x86_64-linux/spoton --check` | `{"version":"1.0.0","passthrough":true,...}` | PASS |
| device_id_shared log message present in source | `grep -c "[spoton] Device ID (shared)" connect.rs` | 1 | PASS |
| No old local device_id computation in Discovery block | `grep -n "let device_id = {" connect.rs` | (empty) | PASS |
| 4 PLUGIN_SPOTON_DISCOVERY strings in strings.txt | `grep -c "PLUGIN_SPOTON_DISCOVERY" strings.txt` | 4 | PASS |
| pref_enableDiscovery in basic.html | `grep -c "pref_enableDiscovery" basic.html` | 3 | PASS |

### Probe Execution

No declared probes found in PLAN files. No `scripts/*/tests/probe-*.sh` present for this phase. Step 7c: SKIPPED (no probe infrastructure declared).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CON-01 | 05.4-01, 05.4-02, 05.4-03 | One librespot Connect daemon per LMS player | SATISFIED (partial) | Phase 05.4 adds mDNS reliability fixes, crash-loop protection, and per-player discovery toggle to the existing one-daemon-per-player infrastructure. Core daemon-per-player implemented in Phase 5. Phase 05.4 fulfills the mDNS/discovery aspect of CON-01. CON-07 (mDNS/ZeroConf discovery, optionally disableable) is also directly addressed by this phase though only CON-01 is declared. |

No orphaned requirements: REQUIREMENTS.md maps CON-01 to Phase 5; Phase 05.4 is a sub-phase of the Phase 5 series. No additional requirement IDs are mapped exclusively to Phase 05.4 in REQUIREMENTS.md.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | — | — | — |

No TBD/FIXME/XXX markers found in any phase-modified file. No placeholder returns or empty handlers found in new code. No unreferenced debt markers.

**Post-review commit c41f5e8 addressed all REVIEW.md findings:**
- CR-01 (BLOCKER): `Slim::Utils::Timers` and `Time::HiRes` missing imports → FIXED, Daemon.pm lines 13-14.
- WR-02 (WARNING): `was_paused=true` on `Stopped` event → FIXED, Stopped handler no longer sets was_paused.
- WR-03 (WARNING): Discovery pref saved after `initHelpers()` → FIXED, save at line 157 before initHelpers at line 160.
- WR-01 (WARNING): Global `disableDiscovery` reset too broad → Accepted with comment, no user-facing global toggle exists.
- IN-02 (INFO): `_isDiscoveryEnabled` dead code → REMOVED. Plan 02 must_have artifact `contains: "_isDiscoveryEnabled"` is no longer present; this is intentional — the helper was unused dead code.

### Human Verification Required

#### 1. SC-4: Crash-Loop Auto-Reset End-to-End

**Test:** Trigger a crash-loop by causing 3 daemon restarts within 5 minutes (e.g., kill the binary process 3 times in quick succession for one player), then wait 30 minutes (or lower `DISCOVERY_COOLDOWN_SECONDS` temporarily for testing).
**Expected:** After crash-loop triggers, `discoveryDisabledByCrashLoop = 1` is set for the player, daemon starts with `--disable-discovery`, the 30-minute timer fires, `discoveryDisabledByCrashLoop` resets to 0, daemon restarts with discovery enabled again. LMS log shows "Discovery cooldown expired" message.
**Why human:** Requires live LMS with daemon crash simulation. Timer mechanism (Slim::Utils::Timers) only executes under LMS; `perl -c` cannot verify timer firing. UAT session skipped this test as "not trivially triggerable".

### Gaps Summary

No blocking gaps. All 5 ROADMAP success criteria are either VERIFIED or UNCERTAIN-pending-human (SC-4). The `_isDiscoveryEnabled` artifact from Plan 02 was intentionally removed as dead code (IN-02 in REVIEW.md; committed in c41f5e8) — the functional behavior is preserved via inline logic in Daemon.pm. This is a plan deviation but not a goal deviation.

Status is `human_needed` solely for SC-4 (crash-loop cooldown timer end-to-end) which cannot be verified programmatically.

---

_Verified: 2026-06-02T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
