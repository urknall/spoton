---
phase: 06-polish-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/API/TokenManager.pm
  - Plugins/SpotOn/Helper.pm
autonomous: true
requirements:
  - LMS-10

must_haves:
  truths:
    - "SPOTON_DEFAULT_CLIENT_ID is defined in exactly one location (Client.pm) and TokenManager.pm imports it from there"
    - "Client.pm exposes a recommendations() method that routes via bundled-token automatically"
    - "A custom binary named spoton-custom placed in the Bin path is preferred over the bundled spoton binary"
  artifacts:
    - path: "Plugins/SpotOn/API/Client.pm"
      provides: "Exporter setup for SPOTON_DEFAULT_CLIENT_ID + recommendations() method"
      exports: ["SPOTON_DEFAULT_CLIENT_ID", "recommendations"]
    - path: "Plugins/SpotOn/API/TokenManager.pm"
      provides: "Imports SPOTON_DEFAULT_CLIENT_ID from Client.pm instead of defining its own"
    - path: "Plugins/SpotOn/Helper.pm"
      provides: "Custom binary override with spoton-custom as first candidate"
  key_links:
    - from: "Plugins/SpotOn/API/TokenManager.pm"
      to: "Plugins/SpotOn/API/Client.pm"
      via: "constant import via require + direct sub call"
      pattern: "Plugins::SpotOn::API::Client::SPOTON_DEFAULT_CLIENT_ID"
    - from: "Plugins/SpotOn/API/Client.pm"
      to: "https://api.spotify.com/v1/recommendations"
      via: "_request('get', 'recommendations', ...)"
      pattern: "recommendations.*_request.*get"
---

<objective>
Client-ID Consolidation (D-04, SC-7), recommendations() API method for DSTM, and Custom Binary Support finalization (LMS-10).

Purpose: Single source of truth for bundled Client-ID eliminates a maintenance hazard. The recommendations() method is a prerequisite for DSTM in Plan 03. Custom binary support completes LMS-10.
Output: Client.pm with Exporter + recommendations(), TokenManager.pm with import, Helper.pm with verified custom-binary support.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-CONTEXT.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-RESEARCH.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/API/Client.pm (line 28):
  use constant SPOTON_DEFAULT_CLIENT_ID => '93aac68fb06348598c1e67734dfaceee';
  -- Currently NOT exported. Needs Exporter setup.

From Plugins/SpotOn/API/Client.pm search() (line 87-96) -- calling convention for new recommendations():
  sub search {
      my ($class, $accountId, $params, $cb) = @_;
      $class->_request('get', 'search', { _accountId => $accountId, ... }, $cb);
  }

From Plugins/SpotOn/API/Client.pm (line 46-56) -- @KNOWN_DEPRECATED_FAMILIES already includes:
  qr{^recommendations\b},
  -- So recommendations() will automatically route via bundled-token. No manual token-switch needed.

From Plugins/SpotOn/API/TokenManager.pm (line 22):
  use constant SPOTON_DEFAULT_CLIENT_ID => '93aac68fb06348598c1e67734dfaceee';
  -- This is the DUPLICATE that must be removed and replaced with import.

From Plugins/SpotOn/Helper.pm (line 119-131):
  sub _findBin {
      my @candidates = (HELPER);    # 'spoton'
      unshift @candidates, HELPER . '-custom';  # already first candidate
  }
  -- Custom binary support is structurally prepared. get() already checks pref-path binary first (line 35-44).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client-ID Exporter + recommendations() in Client.pm</name>
  <files>Plugins/SpotOn/API/Client.pm</files>
  <read_first>
    - Plugins/SpotOn/API/Client.pm (current state: line 28 SPOTON_DEFAULT_CLIENT_ID, lines 87-96 search() as calling convention template, line 46-56 @KNOWN_DEPRECATED_FAMILIES confirming 'recommendations' routing)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md (Pattern Assignment for Client.pm: Exporter setup + recommendations() method)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-RESEARCH.md (Pattern 4: recommendations(), Pattern 8: Client-ID Consolidation, Pitfall 1: Calling Convention, Pitfall 4: Exporter)
  </read_first>
  <action>
    Per D-04 (Client-ID Konsolidierung): Add Exporter setup to Client.pm so SPOTON_DEFAULT_CLIENT_ID is importable.

    1. After the existing `use Time::HiRes;` import block (around line 14), add:
       - `use Exporter 'import';`
       - `our @EXPORT_OK = qw(SPOTON_DEFAULT_CLIENT_ID);`

    2. Add `recommendations()` method after the existing `search()` method (line 96). Signature: `($class, $accountId, $params, $cb)` -- same calling convention as search(). Parameters in $params: seed_tracks (arrayref), seed_artists (arrayref), seed_genres (arrayref), limit (default 25). Convert arrayrefs to comma-separated strings for the query. Use `_noCache => 1` (recommendations should always be fresh). The method calls `_request('get', 'recommendations', ...)`. The endpoint is already in @KNOWN_DEPRECATED_FAMILIES so bundled-token routing is automatic. The callback receives `$result->{tracks}` (arrayref of track objects) or empty arrayref on failure. If no seed_tracks AND no seed_artists provided, call `$cb->([])` immediately and return.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/API/Client.pm 2>&1 | grep -q "syntax OK" && perl -e 'use lib "Plugins"; require Plugins::SpotOn::API::Client; print "export ok\n" if Plugins::SpotOn::API::Client->can("recommendations")' 2>&1 | grep -q "export ok" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Client.pm contains `use Exporter 'import';` and `our @EXPORT_OK = qw(SPOTON_DEFAULT_CLIENT_ID);`
    - Client.pm contains `sub recommendations` with signature `($class, $accountId, $params, $cb)`
    - recommendations() calls `$class->_request('get', 'recommendations', ...)` with `_noCache => 1`
    - recommendations() converts seed_tracks/seed_artists arrayrefs to comma-separated strings
    - recommendations() calls `$cb->([])` when no seeds provided
    - `perl -c Plugins/SpotOn/API/Client.pm` exits with "syntax OK"
  </acceptance_criteria>
  <done>Client.pm exports SPOTON_DEFAULT_CLIENT_ID and provides recommendations() method ready for DSTM consumption</done>
</task>

<task type="auto">
  <name>Task 2: TokenManager.pm Client-ID import + Helper.pm custom binary verification</name>
  <files>Plugins/SpotOn/API/TokenManager.pm, Plugins/SpotOn/Helper.pm</files>
  <read_first>
    - Plugins/SpotOn/API/TokenManager.pm (line 22: duplicate SPOTON_DEFAULT_CLIENT_ID constant, lines 1-33 full header)
    - Plugins/SpotOn/Helper.pm (lines 34-58 get(), lines 116-149 _findBin() with custom-first logic)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md (TokenManager.pm section: circular dependency analysis, safe approach with require + direct call)
  </read_first>
  <action>
    Per D-04 (Client-ID Konsolidierung): Remove duplicate SPOTON_DEFAULT_CLIENT_ID from TokenManager.pm and import from Client.pm.

    1. In TokenManager.pm, remove line 22 (`use constant SPOTON_DEFAULT_CLIENT_ID => '93aac68fb06348598c1e67734dfaceee';`).

    2. Replace with the safe circular-dependency-avoiding pattern (PATTERNS.md recommendation):
       ```
       use constant SPOTON_DEFAULT_CLIENT_ID => do {
           require Plugins::SpotOn::API::Client;
           Plugins::SpotOn::API::Client::SPOTON_DEFAULT_CLIENT_ID();
       };
       ```
       This avoids a compile-time `use` that would trigger a circular load (TokenManager is used by Client). The `require` is lazy and only fires at runtime constant initialization.

    3. Helper.pm: The custom binary support (LMS-10) is already structurally in place -- `_findBin()` line 130 already does `unshift @candidates, HELPER . '-custom'` and `get()` line 35-44 already tries the `binary` pref path first. Verify this works by:
       - Confirming `get()` checks $prefs->get('binary') first (custom path override)
       - Confirming `_findBin()` puts 'spoton-custom' as first candidate
       - Confirming `helperCheck()` validates via `--check` with shell-safe quoting
       No code changes needed in Helper.pm -- only verification that LMS-10 is complete.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/API/TokenManager.pm 2>&1 | grep -q "syntax OK" && grep -v "^#" Plugins/SpotOn/API/TokenManager.pm | grep -c "93aac68fb06348598c1e67734dfaceee" | grep -q "^0$" && grep -c "Plugins::SpotOn::API::Client::SPOTON_DEFAULT_CLIENT_ID" Plugins/SpotOn/API/TokenManager.pm | grep -q "^1$" && grep -c "spoton-custom\|HELPER.*custom" Plugins/SpotOn/Helper.pm | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - TokenManager.pm does NOT contain the literal string `93aac68fb06348598c1e67734dfaceee` in any non-comment line
    - TokenManager.pm contains `Plugins::SpotOn::API::Client::SPOTON_DEFAULT_CLIENT_ID()` as the source of the constant
    - TokenManager.pm compiles without syntax errors (`perl -c`)
    - Helper.pm still contains the `spoton-custom` candidate in `_findBin()` (no regression)
    - Helper.pm::get() still checks `$prefs->get('binary')` first
    - No new code changes needed in Helper.pm (LMS-10 structurally complete from earlier phases)
  </acceptance_criteria>
  <done>SPOTON_DEFAULT_CLIENT_ID defined in exactly one location (Client.pm), TokenManager imports it. Helper.pm custom binary support verified complete (LMS-10).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client.pm Exporter | Exposes a constant to other internal modules — no external trust boundary |
| Helper.pm binary path | User-supplied binary path crosses trust boundary (already mitigated by shell-safe quoting) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Helper.pm binary path | accept | Shell-safe quoting already in helperCheck() line 67: `(my $safe = $candidate) =~ s/'/'\\''/g`. --check validation ensures binary identity. No new attack surface. |
| T-06-02 | Tampering | recommendations() query params | mitigate | seed_tracks/seed_artists come from getMixableProperties() (LMS internal) in DSTM. URI-escape via existing _request() pipeline. No user-controlled input reaches recommendations() directly. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
1. `perl -c Plugins/SpotOn/API/Client.pm` -- syntax OK
2. `perl -c Plugins/SpotOn/API/TokenManager.pm` -- syntax OK
3. `grep -c '93aac68fb06348598c1e67734dfaceee' Plugins/SpotOn/API/Client.pm` -- exactly 1 (the definition)
4. `grep -v '^#' Plugins/SpotOn/API/TokenManager.pm | grep -c '93aac68fb06348598c1e67734dfaceee'` -- exactly 0
5. Client.pm has `sub recommendations` method
6. Helper.pm has `spoton-custom` in _findBin candidates
</verification>

<success_criteria>
SC-7 from ROADMAP: Bundled Spotify Client-ID is defined in exactly one location; changing it requires editing one constant.
LMS-10: Custom binary support verified functional.
DSTM prerequisite: recommendations() API method available for Plan 03.
</success_criteria>

<output>
Create `.planning/phases/06-polish-release-readiness/06-01-SUMMARY.md` when done
</output>
