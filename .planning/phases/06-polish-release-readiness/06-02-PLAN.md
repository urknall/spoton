---
phase: 06-polish-release-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/ProtocolHandler.pm
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
autonomous: true
requirements:
  - LMS-08

must_haves:
  truths:
    - "Setting bitrate to 96 on one player and 320 on another causes each to stream at its configured bitrate"
    - "Format-Dropdown with 5 options (Auto/OGG/PCM/FLAC/MP3) is visible per player in Settings"
    - "Selecting FLAC or MP3 forces transcoding via custom-convert.conf pipeline (canDirectStream returns 0)"
    - "Format-Dropdown applies to BOTH Connect and Browse modes (D-12)"
    - "Volume normalisation flag is passed to Connect daemons (already verified in Daemon.pm line 120)"
  artifacts:
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "Per-player bitrateOverride + streamFormat pref handling"
    - path: "Plugins/SpotOn/ProtocolHandler.pm"
      provides: "canDirectStream() returns 0 for flac/mp3; formatOverride() returns ogg for ogg mode"
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "updateTranscodingTable() with per-player bitrate override"
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      provides: "Format-Dropdown, Bitrate-Override-Dropdown per player"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "New string keys for format/bitrate UI (EN+DE)"
  key_links:
    - from: "Plugins/SpotOn/Settings.pm"
      to: "Plugins/SpotOn/Plugin.pm"
      via: "bitrateOverride pref read in updateTranscodingTable"
      pattern: "prefs->client.*bitrateOverride"
    - from: "Plugins/SpotOn/ProtocolHandler.pm"
      to: "Plugins/SpotOn/Settings.pm"
      via: "streamFormat pref read in canDirectStream/formatOverride"
      pattern: "prefs->client.*streamFormat"
---

<objective>
Per-Player Preferences: Bitrate Override (D-01), unified Format-Dropdown (D-11, D-12), and transcoding engine extensions for per-player format/bitrate.

Purpose: Players can individually override global bitrate and choose streaming format (Auto/OGG/PCM/FLAC/MP3). FLAC/MP3 selections force transcoding via custom-convert.conf. This replaces the old 3-option connectOggOverride with a 5-option unified Format-Dropdown for both Connect and Browse modes.
Output: Complete per-player settings UI, pref handling, and transcoding pipeline support.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-CONTEXT.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-RESEARCH.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Settings.pm handler() (lines 145-161) -- existing per-player pref pattern:
  if ($client) {
      my $enableConnect = $paramRef->{'pref_enableSpotifyConnect'} ? 1 : 0;
      $prefs->client($client)->set('enableSpotifyConnect', $enableConnect);
      if (defined $paramRef->{'pref_connectOggOverride'}) {
          my $override = $paramRef->{'pref_connectOggOverride'};
          $override = 'auto' unless $override =~ /^(?:auto|ogg|pcm)$/;
          $prefs->client($client)->set('connectOggOverride', $override);
      }
  }

From Plugins/SpotOn/Settings.pm (lines 187-193) -- template var pattern:
  if ($client) {
      $paramRef->{connectEnabled}     = $prefs->client($client)->get('enableSpotifyConnect') // 1;
      $paramRef->{connectOggOverride} = $prefs->client($client)->get('connectOggOverride') || 'auto';
      $paramRef->{discoveryEnabled}   = $prefs->client($client)->get('disableDiscovery') ? 0 : 1;
  }

From Plugins/SpotOn/Plugin.pm updateTranscodingTable() (lines 1210-1284):
  my $bitrate = $prefs->get('bitrate') || 320;
  -- Per-player override insertion point: after this line
  -- OGG override at lines 1275-1283: reads connectOggOverride, deletes soc-ogg-*-*

From Plugins/SpotOn/ProtocolHandler.pm:
  formatOverride() lines 41-59: returns 'soc' for Connect, 'son' for Browse
  canDirectStream() lines 66-94: returns HTTP URL for single Connect, 0 for sync/non-Connect

From basic.html:
  Lines 27-33: connectOggOverride dropdown (to be replaced by streamFormat dropdown)
  Lines 50-56: global bitrate dropdown
  Lines 4-37: player section with per-player prefs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-player pref handling in Settings.pm + UI in basic.html + strings.txt</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/strings.txt</files>
  <read_first>
    - Plugins/SpotOn/Settings.pm (full file: per-player pref save block lines 145-161, template vars lines 186-193)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (full file: connectOggOverride dropdown lines 27-33, player section structure)
    - Plugins/SpotOn/strings.txt (full file: existing 66 keys EN+DE)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md (Settings.pm section, basic.html section, strings.txt section)
  </read_first>
  <action>
    Per D-01 (Bitrate per Player), D-03 (Settings layout), D-11 (Format-Dropdown unified), D-12 (OGG for both modes):

    **Settings.pm changes:**

    1. In the `if ($client)` block within `handler()` (after the existing connectOggOverride handling around line 154), add bitrateOverride handling:
       - Read `$paramRef->{'pref_bitrateOverride'}`, validate against `^(?:96|160|320)$`, empty string means "use global". Store via `$prefs->client($client)->set('bitrateOverride', $value)`.

    2. Replace the connectOggOverride handling with streamFormat handling:
       - Read `$paramRef->{'pref_streamFormat'}`, validate against `^(?:auto|ogg|pcm|flac|mp3)$`, default to 'auto'.
       - Store via `$prefs->client($client)->set('streamFormat', $value)`.
       - Keep the existing connectOggOverride save logic for backward compatibility (existing users still have the old pref), but add the new streamFormat save alongside it.

    3. In the template vars section (around line 187-193), add:
       - `$paramRef->{bitrateOverride}` = `$prefs->client($client)->get('bitrateOverride') || ''`
       - `$paramRef->{streamFormat}` = `$prefs->client($client)->get('streamFormat') || $prefs->client($client)->get('connectOggOverride') || 'auto'` (migration fallback: read old key if new key empty)

    **basic.html changes:**

    1. Replace the connectOggOverride dropdown (lines 27-33) with the new unified streamFormat dropdown:
       - 5 options: auto, ogg, pcm, flac, mp3
       - Each option uses string keys: PLUGIN_SPOTON_STREAM_FORMAT_AUTO, _OGG, _PCM, _FLAC, _MP3
       - Wrapper title: PLUGIN_SPOTON_STREAM_FORMAT, desc: PLUGIN_SPOTON_STREAM_FORMAT_DESC
       - Select name: pref_streamFormat, template var: streamFormat

    2. Add per-player Bitrate Override dropdown AFTER the streamFormat dropdown, BEFORE the `[% ELSE %]` at line 35:
       - 4 options: "" (Global/Standard), "320", "160", "96"
       - Wrapper title: PLUGIN_SPOTON_BITRATE_OVERRIDE, desc: PLUGIN_SPOTON_BITRATE_OVERRIDE_DESC
       - Select name: pref_bitrateOverride, template var: bitrateOverride
       - First option label: PLUGIN_SPOTON_BITRATE_GLOBAL string key

    **strings.txt changes (EN+DE only -- full i18n in Plan 04):**

    Add these new keys after the existing PLUGIN_SPOTON_CONNECT_OGG_* keys:
    - PLUGIN_SPOTON_STREAM_FORMAT (DE: Streaming-Format, EN: Streaming Format)
    - PLUGIN_SPOTON_STREAM_FORMAT_DESC (DE: Format fuer Connect und Browse. Bei FLAC/MP3 wird transkodiert., EN: Format for Connect and Browse. FLAC/MP3 use transcoding.)
    - PLUGIN_SPOTON_STREAM_FORMAT_AUTO (DE: Automatisch, EN: Automatic)
    - PLUGIN_SPOTON_STREAM_FORMAT_OGG (DE: OGG Passthrough (DirectStream), EN: OGG Passthrough (DirectStream))
    - PLUGIN_SPOTON_STREAM_FORMAT_PCM (DE: PCM (DirectStream), EN: PCM (DirectStream))
    - PLUGIN_SPOTON_STREAM_FORMAT_FLAC (DE: FLAC (transkodiert), EN: FLAC (transcoded))
    - PLUGIN_SPOTON_STREAM_FORMAT_MP3 (DE: MP3 (transkodiert), EN: MP3 (transcoded))
    - PLUGIN_SPOTON_BITRATE_OVERRIDE (DE: Bitrate (Player), EN: Bitrate (Player))
    - PLUGIN_SPOTON_BITRATE_OVERRIDE_DESC (DE: Ueberschreibt die globale Bitrate fuer diesen Player. Standard nutzt den globalen Wert., EN: Overrides global bitrate for this player. Default uses the global value.)
    - PLUGIN_SPOTON_BITRATE_GLOBAL (DE: Global (Standard), EN: Global (Default))
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/Settings.pm 2>&1 | grep -q "syntax OK" && grep -c "pref_streamFormat\|pref_bitrateOverride" Plugins/SpotOn/Settings.pm | grep -qv "^0$" && grep -c "PLUGIN_SPOTON_STREAM_FORMAT" Plugins/SpotOn/strings.txt | grep -qv "^0$" && grep -c "pref_streamFormat\|pref_bitrateOverride" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Settings.pm contains bitrateOverride validation with regex `^(?:96|160|320)$`
    - Settings.pm contains streamFormat validation with regex `^(?:auto|ogg|pcm|flac|mp3)$`
    - Settings.pm template vars section populates bitrateOverride and streamFormat for the template
    - Settings.pm streamFormat falls back to connectOggOverride for migration
    - basic.html has pref_streamFormat select with 5 options (auto, ogg, pcm, flac, mp3)
    - basic.html has pref_bitrateOverride select with 4 options ("", 320, 160, 96)
    - strings.txt has all 10 new keys with EN and DE translations
    - `perl -c Plugins/SpotOn/Settings.pm` exits 0
  </acceptance_criteria>
  <done>Per-player settings UI complete: Format-Dropdown (5 options) and Bitrate Override dropdown visible in player section</done>
</task>

<task type="auto">
  <name>Task 2: Transcoding engine + ProtocolHandler per-player format support</name>
  <files>Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/ProtocolHandler.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (lines 1200-1284: updateTranscodingTable full function)
    - Plugins/SpotOn/ProtocolHandler.pm (lines 41-94: formatOverride + canDirectStream)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md (Plugin.pm section: updateTranscodingTable extension, ProtocolHandler.pm section: canDirectStream + formatOverride extension)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-RESEARCH.md (Pitfall 2: per-player pref key collision, Pitfall 3: canDirectStream with format-dropdown, Pattern 2: Format-Dropdown behavior table)
  </read_first>
  <action>
    Per D-01 (per-player bitrate), D-11 (format-dropdown), D-12 (unified for both modes):

    **Plugin.pm updateTranscodingTable() changes:**

    1. After line 1213 (`my $bitrate = $prefs->get('bitrate') || 320;`), add per-player bitrate override:
       - If `$client` is defined, read `$prefs->client($client)->get('bitrateOverride')`.
       - If the override matches `^(?:96|160|320)$`, use it instead of the global value.

    2. Replace the connectOggOverride block (lines 1275-1283) with streamFormat logic:
       - Read `$prefs->client($client)->get('streamFormat')` with fallback to `$prefs->client($client)->get('connectOggOverride')` then 'auto'.
       - If streamFormat is NOT 'ogg': delete `$commandTable->{'son-ogg-*-*'}` and `$commandTable->{'soc-ogg-*-*'}` (same as current behavior for pcm, extended to flac/mp3/auto).
       - If streamFormat IS 'ogg': keep both ogg entries (subject to passthrough guard above).
       - The existing passthrough-guard block (lines 1269-1273) remains unchanged above this.

    **ProtocolHandler.pm formatOverride() changes:**

    1. After the `$client` and `$url` assignment (line 44-45) but before the Connect check (line 50), read the per-player streamFormat pref:
       - `my $fmt = $client ? ($prefs->client($client)->get('streamFormat') || $prefs->client($client)->get('connectOggOverride') || 'auto') : 'auto';`

    2. For Browse mode (after the Connect if-block, before the final `return 'son'`):
       - If `$fmt eq 'ogg'`: return 'ogg' (OGG passthrough for Browse)
       - Otherwise: return 'son' (PCM/FLAC/MP3/auto all use son pipeline)

    3. For Connect mode (inside the Connect if-block): return 'soc' regardless of format -- Connect always uses soc pipeline. The format-dropdown affects canDirectStream and OGG-entry presence, not the soc type.

    **ProtocolHandler.pm canDirectStream() changes:**

    1. After the `$client = $client->master` line (around line 71), add format-dropdown check:
       - Read the per-player streamFormat pref (same fallback chain as formatOverride).
       - If streamFormat is 'flac' or 'mp3': return 0 immediately with INFOLOG message "canDirectStream: 0 (streamFormat=$fmt forces transcoding)". This forces LMS to use custom-convert.conf pipeline.
       - If streamFormat is 'pcm': also return 0 (forces PCM via transcoding, not DirectStream).
       - If streamFormat is 'auto' or 'ogg': proceed with existing Connect DirectStream logic.

    Per D-02: Volume normalisation is already handled -- Daemon.pm line 120 passes `--enable-volume-normalisation` when the global normalization pref is set. No changes needed.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -c Plugins/SpotOn/Plugin.pm 2>&1 | grep -q "syntax OK" && perl -c Plugins/SpotOn/ProtocolHandler.pm 2>&1 | grep -q "syntax OK" && grep -c "bitrateOverride" Plugins/SpotOn/Plugin.pm | grep -qv "^0$" && grep -c "streamFormat" Plugins/SpotOn/ProtocolHandler.pm | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm updateTranscodingTable() reads bitrateOverride from per-player pref and applies if valid
    - Plugin.pm updateTranscodingTable() reads streamFormat with connectOggOverride fallback
    - Plugin.pm updateTranscodingTable() deletes son-ogg-*-* and soc-ogg-*-* when streamFormat is not 'ogg'
    - ProtocolHandler.pm canDirectStream() returns 0 when streamFormat is 'flac', 'mp3', or 'pcm'
    - ProtocolHandler.pm formatOverride() returns 'ogg' when streamFormat is 'ogg' (Browse mode)
    - ProtocolHandler.pm formatOverride() returns 'soc' for Connect mode regardless of streamFormat
    - Both files compile without syntax errors (`perl -c`)
    - D-02 verified: Daemon.pm line 120 already handles normalization for Connect mode (no change needed)
  </acceptance_criteria>
  <done>Per-player bitrate and format selection fully integrated into transcoding engine and protocol handler. Bitrate 96 on Player A and 320 on Player B both stream correctly at their respective bitrates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings.pm form input | bitrateOverride and streamFormat come from HTML form POST |
| Plugin.pm commandTable | Bitrate value injected into shell command via regex |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering | Settings.pm bitrateOverride | mitigate | Validate with `$override =~ /^(?:96|160|320)$/` before storage. Invalid values reset to empty string. Same pattern as existing bitrate validation (line 64-66). |
| T-06-04 | Tampering | Settings.pm streamFormat | mitigate | Validate with `$fmt =~ /^(?:auto|ogg|pcm|flac|mp3)$/` before storage. Invalid values default to 'auto'. |
| T-06-05 | Tampering | Plugin.pm bitrate injection | mitigate | bitrateOverride re-validated in updateTranscodingTable() before regex substitution: `$override =~ /^(?:96|160|320)$/`. Only numeric values reach the `s/--bitrate \d+/--bitrate $bitrate/` regex. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
1. `perl -c Plugins/SpotOn/Settings.pm` -- syntax OK
2. `perl -c Plugins/SpotOn/Plugin.pm` -- syntax OK
3. `perl -c Plugins/SpotOn/ProtocolHandler.pm` -- syntax OK
4. Settings.pm contains bitrateOverride + streamFormat pref handling
5. ProtocolHandler.pm returns 0 from canDirectStream when streamFormat is flac/mp3/pcm
6. Plugin.pm updateTranscodingTable reads per-player bitrate override
7. basic.html has Format-Dropdown with 5 options
8. strings.txt has 10 new keys
</verification>

<success_criteria>
ROADMAP SC-1: Setting bitrate to 96 kbps on one player and 320 kbps on another causes each player to stream at its configured bitrate independently.
ROADMAP SC-4: Per-player Settings page shows Format-Dropdown.
ROADMAP SC-5: Player with transcoding fallback receives audio through custom-convert.conf pipeline.
ROADMAP SC-6: Volume normalisation applies to Connect mode (verified: Daemon.pm line 120).
</success_criteria>

<output>
Create `.planning/phases/06-polish-release-readiness/06-02-SUMMARY.md` when done
</output>
