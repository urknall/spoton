---
phase: 06-polish-release-readiness
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified:
  - Plugins/SpotOn/strings.txt
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
autonomous: true
requirements:
  - LMS-03

must_haves:
  truths:
    - "D-10: All UI strings translated to EN, DE, FR, NL, IT, ES, SV, NO, DA, PL, CS by Claude, no missing-key placeholders"
    - "D-07: Setup Guide appears at top of Settings when no account is configured, shows correct step order (Developer App, Client-ID, Spotify App)"
    - "D-08: Credits footer at bottom of Settings page: 'SpotOn nutzt librespot. Inspiriert von Hergers Spotty Plugin.'"
    - "strings.txt contains all keys (66 existing + ~17 new) translated to all 11 languages"
  artifacts:
    - path: "Plugins/SpotOn/strings.txt"
      provides: "All string keys (66 existing + ~17 new) translated to 11 languages"
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      provides: "Setup Guide section + Credits footer"
  key_links:
    - from: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      to: "Plugins/SpotOn/strings.txt"
      via: "string filter: [% 'KEY' | string %]"
      pattern: "PLUGIN_SPOTON_SETUP_GUIDE|PLUGIN_SPOTON_CREDITS"
---

<objective>
Full i18n expansion (D-10), Setup Guide (D-07), and Credits footer (D-08).

Purpose: Expand all 66+ string keys from EN+DE to the full LMS language set (11 languages). Add Setup Guide section for new users and credits for librespot/Herger.
Output: Complete strings.txt with 11 languages, basic.html with Setup Guide and Credits.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-CONTEXT.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-RESEARCH.md
@/home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Plugins/SpotOn/strings.txt format (LMS convention):
  PLUGIN_SPOTON_KEY
  [TAB]CS[TAB]Czech text
  [TAB]DA[TAB]Danish text
  [TAB]DE[TAB]German text
  [TAB]EN[TAB]English text
  [TAB]ES[TAB]Spanish text
  [TAB]FR[TAB]French text
  [TAB]IT[TAB]Italian text
  [TAB]NL[TAB]Dutch text
  [TAB]NO[TAB]Norwegian text
  [TAB]PL[TAB]Polish text
  [TAB]SV[TAB]Swedish text
  [empty line]
  -- TAB indentation is mandatory (not spaces)
  -- Empty line between blocks is mandatory
  -- File must be UTF-8 encoded

From Spotty-NG strings.txt (reference for language palette):
  Uses CS, DA, DE, EN, FR, HU, NL (subset)
  -- SpotOn targets: CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV (11 languages)

From basic.html current structure:
  Line 1: PROCESS header.html
  Line 4: Player section header
  Lines 5-37: Per-player settings (IF playerid)
  Lines 39-76: Global section (bitrate, normalization, clientId)
  Lines 78-125: ZeroConf setup (IF NOT accounts)
  Lines 127-159: Account management
  Line 161: PROCESS footer.html

From Settings.pm template vars:
  $paramRef->{accounts} = hash of configured accounts
  -- Setup Guide shows when: accounts.keys.size == 0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full i18n expansion of strings.txt to 11 languages</name>
  <files>Plugins/SpotOn/strings.txt</files>
  <read_first>
    - Plugins/SpotOn/strings.txt (full file: all 66+ existing keys, currently only EN+DE)
    - /home/sti/spotty-ng/Spotty-Plugin/strings.txt (first 80 lines: reference for LMS language palette format and typical translations)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md (strings.txt section: format, TAB rules, UTF-8 requirement, new keys list)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-RESEARCH.md (Pitfall 6: UTF-8 encoding, Pitfall 7: DSTM handler key)
  </read_first>
  <action>
    Per D-10 (i18n translations from Claude for full LMS language palette):

    Rewrite the entire strings.txt file. For EVERY key (existing ~66 + new ~17 from Plans 02/03/this plan):

    1. Expand from EN+DE to all 11 languages: CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV.
    2. Language codes must be in ALPHABETICAL order within each block (CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV).
    3. Each language line uses TAB indentation (not spaces).
    4. Empty line between each key block.
    5. File MUST be UTF-8 encoded.

    **New string keys to add (if not already present from Plans 02/03):**

    Setup Guide keys:
    - PLUGIN_SPOTON_SETUP_GUIDE_TITLE: EN "Getting Started with SpotOn", DE "Erste Schritte mit SpotOn"
    - PLUGIN_SPOTON_SETUP_GUIDE_STEP1: EN "Create a Spotify Developer App at", DE "Erstelle eine Spotify Developer App auf"
    - PLUGIN_SPOTON_SETUP_GUIDE_STEP2: EN "Enter your Client-ID in the field below", DE "Trage deine Client-ID im Feld unten ein"
    - PLUGIN_SPOTON_SETUP_GUIDE_STEP3: EN "Open the Spotify app and connect to your player", DE "Oeffne die Spotify-App und verbinde dich mit deinem Player"

    Credits key:
    - PLUGIN_SPOTON_CREDITS: EN "SpotOn uses librespot. Inspired by Herger's Spotty plugin.", DE "SpotOn nutzt librespot. Inspiriert von Hergers Spotty Plugin."

    Translation guidelines:
    - Translate naturally for each language, not word-by-word.
    - Technical terms (DirectStream, OGG, PCM, FLAC, MP3, Client-ID, Spotify, LMS) stay in English.
    - For placeholder strings with %s, keep the %s position appropriate for the target language grammar.
    - Keep translations concise -- LMS settings UI has limited space.
    - Community can correct translations later (per D-10).

    After writing, verify UTF-8 encoding: run `file Plugins/SpotOn/strings.txt` -- must report "UTF-8 Unicode text".
  </action>
  <verify>
    <automated>cd /home/sti/spoton && file Plugins/SpotOn/strings.txt | grep -q "UTF-8\|ASCII" && LANGS=$(grep -P "^\t[A-Z]{2}\t" Plugins/SpotOn/strings.txt | awk '{print $1}' | sort -u | tr '\n' ',') && echo "Languages: $LANGS" && LANG_COUNT=$(echo "$LANGS" | tr ',' '\n' | grep -c '.') && echo "Count: $LANG_COUNT" && test "$LANG_COUNT" -ge 11 && KEY_COUNT=$(grep -c "^PLUGIN_SPOTON" Plugins/SpotOn/strings.txt) && echo "Keys: $KEY_COUNT" && test "$KEY_COUNT" -ge 75 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - strings.txt contains at minimum 75 string keys (66 existing + ~11 new)
    - Each key has translations for all 11 languages: CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV
    - Language codes are in alphabetical order within each block
    - TAB indentation (not spaces) on all language lines
    - Empty line between each key block
    - File is UTF-8 encoded (verified by `file` command)
    - PLUGIN_SPOTON_SETUP_GUIDE_TITLE, _STEP1, _STEP2, _STEP3 keys present
    - PLUGIN_SPOTON_CREDITS key present with librespot and Herger mention
    - PLUGIN_SPOTON_RECOMMENDATIONS key present (for DSTM from Plan 03)
    - All format-dropdown and bitrate-override keys present (from Plan 02)
    - No missing-key placeholders in any language
  </acceptance_criteria>
  <done>strings.txt fully translated to 11 languages with all existing and new keys</done>
</task>

<task type="auto">
  <name>Task 2: Setup Guide section + Credits footer in basic.html</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</files>
  <read_first>
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (full file: current layout structure, ZeroConf block pattern lines 78-125 as conditional-section reference)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-PATTERNS.md (basic.html section: Setup Guide placement, Credits footer pattern)
    - /home/sti/spoton/.planning/phases/06-polish-release-readiness/06-RESEARCH.md (Pattern 9: Setup Guide placement -- Claude's Discretion)
  </read_first>
  <action>
    Per D-07 (Setup Guide, Claude's Discretion on placement) and D-08 (Credits footer):

    **Setup Guide section:**
    Per D-07 and RESEARCH Pattern 9: Place a Setup Guide section at the TOP of the settings page, BEFORE the player section header (line 4). Only visible when NO account is configured (`[% IF NOT accounts.keys.size %]`).

    The guide shows 3 steps in a numbered list:
    1. Step 1: Create Spotify Developer App (with link to developer.spotify.com/dashboard)
    2. Step 2: Enter Client-ID in the field below
    3. Step 3: Open Spotify app and connect to player

    Use a styled div with light background, border, padding, and border-radius (matching the existing ZeroConf block style). Wrap in `[% IF NOT accounts.keys.size %]` conditional. Use string keys PLUGIN_SPOTON_SETUP_GUIDE_TITLE, _STEP1, _STEP2, _STEP3 for all text.

    Place this block at line 2 (after `[% title = "PLUGIN_SPOTON" %]`), before the `<h2>` player section header.

    **Credits footer:**
    Per D-08: Dezenter Footer am Ende der Settings-Seite. Place a paragraph with muted styling (color:#888, font-size:0.85em, centered) BEFORE the `[% PROCESS settings/footer.html %]` line. Use the PLUGIN_SPOTON_CREDITS string key. This is always visible (not conditional on accounts).

    Text: "SpotOn nutzt librespot. Inspiriert von Hergers Spotty Plugin." (resolved from string key, language-appropriate).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c "PLUGIN_SPOTON_SETUP_GUIDE_TITLE" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html | grep -qv "^0$" && grep -c "PLUGIN_SPOTON_CREDITS" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html | grep -qv "^0$" && grep -c "developer.spotify.com" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html | grep -qv "^0$" && grep -c "NOT accounts.keys.size" Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html | grep -qv "^0$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - basic.html has Setup Guide section with PLUGIN_SPOTON_SETUP_GUIDE_TITLE heading
    - Setup Guide is conditional: only shown when `NOT accounts.keys.size`
    - Setup Guide has 3 numbered steps using string keys _STEP1, _STEP2, _STEP3
    - Setup Guide includes link to developer.spotify.com/dashboard with target="_blank"
    - Setup Guide is placed BEFORE the player section header
    - Credits footer with PLUGIN_SPOTON_CREDITS string key is placed before footer.html
    - Credits footer has muted styling (small font, gray color, centered)
    - Credits is always visible (not conditional)
  </acceptance_criteria>
  <done>Setup Guide for new users and Credits footer added to Settings page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| basic.html template | Static HTML templates rendered server-side by LMS -- no external input |
| strings.txt | Static translations loaded at LMS startup -- no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | strings.txt | accept | File is read-only at runtime by LMS. Modifications require filesystem access (same privilege as plugin installation). |
| T-06-10 | Information Disclosure | Setup Guide | accept | Guide instructs user to visit developer.spotify.com -- public URL, no sensitive info exposed. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. |
</threat_model>

<verification>
1. `file Plugins/SpotOn/strings.txt` -- reports UTF-8 or ASCII
2. strings.txt has 11 language codes per key block
3. No TAB/space mixing in indentation
4. basic.html has Setup Guide section and Credits footer
5. Setup Guide conditional on no accounts
</verification>

<success_criteria>
ROADMAP SC-8: Settings page shows setup guide for new users with required steps in order.
ROADMAP SC-9: All UI strings translated into standard LMS language set with no missing-key placeholders.
LMS-03: i18n support via LMS strings mechanism for EN, DE, FR, NL, IT, ES, SV, NO, DA, PL, CS.
</success_criteria>

<output>
Create `.planning/phases/06-polish-release-readiness/06-04-SUMMARY.md` when done
</output>
