---
phase: 07-de-en-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Helper.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/Plugin.pm
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "D-01: Idiomatic English rewrite — every comment rephrased naturally, not literal translation"
    - "D-02: Redundant comments deleted rather than translated"
    - "D-03: WHY comments kept and translated/improved"
    - "D-04: Both real Umlauts AND ASCII workarounds (aendern, Laengen, Zeichen, Schutz) eliminated"
    - "D-05: Both Perl (.pm) and Rust (.rs) source files verified clean"
    - "D-06: i18n files (strings.txt) excluded from scope"
    - "D-07: Task-IDs (T-04.4-01, D-09, CR-02, STR-10) and decision references preserved"
    - "D-08: Pitfall references (P-01 through P-20) have short English context added"
    - "All DEBUGLOG, INFOLOG, WARNLOG, ERRORLOG calls emit English strings"
    - "No German special characters or German words remain in any Perl or Rust source comment"
  artifacts:
    - path: "Plugins/SpotOn/Helper.pm"
      provides: "English-only comments in binary helper module"
      contains: "# CRITICAL"
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "English-only comments in settings module"
      contains: "# T-04.4-01"
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "English-only comments in main plugin module"
      contains: "# Context queueing"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Plugins/SpotOn/Helper.pm"
      via: "Plugin uses Helper for binary resolution"
      pattern: "Plugins::SpotOn::Helper"
---

<objective>
Replace all German comments in Perl source files with idiomatic English and verify the entire codebase (Perl + Rust) is German-free.

Purpose: The codebase must read as a consistently English project. ~18 German comment lines exist across 3 files (Helper.pm, Settings.pm, Plugin.pm). All log strings are already English. Rust source is already clean. This plan translates the German comments, removes redundant comments, and runs the CLEAN-03 verification grep.

Output: Three modified Perl files with English-only comments, plus verified clean status across all source files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-de-en-code-cleanup/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate German comments to idiomatic English</name>
  <files>Plugins/SpotOn/Helper.pm, Plugins/SpotOn/Settings.pm, Plugins/SpotOn/Plugin.pm</files>
  <read_first>
    Plugins/SpotOn/Helper.pm (162 LOC — read fully; German at lines 21, 71, 75, 115)
    Plugins/SpotOn/Settings.pm (328 LOC — read fully; German at lines 58, 69-70, 74-77, 80-81, 85, 92, 196, 307-310)
    Plugins/SpotOn/Plugin.pm (1337 LOC — read lines 60-80, 170-180, 335-355, 410-420, 1148-1158 for German; skim rest for any missed instances)
    .planning/phases/07-de-en-code-cleanup/07-CONTEXT.md (decisions D-01 through D-08)
  </read_first>
  <action>
    Rewrite all German comments to idiomatic English per D-01 (natural rephrasing, not literal translation). Apply D-02 (delete redundant comments that restate what the code says) and D-03 (keep and translate comments explaining non-obvious WHY). Preserve all task-IDs and decision references per D-07 (T-04.4-01, D-09, D-10, CR-02, STR-10, D-01, D-02, D-03, D-06).

    Specific translations (per D-04, covering real Umlauts AND ASCII workarounds):

    Helper.pm:
    - L21: "aarch64 kann als Fallback armhf-Binaries verwenden" -> "aarch64 can fall back to armhf binaries"
    - L71: "KRITISCH: 'spoton' nicht 'spotty' im Regex" -> "CRITICAL: match 'spoton', not 'spotty'"
    - L75: "SpotOn-Erweiterung: Mindestversions-Pruefung" -> "Minimum version check"
    - L115: "Angepasster Binary-Finder um findbin() von LMS zu nutzen" -> "Custom binary finder wrapping LMS findbin()"

    Settings.pm:
    - L58: "Binary-Status an Template uebergeben" -> "Pass binary status to template"
    - L69: "Normalization pref speichern (STR-08, T-04-05)" -> "Save normalization pref (STR-08, T-04-05)"
    - L70: "Checkbox: wenn nicht angehakt, sendet Browser keinen Wert — undef/leer wird zu 0" -> "Checkbox: browser sends no value when unchecked — treat undef/empty as 0"
    - L74: "Client-ID pref speichern (D-02, T-04.4-01)" -> "Save Client-ID pref (D-02, T-04.4-01)"
    - L75-77: Translate the 3-line T-04.4-01 input validation block. Keep task-ID. Target: "T-04.4-01: Input validation — alphanumeric only, max 32 chars." / "Spotify Client-IDs are exactly 32 hex chars — regex + length check" / "eliminates shell metacharacter injection vectors for --client-id flag."
    - L80: "nur alphanumerisch (Injection-Schutz)" -> "alphanumeric only (injection guard)"
    - L81: "max 32 Zeichen (Spotify Client-ID-Format)" -> "max 32 chars (Spotify Client-ID format)"
    - L85: "ZeroConf Discovery starten (D-01)" -> "Start ZeroConf Discovery (D-01)"
    - L92: "ZeroConf Discovery stoppen" -> "Stop ZeroConf Discovery"
    - L196: "Client-ID und Degraded-Mode-Status fuer Template (D-02, D-03)" -> "Client-ID and degraded-mode status for template (D-02, D-03)"
    - L307-310: Translate the 4-line degraded-mode helper block. Keep D-03 ref. Target: "Helper: Check degraded mode (D-03)" / "Degraded = no custom Client-ID configured." / "Shows a hint in Settings so the user can enter their own Spotify Developer App." / "Analogous to _isDiscoveryRunning below."

    Plugin.pm:
    - L70: "Orphaned-Process-Cleanup-Timer starten (STR-10)" -> "Start orphaned-process cleanup timer (STR-10)"
    - L176: "CR-02: Whitelist statt Blacklist — verhindert Shell-Injection via &, |, ;" -> "CR-02: allowlist not blocklist — prevents shell injection via &, |, ;"
    - L342-345: Translate the 4-line personal-mix regex block. Keep D-06 ref and RESEARCH.md Pitfall 4 ref (per D-08, add brief English context). Target: "Regex pattern for Spotify-generated personal mix playlists (D-06)." / "Uses \\s+ instead of literal spaces for whitespace variants." / "Both call sites (_madeForYouFeed and _libraryPlaylistsFeed)" / "use _isMadeForYou — changing only this function is sufficient (Pitfall 4: single detection point)."
    - L415: "Kontext-Queueing (D-09/D-10) — XMLBrowser reiht alle Items des Feeds ein" -> "Context queueing (D-09/D-10) — XMLBrowser enqueues all feed items"
    - L1153: "Kontext-Queueing fuer Album-Track-Tap (D-09)" -> "Context queueing for album track tap (D-09)"

    After translating, scan each file one more time for any remaining German (grep for common German words: fuer, nicht, nur, kann, kein, statt, wird, sind, alle, diese, auch, noch, ueber). Fix any found.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -rn '#.*\bfuer\b\|#.*\bnicht\b\|#.*\bnur\b\|#.*\bkann\b\|#.*\bkein\b\|#.*\bstatt\b\|#.*\bwird\b\|#.*\bsind\b\|#.*\balle\b\|#.*\bdiese\b\|#.*\bauch\b\|#.*\bnoch\b\|#.*\bueber\b\|#.*\bvon\b\|#.*\bund\b\|#.*\boder\b\|#.*\bdamit\b\|#.*\bzeigt\b\|#.*\bwenn\b\|#.*\bsendet\b\|#.*\bnutzen\b\|#.*\baendern\b\|#.*\bverwenden\b\|#.*\bspeichern\b\|#.*\bstarten\b\|#.*\bstoppen\b\|#.*\buebergeben\b\|#.*KRITISCH\|#.*Zeichen\|#.*Schutz\|#.*Laengen\|#.*Pruefung\|#.*Erweiterung\|#.*Angepasst\|#.*Muster\|#.*Verwendet\|#.*Aufruf\|#.*Kontext-Q\|#.*eliminiert\|#.*Degraded.*kein\|#.*Hinweis\|#.*Analog\b\|#.*verhindert\|#.*Whitelist.*statt\|#.*reiht' Plugins/SpotOn/ --include='*.pm' | grep -v 'strings.txt'; echo "GERMAN_LINES=$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep command in verify returns exit code 1 (no matches), GERMAN_LINES=1
    - All 18 identified German comment lines in Helper.pm, Settings.pm, Plugin.pm are replaced with English
    - Task-IDs T-04.4-01, D-01, D-02, D-03, D-06, D-09, D-10, CR-02, STR-10, STR-08, T-04-05 still appear in their respective comment lines
    - No German Umlauts (ae/oe/ue/ss ASCII workarounds) remain in any comment
    - No functional code changes — only comment text modified
  </acceptance_criteria>
  <done>All German comments in Helper.pm, Settings.pm, and Plugin.pm are rewritten as idiomatic English. Task-IDs and decision references preserved. Redundant comments deleted per D-02.</done>
</task>

<task type="auto">
  <name>Task 2: Full codebase verification sweep (CLEAN-02, CLEAN-03)</name>
  <files>No files modified — verification only</files>
  <read_first>
    Plugins/SpotOn/Connect.pm (spot-check lines with comments for any German)
    Plugins/SpotOn/API/Client.pm (spot-check)
    Plugins/SpotOn/API/TokenManager.pm (spot-check)
    Plugins/SpotOn/Connect/DaemonManager.pm (spot-check)
    Plugins/SpotOn/Connect/Daemon.pm (spot-check)
    Plugins/SpotOn/DontStopTheMusic.pm (spot-check)
    Plugins/SpotOn/ProtocolHandler.pm (spot-check)
    librespot-spoton/src/main.rs (verify no German — per D-05)
    librespot-spoton/src/connect.rs (verify no German — per D-05)
  </read_first>
  <action>
    Run the CLEAN-03 verification grep: search for German special characters (ae/oe/ue/ss real Umlauts) across ALL Perl and Rust source files, excluding strings.txt and i18n files. Then run a broader German-word grep (fuer, nicht, nur, kann, kein, statt, wird, etc.) as a second pass.

    Also verify CLEAN-02: grep all DEBUGLOG, INFOLOG, WARNLOG, ERRORLOG calls and confirm every log string is English.

    If any German is found in the 7 remaining Perl files or 2 Rust files, fix it in-place using the same D-01/D-02/D-03 rules from Task 1.

    Expected: Zero German text in any file. The CONTEXT.md pre-audit found 0 German in these files, so this task is primarily verification, not translation.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && echo "=== CLEAN-03: Umlaut check ===" && grep -rn '[äöüßÄÖÜ]' Plugins/SpotOn/ librespot-spoton/src/ --include='*.pm' --include='*.rs' | grep -v 'strings.txt' | grep -v 'i18n'; UMLAUT=$?; echo "=== CLEAN-03: ASCII German word check ===" && grep -v '^#' Plugins/SpotOn/*.pm Plugins/SpotOn/**/*.pm librespot-spoton/src/*.rs 2>/dev/null | grep '#' | grep -i '\bfuer\b\|\bnicht\b\|\bnur\b\|\bkann\b\|\bkein\b\|\bstatt\b\|\bwird\b\|\bsind\b\|\balle\b\|\bdiese\b\|\bauch\b\|\bnoch\b\|\bueber\b\|\bspeichern\b\|\bstarten\b\|\bstoppen\b\|\buebergeben\b\|\bverwenden\b\|\bnutzen\b\|\baendern\b\|\bKRITISCH\b\|\bZeichen\b\|\bSchutz\b\|\bLaengen\b\|\bPruefung\b'; GERMAN=$?; echo "=== CLEAN-02: German log strings ===" && grep -rn 'DEBUGLOG\|INFOLOG\|WARNLOG\|ERRORLOG' Plugins/SpotOn/ --include='*.pm' | grep -i '\bfuer\b\|\bnicht\b\|\bnur\b\|\bkann\b\|\bkein\b\|\bstatt\b'; LOGS=$?; echo "UMLAUT_EXIT=$UMLAUT GERMAN_EXIT=$GERMAN LOGS_EXIT=$LOGS"; test "$UMLAUT" = "1" && test "$GERMAN" = "1" && test "$LOGS" = "1" && echo "ALL_CLEAN=YES" || echo "ALL_CLEAN=NO"</automated>
  </verify>
  <acceptance_criteria>
    - UMLAUT_EXIT=1 (no real Umlaut characters in .pm or .rs files, excluding strings.txt)
    - GERMAN_EXIT=1 (no ASCII-transliterated German words in any comment)
    - LOGS_EXIT=1 (no German words in any log call)
    - ALL_CLEAN=YES printed at the end
    - If any fixes were needed in the 7 remaining Perl files or 2 Rust files, those files are modified and pass the same grep checks
  </acceptance_criteria>
  <done>CLEAN-01, CLEAN-02, and CLEAN-03 all pass. The entire Perl+Rust codebase contains only English comments and log strings. German special characters grep returns zero matches. German word grep returns zero matches. All log calls emit English.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries crossed — this plan modifies only comment text, no runtime behavior changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Comment edits | accept | Text-only changes, no code logic modified. Grep verification confirms no functional changes. |
</threat_model>

<verification>
Phase-level verification:

1. `grep -rn '[äöüßÄÖÜ]' Plugins/SpotOn/ librespot-spoton/src/ --include='*.pm' --include='*.rs' | grep -v 'strings.txt'` returns 0 matches
2. Broad German-word grep across all .pm and .rs files returns 0 matches
3. All DEBUGLOG/INFOLOG/WARNLOG/ERRORLOG calls contain only English strings
4. Task-IDs (T-04.4-01, D-01 through D-10, CR-02, STR-08, STR-10) are preserved in their comment lines
5. No functional code changes — `git diff --stat` shows only comment-line changes
</verification>

<success_criteria>
- CLEAN-01: Every German comment across all 10 Perl modules is replaced with idiomatic English
- CLEAN-02: Every log call emits an English string (already true pre-phase, verified post-phase)
- CLEAN-03: `grep -rn` for German special characters returns zero matches (excluding strings.txt/i18n)
- D-04 extended scope: ASCII Umlaut workarounds (fuer, Laengen, Zeichen, etc.) also eliminated
- D-07: All task-IDs and phase references preserved
- D-08: Pitfall references have short English context added
</success_criteria>

<output>
Create `.planning/phases/07-de-en-code-cleanup/07-01-SUMMARY.md` when done
</output>
