---
phase: 09-stream-metadata
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - t/10_stream_metadata.t
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/DontStopTheMusic.pm
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
must_haves:
  truths:
    - "Songinfo for a Browse track displays '(Spotify Browse)' in the type field"
    - "Songinfo for a Connect track displays '(Spotify Connect)' in the type field"
    - "Songinfo shows the stream format matching the streamFormat pref (OGG, FLAC, MP3, PCM)"
    - "Songinfo shows the configured Spotify bitrate (e.g. 320k) before the format"
    - "When streamFormat is 'auto', OGG is shown if passthrough is available, PCM otherwise"
    - "Per-player bitrateOverride takes precedence over global bitrate pref"
  artifacts:
    - path: "t/10_stream_metadata.t"
      provides: "Unit tests for _typeString and grep gates for stale hardcoded values"
      min_lines: 80
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "_typeString helper sub and updated Browse cache-set blocks"
      contains: "_typeString"
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "Dynamic type string in Connect metadata"
      contains: "_typeString"
    - path: "Plugins/SpotOn/DontStopTheMusic.pm"
      provides: "Dynamic type string in DSTM cache"
      contains: "_typeString"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Plugins/SpotOn/Helper"
      via: "getCapability('passthrough') for auto format resolution"
      pattern: "getCapability.*passthrough"
    - from: "Plugins/SpotOn/Connect.pm"
      to: "Plugins/SpotOn/Plugin"
      via: "require + _typeString call"
      pattern: "Plugin->_typeString"
    - from: "Plugins/SpotOn/DontStopTheMusic.pm"
      to: "Plugins/SpotOn/Plugin"
      via: "require + _typeString call"
      pattern: "Plugin->_typeString"
---

<objective>
Implement dynamic stream metadata display in Songinfo: every playing Spotify track shows the active playback mode (Browse/Connect), stream format (OGG/FLAC/MP3/PCM), and bitrate (e.g. "320k, OGG (Spotify Browse)").

Purpose: Users can confirm what the plugin is delivering at a glance -- mode, format, quality.
Output: `_typeString` helper sub in Plugin.pm, all 4 metadata call sites updated, full test coverage in t/10_stream_metadata.t.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-stream-metadata/09-CONTEXT.md
@.planning/phases/09-stream-metadata/09-RESEARCH.md
@.planning/phases/09-stream-metadata/09-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Plugin.pm (lines 1221-1228) — bitrate pref chain:
  my $bitrate = $prefs->get('bitrate') || 320;
  if ($client) {
      my $override = $prefs->client($client)->get('bitrateOverride');
      $bitrate = $override if $override && $override =~ /^(?:96|160|320)$/;
  }

From Plugins/SpotOn/ProtocolHandler.pm (lines 53-57) — streamFormat pref chain:
  my $fmt = $client
      ? ($prefs->client($client)->get('streamFormat')
         || $prefs->client($client)->get('connectOggOverride')
         || 'auto')
      : 'auto';

From Plugins/SpotOn/Helper.pm (lines 104-108) — passthrough capability:
  sub getCapability {
      my ($class, $key) = @_;
      return $helperCapabilities->{$key} if $helperCapabilities && defined $helperCapabilities->{$key};
      return undef;
  }

From Plugins/SpotOn/Connect.pm (line 799) — _fetchTrackMetadata signature:
  sub _fetchTrackMetadata { my ($client, $trackId) = @_; ... }

From Plugins/SpotOn/DontStopTheMusic.pm (line 250) — _cacheAndExtractUris signature:
  sub _cacheAndExtractUris { my ($tracks) = @_; ... }
  # Note: $client is NOT passed — uses global prefs only (no per-player override)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Create failing tests for _typeString and stale-literal grep gates</name>
  <files>t/10_stream_metadata.t</files>
  <read_first>
    - t/09_settings.t (test infrastructure pattern: stub-dir, write_stub, Slim::Utils::Prefs stub with client() support)
    - Plugins/SpotOn/Plugin.pm (understand the module package name and prefs namespace 'plugin.spoton')
    - Plugins/SpotOn/Helper.pm (getCapability interface to stub)
  </read_first>
  <behavior>
    - Test: _typeString($client, 'Browse') with streamFormat=ogg, bitrate=320 returns "320k, OGG (Spotify Browse)"
    - Test: _typeString($client, 'Connect') with streamFormat=flac, bitrate=160 returns "160k, FLAC (Spotify Connect)"
    - Test: _typeString($client, 'Browse') with streamFormat=mp3, bitrate=96 returns "96k, MP3 (Spotify Browse)"
    - Test: _typeString($client, 'Browse') with streamFormat=pcm, bitrate=320 returns "320k, PCM (Spotify Browse)"
    - Test: _typeString($client, 'Browse') with streamFormat=auto + passthrough=1 returns "320k, OGG (Spotify Browse)"
    - Test: _typeString($client, 'Browse') with streamFormat=auto + passthrough=0 returns "320k, PCM (Spotify Browse)"
    - Test: _typeString(undef, 'Browse') with global bitrate=320 returns "320k, OGG (Spotify Browse)" or "320k, PCM (Spotify Browse)" depending on passthrough (no crash)
    - Test: bitrateOverride 160 overrides global 320 — _typeString returns "160k, ..."
    - Test: D-04 guard — when bitrate is 0/undef, string omits bitrate slot: "OGG (Spotify Browse)"
    - Grep gate: no remaining literal "type => 'Spotify'" in Plugin.pm or DontStopTheMusic.pm (excluding test files, comments)
    - Grep gate: no remaining literal "Ogg Vorbis (Spotify)" in Connect.pm
  </behavior>
  <action>
    Create t/10_stream_metadata.t following the established test infrastructure pattern from t/09_settings.t. Copy the stub-dir + write_stub helper, then create stubs for Slim::Utils::Log, Slim::Utils::Prefs (with client() method supporting per-player prefs), Slim::Utils::Cache, Slim::Plugin::OPMLBased, and Plugins::SpotOn::Helper (with controllable getCapability return value via package variable $helperCapabilities).

    Unshift $stub_dir into @INC, then require Plugins::SpotOn::Plugin.

    Write tests that call Plugins::SpotOn::Plugin->_typeString($client, $mode) with various pref combinations. Use the Prefs stub's set() method to configure bitrate, bitrateOverride, streamFormat per test case. Use a mock $client object (blessed hashref with stringified id for the Prefs client() namespace).

    Add grep-gate tests that scan Plugin.pm, DontStopTheMusic.pm for literal "type => 'Spotify'" (non-comment lines) and Connect.pm for "Ogg Vorbis (Spotify)" — these must return 0 matches after implementation. Use File::Find or direct file read + grep.

    Per D-01: template is "{bitrate}k, {format} (Spotify {mode})".
    Per D-03: format names are OGG, FLAC, MP3, PCM (uppercase short form).
    Per D-04: absent bitrate produces "{format} (Spotify {mode})" without leading comma.
    Per D-07: bitrate from global pref with per-player bitrateOverride precedence.
    Per D-08: MP3 shows Spotify source bitrate, not LAME output bitrate.

    Run tests — they MUST fail (RED phase: _typeString does not exist yet, grep gates will find stale literals).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/10_stream_metadata.t 2>&1 | grep -E "^not ok|^ok|Bail" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - t/10_stream_metadata.t exists with at least 10 test cases
    - Running `perl t/10_stream_metadata.t` produces test output (tests run, most/all fail)
    - Tests cover: all 4 explicit format prefs (ogg/flac/mp3/pcm), auto+passthrough, auto-passthrough, bitrateOverride, undef client, D-04 bitrate-absent guard
    - Grep-gate tests scan actual source files for stale hardcoded type values
    - No syntax errors in test file: `perl -c t/10_stream_metadata.t` exits 0
  </acceptance_criteria>
  <done>Test file compiles, runs, and produces failing test output (RED state). All planned behavior cases present as named tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement _typeString in Plugin.pm and update all 4 call sites</name>
  <files>Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/Connect.pm, Plugins/SpotOn/DontStopTheMusic.pm</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (full file — understand _trackItem at ~line 397, _albumTrackItem at ~line 1135, updateTranscodingTable bitrate chain at ~line 1221, end of file before `1;` for placement of new sub)
    - Plugins/SpotOn/Connect.pm (lines 798-860 — _fetchTrackMetadata, the pluginData set block at lines 844-853)
    - Plugins/SpotOn/DontStopTheMusic.pm (lines 250-277 — _cacheAndExtractUris, the cache set block at lines 262-271)
    - Plugins/SpotOn/ProtocolHandler.pm (lines 42-57 — formatOverride, the `require Plugins::SpotOn::Plugin` pattern at line 48)
    - Plugins/SpotOn/Helper.pm (lines 104-108 — getCapability interface)
    - .planning/phases/09-stream-metadata/09-PATTERNS.md (shared pattern section — exact code patterns to follow)
    - .planning/phases/09-stream-metadata/09-RESEARCH.md (anti-patterns section — what NOT to do)
  </read_first>
  <action>
    **Step A: Add _typeString to Plugin.pm**

    Add a new sub `_typeString` to Plugins::SpotOn::Plugin, placed before `1;` at the end of the file. Signature: `sub _typeString { my ($class, $client, $mode) = @_; }`. The sub is a class method callable as `Plugins::SpotOn::Plugin->_typeString($client, $mode)`.

    Implementation per D-01 through D-08 and the RESEARCH.md data flow:

    1. Bitrate chain (per D-07, mirrors updateTranscodingTable lines 1221-1228): read `$prefs->get('bitrate') || 320`, then if `$client` is defined, check `$prefs->client($client)->get('bitrateOverride')` and override if it matches `/^(?:96|160|320)$/`.

    2. streamFormat chain (mirrors ProtocolHandler.pm formatOverride lines 53-57): if `$client` defined, read `$prefs->client($client)->get('streamFormat') || $prefs->client($client)->get('connectOggOverride') || 'auto'`; else `'auto'`.

    3. Auto resolution (per D-05 discretion, RESEARCH recommendation): if `$fmt eq 'auto'`, `require Plugins::SpotOn::Helper; $fmt = Plugins::SpotOn::Helper->getCapability('passthrough') ? 'ogg' : 'pcm';`.

    4. Format label map (per D-03): `my %LABEL = (ogg => 'OGG', flac => 'FLAC', mp3 => 'MP3', pcm => 'PCM');` — `$fmtLabel = $LABEL{$fmt} || uc($fmt)`.

    5. Assembly (per D-01, D-04): `return $bitrate ? "${bitrate}k, ${fmtLabel} (Spotify ${mode})" : "${fmtLabel} (Spotify ${mode})";`.

    **Step B: Update _trackItem cache-set (Plugin.pm ~line 397)**

    Replace `type => 'Spotify'` with `type => __PACKAGE__->_typeString($client, 'Browse')`. Also replace the inline bitrate line `bitrate => ($prefs->get('bitrate') || 320) . 'k'` with `bitrate => ($prefs->get('bitrate') || 320) . 'k'` (bitrate stays as-is in cache; `type` is the only change). Note: `$client` is available as the first argument to `_trackItem` (line ~367: `my ($class, $client, $track, ...)`).

    **Step C: Update _albumTrackItem cache-set (Plugin.pm ~line 1135)**

    Same replacement: `type => __PACKAGE__->_typeString($client, 'Browse')`. `$client` is available as first arg to `_albumTrackItem`.

    **Step D: Update _fetchTrackMetadata pluginData-set (Connect.pm ~line 844)**

    Replace both `originalType => 'Ogg Vorbis (Spotify)'` and `type => 'Ogg Vorbis (Spotify)'` with the dynamic string. Add `require Plugins::SpotOn::Plugin;` before the pluginData call (same on-demand require pattern as ProtocolHandler.pm line 48). Compute `my $type_str = Plugins::SpotOn::Plugin->_typeString($client, 'Connect');` once, then set both `originalType => $type_str` and `type => $type_str`. `$client` is the first parameter of `_fetchTrackMetadata` (line 799).

    **Step E: Update _cacheAndExtractUris cache-set (DontStopTheMusic.pm ~line 262)**

    Replace `type => 'Spotify'` with `type => Plugins::SpotOn::Plugin->_typeString(undef, 'Browse')`. Add `require Plugins::SpotOn::Plugin;` at the top of the sub (or before the loop). Pass `undef` as `$client` because `_cacheAndExtractUris` receives only `$tracks` (no player context) — this triggers global pref fallback per the undef-client guard in `_typeString`.

    Run `perl t/10_stream_metadata.t` — all tests MUST pass (GREEN). Then run `prove -l t/` to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/10_stream_metadata.t && prove -l t/</automated>
  </verify>
  <acceptance_criteria>
    - Plugin.pm contains `sub _typeString` that accepts ($class, $client, $mode) and returns a string matching D-01 template
    - Plugin.pm _trackItem (line ~397) cache-set has `type => __PACKAGE__->_typeString($client, 'Browse')` — no literal 'Spotify'
    - Plugin.pm _albumTrackItem (line ~1135) cache-set has `type => __PACKAGE__->_typeString($client, 'Browse')` — no literal 'Spotify'
    - Connect.pm _fetchTrackMetadata pluginData-set has dynamic `type` and `originalType` from `Plugin->_typeString($client, 'Connect')` — no literal 'Ogg Vorbis (Spotify)'
    - DontStopTheMusic.pm _cacheAndExtractUris cache-set has `type => Plugins::SpotOn::Plugin->_typeString(undef, 'Browse')` — no literal 'Spotify'
    - `grep -rn "type.*=>.*'Spotify'" Plugins/SpotOn/Plugin.pm Plugins/SpotOn/DontStopTheMusic.pm | grep -v '#' | wc -l` returns 0
    - `grep -rn "Ogg Vorbis (Spotify)" Plugins/SpotOn/Connect.pm | grep -v '#' | wc -l` returns 0
    - `perl t/10_stream_metadata.t` exits 0 with all tests passing
    - `prove -l t/` exits 0 (no regressions)
    - `perl -c Plugins/SpotOn/Plugin.pm 2>&1` shows no syntax errors (when run with stub @INC)
  </acceptance_criteria>
  <done>All unit tests pass (GREEN). All 4 call sites updated. No stale hardcoded type literals remain in production code. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: Verify Songinfo display on live LMS</name>
  <what-built>
    Dynamic stream metadata in Songinfo. The type field now shows format, bitrate, and mode for every playing Spotify track.
  </what-built>
  <how-to-verify>
    1. Restart LMS to reload the plugin
    2. Browse-Modus: Play a track via SpotOn Browse menu. Open Songinfo (Material skin: tap track title in NowPlaying, then "Song Info" or "Track Information"). Verify the type line shows something like "320k, OGG (Spotify Browse)" (exact format/bitrate depends on your streamFormat and bitrate prefs).
    3. Connect-Modus: Start playback from the Spotify app to the LMS player. Check Songinfo. Verify it shows "(Spotify Connect)" with format and bitrate, e.g. "320k, OGG (Spotify Connect)".
    4. Format-Wechsel (optional): Change streamFormat pref in SpotOn player settings (e.g. to FLAC), play a new track, verify Songinfo updates accordingly on the next track.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the displayed strings.</resume-signal>
  <verify>
    <human-check>Songinfo type field shows "{bitrate}k, {format} (Spotify {mode})" for both Browse and Connect tracks.</human-check>
  </verify>
  <acceptance_criteria>
    - Browse track Songinfo type line contains "(Spotify Browse)" with format and bitrate
    - Connect track Songinfo type line contains "(Spotify Connect)" with format and bitrate
    - Format label matches the active streamFormat pref (OGG/FLAC/MP3/PCM)
    - Bitrate matches the configured bitrate pref (96k/160k/320k)
  </acceptance_criteria>
  <done>Live Songinfo displays dynamic metadata for both Browse and Connect modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. Phase 9 only modifies display strings derived from prefs data that the user configured. No external input, no network calls, no filesystem writes, no auth changes.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | _typeString display | accept | Display string shows format/bitrate the user configured — no secrets, no PII |
| T-09-02 | Tampering | Prefs input to _typeString | accept | Prefs are written by authenticated LMS Settings UI only; _typeString validates bitrateOverride with /^(?:96|160|320)$/ regex |

No package installs. T-{phase}-SC not applicable.
</threat_model>

<verification>
## Phase Verification

1. `prove -l t/` — full test suite green, including t/10_stream_metadata.t
2. `grep -rn "type.*=>.*'Spotify'" Plugins/SpotOn/Plugin.pm Plugins/SpotOn/DontStopTheMusic.pm | grep -v '#'` — 0 matches
3. `grep -rn "Ogg Vorbis (Spotify)" Plugins/SpotOn/Connect.pm | grep -v '#'` — 0 matches
4. `perl -c Plugins/SpotOn/Plugin.pm` — no syntax errors (with appropriate @INC)
</verification>

<success_criteria>
1. t/10_stream_metadata.t exists with 10+ test cases covering all META-01..03 behaviors, all pass
2. Plugin.pm contains `_typeString` sub that returns the D-01 template string
3. All 4 metadata call sites (Plugin.pm _trackItem, Plugin.pm _albumTrackItem, Connect.pm _fetchTrackMetadata, DontStopTheMusic.pm _cacheAndExtractUris) use _typeString — no hardcoded type strings remain
4. Full test suite (`prove -l t/`) passes with zero failures
5. Live Songinfo shows format+bitrate+mode for both Browse and Connect tracks (human verified)
</success_criteria>

<output>
Create `.planning/phases/09-stream-metadata/09-01-SUMMARY.md` when done
</output>
