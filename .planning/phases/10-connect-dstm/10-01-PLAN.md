---
phase: 10-connect-dstm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - librespot-spoton/src/main.rs
  - librespot-spoton/src/connect.rs
autonomous: true
requirements:
  - DSTM-01
  - DSTM-02
  - DSTM-03
  - DSTM-04

must_haves:
  truths:
    - "Binary accepts --autoplay on/off CLI flag"
    - "Binary --check JSON reports autoplay: true"
    - "run_connect() sets SessionConfig.autoplay before Session::new()"
  artifacts:
    - path: "librespot-spoton/src/main.rs"
      provides: "--autoplay flag parsing + autoplay in --check JSON + autoplay param in run_connect() call"
      contains: '"--autoplay"'
    - path: "librespot-spoton/src/connect.rs"
      provides: "autoplay: Option<bool> parameter + SessionConfig.autoplay override"
      contains: "session_config.autoplay"
  key_links:
    - from: "librespot-spoton/src/main.rs"
      to: "librespot-spoton/src/connect.rs"
      via: "autoplay param passed to connect::run_connect()"
      pattern: "run_connect.*autoplay"
---

<objective>
Add Spirc-native autoplay support to the librespot-spoton binary.

Purpose: Enable the binary to accept a `--autoplay on/off` CLI flag that controls `SessionConfig.autoplay`, activating Spirc's built-in autoplay context resolution. This is the core mechanism per D-01 — no EndOfTrack events, no grace timers, no API queue injection. The binary's --check manifest also reports the `autoplay` capability for Helper.pm detection.

Output: Modified main.rs and connect.rs with autoplay flag parsing, capability reporting, and SessionConfig override.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-CONTEXT.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-RESEARCH.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From librespot-core-0.8.0/src/config.rs line 31:
  SessionConfig.autoplay: Option<bool>
  Default: None (follows Spotify user setting)
  Some(true) = force on, Some(false) = force off

From librespot-spoton/src/connect.rs line 821-828 (current signature):
  pub async fn run_connect(
      cache_dir: &str,
      device_name: &str,
      player_mac: Option<&str>,
      lms_host_port: Option<&str>,
      lms_auth: Option<&str>,
      disable_discovery: bool,
      buffer_latency_ms: u64,
  ) -> Result<(), Box<dyn std::error::Error>>

From librespot-spoton/src/main.rs line 310-318 (current call site):
  connect::run_connect(&cache_dir, &device_name, ..., disable_discovery, buffer_latency_ms)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --autoplay flag parsing and --check capability to main.rs</name>
  <files>librespot-spoton/src/main.rs</files>
  <read_first>
    - librespot-spoton/src/main.rs (full file — current arg loop structure, --check JSON, run_connect call site)
    - .planning/phases/10-connect-dstm/10-PATTERNS.md (exact line numbers and patterns for main.rs)
  </read_first>
  <action>
    Per D-04, D-06: Add autoplay support to main.rs in three locations:

    1. Variable declaration (after line 110, the Connect mode variables block): Declare `let mut autoplay: Option&lt;bool&gt; = None;` alongside existing `disable_discovery` and `buffer_latency_ms`.

    2. Arg-loop match arm (after the `"--buffer-latency-ms"` arm around line 187): Add a new `"--autoplay"` match arm. Use the same value-reading pattern as `"--buffer-latency-ms"`. Check `i + 1 &lt; args.len()`, then match `args[i + 1].as_str()` mapping `"on"` to `Some(true)`, `"off"` to `Some(false)`, anything else to `None`. Increment `i += 1`.

    3. `--check` JSON manifest (inside `Mode::Check` around line 244): Add `"autoplay": true` to the `serde_json::json!({})` object, alongside existing keys like `"discover-once"`, `"token-login"`, etc. The key name `"autoplay"` matches the Spotty-NG convention and the `getCapability('autoplay')` call in Helper.pm.

    4. `run_connect()` call site (inside `Mode::Connect` around line 310): Append `autoplay` as the final argument to `connect::run_connect(...)`, after `buffer_latency_ms`.

    Values `"on"` and `"off"` (not `"true"`/`"false"`) match Spotty-NG convention per D-03.
  </action>
  <acceptance_criteria>
    - main.rs contains `let mut autoplay: Option<bool> = None;` in the Connect mode variables block
    - main.rs contains a `"--autoplay"` match arm that maps "on" to Some(true) and "off" to Some(false)
    - main.rs --check JSON object contains `"autoplay": true`
    - main.rs run_connect() call passes `autoplay` as the last argument (9th positional)
    - `cargo check` in librespot-spoton/ succeeds with no errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>main.rs parses --autoplay on/off, reports autoplay:true in --check, and passes autoplay to run_connect()</done>
</task>

<task type="auto">
  <name>Task 2: Add autoplay parameter to run_connect() in connect.rs</name>
  <files>librespot-spoton/src/connect.rs</files>
  <read_first>
    - librespot-spoton/src/connect.rs lines 820-870 (run_connect signature + SessionConfig setup)
    - .planning/phases/10-connect-dstm/10-PATTERNS.md (connect.rs pattern section)
    - .planning/phases/10-connect-dstm/10-RESEARCH.md (Pitfall 4: session_config.autoplay must be set BEFORE Session::new())
  </read_first>
  <action>
    Per D-05: Modify connect.rs in two locations:

    1. Function signature (line 821-828): Add `autoplay: Option&lt;bool&gt;` as the final parameter after `buffer_latency_ms: u64`.

    2. SessionConfig setup (between lines 868 and 869): After `session_config.device_id = device_id_shared.clone();` and BEFORE `let session = Session::new(session_config.clone(), Some(cache.clone()));`, add:
       - `if let Some(ap) = autoplay { session_config.autoplay = Some(ap); }`
       
    CRITICAL per RESEARCH Pitfall 4: The autoplay override MUST be set BEFORE `Session::new()` because SessionConfig is cloned at session creation. Setting it after has no effect.

    No changes needed to the reconnect path — `session_config.clone()` at line 1053 already carries the autoplay field (RESEARCH Pitfall 5).
  </action>
  <acceptance_criteria>
    - connect.rs run_connect() signature has `autoplay: Option<bool>` as the last parameter
    - connect.rs contains `if let Some(ap) = autoplay { session_config.autoplay = Some(ap); }` BEFORE the `Session::new()` call
    - The autoplay set block appears between `session_config.device_id = ...` and `let session = Session::new(...)`
    - `cargo check` in librespot-spoton/ succeeds with no errors
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo check 2>&1 | tail -5</automated>
  </verify>
  <done>run_connect() accepts autoplay param and sets SessionConfig.autoplay before Session::new()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → binary | --autoplay value comes from Perl DaemonManager, not from user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | --autoplay CLI arg | accept | Value originates from Perl code (hardcoded "on"/"off"), not from external user input. Unknown values map to None (Spotify user setting applies). |
| T-10-SC | Tampering | cargo build | mitigate | No new crate dependencies added — only using existing librespot-core SessionConfig field |
</threat_model>

<verification>
After both tasks: `cd /home/sti/spoton/librespot-spoton && cargo check` succeeds.
Grep verification: `grep -c '"--autoplay"' librespot-spoton/src/main.rs` returns 1.
Grep verification: `grep -c 'session_config.autoplay' librespot-spoton/src/connect.rs` returns 1.
</verification>

<success_criteria>
- cargo check passes with zero errors
- main.rs has --autoplay flag parsing, --check JSON autoplay:true, and autoplay in run_connect() call
- connect.rs has autoplay: Option<bool> in signature and sets SessionConfig.autoplay before Session::new()
</success_criteria>

<output>
Create `.planning/phases/10-connect-dstm/10-01-SUMMARY.md` when done
</output>
