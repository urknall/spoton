---
phase: 10-connect-dstm
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Connect/Daemon.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
autonomous: true
requirements:
  - DSTM-05
  - DSTM-06

must_haves:
  truths:
    - "D-08: Per-player enableAutoplay pref exists with default 1"
    - "D-09: DaemonManager passes --autoplay on/off to daemon based on pref"
    - "D-10: Settings UI shows Autoplay toggle only when getCapability('autoplay') is true"
    - "D-11: Autoplay OFF sets LMS DSTM provider to disabled for that player"
    - "D-12: Autoplay ON sets LMS DSTM provider to PLUGIN_SPOTON_RECOMMENDATIONS"
    - "D-13: DSTM dropdown SpotOn selection syncs Autoplay toggle to ON"
    - "D-14: DSTM dropdown Off/other syncs Autoplay toggle to OFF"
    - "D-15: DSTM provider stays registered regardless of toggle state"
    - "Browse-DSTM handler registration is unchanged (DSTM-06)"
    - "i18n strings exist in all 11 languages for Autoplay toggle"
  artifacts:
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "enableAutoplay => 1 in prefs init"
      contains: "enableAutoplay"
    - path: "Plugins/SpotOn/Connect/Daemon.pm"
      provides: "--autoplay on/off flag in @helperArgs"
      contains: "--autoplay"
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "enableAutoplay save handler + DSTM sync + template vars"
      contains: "enableAutoplay"
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      provides: "Autoplay checkbox UI element"
      contains: "pref_enableAutoplay"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "PLUGIN_SPOTON_AUTOPLAY_ENABLED + _DESC + _LABEL in 11 languages"
      contains: "PLUGIN_SPOTON_AUTOPLAY_ENABLED"
  key_links:
    - from: "Plugins/SpotOn/Settings.pm"
      to: "preferences('plugin.dontstopthemusic')"
      via: "cross-namespace pref write for DSTM sync"
      pattern: "preferences.*dontstopthemusic.*provider"
    - from: "Plugins/SpotOn/Connect/Daemon.pm"
      to: "Plugins/SpotOn/Helper.pm"
      via: "getCapability('autoplay') gate"
      pattern: "getCapability.*autoplay"
    - from: "Plugins/SpotOn/Settings.pm"
      to: "Plugins/SpotOn/Connect/DaemonManager.pm"
      via: "stopForSync + initHelpers for daemon restart"
      pattern: "stopForSync.*initHelpers"
---

<objective>
Wire the Spirc autoplay capability through the Perl plugin layer: pref initialization, daemon flag passing, Settings UI toggle, bidirectional DSTM provider sync, and i18n strings.

Purpose: Per D-08 through D-15, expose the binary's autoplay capability as a per-player toggle in the Settings UI. When toggled, the DSTM provider selection is synchronized bidirectionally, and the Connect daemon restarts with the correct --autoplay flag. Browse-DSTM (DSTM-06) remains unchanged — the DontStopTheMusic.pm handler is not modified.

Output: Five modified Perl/template/i18n files enabling the full Settings-to-daemon-to-Spirc autoplay pipeline.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-CONTEXT.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-RESEARCH.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Plugin.pm line 41-50 (current prefs init):
  $prefs->init({ ..., enableSpotifyConnect => 1, connectOggOverride => 'auto', disableDiscovery => 0 });

From Plugins/SpotOn/Connect/Daemon.pm lines 60-120 (start method):
  sub start { ... my $client = Slim::Player::Client::getClient($self->mac); ... push @helperArgs, ... }

From Plugins/SpotOn/Settings.pm lines 145-176 (per-player save block):
  if ($client) { my $enableConnect = $paramRef->{'pref_enableSpotifyConnect'} ? 1 : 0; ... }

From Plugins/SpotOn/Settings.pm lines 200-214 (template vars block):
  if ($client) { $paramRef->{connectEnabled} = ...; $paramRef->{streamFormat} = ...; }

From Plugins/SpotOn/Helper.pm line 104-108:
  sub getCapability { my ($class, $key) = @_; return $helperCapabilities->{$key} ... }

From Plugins/SpotOn/DontStopTheMusic.pm line 22-25:
  registerHandler('PLUGIN_SPOTON_RECOMMENDATIONS', \&dontStopTheMusic);

From basic.html lines 21-26 (Connect checkbox pattern):
  [% WRAPPER setting title="PLUGIN_SPOTON_CONNECT_ENABLED" desc="..." %]
    <input type="checkbox" ... name="pref_enableSpotifyConnect" ... />
  [% END %]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add enableAutoplay pref, daemon flag, and i18n strings</name>
  <files>Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/Connect/Daemon.pm, Plugins/SpotOn/strings.txt</files>
  <read_first>
    - Plugins/SpotOn/Plugin.pm (lines 41-50 for prefs init block)
    - Plugins/SpotOn/Connect/Daemon.pm (lines 95-135 for start() helperArgs construction)
    - Plugins/SpotOn/strings.txt (lines 690-730 for CONNECT_ENABLED block structure — use as template for new keys)
    - Plugins/SpotOn/DontStopTheMusic.pm (lines 22-25 — verify handler key is PLUGIN_SPOTON_RECOMMENDATIONS)
    - .planning/phases/10-connect-dstm/10-PATTERNS.md (Plugin.pm, Daemon.pm, strings.txt sections)
  </read_first>
  <action>
    Three files to modify:

    **Plugin.pm** (per D-08): Add `enableAutoplay => 1` to the `$prefs->init({})` hash at line 41-50. Place it after `disableDiscovery => 0`. Add comment: `# D-08: Autoplay toggle, default on (controls Connect autoplay + DSTM)`. No other changes to Plugin.pm — DSTM registration at lines 87-92 stays as-is per D-15.

    **Daemon.pm** (per D-09): In the `start()` method, after the `push @helperArgs, '--enable-volume-normalisation'` line (line 120), add a capability-gated autoplay flag block. Gate on `Plugins::SpotOn::Helper->getCapability('autoplay')`. Inside the gate: read the pref with `$prefs->client($client)->get('enableAutoplay')`, default to 1 if undefined (`$enableAutoplay = 1 unless defined $enableAutoplay`), then `push @helperArgs, '--autoplay', ($enableAutoplay ? 'on' : 'off')`. This matches Spotty-NG Connect/Daemon.pm lines 102-103 exactly. Place BEFORE the security log line at line 123 (args are logged before --lms-auth is added).

    **strings.txt**: Add three new i18n key blocks adjacent to the PLUGIN_SPOTON_CONNECT_ENABLED block (around line 728, after the CONNECT_ENABLED_LABEL block ends). All 11 languages (CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV), following the exact same indentation and language-code pattern as existing blocks. Keys and translations per RESEARCH.md:
    - `PLUGIN_SPOTON_AUTOPLAY_ENABLED` (title)
    - `PLUGIN_SPOTON_AUTOPLAY_ENABLED_DESC` (description)
    - `PLUGIN_SPOTON_AUTOPLAY_ENABLED_LABEL` (checkbox label)
    The exact translations are specified in 10-RESEARCH.md "Strings.txt — Neue i18n-Keys" section.
  </action>
  <acceptance_criteria>
    - Plugin.pm $prefs->init hash contains `enableAutoplay => 1`
    - Daemon.pm start() contains `push @helperArgs, '--autoplay', ($enableAutoplay ? 'on' : 'off')` inside a getCapability('autoplay') guard
    - Daemon.pm references `$prefs->client($client)->get('enableAutoplay')` with fallback default 1
    - strings.txt contains PLUGIN_SPOTON_AUTOPLAY_ENABLED with exactly 11 language entries (CS through SV)
    - strings.txt contains PLUGIN_SPOTON_AUTOPLAY_ENABLED_DESC with exactly 11 language entries
    - strings.txt contains PLUGIN_SPOTON_AUTOPLAY_ENABLED_LABEL with exactly 11 language entries
    - `prove -l t/05_perl_syntax.t t/02_strings.t` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/05_perl_syntax.t t/02_strings.t 2>&1 | tail -10</automated>
  </verify>
  <done>enableAutoplay pref initialized with default 1; Daemon passes --autoplay on/off; all i18n strings present in 11 languages</done>
</task>

<task type="auto">
  <name>Task 2: Add Settings UI toggle with DSTM bidirectional sync</name>
  <files>Plugins/SpotOn/Settings.pm, Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html</files>
  <read_first>
    - Plugins/SpotOn/Settings.pm (lines 140-216 for full handler method — save block + template vars block)
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (full file — checkbox patterns)
    - .planning/phases/10-connect-dstm/10-PATTERNS.md (Settings.pm and basic.html sections)
    - .planning/phases/10-connect-dstm/10-RESEARCH.md (Pitfall 1: stopForSync before initHelpers; Pitfall 2: cross-namespace pref; Pitfall 3: callback loop prevention; Open Question 1: D-13/D-14 via page-load read)
  </read_first>
  <action>
    Two files to modify:

    **Settings.pm — Save handler** (per D-08, D-09, D-11, D-12): In the `if ($client)` block (starts at line 145), add an enableAutoplay save block BEFORE the existing `require Plugins::SpotOn::Connect::DaemonManager; DaemonManager->initHelpers();` call (lines 174-175). The new block:
    1. Read `$paramRef->{'pref_enableAutoplay'}`, coerce to 0/1 (checkbox unchecked = absent = 0, per established pattern at line 146).
    2. `$prefs->client($client)->set('enableAutoplay', $enableAutoplay)`.
    3. DSTM sync (D-11/D-12): Check `Slim::Utils::PluginManager->isEnabled('Slim::Plugin::DontStopTheMusic::Plugin')`. If enabled, create a separate prefs object: `my $dstmPrefs = preferences('plugin.dontstopthemusic')`. If autoplay ON: `$dstmPrefs->client($client)->set('provider', 'PLUGIN_SPOTON_RECOMMENDATIONS')`. If autoplay OFF: `$dstmPrefs->client($client)->set('provider', 0)`. The provider key `'PLUGIN_SPOTON_RECOMMENDATIONS'` must match the key used in DontStopTheMusic.pm::init() exactly.
    4. Daemon restart (RESEARCH Pitfall 1): Stop the live daemon explicitly before initHelpers — `my $helper = Plugins::SpotOn::Connect::DaemonManager->helperForClient($client); $helper->stopForSync() if $helper && $helper->alive;`. This is needed because `startHelper()` skips live daemons. Place this INSIDE the enableAutoplay block, not at the existing initHelpers call (which handles other pref changes).

    **Settings.pm — Template vars** (per D-10, D-13, D-14): In the `if ($client)` block (starts at line 202), add two template vars:
    1. `$paramRef->{canAutoplay} = Plugins::SpotOn::Helper->getCapability('autoplay') ? 1 : 0;`
    2. `$paramRef->{autoplayEnabled} = $prefs->client($client)->get('enableAutoplay') // 1;`
    3. D-13/D-14 reverse sync (DSTM->SpotOn direction): After setting autoplayEnabled, if canAutoplay is true AND DontStopTheMusic is enabled, read the current DSTM provider: `my $dstmPrefs = preferences('plugin.dontstopthemusic'); my $dstmProvider = $dstmPrefs->client($client)->get('provider') // '';`. Override autoplayEnabled: if `$dstmProvider eq 'PLUGIN_SPOTON_RECOMMENDATIONS'` then 1, else 0. This implements D-13/D-14 at page-load time without callbacks, avoiding the loop risk described in RESEARCH Pitfall 3.

    **basic.html** (per D-10): Add a new checkbox block AFTER the pref_enableSpotifyConnect block (after line 26, before the Discovery block at line 28). Wrap in `[% IF canAutoplay %]...[% END %]` for capability gating. Use the WRAPPER setting pattern with title `PLUGIN_SPOTON_AUTOPLAY_ENABLED`, desc `PLUGIN_SPOTON_AUTOPLAY_ENABLED_DESC`. Checkbox: name `pref_enableAutoplay`, value `1`, checked if `autoplayEnabled`. Label: string filter `PLUGIN_SPOTON_AUTOPLAY_ENABLED_LABEL`. Follow the exact HTML structure of the Connect checkbox at lines 21-26.
  </action>
  <acceptance_criteria>
    - Settings.pm save handler contains `$prefs->client($client)->set('enableAutoplay', ...)` with 0/1 coercion
    - Settings.pm contains `preferences('plugin.dontstopthemusic')` for cross-namespace DSTM sync
    - Settings.pm contains `$dstmPrefs->client($client)->set('provider', 'PLUGIN_SPOTON_RECOMMENDATIONS')` (ON path)
    - Settings.pm contains `$dstmPrefs->client($client)->set('provider', 0)` (OFF path)
    - Settings.pm contains `stopForSync()` call before `initHelpers()` in the enableAutoplay block
    - Settings.pm template vars block contains `canAutoplay` and `autoplayEnabled` vars
    - Settings.pm has D-13/D-14 reverse sync: reads DSTM provider at page-load to override autoplayEnabled
    - basic.html contains `[% IF canAutoplay %]` guard around the Autoplay checkbox block
    - basic.html Autoplay checkbox is placed between Connect checkbox and Discovery checkbox
    - `prove -l t/05_perl_syntax.t` passes
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton && prove -l t/05_perl_syntax.t 2>&1 | tail -5</automated>
  </verify>
  <done>Settings page shows capability-gated Autoplay toggle; save handler syncs DSTM provider bidirectionally and restarts daemon with new flag</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → Settings.pm | pref_enableAutoplay checkbox value from HTTP POST |
| Settings.pm → plugin.dontstopthemusic namespace | Cross-namespace pref write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Tampering | pref_enableAutoplay form field | mitigate | Coerce to 0/1 via `? 1 : 0` pattern (established in Settings.pm for all checkbox prefs) |
| T-10-03 | Tampering | --autoplay CLI flag value | accept | Value is statically 'on' or 'off' from Perl code, not user input |
| T-10-04 | Denial of Service | DSTM callback loop | mitigate | D-13/D-14 sync implemented as page-load read, not pref-change callback — no loop possible |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
After both tasks: `prove -l t/05_perl_syntax.t t/02_strings.t` passes.
Grep verification: `grep -c 'enableAutoplay' Plugins/SpotOn/Plugin.pm` returns >= 1.
Grep verification: `grep -c 'enableAutoplay' Plugins/SpotOn/Settings.pm` returns >= 3.
Grep verification: `grep -c 'pref_enableAutoplay' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html` returns >= 1.
Grep verification: `grep -c 'PLUGIN_SPOTON_AUTOPLAY_ENABLED' Plugins/SpotOn/strings.txt` returns >= 3 (three key blocks).
DontStopTheMusic.pm is NOT modified (DSTM-06 regression safety).
</verification>

<success_criteria>
- enableAutoplay pref initialized in Plugin.pm with default 1
- Daemon.pm passes --autoplay on/off flag gated on binary capability
- Settings UI shows Autoplay checkbox when binary supports it
- Save handler syncs DSTM provider and restarts daemon
- i18n strings present in all 11 languages
- DontStopTheMusic.pm unchanged (Browse-DSTM regression-safe)
- All Perl syntax and strings tests pass
</success_criteria>

<output>
Create `.planning/phases/10-connect-dstm/10-02-SUMMARY.md` when done
</output>
