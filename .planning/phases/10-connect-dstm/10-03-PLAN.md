---
phase: 10-connect-dstm
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - Plugins/SpotOn/Bin/x86_64-linux/spoton
  - Plugins/SpotOn/Bin/aarch64-linux/spoton
  - Plugins/SpotOn/Bin/armhf-linux/spoton
  - Plugins/SpotOn/Bin/arm-linux/spoton
  - Plugins/SpotOn/Bin/i386-linux/spoton
  - Plugins/SpotOn/Bin/x86_64-win64/spoton.exe
autonomous: false
requirements:
  - DSTM-01
  - DSTM-02
  - DSTM-03
  - DSTM-04
  - DSTM-05
  - DSTM-06

must_haves:
  truths:
    - "All 6 platform binaries report autoplay:true in --check JSON"
    - "x86_64-linux binary is musl-static (ldd returns 'not a dynamic executable')"
    - "Connect autoplay continues playback after Spotify queue exhaustion"
    - "Per-player Autoplay toggle controls Connect and Browse DSTM"
    - "Browse-DSTM works without regression"
  artifacts:
    - path: "Plugins/SpotOn/Bin/x86_64-linux/spoton"
      provides: "x86_64 Linux binary with autoplay support"
    - path: "Plugins/SpotOn/Bin/aarch64-linux/spoton"
      provides: "aarch64 Linux binary with autoplay support"
    - path: "Plugins/SpotOn/Bin/armhf-linux/spoton"
      provides: "ARMv7 Linux binary with autoplay support"
    - path: "Plugins/SpotOn/Bin/arm-linux/spoton"
      provides: "ARMv6 Linux binary with autoplay support"
    - path: "Plugins/SpotOn/Bin/i386-linux/spoton"
      provides: "i386 Linux binary with autoplay support"
    - path: "Plugins/SpotOn/Bin/x86_64-win64/spoton.exe"
      provides: "Windows binary with autoplay support"
  key_links:
    - from: "Plugins/SpotOn/Bin/x86_64-linux/spoton"
      to: "Plugins/SpotOn/Helper.pm"
      via: "--check JSON parsed by Helper::init()"
      pattern: "getCapability.*autoplay"
---

<objective>
Cross-compile the updated librespot-spoton binary for all 6 platform targets and verify end-to-end Connect autoplay functionality.

Purpose: Per D-07, the binary changes from Plan 01 must be compiled for all 8 platform targets (6 cross-compiled + 2 macOS deferred). The rebuilt binaries enable Helper.pm to detect the autoplay capability, which unlocks the Settings UI toggle from Plan 02. End-to-end verification confirms that Spirc-native autoplay, the per-player toggle, and Browse-DSTM all work correctly.

Output: Six updated platform binaries in Bin/ subdirectories, all reporting autoplay:true in --check.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-CONTEXT.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-RESEARCH.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-PATTERNS.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-01-SUMMARY.md
@/home/sti/spoton/.planning/phases/10-connect-dstm/10-02-SUMMARY.md

<interfaces>
<!-- Build configuration from Phase 8. -->

From librespot-spoton/Cross.toml:
  6 targets configured for cross-rs

Target → Bin/ mapping:
  x86_64-unknown-linux-musl     → Plugins/SpotOn/Bin/x86_64-linux/spoton
  aarch64-unknown-linux-musl    → Plugins/SpotOn/Bin/aarch64-linux/spoton
  armv7-unknown-linux-musleabihf → Plugins/SpotOn/Bin/armhf-linux/spoton
  arm-unknown-linux-musleabihf  → Plugins/SpotOn/Bin/arm-linux/spoton
  i686-unknown-linux-musl       → Plugins/SpotOn/Bin/i386-linux/spoton
  x86_64-pc-windows-gnu         → Plugins/SpotOn/Bin/x86_64-win64/spoton.exe

Phase 8 lesson: Clean target/release/build/ and target/release/deps/ between cross-rs Docker targets to prevent GLIBC ABI conflicts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-compile all 6 platform binaries and deploy to Bin/</name>
  <files>
    Plugins/SpotOn/Bin/x86_64-linux/spoton,
    Plugins/SpotOn/Bin/aarch64-linux/spoton,
    Plugins/SpotOn/Bin/armhf-linux/spoton,
    Plugins/SpotOn/Bin/arm-linux/spoton,
    Plugins/SpotOn/Bin/i386-linux/spoton,
    Plugins/SpotOn/Bin/x86_64-win64/spoton.exe
  </files>
  <read_first>
    - librespot-spoton/Cross.toml (cross-rs target configuration)
    - librespot-spoton/Cargo.toml (version, features)
    - .planning/phases/10-connect-dstm/10-RESEARCH.md (Binary Rebuild Workflow section)
    - .planning/phases/10-connect-dstm/10-01-SUMMARY.md (verify binary source changes completed)
  </read_first>
  <action>
    Per D-07: Rebuild all 6 platform binaries with the new --autoplay support.

    Before starting, verify Docker is available: `docker info` must succeed (required by cross-rs).

    Build sequence — execute each target sequentially, cleaning build artifacts between targets to prevent GLIBC ABI conflicts (Phase 8 lesson):

    For each of the 6 targets (x86_64-unknown-linux-musl, aarch64-unknown-linux-musl, armv7-unknown-linux-musleabihf, arm-unknown-linux-musleabihf, i686-unknown-linux-musl, x86_64-pc-windows-gnu):
    1. Clean: `rm -rf target/release/build/ target/release/deps/`
    2. Build: `cross build --release --target {TARGET}`
    3. Copy: `cp target/{TARGET}/release/spoton{.exe} {BIN_DIR}/`
    4. Verify size: `ls -la {BIN_DIR}/spoton{.exe}`

    Target-to-directory mapping:
    - x86_64-unknown-linux-musl → Plugins/SpotOn/Bin/x86_64-linux/spoton
    - aarch64-unknown-linux-musl → Plugins/SpotOn/Bin/aarch64-linux/spoton
    - armv7-unknown-linux-musleabihf → Plugins/SpotOn/Bin/armhf-linux/spoton
    - arm-unknown-linux-musleabihf → Plugins/SpotOn/Bin/arm-linux/spoton
    - i686-unknown-linux-musl → Plugins/SpotOn/Bin/i386-linux/spoton
    - x86_64-pc-windows-gnu → Plugins/SpotOn/Bin/x86_64-win64/spoton.exe

    After all builds, verify x86_64-linux binary:
    - `ldd Plugins/SpotOn/Bin/x86_64-linux/spoton` → "not a dynamic executable" (musl-static)
    - `Plugins/SpotOn/Bin/x86_64-linux/spoton --check` → line 2 JSON contains `"autoplay":true`
    - `Plugins/SpotOn/Bin/x86_64-linux/spoton --check | tail -1 | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['autoplay']==True; print('autoplay: OK')"` → confirms capability
  </action>
  <acceptance_criteria>
    - All 6 Bin/ directories contain updated binaries (file timestamp is after build start)
    - x86_64-linux binary: `ldd` returns "not a dynamic executable"
    - x86_64-linux binary: `--check` output line 2 JSON contains `"autoplay":true`
    - x86_64-linux binary: `--check` output line 1 matches `ok spoton v{VERSION}`
    - All 5 Linux binaries are ELF format: `file Plugins/SpotOn/Bin/*/spoton` returns "ELF" for each
    - Windows binary is PE format: `file Plugins/SpotOn/Bin/x86_64-win64/spoton.exe` returns "PE32+"
  </acceptance_criteria>
  <verify>
    <automated>cd /home/sti/spoton && Plugins/SpotOn/Bin/x86_64-linux/spoton --check 2>&1 | tail -1 | python3 -c "import sys,json; d=json.load(sys.stdin); assert d.get('autoplay')==True; print('autoplay capability: OK')"</automated>
  </verify>
  <done>All 6 platform binaries rebuilt with autoplay support; x86_64-linux verified musl-static with autoplay:true in --check</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end Connect autoplay and DSTM verification</name>
  <what-built>
    Complete Connect-DSTM implementation:
    - Binary supports --autoplay on/off (Spirc-native autoplay)
    - Per-player Autoplay toggle in Settings UI
    - Bidirectional DSTM provider sync
    - All 6 platform binaries rebuilt
  </what-built>
  <how-to-verify>
    **Setup:** Restart LMS to load updated plugin + binary.

    **1. Settings UI verification:**
    - Open LMS Settings → SpotOn → select a player
    - Verify Autoplay checkbox appears (between Connect and Discovery toggles)
    - Verify checkbox is checked by default (enableAutoplay default = 1)

    **2. DSTM sync verification (D-11/D-12):**
    - Uncheck the Autoplay toggle, save
    - Navigate to Player Settings → Don't Stop The Music dropdown → should show "Off" (not SpotOn)
    - Re-check the Autoplay toggle, save
    - DSTM dropdown should now show "SpotOn Empfehlungen"

    **3. Connect autoplay verification (DSTM-01 through DSTM-04):**
    - With Autoplay ON: Open Spotify app → select the LMS player as Connect device
    - Play a single track (not from a playlist — use search to find one track)
    - Wait for the track to end
    - Verify: playback continues automatically with a related track (Spirc autoplay)
    - Note: no gap longer than ~10 seconds expected

    **4. Autoplay OFF verification (DSTM-05):**
    - Disable Autoplay toggle in Settings, save
    - Play a single track via Connect
    - Wait for track to end
    - Verify: playback stops (no autoplay continuation)

    **5. Browse-DSTM regression (DSTM-06):**
    - Re-enable Autoplay toggle
    - Play a track via LMS Browse menu (not Connect)
    - Wait for track to end
    - Verify: DSTM fires and next track plays (Browse-DSTM unchanged)
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which check(s) failed with details</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cross-rs Docker → binary | Build toolchain produces trusted binary |
| Binary → Spotify server | Spirc protocol for autoplay context requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Tampering | cross-rs build | mitigate | Use same cross-rs Docker images as Phase 8; verify binary with --check and ldd |
| T-10-06 | Information Disclosure | Spirc autoplay | accept | Autoplay context requests go to Spotify servers via existing authenticated session — no new data exposure |
| T-10-SC | Tampering | cargo build dependencies | accept | No new crate dependencies added in Phase 10 — same Cargo.lock as Phase 8 |
</threat_model>

<verification>
After Task 1: `Plugins/SpotOn/Bin/x86_64-linux/spoton --check` reports autoplay:true.
After Task 1: `prove -l t/06_binary_check.t` passes (binary --check format validated).
After Task 2: Human confirms Connect autoplay, Settings toggle, DSTM sync, and Browse-DSTM regression.
Full test suite: `prove -l t/` passes.
</verification>

<success_criteria>
- All 6 platform binaries rebuilt and deployed to Bin/ directories
- x86_64-linux binary verified musl-static with autoplay:true capability
- Human confirms: Connect autoplay works, toggle controls behavior, DSTM syncs bidirectionally, Browse-DSTM works
</success_criteria>

<output>
Create `.planning/phases/10-connect-dstm/10-03-SUMMARY.md` when done
</output>
