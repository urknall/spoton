---
phase: 11-track-history-metadata
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - Plugins/SpotOn/ProtocolHandler.pm
  - t/11_track_history.t
autonomous: false
requirements:
  - HIST-03
  - HIST-04
must_haves:
  truths:
    - "getMetadataFor returns placeholder metadata on cache miss instead of empty hashref"
    - "Async re-fetch fires API::Client->getTrack and populates cache after callback"
    - "Debounce hash prevents duplicate concurrent fetches for the same URL"
    - "Connect history URLs with spotifyUri in cache return Browse-pipeline metadata"
    - "Connect history tracks show Browse mode label, not Connect label (D-07)"
  artifacts:
    - path: "Plugins/SpotOn/ProtocolHandler.pm"
      provides: "Async re-fetch, debounce, placeholder, Connect-to-Browse translation"
      contains: "_asyncRefetch"
    - path: "Plugins/SpotOn/ProtocolHandler.pm"
      provides: "Debounce hash"
      contains: "%_pendingRefetch"
    - path: "t/11_track_history.t"
      provides: "Tests for re-fetch, debounce, Connect translation"
  key_links:
    - from: "Plugins/SpotOn/ProtocolHandler.pm"
      to: "Plugins::SpotOn::API::Client"
      via: "require + getTrack in _asyncRefetch"
      pattern: "API::Client->getTrack"
    - from: "Plugins/SpotOn/ProtocolHandler.pm"
      to: "Slim::Control::Request"
      via: "notifyFromArray in async callback"
      pattern: "notifyFromArray.*newmetadata"
---

<objective>
Implement async cache-miss re-fetch with debounce and Connect-to-Browse URL translation in ProtocolHandler.pm's getMetadataFor. Cache misses return placeholder metadata immediately while an async API call repopulates the cache. Connect history URLs with a cached spotifyUri are transparently translated to Browse-pipeline metadata.

Purpose: History tracks that have expired from cache are re-fetched on demand (HIST-04), and Connect tracks in history become replayable via Browse pipeline (HIST-03). Combined with Plan 01's TTL bump, this completes the "Was lief da eben?" experience.
Output: Modified ProtocolHandler.pm, updated t/11_track_history.t (TODO tests become real)
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/phases/11-track-history-metadata/11-CONTEXT.md
@/home/sti/spoton/.planning/phases/11-track-history-metadata/11-RESEARCH.md
@/home/sti/spoton/.planning/phases/11-track-history-metadata/11-PATTERNS.md
@/home/sti/spoton/.planning/phases/11-track-history-metadata/11-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 and existing codebase -->

From Plugins/SpotOn/ProtocolHandler.pm — current getMetadataFor (lines 268-304):
  sub getMetadataFor {
      my ($class, $client, $url) = @_;
      # Connect pluginData check...
      # Normalize spotify: -> spotify:// ...
      # $meta = $cache->get('spoton_meta_' . md5_hex($canonical));
      # Fallback: try original URL...
      return {} unless $meta;    # <-- THIS LINE is the insertion point for async re-fetch
      # if ($client) { return enriched $meta with type + bitrate }
      return $meta;
  }

From Plugins/SpotOn/Connect.pm — cache entry structure (set by Plan 01):
  $cache->set('spoton_meta_' . md5_hex($connectUrl), {
      title, artist, album, duration, cover, icon,
      bitrate, type,
      spotifyUri => 'spotify:track:ABC123'   # <-- Plan 02 reads this
  }, 604800);

From Plugins/SpotOn/Connect.pm (lines 805-807) — accountId resolution pattern:
  my $accountId = $prefs->client($client)->get('activeAccount')
               || $prefs->get('activeAccount')
               || '';

From Plugins/SpotOn/Connect.pm (lines 880-888) — _largestImage helper:
  sub _largestImage { ... sort by width, return URL ... }
  NOTE: This function does NOT exist in ProtocolHandler.pm — must be added or inlined.

From Plugins/SpotOn/Connect.pm (line 21) — IMG_TRACK constant:
  use constant IMG_TRACK => '/html/images/cover.png';
  NOTE: NOT available in ProtocolHandler.pm — use string literal directly.

From Plugins/SpotOn/Plugin.pm — _typeString and _bitrateForClient:
  Plugins::SpotOn::Plugin->_typeString($client, 'Browse') — returns format string
  Plugins::SpotOn::Plugin->_bitrateForClient($client) — returns bitrate number

From Plugins/SpotOn/API/Client.pm — getTrack:
  Plugins::SpotOn::API::Client->getTrack($accountId, $trackId, sub { my ($trackInfo) = @_; ... });
  $trackInfo->{name}, $trackInfo->{artists}[]{name}, $trackInfo->{album}{name}, $trackInfo->{album}{images}[], $trackInfo->{duration_ms}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement async re-fetch, debounce, placeholder, and Connect-to-Browse translation in getMetadataFor</name>
  <files>Plugins/SpotOn/ProtocolHandler.pm, t/11_track_history.t</files>
  <read_first>
    Plugins/SpotOn/ProtocolHandler.pm
    Plugins/SpotOn/Connect.pm
    Plugins/SpotOn/Plugin.pm
    t/11_track_history.t
    .planning/phases/11-track-history-metadata/11-CONTEXT.md
    .planning/phases/11-track-history-metadata/11-RESEARCH.md
    .planning/phases/11-track-history-metadata/11-PATTERNS.md
  </read_first>
  <action>
    Modify ProtocolHandler.pm with four additions. All changes are within or after getMetadataFor.

    **A. Package-level debounce hash (D-05)**
    After the `my $cache = Slim::Utils::Cache->new();` line (line 17), add:
    `my %_pendingRefetch;`
    Comment: "# D-05: debounce — one in-flight re-fetch per URL"

    **B. Extend getMetadataFor — Connect history with spotifyUri (D-06, D-07)**
    AFTER the existing Connect pluginData block (lines 272-278) and BEFORE the canonical URL normalization (line 281), add a new block for Connect history URLs:
    - Condition: `$url && $url =~ m{spotify://connect-}`  (the pluginData check above only fires when $client AND playingSong exist — history items won't have an active song)
    - Look up `$cache->get('spoton_meta_' . md5_hex($url))`
    - If found AND has `spotifyUri` field matching `m/^spotify:track:([A-Za-z0-9]+)$/`, extract $trackId
    - Construct Browse URL: `"spotify://track:$trackId"`
    - Per D-07, return metadata with Browse mode label (not Connect):
      If $client: return hash with cached fields overridden by `type => Plugins::SpotOn::Plugin->_typeString($client, 'Browse')` and `bitrate => Plugins::SpotOn::Plugin->_bitrateForClient($client) . 'k'` and `play => $browseUrl`
      If no $client: return cached metadata hash with `play => $browseUrl` added
    - This block returns early if cache hit with spotifyUri — no fallthrough to Browse path
    - Requires `require Plugins::SpotOn::Plugin;` before calling _typeString/_bitrateForClient

    **C. Replace `return {} unless $meta` with async re-fetch trigger (D-03, D-04, D-05)**
    The line `return {} unless $meta;` (line 293) becomes:
    - `unless ($meta) { _asyncRefetch($class, $client, $url, $canonical); return _placeholderMeta($url); }`
    - This returns placeholder immediately, fires async fetch in background

    **D. Add three new subs after getMetadataFor (before the closing `1;`)**

    `_placeholderMeta($url)` — returns minimal metadata hashref:
    - cover and icon: '/html/images/cover.png' (the generic LMS fallback artwork path)
    - title: "Loading..." if url matches `spotify://track:`, empty string otherwise
    - Return the hashref

    `_asyncRefetch($class, $client, $url, $canonical)` — async re-fetch with debounce:
    - Return early if `$_pendingRefetch{$url || ''}` is truthy (D-05 debounce)
    - Extract track ID: if $canonical matches `m{spotify://track:([A-Za-z0-9]+)}`, capture $trackId. Else if $url matches `m{spotify://connect-}`, look up the connect-URL cache entry for spotifyUri field, extract track ID from it (strip `spotify:track:` prefix). Return if no trackId extractable.
    - Resolve accountId using the established pattern: `$prefs->client($client)->get('activeAccount') || $prefs->get('activeAccount') || ''` with a guard: if $client is undef, use `$prefs->get('activeAccount') || ''`
    - Set `$_pendingRefetch{$url} = 1`
    - `require Plugins::SpotOn::API::Client;`
    - Call `Plugins::SpotOn::API::Client->getTrack($accountId, $trackId, sub { ... })`
    - In the callback (IMPORTANT — Pitfall 4): FIRST thing is `delete $_pendingRefetch{$url}` — unconditionally, before any return guards
    - Then: `return unless $trackInfo && $trackInfo->{name};`
    - Build metadata: title from $trackInfo->{name}, artist from join of artists array, album from $trackInfo->{album}{name}, duration from duration_ms / 1000, cover from _largestImage (define locally — see below) or fallback '/html/images/cover.png'
    - Determine cache URL: if $url matches connect-, use `'spotify://track:' . $trackId` (canonical Browse URL per Pitfall 3). Otherwise use $canonical.
    - $cache->set with spoton_meta_ + md5_hex($cacheUrl) key, metadata hash (title, artist, album, duration, cover, icon=cover), TTL 604800 per D-02
    - Fire `Slim::Control::Request::notifyFromArray($client, ['newmetadata'])` ONLY if $client is defined (Pitfall 2)

    `_largestImage($images_arrayref)` — copy from Connect.pm lines 880-888:
    - Return '' unless ref $images is ARRAY and non-empty
    - Sort by width descending, return first URL
    - This is a private helper, not a method

    **E. Update t/11_track_history.t — remove TODO wrappers, add real tests**
    - The TODO-marked test for "placeholder on cache miss" becomes a real test: call getMetadataFor for an uncached Browse URL, assert returned hash has cover => '/html/images/cover.png' and is NOT an empty hashref.
    - The TODO-marked test for "Connect URL with spotifyUri" becomes a real test: set cache entry keyed on connect-URL md5 with spotifyUri => 'spotify:track:XYZ789', call getMetadataFor, assert result has `play` field containing 'spotify://track:XYZ789'.
    - Add new test: "debounce prevents duplicate fetch" — set $_pendingRefetch for a URL via package variable access, call getMetadataFor for that URL (cache miss), verify no API call was made (check $Plugins::SpotOn::API::Client::mock_track call count or similar).
    - Add new test: "async re-fetch populates cache and fires newmetadata" — set $Plugins::SpotOn::API::Client::mock_track to a valid track info hashref, call getMetadataFor for uncached URL (triggers _asyncRefetch which calls getTrack synchronously via stub), then check that cache now contains the metadata AND $Slim::Control::Request::notify_count incremented.
    - Add new test: "Connect URL returns Browse mode label not Connect" — per D-07, verify the type field does NOT contain 'Connect'.

    Pitfall avoidance summary:
    - _largestImage defined locally in ProtocolHandler.pm (not imported from Connect.pm) — avoids Pitfall 1
    - Guard $client before notifyFromArray and _bitrateForClient — avoids Pitfall 2
    - Connect re-fetch stores under Browse URL cache key — avoids Pitfall 3
    - delete $_pendingRefetch{$url} is FIRST line of callback — avoids Pitfall 4
    - Use $canonical (normalized) URL for cache key in re-fetch — avoids Pitfall 5
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl t/05_perl_syntax.t && perl t/11_track_history.t 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - perl -c Plugins/SpotOn/ProtocolHandler.pm exits 0
    - grep -c '_asyncRefetch' Plugins/SpotOn/ProtocolHandler.pm returns >= 2 (definition + call)
    - grep -c '_placeholderMeta' Plugins/SpotOn/ProtocolHandler.pm returns >= 2 (definition + call)
    - grep -c '%_pendingRefetch' Plugins/SpotOn/ProtocolHandler.pm returns >= 1
    - grep -c '_largestImage' Plugins/SpotOn/ProtocolHandler.pm returns >= 1
    - ProtocolHandler.pm line `return {} unless $meta;` is GONE — replaced by async re-fetch + placeholder
    - getMetadataFor for an uncached Browse URL returns hashref with cover field (not empty {})
    - getMetadataFor for a Connect URL with cached spotifyUri returns hashref with play field containing 'spotify://track:...'
    - The type field for Connect history URLs contains 'Browse' not 'Connect' (D-07)
    - All tests in t/11_track_history.t pass (no TODO remaining for Plan 02 scope)
    - prove t/ full suite green
  </acceptance_criteria>
  <done>getMetadataFor handles cache misses with async re-fetch and placeholder, Connect history URLs are translated to Browse URLs, all tests pass including debounce and D-07 label verification</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Verify history metadata on live LMS system</name>
  <what-built>
    Phase 11 implementation: 7-day cache TTL, Connect cache persistence with spotifyUri, async re-fetch on cache miss, Connect-to-Browse URL translation. All automated tests pass.
  </what-built>
  <how-to-verify>
    1. Deploy updated plugin files to LMS (copy Plugins/SpotOn/ to LMS plugin dir, restart LMS)
    2. Play a Spotify track via Browse menu, let it finish
    3. Open Track History (Verlauf) — verify the track shows correct artwork and metadata
    4. Play a track via Spotify Connect, let it finish
    5. Open Track History — verify the Connect track shows artwork (cover, not generic icon)
    6. Wait >1 hour (or manually clear cache entry via LMS CLI), revisit history — verify re-fetch populates metadata after brief "Loading..." placeholder
    7. Click a former Connect track in history — verify it starts playing via Browse pipeline (not an error)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL string -> Track ID extraction | Regex parses Track ID from spotify:// URLs in history |
| Cache -> API call | spotifyUri from cache used to construct API request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | Track ID regex extraction | mitigate | Regex `m{spotify://track:([A-Za-z0-9]+)}` restricts Track ID to alphanumeric characters only — no special chars, no path traversal, no injection |
| T-11-04 | Tampering | spotifyUri from cache | accept | Cache is local to LMS process, populated only by trusted API::Client responses. spotifyUri is validated by same alphanumeric regex before use |
| T-11-05 | Denial of Service | Unbounded re-fetch | mitigate | %_pendingRefetch debounce limits to one in-flight API call per URL. Callback always clears the flag (even on error). No retry loop |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — pure Perl code changes |
</threat_model>

<verification>
1. `perl t/05_perl_syntax.t` — all modified files pass syntax check
2. `perl t/11_track_history.t` — all tests pass (no TODO remaining)
3. `prove t/` — full suite green, no regressions
4. `grep -c 'return {} unless' Plugins/SpotOn/ProtocolHandler.pm` returns 0 (old pattern removed)
5. `grep -c '_asyncRefetch' Plugins/SpotOn/ProtocolHandler.pm` returns >= 2
6. `grep -c 'spotifyUri' Plugins/SpotOn/ProtocolHandler.pm` returns >= 1
</verification>

<success_criteria>
- Cache miss in getMetadataFor returns placeholder (not empty hashref) and triggers async re-fetch
- Async re-fetch via API::Client->getTrack populates cache and fires newmetadata notification
- Debounce hash prevents concurrent duplicate fetches for the same URL
- Connect history URLs with spotifyUri in cache return Browse-pipeline metadata with play field
- Type string for translated Connect tracks says "Browse" not "Connect" (D-07 invisible translation)
- All automated tests pass, full suite green
</success_criteria>

<output>
Create `.planning/phases/11-track-history-metadata/11-02-SUMMARY.md` when done
</output>
