---
phase: 12-protocol-handler-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - t/12_protocol_rename.t
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/ProtocolHandler.pm
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/DontStopTheMusic.pm
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/API/TokenManager.pm
  - librespot-spoton/src/main.rs
  - t/11_track_history.t
autonomous: true
requirements:
  - PROTO-01
  - PROTO-02
  - PROTO-03
  - PROTO-04
  - PROTO-05
  - PROTO-06

must_haves:
  truths:
    - "All LMS routing URLs use spoton:// scheme, not spotify://"
    - "ProtocolHandler is registered as 'spoton' in LMS"
    - "Connect URLs use spoton://connect- prefix"
    - "Cache namespace is named 'spoton' with version 2 for atomic flush"
    - "Rust binary normalizes spoton:// to spotify: for SpotifyUri"
    - "Spotify API URIs (spotify:track:ID) remain unchanged"
    - "custom-convert.conf is NOT modified (son/soc content types already SpotOn-specific)"
  artifacts:
    - path: "t/12_protocol_rename.t"
      provides: "Grep-based validation of PROTO-01 through PROTO-06"
      contains: "PROTO-01"
    - path: "Plugins/SpotOn/Plugin.pm"
      provides: "registerHandler('spoton'), SPOTON_CACHE_VERSION, cacheSchemaVersion pref"
      contains: "registerHandler"
    - path: "Plugins/SpotOn/ProtocolHandler.pm"
      provides: "All spoton:// URL patterns and cache namespace"
      contains: "spoton://"
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "spoton://connect- URL construction and matching"
      contains: "spoton://connect-"
    - path: "librespot-spoton/src/main.rs"
      provides: "URI normalization spoton:// to spotify:"
      contains: "spoton://"
  key_links:
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Slim::Player::ProtocolHandlers"
      via: "registerHandler('spoton', ...)"
      pattern: "registerHandler.*spoton"
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Slim::Utils::Cache"
      via: "Cache->new('spoton', SPOTON_CACHE_VERSION)"
      pattern: "Cache->new.*spoton.*SPOTON_CACHE_VERSION"
    - from: "librespot-spoton/src/main.rs"
      to: "SpotifyUri::from_uri"
      via: "replace spoton:// with spotify:"
      pattern: 'replace\("spoton://"'
---

<objective>
Rename all LMS-internal routing URLs from spotify:// to spoton:// across the entire plugin codebase (Perl + Rust), switch to a named cache namespace with version-based flush, and create the validation test scaffold.

Purpose: Enable coexistence of SpotOn and Spotty in the same LMS instance by eliminating the protocol handler name collision on 'spotify'.
Output: All 7 Perl/Rust source files updated, test scaffold created, existing regression tests adapted, full test suite green.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/12-protocol-handler-rename/12-CONTEXT.md
@/home/sti/spoton/.planning/phases/12-protocol-handler-rename/12-RESEARCH.md
@/home/sti/spoton/.planning/phases/12-protocol-handler-rename/12-PATTERNS.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Plugin.pm:
- Line 24: my $cache = Slim::Utils::Cache->new();
- Line 41-51: $prefs->init({...}) block — add cacheSchemaVersion here
- Line 82-85: registerHandler('spotify', 'Plugins::SpotOn::ProtocolHandler')
- Line 182: next unless $url =~ m{^spotify://};
- Line 418: my $spotify_url = 'spotify://' . $track_path;
- Line 1155: my $spotify_url = 'spotify://' . $track_path;

From Plugins/SpotOn/ProtocolHandler.pm:
- Line 17: my $cache = Slim::Utils::Cache->new();
- 25 occurrences of spotify:// in regex/string contexts (see RESEARCH.md Pattern 3 full map)
- Lines 266, 351, 431: spotify:track: API URIs — DO NOT CHANGE (D-06)
- Lines 368-371: canonical normalization s{^spotify:}{spotify://} — CRITICAL change

From Plugins/SpotOn/Connect.pm:
- Line 43: my $cache = Slim::Utils::Cache->new();
- Lines 630, 722, 828: sprintf("spotify://connect-%u", $ts)
- Lines 617, 695: m{^spotify://} && !~ m{spotify://connect-}
- Lines 624, 714, 771, 918: spotify:track: API URIs — DO NOT CHANGE (D-06)

From Plugins/SpotOn/DontStopTheMusic.pm:
- Line 16: my $cache = Slim::Utils::Cache->new();
- Line 259: my $uri = "spotify://$1";

From Plugins/SpotOn/API/Client.pm:
- Line 35: my $cache = Slim::Utils::Cache->new();

From Plugins/SpotOn/API/TokenManager.pm:
- Line 33: my $cache = Slim::Utils::Cache->new();

From librespot-spoton/src/main.rs:
- Lines 666-667: let normalized_uri = track_uri.replace("spotify://", "spotify:");

From t/11_track_history.t:
- 9 occurrences of spotify:// in mock URLs (lines 432, 467, 483, 505, 526, 529, 535, 557, 586)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create t/12_protocol_rename.t test scaffold (Wave 0)</name>
  <files>t/12_protocol_rename.t</files>
  <read_first>
    t/03_convert_conf.t
    t/11_track_history.t
    .planning/phases/12-protocol-handler-rename/12-VALIDATION.md
  </read_first>
  <action>
Create a new grep-based test file `t/12_protocol_rename.t` using the file-scanning pattern from `t/03_convert_conf.t` (open file, scan lines, collect matches, assert count).

The test MUST start failing (RED state) because the source files still contain `spotify://`.

Required subtests (mapping to PROTO-01 through PROTO-06):

PROTO-01: Scan Plugin.pm, ProtocolHandler.pm, Connect.pm, DontStopTheMusic.pm non-comment lines for `spotify://` (double-slash). Assert zero matches. Comments (lines starting with optional whitespace then `#`) are excluded from the check.

PROTO-02: Scan Plugin.pm for `registerHandler('spoton'` — assert exactly 1 match. Scan Plugin.pm for `registerHandler('spotify'` — assert 0 matches (non-comment lines).

PROTO-04: Scan Connect.pm for `sprintf.*spoton://connect-` — assert >= 3 matches (lines 630, 722, 828 after rename).

PROTO-05: Scan ALL .pm files under `Plugins/SpotOn/` recursively for `registerHandler('spotify'` — assert 0 matches.

PROTO-06: Scan Plugin.pm for `cacheSchemaVersion` — assert >= 2 matches (one in prefs init, one in the version guard). Scan Plugin.pm for `SPOTON_CACHE_VERSION` — assert >= 1 match.

Rust binary check: Scan `librespot-spoton/src/main.rs` for the `replace(` call — assert it contains `spoton://` not `spotify://`. Specifically: grep for `replace("spoton://"` and assert >= 1 match; grep for `replace("spotify://"` and assert 0 matches.

Cache namespace check: Scan all 6 Perl modules for `Cache->new('spoton'` — assert 6 matches total. Scan for `Cache->new()` (no args) — assert 0 matches in those files.

Use `$project_dir` derived from `dirname(dirname(abs_path($0)))` for file paths. Use `Test::More` with `plan tests => N` where N is the total assertion count.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/12_protocol_rename.t 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - t/12_protocol_rename.t exists and is executable
    - Running `prove t/12_protocol_rename.t` reports FAILURES (expected — source files not yet renamed)
    - Test file contains subtests for PROTO-01, PROTO-02, PROTO-04, PROTO-05, PROTO-06
    - Test file contains Rust binary normalization check
    - Test file contains cache namespace check for all 6 modules
  </acceptance_criteria>
  <done>Test scaffold exists, runs, and fails on current codebase (RED state). All PROTO requirements have corresponding assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Rename spotify:// to spoton:// in all Perl source files + Rust binary + cache namespace</name>
  <files>
    Plugins/SpotOn/Plugin.pm
    Plugins/SpotOn/ProtocolHandler.pm
    Plugins/SpotOn/Connect.pm
    Plugins/SpotOn/DontStopTheMusic.pm
    Plugins/SpotOn/API/Client.pm
    Plugins/SpotOn/API/TokenManager.pm
    librespot-spoton/src/main.rs
  </files>
  <read_first>
    Plugins/SpotOn/Plugin.pm
    Plugins/SpotOn/ProtocolHandler.pm
    Plugins/SpotOn/Connect.pm
    Plugins/SpotOn/DontStopTheMusic.pm
    Plugins/SpotOn/API/Client.pm
    Plugins/SpotOn/API/TokenManager.pm
    librespot-spoton/src/main.rs
    .planning/phases/12-protocol-handler-rename/12-RESEARCH.md
    .planning/phases/12-protocol-handler-rename/12-PATTERNS.md
  </read_first>
  <action>
Apply ALL changes across 7 files. This is the core task of the phase. Work file by file.

**CRITICAL RULE (D-06):** Only replace `spotify://` (double-slash LMS routing URLs). NEVER replace `spotify:track:`, `spotify:episode:`, or bare `spotify:` (single-colon Spotify API URIs). Lines explicitly excluded are listed in RESEARCH.md "Lines that must NOT change" tables.

**Plugin.pm changes:**
1. Add `use constant SPOTON_CACHE_VERSION => 2;` after line 21 (next to existing `KILL_PROCESS_INTERVAL` constant).
2. Change `my $cache = Slim::Utils::Cache->new();` (line 24) to `my $cache = Slim::Utils::Cache->new('spoton', SPOTON_CACHE_VERSION);`.
3. Add `cacheSchemaVersion => 0,` to the `$prefs->init({...})` block (after `enableAutoplay`).
4. After the `$prefs->init({})` block, before `require Plugins::SpotOn::Helper`, add the version guard:
   - If `($prefs->get('cacheSchemaVersion') || 0) < SPOTON_CACHE_VERSION` then log info "SpotOn cache schema version changed - cache cleared by namespace version bump" and set `$prefs->set('cacheSchemaVersion', SPOTON_CACHE_VERSION)`.
5. Change `registerHandler('spotify', ...)` to `registerHandler('spoton', ...)` (line 82-85).
6. Change `m{^spotify://}` to `m{^spoton://}` (line 182).
7. Change `'spotify://' . $track_path` to `'spoton://' . $track_path` (lines 418, 1155). Variable name `$spotify_url` may be renamed to `$spoton_url` at discretion — update all references in the same function scope.

**ProtocolHandler.pm changes:**
1. Change `my $cache = Slim::Utils::Cache->new();` (line 17) to `my $cache = Slim::Utils::Cache->new('spoton', 2);` (literal `2` since SPOTON_CACHE_VERSION constant is in Plugin.pm).
2. Apply all 25 `spotify://` to `spoton://` substitutions per the RESEARCH.md Pattern 3 full line map (lines 36, 65, 95, 209, 224, 257, 267, 298, 306, 316, 337, 348, 353, 371, 403, 426, 428, 477-478).
3. CRITICAL: Lines 368-371 canonical normalization — change `m{^spotify:(?!//)}` to `m{^spoton:(?!//)}` and `s{^spotify:}{spotify://}` to `s{^spoton:}{spoton://}`.
4. DO NOT touch lines 266, 351, 431 (`m/^spotify:track:/` Spotify API URI patterns).
5. Update comment on line 32 from `spotify://connect-*` to `spoton://connect-*`.

**Connect.pm changes:**
1. Change `my $cache = Slim::Utils::Cache->new();` (line 43) to `my $cache = Slim::Utils::Cache->new('spoton', 2);`.
2. Apply all `spotify://` to `spoton://` substitutions per RESEARCH.md (lines 107, 285, 592, 598, 617, 630, 695, 722, 828).
3. Line 101 comment: update `spotify://connect-*` to `spoton://connect-*`.
4. DO NOT touch lines 624, 714, 771, 918 (`spotify:track:` API URIs and `spotifyUri` field).

**DontStopTheMusic.pm changes:**
1. Change `my $cache = Slim::Utils::Cache->new();` (line 16) to `my $cache = Slim::Utils::Cache->new('spoton', 2);`.
2. Change `"spotify://$1"` to `"spoton://$1"` (line 259).

**API/Client.pm changes:**
1. Change `my $cache = Slim::Utils::Cache->new();` (line 35) to `my $cache = Slim::Utils::Cache->new('spoton', 2);`.
2. No URL pattern changes needed in this file.

**API/TokenManager.pm changes:**
1. Change `my $cache = Slim::Utils::Cache->new();` (line 33) to `my $cache = Slim::Utils::Cache->new('spoton', 2);`.
2. No URL pattern changes needed in this file.

**librespot-spoton/src/main.rs changes:**
1. Line 666 comment: change "LMS passes spotify://track:ID" to "LMS passes spoton://track:ID".
2. Line 667: change `track_uri.replace("spotify://", "spotify:")` to `track_uri.replace("spoton://", "spotify:")`.

After all edits, run `prove t/05_perl_syntax.t` to verify Perl syntax, then run `prove t/12_protocol_rename.t` to verify all PROTO assertions pass (GREEN state).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/05_perl_syntax.t t/12_protocol_rename.t t/03_convert_conf.t</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'spotify://' Plugins/SpotOn/*.pm Plugins/SpotOn/API/*.pm | grep -v '^\s*#'` returns 0 matches
    - `grep -c "registerHandler.*'spoton'" Plugins/SpotOn/Plugin.pm` returns 1
    - `grep -c "registerHandler.*'spotify'" Plugins/SpotOn/Plugin.pm` returns 0
    - `grep -c "Cache->new('spoton'" Plugins/SpotOn/Plugin.pm Plugins/SpotOn/ProtocolHandler.pm Plugins/SpotOn/Connect.pm Plugins/SpotOn/DontStopTheMusic.pm Plugins/SpotOn/API/Client.pm Plugins/SpotOn/API/TokenManager.pm` returns 6 total
    - `grep -c 'Cache->new()' Plugins/SpotOn/Plugin.pm Plugins/SpotOn/ProtocolHandler.pm Plugins/SpotOn/Connect.pm Plugins/SpotOn/DontStopTheMusic.pm Plugins/SpotOn/API/Client.pm Plugins/SpotOn/API/TokenManager.pm` returns 0 total
    - `grep -c 'replace("spoton://"' librespot-spoton/src/main.rs` returns 1
    - `grep -c 'SPOTON_CACHE_VERSION' Plugins/SpotOn/Plugin.pm` returns >= 2
    - `grep -c 'cacheSchemaVersion' Plugins/SpotOn/Plugin.pm` returns >= 2
    - `prove t/05_perl_syntax.t` passes (no Perl syntax errors)
    - `prove t/12_protocol_rename.t` passes (all PROTO assertions green)
    - `prove t/03_convert_conf.t` passes (no regression in convert conf)
    - Spotify API URIs (spotify:track:ID) remain unchanged in ProtocolHandler.pm lines 266, 351, 431 and Connect.pm lines 624, 714, 771, 918
  </acceptance_criteria>
  <done>All 7 source files updated with spoton:// scheme, named cache namespace, cacheSchemaVersion pref, and Rust normalization. t/12_protocol_rename.t is GREEN. No Perl syntax errors.</done>
</task>

<task type="auto">
  <name>Task 3: Update t/11_track_history.t mock URLs + full regression suite</name>
  <files>t/11_track_history.t</files>
  <read_first>
    t/11_track_history.t
  </read_first>
  <action>
Update all 9 occurrences of `spotify://` in `t/11_track_history.t` mock URLs and assertions to `spoton://`:

- Line 432: `'spotify://track:ABC123'` to `'spoton://track:ABC123'`
- Line 467: `'spotify://track:NOTCACHED'` to `'spoton://track:NOTCACHED'`
- Line 483: `'spotify://connect-20260604-120000'` to `'spoton://connect-20260604-120000'`
- Line 505: `qr{spotify://track:XYZ789}` to `qr{spoton://track:XYZ789}`
- Line 526: `'spotify://track:DEBOUNCE'` key in `%_pendingRefetch` to `'spoton://track:DEBOUNCE'`
- Line 529: `'spotify://track:DEBOUNCE'` to `'spoton://track:DEBOUNCE'`
- Line 535: `'spotify://track:DEBOUNCE'` to `'spoton://track:DEBOUNCE'`
- Line 557: `'spotify://track:ASYNCTEST'` to `'spoton://track:ASYNCTEST'`
- Line 586: `'spotify://connect-20260604-130000'` to `'spoton://connect-20260604-130000'`

DO NOT change any `spotify:track:` references (single-colon API URIs) that may exist in the test — these are Spotify API format and remain unchanged per D-06.

After updating, run the full test suite to confirm zero regressions.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'spotify://' t/11_track_history.t` returns 0
    - `grep -c 'spoton://' t/11_track_history.t` returns >= 9
    - `prove t/11_track_history.t` passes
    - `prove t/` (full suite) passes with 0 failures
  </acceptance_criteria>
  <done>All test mock URLs updated to spoton://, full test suite green, zero regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS URL -> binary | LMS passes spoton://track:ID via $URL$ to binary stdin; binary normalizes to spotify:track:ID |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | URL scheme string | accept | URL scheme is a compile-time constant, not user-controlled. No user input influences the scheme prefix. |
| T-12-02 | Information Disclosure | Cache namespace migration | accept | Cache flush clears all SpotOn entries. No cross-plugin data leakage — spoton.db is separate from cache.db. |
| T-12-03 | Denial of Service | Stale binary with old normalization | mitigate | t/12_protocol_rename.t includes grep assertion that main.rs contains `replace("spoton://")`. Plan 02 rebuilds all binaries. |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase. |
</threat_model>

<verification>
After all 3 tasks complete:
1. `prove t/` — full test suite passes (0 failures)
2. `grep -rn 'spotify://' Plugins/SpotOn/*.pm Plugins/SpotOn/API/*.pm | grep -v '^\s*#'` — returns 0 matches
3. `grep -c "registerHandler.*'spoton'" Plugins/SpotOn/Plugin.pm` — returns 1
4. `grep -rn "Cache->new()" Plugins/SpotOn/*.pm Plugins/SpotOn/API/*.pm` — returns 0 matches
5. `grep -c 'replace("spoton://"' librespot-spoton/src/main.rs` — returns 1
</verification>

<success_criteria>
- All Perl source files use spoton:// for LMS routing URLs
- ProtocolHandler registered as 'spoton'
- Named cache namespace 'spoton' with version 2 across all 6 modules
- cacheSchemaVersion pref in Plugin.pm for idempotent migration tracking
- Rust binary normalizes spoton:// to spotify: for SpotifyUri
- Spotify API URIs (spotify:track:ID) remain untouched
- custom-convert.conf unchanged (PROTO-03 confirmed by existing t/03_convert_conf.t)
- Full test suite passes including new t/12_protocol_rename.t
</success_criteria>

<output>
Create `.planning/phases/12-protocol-handler-rename/12-01-SUMMARY.md` when done
</output>
