---
phase: 13-repo-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .github/workflows/perl-tests.yml
autonomous: true
requirements: [REPO-01, REPO-02]

must_haves:
  truths:
    - "A push to main triggers GitHub Actions and runs prove t/ against Perl 5.36 and 5.38"
    - "CI results are visible in commit status checks"
    - "SpotOn-v*.zip files are ignored by git"
    - "Unstaged phase deletes from phases 10-12 are committed"
  artifacts:
    - path: ".github/workflows/perl-tests.yml"
      provides: "Perl CI workflow with matrix strategy"
      contains: "shogo82148/actions-setup-perl"
    - path: ".gitignore"
      provides: "Updated ignore rules including zip archives"
      contains: "SpotOn-v*.zip"
  key_links:
    - from: ".github/workflows/perl-tests.yml"
      to: "t/"
      via: "prove t/ command"
      pattern: "prove t/"
---

<objective>
Clean up repo hygiene (stale artifacts, .gitignore) and create the GitHub Actions CI workflow for Perl tests.

Purpose: REPO-01 and REPO-02 require a working CI pipeline. D-10 and D-13 require repo cleanup before the CI badge can reflect a clean state.
Output: Working perl-tests.yml workflow, clean .gitignore, stale artifacts removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-repo-maintenance/13-RESEARCH.md
@.planning/phases/13-repo-maintenance/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo Hygiene -- .gitignore + artifact cleanup</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current content -- 15 lines, modify in place)
  </read_first>
  <action>
    Per D-10: Add `SpotOn-v*.zip` entry to .gitignore under a new comment block `# Release ZIP archives (built locally, distributed via GitHub Releases)`. Place it after the existing `# Rust build artifacts` block.

    Per D-13: Stage and commit all unstaged phase deletes (the 25 files showing ` D` in git status under `.planning/phases/10-*/`, `.planning/phases/11-*/`, `.planning/phases/12-*/`). Use `git add -u .planning/phases/10-connect-dstm/ .planning/phases/11-track-history-metadata/ .planning/phases/12-protocol-handler-rename/` to stage the deletes.

    Per D-13: Delete the 10 SpotOn-v1.2.*.zip files from the working tree root: `rm SpotOn-v1.2.0.zip SpotOn-v1.2.1.zip SpotOn-v1.2.2.zip SpotOn-v1.2.3.zip SpotOn-v1.2.4.zip SpotOn-v1.2.5.zip SpotOn-v1.2.6.zip SpotOn-v1.2.7.zip SpotOn-v1.2.8.zip SpotOn-v1.2.9.zip`.

    Commit the .gitignore update, the phase deletes, and the zip removal together as a hygiene commit.
  </action>
  <verify>
    <automated>git diff --cached --stat 2>/dev/null || true && grep -c 'SpotOn-v\*\.zip' .gitignore && ls SpotOn-v*.zip 2>&1 | grep -c 'nicht möglich\|No such file'</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains the line `SpotOn-v*.zip`
    - `ls SpotOn-v*.zip` returns "No such file or directory"
    - `git status` shows no ` D` entries for `.planning/phases/10-*/`, `.planning/phases/11-*/`, `.planning/phases/12-*/`
    - `git status` shows no `?? SpotOn-v1.2.*.zip` entries
  </acceptance_criteria>
  <done>Repo is clean: zip archives removed and ignored, stale phase deletes committed</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions Perl CI Workflow</name>
  <files>.github/workflows/perl-tests.yml</files>
  <read_first>
    - .github/workflows/build-librespot.yml (analog -- Actions syntax conventions, step naming, fail-fast pattern)
  </read_first>
  <action>
    Per D-01, D-02, D-03, D-04: Create `.github/workflows/perl-tests.yml`.

    Workflow name: `Perl Tests`.
    Triggers: `on: push: branches: [main]` and `on: pull_request: branches: [main]` (per D-01).
    Job name: `test`, display name: `Perl ${{ matrix.perl }}`.
    Runner: `ubuntu-latest`.
    Strategy: `fail-fast: false` (matching build-librespot.yml convention), matrix with `perl: ["5.36", "5.38"]` (per D-02).

    Steps (following build-librespot.yml step naming convention -- sentence case):
    1. `Checkout repository` -- uses `actions/checkout@v4`
    2. `Set up Perl ${{ matrix.perl }}` -- uses `shogo82148/actions-setup-perl@v1` with `perl-version: ${{ matrix.perl }}`
    3. `Run test suite` -- runs `prove t/`

    No `cpanm` step (all test modules are Perl core -- verified in RESEARCH.md). No `timeout-minutes` (suite runs in < 1s). No `env:` block needed.
  </action>
  <verify>
    <automated>test -f .github/workflows/perl-tests.yml && grep -c 'shogo82148/actions-setup-perl' .github/workflows/perl-tests.yml && grep -c 'prove t/' .github/workflows/perl-tests.yml && grep -c '"5.36"' .github/workflows/perl-tests.yml && grep -c '"5.38"' .github/workflows/perl-tests.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/perl-tests.yml` exists
    - Contains `name: Perl Tests`
    - Contains trigger `push: branches: [main]` and `pull_request: branches: [main]`
    - Contains `shogo82148/actions-setup-perl@v1`
    - Matrix includes `"5.36"` and `"5.38"`
    - Contains `prove t/` as the test command
    - Contains `fail-fast: false`
    - Does NOT contain `cpanm` or `installdeps`
    - Uses `actions/checkout@v4` (not v3)
  </acceptance_criteria>
  <done>perl-tests.yml workflow created with Perl 5.36+5.38 matrix, triggered on push/PR to main</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions Runner | CI runs on GitHub-hosted runner with repo checkout access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | actions-setup-perl@v1 | accept | Pinned to @v1 major version (GitHub Actions standard practice); action is well-established with 400+ stars |
| T-13-02 | Tampering | actions/checkout@v4 | accept | Official GitHub Action, already used in build-librespot.yml |
| T-13-03 | Information Disclosure | prove t/ output | accept | Test output contains no secrets; tests use stubs, no real API calls |
</threat_model>

<verification>
After push to main:
1. GitHub Actions tab shows "Perl Tests" workflow
2. Workflow runs 2 matrix jobs (Perl 5.36 and 5.38)
3. Both jobs pass with `prove t/` output showing 230 tests
4. Commit status check shows green
</verification>

<success_criteria>
- perl-tests.yml exists and follows repo Actions conventions
- .gitignore includes SpotOn-v*.zip pattern
- No stale zip archives in working tree
- Phase 10-12 deletes are committed
- On next push to main, CI runs and passes
</success_criteria>

<output>
Create `.planning/phases/13-repo-maintenance/13-01-SUMMARY.md` when done
</output>
