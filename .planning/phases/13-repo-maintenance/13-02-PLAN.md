---
phase: 13-repo-maintenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - .github/ISSUE_TEMPLATE/feature_request.yml
  - .github/ISSUE_TEMPLATE/config.yml
  - CONTRIBUTING.md
  - LICENSE
  - README.md
autonomous: true
requirements: [REPO-03, REPO-04, REPO-05]

must_haves:
  truths:
    - "A contributor filing a bug sees a structured form with LMS version, OS, reproduction steps, log excerpt, and player type fields"
    - "A contributor filing a feature request sees a structured form with problem statement, proposed solution, and alternatives fields"
    - "A developer new to the project can follow CONTRIBUTING.md to run the test suite locally and submit a PR"
    - "The repository has an MIT license"
    - "The README shows current features, install instructions, and CI badge"
  artifacts:
    - path: ".github/ISSUE_TEMPLATE/bug_report.yml"
      provides: "Structured bug report form with required/optional fields"
      contains: "lms-version"
    - path: ".github/ISSUE_TEMPLATE/feature_request.yml"
      provides: "Structured feature request form"
      contains: "problem"
    - path: ".github/ISSUE_TEMPLATE/config.yml"
      provides: "Template chooser configuration"
      contains: "blank_issues_enabled"
    - path: "CONTRIBUTING.md"
      provides: "Contributor guide with dev setup, test running, PR guidelines"
      contains: "prove t/"
    - path: "LICENSE"
      provides: "MIT license"
      contains: "MIT License"
    - path: "README.md"
      provides: "Project README with badge, features, install instructions"
      contains: "perl-tests.yml"
  key_links:
    - from: "README.md"
      to: ".github/workflows/perl-tests.yml"
      via: "CI badge URL"
      pattern: "actions/workflows/perl-tests.yml/badge.svg"
    - from: "CONTRIBUTING.md"
      to: "t/"
      via: "test running instructions"
      pattern: "prove t/"
---

<objective>
Create issue templates (bug report + feature request), contributor documentation (CONTRIBUTING.md), LICENSE, and README.

Purpose: REPO-03, REPO-04, REPO-05 require structured contributor infrastructure. D-05 through D-12 specify the content and format.
Output: 6 new files that make the repo contributor-ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-repo-maintenance/13-RESEARCH.md
@.planning/phases/13-repo-maintenance/13-PATTERNS.md

<interfaces>
<!-- Key references the executor needs -->

From repo.xml (version + install URL):
  version="1.2.9"
  minTarget="8.0"
  creator="Marek Stiefenhofer"
  url="https://github.com/stiefenm/spoton/releases/download/v1.2.9/SpotOn-v1.2.9.zip"
  repo URL: https://raw.githubusercontent.com/stiefenm/spoton/main/repo.xml

GitHub owner/repo: stiefenm/spoton

CI badge URL pattern:
  [![Perl Tests](https://github.com/stiefenm/spoton/actions/workflows/perl-tests.yml/badge.svg)](https://github.com/stiefenm/spoton/actions/workflows/perl-tests.yml)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Issue Templates (Bug Report + Feature Request + config.yml)</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, .github/ISSUE_TEMPLATE/feature_request.yml, .github/ISSUE_TEMPLATE/config.yml</files>
  <read_first>
    - .planning/phases/13-repo-maintenance/13-PATTERNS.md (exact YAML patterns for bug_report.yml and feature_request.yml verified against GitHub docs)
  </read_first>
  <action>
    Create directory `.github/ISSUE_TEMPLATE/`.

    Per D-05: Use YAML Forms format (`.yml` files), not Markdown templates.

    Per D-06: Create `bug_report.yml` with:
    - `name: Bug Report`, `description: Report a problem with SpotOn`, `labels: ["bug"]`
    - Fields: `lms-version` (type: input, required: true, placeholder: "e.g. 9.1.1"), `os` (type: dropdown, required: true, options: Linux x86_64 / Linux ARM-Raspberry Pi / macOS / Windows / Other), `reproduction` (type: textarea, required: true, placeholder: "1. ...\n2. ...\n3. ..."), `logs` (type: textarea, required: false, description: "Relevant lines from LMS server.log"), `player-type` (type: dropdown, required: false, options: squeezelite / UPnPBridge-BridgeSDK / piCorePlayer / Other)

    Per D-07: Create `feature_request.yml` with:
    - `name: Feature Request`, `description: Suggest a new feature or improvement`, `labels: ["enhancement"]`
    - Fields: `problem` (type: textarea, required: true, description: "What problem does this feature solve?"), `solution` (type: textarea, required: true, label: "Proposed Solution"), `alternatives` (type: textarea, required: false, label: "Alternatives Considered")

    Claude's Discretion: Create `config.yml` with `blank_issues_enabled: false` and a `contact_links` entry pointing to the Lyrion community forum at `https://forums.slimdevices.com/` for general LMS questions.
  </action>
  <verify>
    <automated>test -f .github/ISSUE_TEMPLATE/bug_report.yml && test -f .github/ISSUE_TEMPLATE/feature_request.yml && test -f .github/ISSUE_TEMPLATE/config.yml && grep -c 'required: true' .github/ISSUE_TEMPLATE/bug_report.yml && grep -c 'required: true' .github/ISSUE_TEMPLATE/feature_request.yml && grep -c 'blank_issues_enabled: false' .github/ISSUE_TEMPLATE/config.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/ISSUE_TEMPLATE/bug_report.yml` exists with `name: Bug Report`
    - bug_report.yml has 3 required fields (lms-version, os, reproduction) and 2 optional fields (logs, player-type)
    - `.github/ISSUE_TEMPLATE/feature_request.yml` exists with `name: Feature Request`
    - feature_request.yml has 2 required fields (problem, solution) and 1 optional field (alternatives)
    - bug_report.yml contains `labels: ["bug"]`
    - feature_request.yml contains `labels: ["enhancement"]`
    - `.github/ISSUE_TEMPLATE/config.yml` contains `blank_issues_enabled: false`
    - All files use YAML syntax (`.yml` extension), not Markdown
  </acceptance_criteria>
  <done>Three issue template files created: structured bug report form, feature request form, and template chooser config</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING.md + LICENSE + README.md</name>
  <files>CONTRIBUTING.md, LICENSE, README.md</files>
  <read_first>
    - repo.xml (version 1.2.9, minTarget 8.0, creator, install URL)
    - .planning/phases/13-repo-maintenance/13-PATTERNS.md (README section structure, MIT license template, CONTRIBUTING structure)
    - .planning/phases/13-repo-maintenance/13-RESEARCH.md (CI badge URL pattern)
  </read_first>
  <action>
    Per D-08, D-09: Create `CONTRIBUTING.md` in English, compact (1-2 pages). Sections:
    1. Prerequisites -- Perl 5.36+, git. Note: LMS installation is NOT required (tests run standalone with built-in stubs).
    2. Running Tests -- command `prove t/`, expected output: 12 test files, 230 tests, completes in under 1 second. All test modules are Perl core (Test::More, File::Temp, JSON::PP, File::Path, Cwd) -- no CPAN setup required.
    3. Project Structure -- brief map: `Plugins/SpotOn/` (plugin source), `t/` (test suite), `.github/workflows/` (CI), `Bin/` subdirectories (librespot binaries per architecture).
    4. Pull Request Guidelines -- branch from `main`, write descriptive PR title and body, CI must pass (Perl 5.36 + 5.38), keep changes focused.

    Per D-11: Create `LICENSE` file with standard MIT License text. Copyright holder: `Marek Stiefenhofer`. Year: `2024`.

    Per D-12: Create `README.md` with sections:
    1. First line: CI badge -- `[![Perl Tests](https://github.com/stiefenm/spoton/actions/workflows/perl-tests.yml/badge.svg)](https://github.com/stiefenm/spoton/actions/workflows/perl-tests.yml)`
    2. `# SpotOn` heading + one-sentence description: "A Spotify plugin for Lyrion Music Server (LMS)"
    3. Features section: Browse (search, playlists, albums, artists), Streaming (single-track via librespot, OGG passthrough + PCM/FLAC/MP3 transcoding), Spotify Connect (appear as Spotify Connect device, control from Spotify app), Library (liked songs, recently played, top tracks)
    4. Requirements: LMS 8.0+ (recommended 9.x), Spotify Premium account
    5. Installation: Add repo URL `https://raw.githubusercontent.com/stiefenm/spoton/main/repo.xml` to LMS Settings > Plugins > Additional Repositories, then install SpotOn from the plugin list
    6. License: MIT -- see LICENSE file
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && test -f LICENSE && test -f README.md && grep -c 'prove t/' CONTRIBUTING.md && grep -c 'MIT License' LICENSE && grep -c 'perl-tests.yml/badge.svg' README.md && grep -c 'Marek Stiefenhofer' LICENSE</automated>
  </verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` exists, is in English, contains `prove t/` command, mentions "no CPAN" or "Perl core", has sections for Prerequisites, Running Tests, Project Structure, Pull Request Guidelines
    - `LICENSE` exists, contains `MIT License`, copyright line includes `Marek Stiefenhofer` and year `2024`
    - `README.md` exists, first content line contains CI badge URL referencing `perl-tests.yml/badge.svg`
    - README.md contains `# SpotOn` heading
    - README.md contains repo.xml URL `https://raw.githubusercontent.com/stiefenm/spoton/main/repo.xml`
    - README.md mentions LMS 8.0+ as minimum version
    - README.md mentions Spotify Premium requirement
    - None of the files contain deferred items (Debug-Info-Tool, Changelog Automation)
  </acceptance_criteria>
  <done>CONTRIBUTING.md, LICENSE, and README.md created with all required content per D-08 through D-12</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries applicable -- this plan creates static documentation and YAML configuration files. No code execution, no API calls, no auth flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information Disclosure | README.md | accept | README only contains public repo URL and version info already visible in repo.xml |
| T-13-05 | Information Disclosure | CONTRIBUTING.md | accept | No secrets in contributor docs; test suite uses stubs only |
</threat_model>

<verification>
After push to main:
1. "New Issue" on GitHub shows "Bug Report" and "Feature Request" as structured forms
2. Bug Report form has LMS Version and OS as required fields
3. Feature Request form has Problem Statement as required field
4. Blank issues are disabled (config.yml)
5. README.md renders with CI badge, features list, install instructions
6. LICENSE shows MIT with correct copyright
7. CONTRIBUTING.md shows test running instructions
</verification>

<success_criteria>
- 3 issue template files exist in .github/ISSUE_TEMPLATE/
- CONTRIBUTING.md exists with dev setup + test + PR guidelines
- LICENSE exists with MIT License, Marek Stiefenhofer, 2024
- README.md exists with CI badge, features, requirements, installation
- All 6 files use correct format (YAML for templates, Markdown for docs)
</success_criteria>

<output>
Create `.planning/phases/13-repo-maintenance/13-02-SUMMARY.md` when done
</output>
