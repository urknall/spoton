---
phase: 14-connect-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - librespot-spoton/src/main.rs
  - librespot-spoton/src/connect.rs
autonomous: true
requirements: [CON-02]
must_haves:
  truths:
    - "Binary accepts --initial-volume <0-100> and converts to u16 0-65535 for ConnectConfig"
    - "Binary accepts --volume-ctrl linear|log|fixed and applies VolumeCtrl enum to MixerConfig"
    - "Both primary and reconnect ConnectConfig blocks use dynamic initial_volume"
    - "Both primary and reconnect MixerConfig blocks use the parsed volume_ctrl"
    - "Binary compiles without errors on the host platform"
  artifacts:
    - path: "librespot-spoton/src/main.rs"
      provides: "--initial-volume and --volume-ctrl CLI flag parsing + run_connect() call update"
      contains: "initial_volume"
    - path: "librespot-spoton/src/connect.rs"
      provides: "run_connect() signature with initial_volume and volume_ctrl_str params, VolumeCtrl import, MixerConfig with volume_ctrl, ConnectConfig with dynamic initial_volume"
      contains: "VolumeCtrl"
  key_links:
    - from: "librespot-spoton/src/main.rs"
      to: "librespot-spoton/src/connect.rs"
      via: "run_connect() call passing initial_volume_u16 and volume_ctrl_str"
      pattern: "initial_volume_u16.*volume_ctrl_str"
---

<objective>
Extend the librespot-spoton Rust binary with --initial-volume and --volume-ctrl CLI flags so that the Perl Daemon.pm can pass the LMS player volume and linear volume curve at Connect daemon startup.

Purpose: CON-02 requires Connect sessions to start with the correct LMS player volume instead of a hardcoded 50%. The binary must accept these flags and apply them to ConnectConfig.initial_volume and MixerConfig.volume_ctrl respectively -- both in the primary start path and the reconnect path (Pitfall 2).

Output: Recompiled librespot-spoton binary with two new CLI flags.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/14-connect-fixes/14-RESEARCH.md
@/home/sti/spoton/.planning/phases/14-connect-fixes/14-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase + cargo registry. -->

From librespot_playback::config (librespot-playback-0.8.0/src/config.rs):
  #[derive(Clone, Copy, Debug)]
  pub enum VolumeCtrl {
      Fixed,
      Linear,
      Log(f64),
  }
  impl VolumeCtrl {
      pub const DEFAULT_DB_RANGE: f64 = 60.0;
  }

From librespot_playback::mixer (librespot-playback-0.8.0/src/mixer/mod.rs):
  #[derive(Debug, Clone)]
  pub struct MixerConfig {
      pub volume_ctrl: VolumeCtrl,
      // ... other fields via ..MixerConfig::default()
  }

From librespot-spoton/src/connect.rs (current signature, line 821-830):
  pub async fn run_connect(
      cache_dir: &str,
      device_name: &str,
      player_mac: Option<&str>,
      lms_host_port: Option<&str>,
      lms_auth: Option<&str>,
      disable_discovery: bool,
      buffer_latency_ms: u64,
      autoplay: Option<bool>,
  ) -> Result<(), Box<dyn std::error::Error>>

From librespot-spoton/src/main.rs (run_connect call, lines 322-331):
  connect::run_connect(&cache_dir, &device_name, ..., autoplay)

ConnectConfig primary block at connect.rs line 967-973:
  initial_volume: u16::MAX / 2

ConnectConfig reconnect block at connect.rs line 1062-1068:
  initial_volume: u16::MAX / 2

MixerConfig primary at connect.rs line 851:
  MixerConfig::default()

MixerConfig reconnect at connect.rs line 1075:
  mixer_fn(MixerConfig::default())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --initial-volume and --volume-ctrl flag parsing to main.rs</name>
  <files>librespot-spoton/src/main.rs</files>
  <read_first>
    librespot-spoton/src/main.rs
    .planning/phases/14-connect-fixes/14-PATTERNS.md
    .planning/phases/14-connect-fixes/14-RESEARCH.md
  </read_first>
  <action>
    Add two new variables in the Connect mode variables block (after line 111, after `autoplay`):
    - `initial_volume_lms: Option[u8]` initialized to None (0-100 LMS scale)
    - `volume_ctrl_str: String` initialized to "log" (librespot default)

    Add two new match arms in the CLI parsing loop (after the "--autoplay" arm at lines 189-198). Follow the exact same guard pattern (`if i + 1 < args.len()`, `i += 1`):
    - "--initial-volume" arm: parse `args[i+1]` as `u8`, clamp to 100 via `.min(100)`, store in `initial_volume_lms`
    - "--volume-ctrl" arm: clone `args[i+1]` into `volume_ctrl_str`

    After the `while i < args.len()` loop ends (before the `match mode` block), add the LMS-to-librespot volume conversion:
    - `initial_volume_u16: Option[u16]` = map `initial_volume_lms` via `(v as u32 * 65535 / 100) as u16`

    Update the `connect::run_connect(...)` call in the `Mode::Connect` match arm (lines 322-331) to pass two additional trailing arguments: `initial_volume_u16` and `&volume_ctrl_str`.

    Per D-07: LMS sends 0-100 scale, Rust converts to 0-65535 internally.
    Per D-08: volume_ctrl defaults to "log" to match librespot default; Perl will pass "linear".
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - main.rs contains `let mut initial_volume_lms: Option<u8> = None`
    - main.rs contains `let mut volume_ctrl_str = String::from("log")`
    - main.rs contains match arm `"--initial-volume"` with `.parse::<u8>()` and `.min(100)`
    - main.rs contains match arm `"--volume-ctrl"` with `args[i + 1].clone()`
    - main.rs contains `let initial_volume_u16: Option<u16> = initial_volume_lms.map(|v| (v as u32 * 65535 / 100) as u16)`
    - run_connect() call includes `initial_volume_u16` and `&volume_ctrl_str` as trailing arguments
    - `cargo check` exits 0
  </acceptance_criteria>
  <done>main.rs parses --initial-volume and --volume-ctrl flags, converts LMS 0-100 to u16 0-65535, and passes both to run_connect(). cargo check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extend run_connect() with VolumeCtrl and initial_volume support</name>
  <files>librespot-spoton/src/connect.rs</files>
  <read_first>
    librespot-spoton/src/connect.rs
    .planning/phases/14-connect-fixes/14-PATTERNS.md
    .planning/phases/14-connect-fixes/14-RESEARCH.md
  </read_first>
  <action>
    Add import for VolumeCtrl. In the existing `use librespot_playback::config::{AudioFormat, PlayerConfig}` line, add `VolumeCtrl` to the import list. Alternatively add a separate `use librespot_playback::config::VolumeCtrl;` line.

    Update `run_connect()` function signature (lines 821-830) to add two new parameters at the end:
    - `initial_volume: Option[u16]` -- already scaled 0-65535 by main.rs
    - `volume_ctrl_str: &str` -- "linear", "log", or "fixed"

    Inside `run_connect()`, before the SoftMixer construction at line 851, parse `volume_ctrl_str` into a `VolumeCtrl` enum value stored in a local `let volume_ctrl_enum`:
    - "linear" maps to `VolumeCtrl::Linear`
    - "fixed" maps to `VolumeCtrl::Fixed`
    - anything else (including "log") maps to `VolumeCtrl::Log(VolumeCtrl::DEFAULT_DB_RANGE)`
    Since VolumeCtrl is Copy, the variable can be reused in the reconnect path without move issues.

    Update the primary MixerConfig at line 851: replace `MixerConfig::default()` with `MixerConfig { volume_ctrl: volume_ctrl_enum, ..MixerConfig::default() }`.

    Update the primary ConnectConfig at line 967-973: replace `initial_volume: u16::MAX / 2` with `initial_volume: initial_volume.unwrap_or(u16::MAX / 2)`.

    Update the reconnect ConnectConfig at line 1062-1068: same replacement -- `initial_volume: initial_volume.unwrap_or(u16::MAX / 2)` (Pitfall 2: reconnect must also use dynamic volume).

    Update the reconnect MixerConfig at line 1075: replace `MixerConfig::default()` with `MixerConfig { volume_ctrl: volume_ctrl_enum, ..MixerConfig::default() }`.
  </action>
  <verify>
    <automated>cd /home/sti/spoton/librespot-spoton && cargo build --release 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - connect.rs imports VolumeCtrl from librespot_playback::config
    - run_connect() signature includes `initial_volume: Option<u16>` and `volume_ctrl_str: &str`
    - connect.rs contains `let volume_ctrl_enum = match volume_ctrl_str` with arms for "linear", "fixed", and default "log"
    - Primary MixerConfig (around line 851) uses `volume_ctrl: volume_ctrl_enum`
    - Primary ConnectConfig (around line 970) uses `initial_volume: initial_volume.unwrap_or(u16::MAX / 2)`
    - Reconnect ConnectConfig (around line 1065) uses `initial_volume: initial_volume.unwrap_or(u16::MAX / 2)`
    - Reconnect MixerConfig (around line 1075) uses `volume_ctrl: volume_ctrl_enum`
    - `cargo build --release` exits 0
  </acceptance_criteria>
  <done>connect.rs accepts initial_volume and volume_ctrl_str, applies VolumeCtrl to both primary and reconnect MixerConfig, applies dynamic initial_volume to both ConnectConfig blocks. Binary compiles and builds successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Perl Daemon.pm -> Binary CLI | Perl passes --initial-volume and --volume-ctrl as CLI args to the binary process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | main.rs --initial-volume parsing | mitigate | Parse as u8, clamp to .min(100) -- out-of-range values capped; non-numeric silently ignored (parse fails, None retained) |
| T-14-02 | Tampering | main.rs --volume-ctrl parsing | mitigate | Match against known strings only; unknown values fall through to default VolumeCtrl::Log |
| T-14-03 | Denial of Service | ConnectConfig initial_volume | accept | Value is already validated 0-65535 by u16 type; worst case is wrong volume, not crash |
</threat_model>

<verification>
- `cargo check` passes after Task 1 (syntax correctness)
- `cargo build --release` passes after Task 2 (full compilation)
- `grep -c "initial_volume" librespot-spoton/src/main.rs` returns >= 3 (variable + parsing + conversion + call)
- `grep -c "volume_ctrl" librespot-spoton/src/connect.rs` returns >= 4 (import + variable + primary mixer + reconnect mixer)
- `grep -c "unwrap_or" librespot-spoton/src/connect.rs` returns >= 2 (two ConnectConfig blocks with fallback)
</verification>

<success_criteria>
Binary compiles with --initial-volume and --volume-ctrl support. Both primary and reconnect paths use dynamic values. The Perl Daemon.pm can pass these flags via @helperArgs (handled in Plan 02).
</success_criteria>

<output>
Create `.planning/phases/14-connect-fixes/14-01-SUMMARY.md` when done
</output>
