---
phase: 14-connect-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Connect/Daemon.pm
  - Plugins/SpotOn/Connect.pm
  - t/05_perl_syntax.t
autonomous: true
requirements: [CON-01, CON-02, CON-03]
must_haves:
  truths:
    - "Each Connect daemon uses a per-player cache directory spoton/connect-{mac}/ preventing credential overwrite"
    - "Connect daemon starts with --volume-ctrl linear and --initial-volume matching LMS player volume"
    - "Volume events from Spotify are processed within 3 seconds of daemon start (not 20)"
    - "Perl syntax checks pass for Connect.pm and Connect/Daemon.pm in CI"
  artifacts:
    - path: "Plugins/SpotOn/Connect/Daemon.pm"
      provides: "Per-player cache dir isolation + --volume-ctrl linear + --initial-volume args"
      contains: "connect-"
    - path: "Plugins/SpotOn/Connect.pm"
      provides: "Reduced grace period constant"
      contains: "VOLUME_GRACE_PERIOD => 3"
    - path: "t/05_perl_syntax.t"
      provides: "Syntax coverage for Connect.pm and Connect/Daemon.pm"
      contains: "Connect.pm"
  key_links:
    - from: "Plugins/SpotOn/Connect/Daemon.pm"
      to: "librespot-spoton binary"
      via: "@helperArgs --volume-ctrl linear --initial-volume"
      pattern: "volume-ctrl.*linear"
    - from: "Plugins/SpotOn/Connect/Daemon.pm"
      to: "filesystem"
      via: "catdir for per-player cache dir"
      pattern: "connect-.*self->id"
    - from: "Plugins/SpotOn/Connect.pm"
      to: "Plugins/SpotOn/Connect/DaemonManager.pm"
      via: "VOLUME_GRACE_PERIOD constant consumed by volume handler"
      pattern: "VOLUME_GRACE_PERIOD"
---

<objective>
Implement credential isolation per player MAC, volume synchronization at Connect session start, and reduce the volume grace period from 20s to 3s.

Purpose: Three Connect fixes -- CON-01 prevents credential overwrite when different Spotify users connect to different players, CON-02 ensures the Spotify volume matches the LMS volume at session start, CON-03 enables volume sync within 3 seconds.

Output: Modified Daemon.pm (cache dir + helperArgs), Connect.pm (grace period), expanded syntax test.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/14-connect-fixes/14-RESEARCH.md
@/home/sti/spoton/.planning/phases/14-connect-fixes/14-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Connect/Daemon.pm:
  __PACKAGE__->mk_accessor( rw => qw(id mac name cache ...) );
  # id = MAC without colons, set in new(): $id =~ s/://g; $self->id($id)
  # cache = rw accessor for cache directory path

  # Current cache dir calculation (lines 87-94, to be REPLACED):
  my $activeAccountId = $prefs->client($client)->get('activeAccount') || ...
  my $cacheDir = $activeAccountId ? catdir(..., 'spoton', $activeAccountId) : catdir(..., 'spoton');

  # @helperArgs push pattern (lines 119-127):
  push @helperArgs, '--disable-discovery' if $disableDiscovery;
  push @helperArgs, '--enable-volume-normalisation' if $prefs->get('normalization');

From Plugins/SpotOn/Connect.pm:
  use constant VOLUME_GRACE_PERIOD => 20;   # line 27

From t/05_perl_syntax.t:
  @pm_files list (lines 14-22)
  write_stub() helper function (lines 42-51)
  Existing stubs: Slim::Utils::Log, Slim::Utils::Prefs, Slim::Utils::Timers,
                  Slim::Utils::Cache, Slim::Networking::SimpleAsyncHTTP, etc.

From Slim::Player::Client (LMS API):
  $client->volume   # returns 0-100 (LMS volume scale)
  $client->isSynced()
  $client->name
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credential isolation and volume flags in Daemon.pm + grace period in Connect.pm</name>
  <files>Plugins/SpotOn/Connect/Daemon.pm, Plugins/SpotOn/Connect.pm</files>
  <read_first>
    Plugins/SpotOn/Connect/Daemon.pm
    Plugins/SpotOn/Connect.pm
    .planning/phases/14-connect-fixes/14-PATTERNS.md
    .planning/phases/14-connect-fixes/14-RESEARCH.md
  </read_first>
  <action>
    **Daemon.pm -- CON-01: Cache-Dir Isolation (per D-01, D-03, D-06)**

    Replace lines 87-94 (the entire `$activeAccountId` / `$cacheDir` block) with a single per-player cache dir calculation:
    - `$cacheDir = catdir($serverPrefs->get('cachedir'), 'spoton', 'connect-' . $self->id)`
    - `$self->cache($cacheDir)`
    The `$activeAccountId` lookup is removed entirely for Connect. `$self->id` is the MAC without colons (set in `new()` at line 52). `catdir` is already imported at line 7. Add a comment referencing CON-01 and P-49.

    **Daemon.pm -- CON-02: Volume flags (per D-07, D-08)**

    After the existing `push @helperArgs, '--enable-volume-normalisation'` line (line 120) and before the `getCapability('autoplay')` block (line 122), add two pushes:
    - `push @helperArgs, '--volume-ctrl', 'linear'` -- unconditional, per D-08 (no pref gate, squeezelite uses linear)
    - `push @helperArgs, '--initial-volume', int($client->volume // 50)` -- per D-07, defensive fallback 50 if undef per A3

    Add a comment block referencing CON-02, P-50, and the SoftMixer linear curve rationale.

    **Connect.pm -- CON-03: Grace period reduction (per D-09, D-10)**

    Change line 27 from `use constant VOLUME_GRACE_PERIOD => 20;` to `use constant VOLUME_GRACE_PERIOD => 3;`. Nothing else in Connect.pm changes. The comment block above (lines 24-26) remains accurate.

    **Discretion decisions:**
    - Echo suppression: 3s grace period is sufficient. No event-delta-filter needed. Rationale: `--initial-volume` starts with correct value (no initial mismatch), `suppress_next_volume` AtomicBool blocks the first echo, 3s covers subsequent startup transients. Per RESEARCH.md anti-pattern guidance, a delta filter would be overengineering.
    - Placement: --volume-ctrl and --initial-volume go between --enable-volume-normalisation and the getCapability('autoplay') block, following the existing flag-order convention (global unconditional flags first, then capability-gated flags).
    - getCapability() gate: Not needed. Both flags are unconditional (binary always supports them after Plan 01; old binaries ignore unknown flags per main.rs line 242 fallthrough).
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -Ilib -e 'use strict; use warnings; 1' && grep -c "connect-" Plugins/SpotOn/Connect/Daemon.pm && grep -c "volume-ctrl" Plugins/SpotOn/Connect/Daemon.pm && grep "VOLUME_GRACE_PERIOD" Plugins/SpotOn/Connect.pm</automated>
  </verify>
  <acceptance_criteria>
    - Daemon.pm lines 87-94 replaced: no reference to `activeAccountId` in the cache-dir block
    - Daemon.pm contains `catdir($serverPrefs->get('cachedir'), 'spoton', 'connect-' . $self->id)` for cache dir
    - Daemon.pm contains `push @helperArgs, '--volume-ctrl', 'linear'` (unconditional, two-element push)
    - Daemon.pm contains `push @helperArgs, '--initial-volume', int($client->volume // 50)` (two-element push with int() and // 50 fallback)
    - Both new pushes appear AFTER `--enable-volume-normalisation` and BEFORE the `getCapability('autoplay')` block
    - Connect.pm line 27 reads `use constant VOLUME_GRACE_PERIOD => 3;` (was 20)
    - No other lines in Connect.pm changed
    - No changes to TokenManager.pm (per D-02)
  </acceptance_criteria>
  <done>Daemon.pm uses per-player cache dir (connect-{mac}/), passes --volume-ctrl linear and --initial-volume to binary. Connect.pm grace period reduced to 3s.</done>
</task>

<task type="auto">
  <name>Task 2: Extend t/05_perl_syntax.t with Connect.pm and Daemon.pm coverage</name>
  <files>t/05_perl_syntax.t</files>
  <read_first>
    t/05_perl_syntax.t
    Plugins/SpotOn/Connect.pm
    Plugins/SpotOn/Connect/Daemon.pm
    .planning/phases/14-connect-fixes/14-PATTERNS.md
  </read_first>
  <action>
    **@pm_files expansion:**
    Add two entries to the `@pm_files` array (lines 14-22):
    - `"$project_dir/Plugins/SpotOn/Connect.pm"`
    - `"$project_dir/Plugins/SpotOn/Connect/Daemon.pm"`
    Place them after the existing `Settings/Callback.pm` entry, maintaining alphabetical grouping by module path.

    **New stubs (using the existing write_stub pattern from lines 42-51):**

    Add the following stubs after the existing stubs (after the `Slim::Web::Pages` stub around line 265):

    1. `Slim::Utils::Accessor` -- base class for Daemon.pm. Needs: `new` (bless constructor), `mk_accessor` (no-op), `AUTOLOAD` (catch-all), `can` (returns 1). The mk_accessor sub must be present because Daemon.pm calls `__PACKAGE__->mk_accessor(rw => qw(...))` at compile time.

    2. `Slim::Player::Client` -- Daemon.pm calls `Slim::Player::Client::getClient()` at runtime within subs. Stub with `getClient` (returns undef), `AUTOLOAD`, `can`.

    3. `Slim::Player::Sync` -- Daemon.pm calls `Slim::Player::Sync::syncname()` at runtime. Stub with `syncname` returning empty string.

    4. `Slim::Control::Request` -- Connect.pm uses for event dispatch and subscription. Stub with `new` (bless), `addDispatch` (no-op), `subscribe` (no-op), `AUTOLOAD`, `can`.

    5. `Slim::Music::Info` -- Connect.pm references for metadata. Stub with `setRemoteMetadata` (no-op), `AUTOLOAD`, `can`.

    Note: `Proc::Background` does NOT need a stub -- it is `require`'d inside sub body, and `perl -c` does not execute sub bodies. `IO::Select`, `MIME::Base64`, `Time::HiRes`, `Digest::MD5`, `File::Path`, `Scalar::Util` are all Perl core and do not need stubs.

    **Validation:** Run `prove t/05_perl_syntax.t` to confirm all 9 files pass (7 existing + 2 new). If any stub is missing (perl -c fails with "Can't locate"), add the missing stub and retry.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/05_perl_syntax.t 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `@pm_files` array contains 9 entries (7 existing + Connect.pm + Connect/Daemon.pm)
    - `t/05_perl_syntax.t` contains write_stub calls for: Slim::Utils::Accessor, Slim::Player::Client, Slim::Player::Sync, Slim::Control::Request, Slim::Music::Info
    - `prove t/05_perl_syntax.t` exits 0 with "All tests successful"
    - Output shows "ok 8" and "ok 9" (or equivalent) for Connect.pm and Connect/Daemon.pm
    - No Proc::Background stub exists (not needed for perl -c)
  </acceptance_criteria>
  <done>Syntax tests cover Connect.pm and Daemon.pm. All 9 module files pass perl -c. CI will catch Perl syntax errors in Connect modules going forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LMS player volume -> binary CLI arg | $client->volume (0-100) passed as --initial-volume string |
| Player MAC -> filesystem path | $self->id used in cache directory name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Spoofing | Daemon.pm credential isolation | mitigate | Separate --cache dir per player MAC (CON-01) prevents credential-clobber when different Spotify users connect (P-49) |
| T-14-05 | Tampering | Daemon.pm --initial-volume | mitigate | int($client->volume // 50) ensures numeric value 0-100; Rust side clamps to .min(100) and u16 type bounds |
| T-14-06 | Information Disclosure | Stale credentials in old path | accept | Old credentials.json in spoton/{accountId}/ remain but are no longer read by Connect daemon. Browse token path is unaffected (D-02). No active exposure -- credentials are only used by the binary that reads them |
| T-14-07 | Tampering | Cache dir path injection via MAC | accept | $self->id is derived from $client->mac which comes from LMS player registration (trusted); MAC format is hex digits only after s/://g |
</threat_model>

<verification>
- `prove t/05_perl_syntax.t` passes with 9/9 tests ok
- `prove -lv t/` passes (full test suite)
- `grep "connect-" Plugins/SpotOn/Connect/Daemon.pm` returns the new cache-dir line
- `grep "volume-ctrl" Plugins/SpotOn/Connect/Daemon.pm` returns the --volume-ctrl push
- `grep "initial-volume" Plugins/SpotOn/Connect/Daemon.pm` returns the --initial-volume push
- `grep "VOLUME_GRACE_PERIOD => 3" Plugins/SpotOn/Connect.pm` returns the updated constant
- `grep -c "activeAccount" Plugins/SpotOn/Connect/Daemon.pm` returns 0 (removed from cache-dir block)
</verification>

<success_criteria>
Connect daemon uses per-player isolated cache dirs, passes --volume-ctrl linear and --initial-volume to the binary, and the volume grace period is 3 seconds. All Perl syntax tests pass including the two new Connect modules.
</success_criteria>

<output>
Create `.planning/phases/14-connect-fixes/14-02-SUMMARY.md` when done
</output>
