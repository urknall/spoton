---
phase: 15-like-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/API/TokenManager.pm
  - Plugins/SpotOn/strings.txt
  - t/08_api_client.t
  - t/02_strings.t
autonomous: true
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-04
  - LIB-05

must_haves:
  truths:
    - "Client.pm exposes saveTracks, removeTracks, checkTracks as public class methods"
    - "D-12: saveTracks sends PUT to /me/library with uris query param (unified endpoint)"
    - "D-13: removeTracks sends DELETE to /me/library with uris query param"
    - "D-14: checkTracks sends GET to /me/library/contains with uris query param"
    - "D-15: All three methods route through me/* prefix triggering own-token flavor"
    - "Empty 200-body responses from PUT/DELETE do not trigger parse_error callback"
    - "D-05: strings.txt contains PLUGIN_SPOTON_LIKE and PLUGIN_SPOTON_UNLIKE in all 11 languages"
    - "Cache namespace version is 3 in both Client.pm and TokenManager.pm (LIB-05)"
    - "removeTracks can be verified in tests via DELETE HTTP method dispatch"
  artifacts:
    - path: "Plugins/SpotOn/API/Client.pm"
      provides: "saveTracks, removeTracks, checkTracks methods + empty-body guard"
      contains: "sub saveTracks"
    - path: "Plugins/SpotOn/API/TokenManager.pm"
      provides: "Cache version bump"
      contains: "new('spoton', 3)"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "Like/Unlike i18n strings"
      contains: "PLUGIN_SPOTON_LIKE"
    - path: "t/08_api_client.t"
      provides: "Tests for library methods"
      contains: "LIB-01"
  key_links:
    - from: "Client.pm::saveTracks"
      to: "Client.pm::_request"
      via: "method dispatch with 'put' + me/library path"
      pattern: "_request\\('put', 'me/library'"
    - from: "Client.pm::removeTracks"
      to: "Client.pm::_request"
      via: "method dispatch with 'delete' + me/library path"
      pattern: "_request\\('delete', 'me/library'"
    - from: "Client.pm::checkTracks"
      to: "Client.pm::_request"
      via: "method dispatch with 'get' + me/library/contains path"
      pattern: "_request\\('get', 'me/library/contains'"
---

<objective>
Add the three Spotify Unified Library API methods to Client.pm (saveTracks, removeTracks, checkTracks), fix the empty-body parse issue in _doFlavouredRequest, bump cache namespace version to 3, add all i18n strings, and extend the test suite to cover the new methods.

Purpose: Establishes the API foundation that Plan 02 wires into the trackInfoMenu UI. Without these methods, no Like/Unlike actions can be executed.
Output: Client.pm with 3 new methods + empty-body guard, TokenManager.pm with version bump, strings.txt with 7 new keys, tests passing.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/15-like-button/15-RESEARCH.md
@/home/sti/spoton/.planning/phases/15-like-button/15-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/API/Client.pm (line 0-39):
- Package: Plugins::SpotOn::API::Client
- Constants: API_BASE => 'https://api.spotify.com/v1', SPOTON_DEFAULT_CLIENT_ID
- Module-level: $cache = Slim::Utils::Cache->new('spoton', 2)  [line 34]
- All public methods are class methods: $class->methodName($accountId, ...)

From Plugins/SpotOn/API/Client.pm (line 169-176 — getSavedTracks pattern):
- sub getSavedTracks { my ($class, $accountId, $params, $cb) = @_; $class->_request('get', 'me/tracks', { _accountId => $accountId, ... }, $cb); }

From Plugins/SpotOn/API/Client.pm (line 308-314 — playerPause pattern):
- sub playerPause { my ($class, $accountId, $cb) = @_; $class->_request('put', 'me/player/pause', { _accountId => $accountId, _noCache => 1, }, $cb); }

From Plugins/SpotOn/API/Client.pm (line 480-489 — success callback in _doFlavouredRequest):
- my $result = eval { from_json($http->content) };
- if ($@) { $log->error(...); $userCb->(undef, { error => 'parse_error' }); return; }
- Empty-body guard contract: after this plan, empty/whitespace-only $http->content will set $result = undef and skip JSON parsing, then call $userCb->($result) normally (i.e., $err will be undef on success).

From Plugins/SpotOn/API/TokenManager.pm (line 33):
- my $cache = Slim::Utils::Cache->new('spoton', 2);

From t/08_api_client.t (line 270-272 — HTTP stub):
- sub get  { _dispatch(shift, 'GET',  @_) }
- sub post { _dispatch(shift, 'POST', @_) }
- sub put  { _dispatch(shift, 'PUT',  @_) }

From t/02_strings.t (line 25-54 — @bilingual_keys array):
- Array of string key names that must have both EN and DE translations
- PLUGIN_SPOTON_LIKE and PLUGIN_SPOTON_UNLIKE must be added here (11-language keys)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add library API methods to Client.pm + empty-body guard + cache version bump</name>
  <files>Plugins/SpotOn/API/Client.pm, Plugins/SpotOn/API/TokenManager.pm</files>
  <read_first>
    Plugins/SpotOn/API/Client.pm (full file — understand _request dispatch, _doFlavouredRequest success callback at line 480-489, existing method patterns at lines 169-176 and 308-314)
    Plugins/SpotOn/API/TokenManager.pm (line 33 — cache version)
    .planning/phases/15-like-button/15-RESEARCH.md (Pattern 5: new methods, Pitfall 6: empty body)
    .planning/phases/15-like-button/15-PATTERNS.md (Client.pm section — exact method signatures)
  </read_first>
  <action>
    In Client.pm, add three new public class methods after the existing library methods section (near getSavedTracks/getSavedAlbums/getFollowedArtists):

    1. saveTracks($class, $accountId, $uris, $cb) — calls _request('put', 'me/library', { _accountId => $accountId, _noCache => 1, uris => join(',', @{$uris || []}) }, $cb) per D-12. The uris param contains full Spotify URIs (e.g. spotify:track:ID), not bare IDs.

    2. removeTracks($class, $accountId, $uris, $cb) — calls _request('delete', 'me/library', { _accountId => $accountId, _noCache => 1, uris => join(',', @{$uris || []}) }, $cb) per D-13. DELETE is supported by SimpleAsyncHTTP::Base (RESEARCH.md verified).

    3. checkTracks($class, $accountId, $uris, $cb) — calls _request('get', 'me/library/contains', { _accountId => $accountId, _noCache => 1, uris => join(',', @{$uris || []}) }, $cb) per D-14. _noCache => 1 because caching is managed manually in Plugin.pm per D-07 and Pitfall 2.

    All three use _noCache => 1. All three route through me/* path prefix which triggers own-token flavor automatically per D-15.

    4. Fix Pitfall 6 (empty-body guard) in _doFlavouredRequest success callback (line ~484): Change the eval block so that an empty $http->content is treated as success with undef result, NOT as a parse error. The guard: if content is empty/whitespace-only, set $result to undef and skip JSON parsing, then fall through to the normal $userCb->($result) call. This prevents PUT/DELETE /me/library 200-OK responses from triggering parse_error. Contract: on empty-body success, $userCb->($result) is called with $result = undef and NO $err argument (i.e., $err is undef in the callback).

    5. Bump cache version: Change line 34 from Slim::Utils::Cache->new('spoton', 2) to new('spoton', 3) per LIB-05.

    In TokenManager.pm, bump cache version: Change line 33 from Slim::Utils::Cache->new('spoton', 2) to new('spoton', 3) per LIB-05 and PATTERNS.md implementation note 2.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && grep -c "sub saveTracks\|sub removeTracks\|sub checkTracks" Plugins/SpotOn/API/Client.pm | grep -q 3 && grep -q "new('spoton', 3)" Plugins/SpotOn/API/Client.pm && grep -q "new('spoton', 3)" Plugins/SpotOn/API/TokenManager.pm && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Client.pm contains sub saveTracks with _request('put', 'me/library', ...) and _noCache => 1
    - Client.pm contains sub removeTracks with _request('delete', 'me/library', ...) and _noCache => 1
    - Client.pm contains sub checkTracks with _request('get', 'me/library/contains', ...) and _noCache => 1
    - Client.pm _doFlavouredRequest success callback (line ~484) has empty-body guard: skips from_json when $http->content is empty, calls $userCb->(undef) with no error argument
    - Client.pm line 34: Slim::Utils::Cache->new('spoton', 3)
    - TokenManager.pm line 33: Slim::Utils::Cache->new('spoton', 3)
    - No new external dependencies added
  </acceptance_criteria>
  <done>Three new Client.pm methods exist with correct _request calls, empty-body guard prevents parse_error on 200-OK empty responses (contract: $err is undef on empty-body success), cache version is 3 in both Client.pm and TokenManager.pm</done>
</task>

<task type="auto">
  <name>Task 2: Add i18n strings + bilingual test enforcement + extend test suite with DELETE stub and library method tests</name>
  <files>Plugins/SpotOn/strings.txt, t/08_api_client.t, t/02_strings.t</files>
  <read_first>
    Plugins/SpotOn/strings.txt (last 30 lines — to see format and append location)
    t/08_api_client.t (full file — understand mock infrastructure, existing test blocks, request tracking)
    t/02_strings.t (full file — understand @bilingual_keys array and @en_only_keys array)
    .planning/phases/15-like-button/15-PATTERNS.md (strings.txt section — exact keys and translations; test section — exact test block structure)
    .planning/phases/15-like-button/15-RESEARCH.md (Pattern 5 — string translations)
  </read_first>
  <action>
    In strings.txt, append 7 new string keys at end of file before final newline, following existing indentation convention (tab before language code, tab before translation). Language order: CS DA DE EN ES FR IT NL NO PL SV. Keys to add per D-05 and PATTERNS.md:

    1. PLUGIN_SPOTON_LIKE — 11 languages (translations in RESEARCH.md/PATTERNS.md)
    2. PLUGIN_SPOTON_UNLIKE — 11 languages
    3. PLUGIN_SPOTON_LIKED — EN only ("Liked!")
    4. PLUGIN_SPOTON_UNLIKED — EN only ("Removed")
    5. PLUGIN_SPOTON_LIKE_ERROR — EN only ("Could not save track")
    6. PLUGIN_SPOTON_LIKE_ERROR_SCOPE — EN only ("Could not save track - missing user-library-modify scope. Re-authenticate via Settings.")
    7. PLUGIN_SPOTON_MANAGE_LIKE — EN only ("Like / Unlike")

    EN-only keys use LMS string fallback for non-EN locales (LMS standard behavior).

    In t/02_strings.t:

    1. Add PLUGIN_SPOTON_LIKE and PLUGIN_SPOTON_UNLIKE to the @bilingual_keys array (these have all 11 language translations including DE, so they must be enforced as bilingual).
    2. Add PLUGIN_SPOTON_LIKED, PLUGIN_SPOTON_UNLIKED, PLUGIN_SPOTON_LIKE_ERROR, PLUGIN_SPOTON_LIKE_ERROR_SCOPE, and PLUGIN_SPOTON_MANAGE_LIKE to the @en_only_keys array (these are EN-only by design).

    In t/08_api_client.t:

    1. Add sub delete { _dispatch(shift, 'DELETE', @_) } to the Slim::Networking::SimpleAsyncHTTP mock package (after the existing sub put at line 272).

    2. Add test blocks before done_testing() for the three new methods plus the cache-hit test:
       - LIB-01 test: Call saveTracks, verify request method is PUT, URL contains /me/library, URL contains uris= query param
       - LIB-02 test: Call removeTracks, verify request method is DELETE, URL contains /me/library
       - LIB-03 test: Call checkTracks, verify request method is GET, URL contains me/library/contains
       - LIB-04 test (cache-hit, no second API call): Call checkTracks twice with the same accountId and trackUri. After the first call completes (auto_mode=success with a JSON response of [true]), the callback in Plugin.pm would cache the result. However, since this test file tests Client.pm directly (not Plugin.pm caching), test LIB-04 at the Client.pm level by verifying that checkTracks with _noCache => 1 always dispatches an HTTP request (the caching is Plugin.pm's responsibility per D-07). To verify the contract: call checkTracks once, confirm exactly 1 request issued, then note in the test name that Plugin.pm-level cache behavior is verified manually per VALIDATION.md. Alternatively, if the mock infrastructure supports it, create a mock-sequence test: first call issues HTTP, second call within the same test block also issues HTTP (proving Client.pm does NOT cache, which is correct because _noCache => 1), confirming the caching responsibility lies in Plugin.pm.
       - LIB-05 test: Read Plugin.pm source, verify SPOTON_CACHE_VERSION => 3

    Follow the existing test block pattern (reset_requests, set auto_mode, call method, inspect @requests). Each test block in its own lexical scope with descriptive test names using the LIB-NN prefix.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/08_api_client.t t/05_perl_syntax.t t/02_strings.t 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - strings.txt contains PLUGIN_SPOTON_LIKE with all 11 language translations (CS DA DE EN ES FR IT NL NO PL SV)
    - strings.txt contains PLUGIN_SPOTON_UNLIKE with all 11 language translations
    - strings.txt contains PLUGIN_SPOTON_LIKED, PLUGIN_SPOTON_UNLIKED, PLUGIN_SPOTON_LIKE_ERROR, PLUGIN_SPOTON_LIKE_ERROR_SCOPE, PLUGIN_SPOTON_MANAGE_LIKE (EN only each)
    - t/02_strings.t @bilingual_keys includes PLUGIN_SPOTON_LIKE and PLUGIN_SPOTON_UNLIKE
    - t/02_strings.t @en_only_keys includes PLUGIN_SPOTON_LIKED, PLUGIN_SPOTON_UNLIKED, PLUGIN_SPOTON_LIKE_ERROR, PLUGIN_SPOTON_LIKE_ERROR_SCOPE, PLUGIN_SPOTON_MANAGE_LIKE
    - t/08_api_client.t mock package has sub delete method
    - t/08_api_client.t has test blocks for LIB-01 (saveTracks PUT), LIB-02 (removeTracks DELETE), LIB-03 (checkTracks GET), LIB-04 (checkTracks _noCache behavior), LIB-05 (SPOTON_CACHE_VERSION == 3)
    - prove t/08_api_client.t exits 0
    - prove t/05_perl_syntax.t exits 0
    - prove t/02_strings.t exits 0
  </acceptance_criteria>
  <done>All 7 string keys present in strings.txt, PLUGIN_SPOTON_LIKE and PLUGIN_SPOTON_UNLIKE enforced as bilingual in t/02_strings.t, EN-only keys registered in @en_only_keys, DELETE stub added to test mock, all 5 new test blocks pass (LIB-01 through LIB-05), full test suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin -> Spotify API | Untrusted HTTP responses from Spotify endpoints |
| LMS TrackInfo -> Plugin | $remoteMeta->{uri} from LMS could contain unexpected values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | Client.pm saveTracks/removeTracks uris param | mitigate | URI format validated in Plugin.pm trackInfoMenu guard: $trackUri =~ /^spotify:track:[A-Za-z0-9]+$/ before any API call (Plan 02 Task 1) |
| T-15-02 | Info Disclosure | Client.pm _request logging | accept | Existing T-02-10 convention: never log Authorization header. New methods use _request which already enforces this (line 478) |
| T-15-03 | Denial of Service | checkTracks called per menu open | mitigate | D-07: 60s TTL cache prevents repeated API calls; D-06: on-demand only, no pre-fetch on track listings |
| T-15-04 | Elevation of Privilege | Token scope mismatch | mitigate | LIB-05: cacheSchemaVersion 2->3 forces token refresh; D-10: 403 fallback with user-visible error message |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this phase — all modules already bundled with LMS |
</threat_model>

<verification>
- prove t/ exits 0 (full suite)
- grep -c "sub saveTracks\|sub removeTracks\|sub checkTracks" Plugins/SpotOn/API/Client.pm returns 3
- grep "SPOTON_CACHE_VERSION" Plugins/SpotOn/Plugin.pm shows => 3
- grep -c "PLUGIN_SPOTON_LIKE\b" Plugins/SpotOn/strings.txt returns 1
- grep -c "PLUGIN_SPOTON_UNLIKE\b" Plugins/SpotOn/strings.txt returns 1
</verification>

<success_criteria>
Client.pm has three working library methods (saveTracks/PUT, removeTracks/DELETE, checkTracks/GET) using unified /me/library endpoints with query-param URIs. Empty-body guard prevents parse errors on 200-OK PUT/DELETE responses. Cache version is 3 across all modules. strings.txt has all 7 new keys. Tests cover all new methods and pass. Bilingual keys enforced in t/02_strings.t.
</success_criteria>

<output>
Create `.planning/phases/15-like-button/15-01-SUMMARY.md` when done
</output>
