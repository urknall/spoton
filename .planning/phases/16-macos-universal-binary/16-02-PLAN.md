---
phase: 16-macos-universal-binary
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Helper.pm
  - Plugins/SpotOn/Settings.pm
  - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html
  - Plugins/SpotOn/strings.txt
  - README.md
autonomous: true
requirements:
  - PLT-02
  - PLT-03

must_haves:
  truths:
    - "D-02: Helper.pm ISMAC block with addFindBinPaths on Bin/darwin/ — consistent with Spotty pattern"
    - "D-07: README lists macOS in platform list with xattr workaround for manual downloads"
    - "D-08: Settings page shows dynamic Gatekeeper hint when binary check fails on macOS"
    - "D-09: Gatekeeper warning in strings.txt for all 11 languages via established i18n pattern"
  artifacts:
    - path: "Plugins/SpotOn/Helper.pm"
      provides: "ISMAC block for darwin binary path registration"
      contains: "main::ISMAC"
    - path: "Plugins/SpotOn/Settings.pm"
      provides: "isMac template variable"
      contains: "isMac"
    - path: "Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html"
      provides: "Gatekeeper warning div conditional on isMac"
      contains: "PLUGIN_SPOTON_GATEKEEPER_HINT"
    - path: "Plugins/SpotOn/strings.txt"
      provides: "PLUGIN_SPOTON_GATEKEEPER_HINT in 11 languages"
      contains: "PLUGIN_SPOTON_GATEKEEPER_HINT"
    - path: "README.md"
      provides: "macOS in supported platform list"
      contains: "macOS"
  key_links:
    - from: "Plugins/SpotOn/Settings.pm"
      to: "basic.html"
      via: "isMac template variable"
      pattern: "isMac"
    - from: "basic.html"
      to: "strings.txt"
      via: "PLUGIN_SPOTON_GATEKEEPER_HINT string reference"
      pattern: "PLUGIN_SPOTON_GATEKEEPER_HINT"
    - from: "Plugins/SpotOn/Helper.pm"
      to: "Plugins/SpotOn/Bin/darwin/"
      via: "addFindBinPaths call"
      pattern: "Bin.*darwin"
---

<objective>
Add macOS binary detection in Helper.pm (ISMAC block), Gatekeeper documentation in README, and a dynamic Gatekeeper warning on the Settings page with i18n support in all 11 languages.

Purpose: Ensure the macOS Universal Binary is found by LMS on macOS systems (PLT-02) and that users who encounter Gatekeeper blocking have clear remediation steps (PLT-03).

Output: Updated Helper.pm, Settings.pm, basic.html, strings.txt, and README.md.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/PROJECT.md
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/16-macos-universal-binary/16-RESEARCH.md
@/home/sti/spoton/.planning/phases/16-macos-universal-binary/16-PATTERNS.md

<interfaces>
<!-- Helper.pm init() structure (lines 19-37): -->
<!-- Line 21-26: aarch64 fallback (guarded by !ISWINDOWS && !ISMAC) -->
<!-- Line 28-32: ISWINDOWS block with addFindBinPaths('Bin', 'x86_64-win64') -->
<!-- Line 34-36: $prefs->setChange callback -->
<!-- INSERT ISMAC block between line 32 and line 34 -->

<!-- Settings.pm handler() (lines 56-61): -->
<!-- $paramRef->{helperMissing}, $paramRef->{binaryVersion}, $paramRef->{binaryPath} -->
<!-- Line 217: $paramRef->{degradedMode} = _isDegradedMode() -->
<!-- isMac goes near the helperMissing/binaryPath block -->

<!-- basic.html binary status section (lines 76-82): -->
<!-- [% WRAPPER setting title="PLUGIN_SPOTON_BINARY_STATUS" %] -->
<!--   [% IF binaryPath %] ... [% ELSE %] red BINARY_MISSING [% END %] -->
<!-- Gatekeeper hint goes inside the ELSE block, after the red error -->

<!-- strings.txt i18n: 11 languages (CS DA DE EN ES FR IT NL NO PL SV), tab-indented -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ISMAC block to Helper.pm and isMac to Settings.pm</name>
  <files>Plugins/SpotOn/Helper.pm, Plugins/SpotOn/Settings.pm</files>
  <read_first>
    - Plugins/SpotOn/Helper.pm (full file -- understand init() structure, existing ISWINDOWS block at lines 28-32, insertion point before line 34)
    - Plugins/SpotOn/Settings.pm (full file -- understand handler() template variable passing at lines 56-61 and degradedMode at line 217)
    - .planning/phases/16-macos-universal-binary/16-PATTERNS.md (ISMAC pattern, Settings.pm isMac pattern)
  </read_first>
  <action>
    Two files to modify:

    **Helper.pm** (per D-02): Insert a new ISMAC block in init() after the ISWINDOWS block (line 32) and before the $prefs->setChange call (line 34). The block calls Slim::Utils::Misc::addFindBinPaths with catdir(..., 'Bin', 'darwin') -- identical structure to the ISWINDOWS block but using main::ISMAC as the guard and 'darwin' as the directory name. This ensures LMS finds the Universal Binary at Bin/darwin/spoton on macOS.

    **Settings.pm** (per D-08): Add $paramRef->{isMac} = main::ISMAC ? 1 : 0; in the handler() method near the existing helperMissing/binaryPath block (around line 61). This passes the macOS detection flag to the template so basic.html can conditionally show the Gatekeeper warning. Place it after the binaryPath assignment.
  </action>
  <verify>
    <automated>grep -c 'main::ISMAC' Plugins/SpotOn/Helper.pm | grep -q '[2-9]' && grep -c 'isMac' Plugins/SpotOn/Settings.pm | grep -q '[1-9]' && prove t/05_perl_syntax.t && echo "ALL CHECKS PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - Helper.pm contains if ( main::ISMAC ) block with addFindBinPaths(..., 'Bin', 'darwin')
    - Helper.pm ISMAC block is placed after the ISWINDOWS block and before $prefs->setChange
    - Settings.pm handler() sets $paramRef->{isMac} using main::ISMAC
    - prove t/05_perl_syntax.t passes (all Perl files compile cleanly)
    - prove t/ full suite passes (no regressions)
  </acceptance_criteria>
  <done>Helper.pm registers Bin/darwin/ on macOS via ISMAC guard; Settings.pm passes isMac flag to template. All Perl syntax tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add Gatekeeper hint to basic.html + strings.txt + README.md</name>
  <files>Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html, Plugins/SpotOn/strings.txt, README.md</files>
  <read_first>
    - Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html (full file -- understand binary status section lines 76-82, conditional warning pattern lines 29-33 and 99-105)
    - Plugins/SpotOn/strings.txt (full file -- understand i18n block format: key line, then tab+lang+tab+translation for all 11 languages)
    - README.md (full file -- line 36 platform list to update)
    - .planning/phases/16-macos-universal-binary/16-RESEARCH.md (i18n string examples, README update example)
    - .planning/phases/16-macos-universal-binary/16-PATTERNS.md (conditional warning div pattern, i18n string format, README platform list)
  </read_first>
  <action>
    Three files to modify:

    **basic.html** (per D-08): In the binary status section (WRAPPER setting title="PLUGIN_SPOTON_BINARY_STATUS"), inside the ELSE block (when binaryPath is falsy, line 79-81), add a Gatekeeper hint div AFTER the existing red BINARY_MISSING div. The new block is guarded by [% IF isMac %] and uses the established orange warning pattern (color: orange, margin-top:8px). The div content is [% 'PLUGIN_SPOTON_GATEKEEPER_HINT' | string %]. This follows the same conditional-warning pattern as discoveryByCrashLoop (lines 29-33) and degradedMode (lines 99-105).

    **strings.txt** (per D-09): Append a new PLUGIN_SPOTON_GATEKEEPER_HINT string block after the last existing block (after PLUGIN_SPOTON_MANAGE_LIKE). Format: key on its own line, then one line per language (tab + 2-letter code + tab + translation) for all 11 languages: CS, DA, DE, EN, ES, FR, IT, NL, NO, PL, SV. The English text is: "The SpotOn binary is blocked by macOS. Run in Terminal: xattr -d com.apple.quarantine /path/to/spoton" -- translations from the RESEARCH.md i18n section. Use the exact translations from 16-RESEARCH.md "i18n String-Pattern fuer Gatekeeper-Warnung" section. Ensure a blank line before the new block (consistent with existing format).

    **README.md** (per D-07): Replace line 36 (the "Supported platforms:" line ending with "macOS binaries not yet included.") with the updated text that adds macOS Universal Binary to the list and includes the xattr hint for manual downloads. New text: "Supported platforms: x86_64 Linux, i386 Linux, aarch64 Linux (Pi 4+), armhf Linux (Pi 2/3), arm Linux, x86_64 Windows, macOS (Universal Binary: Intel + Apple Silicon). On macOS, if you download the binary manually (not via LMS plugin manager), you may need to run `xattr -d com.apple.quarantine /path/to/spoton` in Terminal before first use."
  </action>
  <verify>
    <automated>grep -c 'PLUGIN_SPOTON_GATEKEEPER_HINT' Plugins/SpotOn/strings.txt | grep -q '[1-9]' && grep -c 'PLUGIN_SPOTON_GATEKEEPER_HINT' Plugins/SpotOn/HTML/EN/plugins/SpotOn/settings/basic.html | grep -q '[1-9]' && grep -c 'Universal Binary' README.md | grep -q '[1-9]' && prove t/02_strings.t && echo "ALL CHECKS PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - basic.html contains [% IF isMac %] block with PLUGIN_SPOTON_GATEKEEPER_HINT inside the binary-missing ELSE section
    - basic.html Gatekeeper div uses color: orange (not red -- warning, not error)
    - strings.txt contains PLUGIN_SPOTON_GATEKEEPER_HINT key with exactly 11 language translations (CS DA DE EN ES FR IT NL NO PL SV)
    - strings.txt new block follows the tab-indented format consistent with all other entries
    - prove t/02_strings.t passes (string completeness check)
    - README.md line 36 (or nearby) contains "macOS (Universal Binary: Intel + Apple Silicon)"
    - README.md contains "xattr -d com.apple.quarantine" for manual download workaround
    - README.md no longer contains "macOS binaries not yet included"
  </acceptance_criteria>
  <done>Settings page shows orange Gatekeeper hint when binary missing on macOS. All 11 language translations present. README lists macOS as supported with xattr workaround. String completeness test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings page (HTTP) | Template renders user-visible content; no user input in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | Settings page isMac flag | accept | Boolean flag only (0/1), reveals OS type which is already observable |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan |
</threat_model>

<verification>
1. prove t/ -- full test suite passes (syntax, strings, binary check)
2. grep confirms ISMAC block in Helper.pm, isMac in Settings.pm
3. grep confirms GATEKEEPER_HINT in basic.html and strings.txt (11 languages)
4. grep confirms "Universal Binary" in README.md
</verification>

<success_criteria>
- Helper.pm has ISMAC block registering Bin/darwin/
- Settings.pm passes isMac flag to template
- basic.html shows Gatekeeper hint when binary missing on macOS
- strings.txt has PLUGIN_SPOTON_GATEKEEPER_HINT in all 11 languages
- README.md lists macOS as supported platform with xattr workaround
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/16-macos-universal-binary/16-02-SUMMARY.md` when done
</output>
