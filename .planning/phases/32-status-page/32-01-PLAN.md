---
phase: 32-status-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/Status.pm
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/Plugin.pm
  - t/05_perl_syntax.t
  - t/13_status_page.t
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /plugins/SpotOn/status/data returns valid JSON with daemons, api, errors, tokens, system keys"
    - "API request counter increments on every Client.pm request"
    - "429 counter increments on every rate-limit response"
    - "Error ring-buffer stores up to 30 entries with timestamp, level, module, message"
    - "System info is loaded once and cached in module variable"
    - "Status.pm compiles cleanly under perl -c"
  artifacts:
    - path: "Plugins/SpotOn/Status.pm"
      provides: "JSON endpoint handler, ring-buffer, data aggregation"
      exports: ["new", "recordError"]
    - path: "t/13_status_page.t"
      provides: "Unit tests for Status.pm"
      min_lines: 40
  key_links:
    - from: "Plugins/SpotOn/Status.pm"
      to: "Plugins/SpotOn/API/Client.pm"
      via: "Client->statusSnapshot()"
      pattern: "statusSnapshot"
    - from: "Plugins/SpotOn/API/Client.pm"
      to: "Plugins/SpotOn/Status.pm"
      via: "INC-guarded recordError call"
      pattern: "INC.*Status.pm"
    - from: "Plugins/SpotOn/Plugin.pm"
      to: "Plugins/SpotOn/Status.pm"
      via: "require + new() in WEBUI block"
      pattern: "require Plugins::SpotOn::Status"
---

<objective>
Create the Perl backend for the SpotOn Status Page: the Status.pm module with JSON endpoint and error ring-buffer, API telemetry counters in Client.pm, Plugin.pm registration, and test coverage.

Purpose: Provides the data layer that the frontend dashboard (Plan 02) will poll every 5 seconds.
Output: Status.pm module, extended Client.pm with statusSnapshot(), test file t/13_status_page.t, syntax test extended.
</objective>

<execution_context>
@/home/sti/.claude/get-shit-done/workflows/execute-plan.md
@/home/sti/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/sti/spoton/.planning/ROADMAP.md
@/home/sti/spoton/.planning/STATE.md
@/home/sti/spoton/.planning/phases/32-status-page/32-CONTEXT.md
@/home/sti/spoton/.planning/phases/32-status-page/32-RESEARCH.md
@/home/sti/spoton/.planning/phases/32-status-page/32-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Plugins/SpotOn/Settings.pm (lines 466-475) — _jsonResponse helper to copy verbatim:
  sub _jsonResponse {
      my ($httpClient, $response, $data, $code) = @_;
      $code //= 200;
      my $bytes = encode('UTF-8', to_json($data));
      $response->header('Content-Length' => length($bytes));
      $response->code($code);
      $response->header('Connection' => 'close');
      $response->content_type('application/json');
      Slim::Web::HTTP::addHTTPResponse($httpClient, $response, \$bytes);
  }

From Plugins/SpotOn/Settings.pm (lines 24-57) — addRawFunction registration:
  require Slim::Web::Pages;
  Slim::Web::Pages->addRawFunction('plugins/SpotOn/...', \&handler);

From Plugins/SpotOn/API/Client.pm (lines 37-72) — existing module vars and reset:
  my $inflightCount = 0;
  sub reset { my ($class) = @_; $inflightCount = 0; ... }

From Plugins/SpotOn/API/Client.pm (line 546) — inflightCount increment point:
  $inflightCount++;

From Plugins/SpotOn/API/Client.pm (lines 684-701) — 429 handler with cache set:
  if ($code == 429) { ... $cache->set($rlKey, 1, $retryAfter); ... }

From Plugins/SpotOn/Unified/DaemonManager.pm — helperInstances API:
  for my $helper (Plugins::SpotOn::Unified::DaemonManager->helperInstances()) {
      $helper->mac; $helper->pid; $helper->alive; $helper->uptime;
      $helper->_connectEnabled; $helper->_streamPort;
  }

From Plugins/SpotOn/API/TokenManager.pm — getAccountIds, isDiscoveryRunning:
  Plugins::SpotOn::API::TokenManager->getAccountIds()
  Plugins::SpotOn::API::TokenManager->isDiscoveryRunning()

From Plugins/SpotOn/Helper.pm — get(), getVersion(), getCapability():
  my ($helperPath, $helperVersion) = Plugins::SpotOn::Helper->get();

From Plugins/SpotOn/Plugin.pm (lines 112-143) — WEBUI block for Status registration:
  if (main::WEBUI) {
      require Plugins::SpotOn::Settings; ...
      # ADD: require Plugins::SpotOn::Status; Plugins::SpotOn::Status->new();
  }

INC-guard pattern (Settings.pm line 513, Plugin.pm line 132):
  if ($INC{'Plugins/SpotOn/ModuleName.pm'}) { ... }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Status.pm module with JSON endpoint and ring-buffer</name>
  <files>Plugins/SpotOn/Status.pm</files>
  <read_first>
    Plugins/SpotOn/Settings.pm (lines 1-57 for imports, new(), addRawFunction pattern; lines 466-475 for _jsonResponse)
    Plugins/SpotOn/API/Client.pm (lines 37-72 for module vars + reset pattern)
    Plugins/SpotOn/Unified/DaemonManager.pm (helperInstances method)
    Plugins/SpotOn/API/TokenManager.pm (getAccountIds, isDiscoveryRunning)
    Plugins/SpotOn/Helper.pm (get, getVersion, getCapability)
    .planning/phases/32-status-page/32-PATTERNS.md (full pattern assignments for Status.pm)
    .planning/phases/32-status-page/32-RESEARCH.md (Pattern 1-5, Pitfall 1-3)
  </read_first>
  <action>
    Create new file Plugins/SpotOn/Status.pm with package Plugins::SpotOn::Status.

    Imports: use strict; use warnings; use Encode qw(encode); use JSON::XS::VersionOneAndTwo; use Slim::Utils::Log; use Slim::Utils::Prefs; use POSIX qw(); Module-level state: my $log = Slim::Utils::Log->logger('plugin.spoton'); my $prefs = preferences('plugin.spoton'); my $cache = Slim::Utils::Cache->new('spoton', 4);

    Ring-buffer state: my @_errorHistory; use constant MAX_ERROR_HISTORY => 30; (per Claude's Discretion, 30 entries).

    System info cache (D-03): my $_systemInfo; sub _systemInfo { return $_systemInfo if $_systemInfo; require Plugins::SpotOn::Helper; my ($helperPath, $helperVersion) = Plugins::SpotOn::Helper->get(); $_systemInfo = { pluginVersion => Plugins::SpotOn::Plugin->_pluginDataFor('version') || 'unknown', binaryVersion => $helperVersion || 'unknown', lmsVersion => $::VERSION, perlVersion => $], os => $^O }; return $_systemInfo; }

    Constructor sub new: bless {}, $class (NOT inheriting from Slim::Web::Settings, per D-09). Register endpoint via require Slim::Web::Pages; Slim::Web::Pages->addRawFunction('plugins/SpotOn/status/data', \&_statusDataHandler);

    Handler sub _statusDataHandler($httpClient, $response): No CSRF check (D-12, read-only). Collect data into %data hash with these keys:

    daemons: require Plugins::SpotOn::Unified::DaemonManager; iterate helperInstances(), for each build hash with mac, alive (ternary 1:0), pid (or 0), uptime (int()), connectEnabled (ternary 1:0), streamPort (or JSON::null). Also resolve player name via Slim::Player::Client->getClient($helper->mac) and use $client->name if defined, otherwise mac as name.

    api: call Plugins::SpotOn::API::Client->statusSnapshot() (created in Task 2).

    errors: call _errorHistory() returning \@_errorHistory in reverse order (newest first).

    tokens: require Plugins::SpotOn::API::TokenManager; build hash with accountCount => scalar(getAccountIds()), discoveryRunning => ternary 1:0.

    system: call _systemInfo() (D-05 — included in every response, cached in module var so only computed once).

    Respond via _jsonResponse($httpClient, $response, \%data).

    Copy _jsonResponse verbatim from Settings.pm lines 466-475 as a private sub (do not call across modules, per PATTERNS.md).

    Public class method sub recordError($class, $level, $module, $message): push to @_errorHistory with ts => time(), level, module, message keys. Shift oldest if scalar @_errorHistory > MAX_ERROR_HISTORY.

    Sub _errorHistory: return a new arrayref containing @_errorHistory in reverse order (newest first per UI-SPEC).

    All cross-module calls use require (lazy), not use, to avoid circular dependency (Pitfall 1). The module does NOT inherit from Slim::Web::Settings.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && perl -I. -e 'use strict; use warnings; require Plugins::SpotOn::Status; print "ok\n"' 2>&1 | grep -v 'Can.t locate' || echo "FAIL: Status.pm has compile issues"</automated>
  </verify>
  <acceptance_criteria>
    - File Plugins/SpotOn/Status.pm exists with package Plugins::SpotOn::Status
    - Contains sub new that calls Slim::Web::Pages->addRawFunction('plugins/SpotOn/status/data', \&_statusDataHandler)
    - Contains sub _statusDataHandler that builds %data with keys: daemons, api, errors, tokens, system
    - Contains sub recordError($class, $level, $module, $message) that pushes to @_errorHistory
    - Contains sub _errorHistory that returns arrayref in reverse chronological order
    - Contains sub _systemInfo that caches result in $_systemInfo module var (D-03)
    - Contains private sub _jsonResponse copied verbatim from Settings.pm lines 466-475
    - MAX_ERROR_HISTORY constant equals 30
    - No use base or SUPER::new (not a Slim::Web::Settings subclass)
    - No _csrfCheck call in _statusDataHandler (D-12)
    - All cross-module references use require, not use
  </acceptance_criteria>
  <done>Status.pm module exists with JSON endpoint handler, ring-buffer, data aggregation from DaemonManager/Client/TokenManager/Helper, and _jsonResponse helper.</done>
</task>

<task type="auto">
  <name>Task 2: Add API telemetry counters and statusSnapshot to Client.pm, register Status in Plugin.pm, extend tests</name>
  <files>Plugins/SpotOn/API/Client.pm, Plugins/SpotOn/Plugin.pm, t/05_perl_syntax.t, t/13_status_page.t</files>
  <read_first>
    Plugins/SpotOn/API/Client.pm (lines 37-72 for module vars + reset; line 546 for inflightCount++; lines 684-701 for 429 handler)
    Plugins/SpotOn/Plugin.pm (lines 112-143 for WEBUI block)
    t/05_perl_syntax.t (full file — understand stub infrastructure and @pm_files list)
    t/08_api_client.t (lines 1-50 for test stub patterns)
    .planning/phases/32-status-page/32-PATTERNS.md (Client.pm extension section)
    .planning/phases/32-status-page/32-RESEARCH.md (Pattern 3, Pitfall 2-3)
  </read_first>
  <action>
    **Client.pm changes** (3 locations):

    Location 1 — Module vars (after line 40, alongside existing $inflightCount = 0): Add two new module-level vars: my $apiRequestCount = 0; my $api429Count = 0;

    Location 2 — reset() method (line 68-72): Add $apiRequestCount = 0; $api429Count = 0; after the existing $inflightCount = 0; reset. Update the INFOLOG message to mention all three counters.

    Location 3a — _request() increment (after line 546 $inflightCount++): Add $apiRequestCount++;

    Location 3b — 429 handler (after line 698 $cache->set): Add $api429Count++; Then add INC-guarded recordError call: if ($INC{'Plugins/SpotOn/Status.pm'}) { Plugins::SpotOn::Status->recordError('warn', 'API', "429 on $cleanPath"); }

    Location 4 — New public method statusSnapshot() (insert after reset() sub, around line 73): Returns hashref with keys inflightCount, apiRequestCount, api429Count, rateLimitedOwn (ternary from $cache->get('spoton_rate_limit_own')), rateLimitedBundled (ternary from $cache->get('spoton_rate_limit_bundled')).

    **Plugin.pm change** (1 location):

    In the main::WEBUI block (after line 117 Settings::Player->new()): Add require Plugins::SpotOn::Status; Plugins::SpotOn::Status->new(); Place before the autoStartDiscovery timer (line 121).

    **t/05_perl_syntax.t change** (2 locations):

    Add Status.pm to @pm_files array (after the Connect::Daemon entry): "$project_dir/Plugins/SpotOn/Status.pm"

    Update Slim::Web::Pages stub to include addRawFunction: sub addRawFunction { } alongside existing sub addPageFunction { }

    **t/13_status_page.t** (new file):

    Create new test file that tests Status.pm in isolation using the same stub infrastructure as t/08_api_client.t. Tests to include:

    1. Status.pm compiles: require Plugins::SpotOn::Status passes.
    2. recordError pushes entries: call recordError 3 times, verify @_errorHistory length is 3 (access via _errorHistory() method which returns arrayref).
    3. Ring-buffer trims at MAX_ERROR_HISTORY: push 35 entries via recordError, verify returned array length is 30, verify the first entry is entry #6 (oldest 5 trimmed).
    4. recordError stores correct fields: verify each entry has keys ts, level, module, message.
    5. _errorHistory returns reverse order: push entries A then B then C, verify returned[0] is C (newest first).
    6. _systemInfo returns cached hash: call twice, verify same reference returned (caching).
    7. Client->statusSnapshot returns expected keys: inflightCount, apiRequestCount, api429Count, rateLimitedOwn, rateLimitedBundled.
    8. Client->reset resets new counters: verify apiRequestCount and api429Count are 0 after reset.

    Use the same stub infrastructure pattern from t/08_api_client.t: write_stub for Slim modules, define main:: constants, set up include paths. Need stubs for: Slim::Utils::Log, Slim::Utils::Prefs, Slim::Utils::Cache, Slim::Utils::Timers, Slim::Web::Pages (with addRawFunction), Slim::Web::HTTP (with addHTTPResponse), Slim::Networking::SimpleAsyncHTTP, JSON::XS::VersionOneAndTwo, Log::Log4perl, Slim::Plugin::OPMLBased.
  </action>
  <verify>
    <automated>cd /home/sti/spoton && prove t/05_perl_syntax.t t/13_status_page.t -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Client.pm has my $apiRequestCount = 0; and my $api429Count = 0; at module level alongside $inflightCount
    - Client.pm reset() sets $apiRequestCount = 0 and $api429Count = 0
    - Client.pm has $apiRequestCount++ after $inflightCount++ at line ~547
    - Client.pm has $api429Count++ in the 429 handler block after $cache->set
    - Client.pm 429 handler has INC-guarded call: if ($INC{'Plugins/SpotOn/Status.pm'}) { Plugins::SpotOn::Status->recordError(...) }
    - Client.pm has sub statusSnapshot returning hashref with 5 keys: inflightCount, apiRequestCount, api429Count, rateLimitedOwn, rateLimitedBundled
    - Plugin.pm WEBUI block has require Plugins::SpotOn::Status; Plugins::SpotOn::Status->new();
    - t/05_perl_syntax.t @pm_files includes Plugins/SpotOn/Status.pm
    - t/05_perl_syntax.t Slim::Web::Pages stub includes sub addRawFunction { }
    - t/13_status_page.t exists with at least 8 test assertions
    - prove t/05_perl_syntax.t passes (Status.pm syntax check green)
    - prove t/13_status_page.t passes (all ring-buffer, statusSnapshot, reset tests green)
  </acceptance_criteria>
  <done>Client.pm has API telemetry counters with statusSnapshot() and INC-guarded recordError calls. Plugin.pm registers Status.pm. All tests pass including new t/13_status_page.t.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to LMS HTTP | Status data request crosses from browser to LMS web server |
| Status.pm to internal modules | Status.pm reads from DaemonManager, Client, TokenManager — all internal, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Information Disclosure | _statusDataHandler | accept | Exposes PIDs, uptime, version info — same sensitivity as existing Settings page. LMS-Auth gatekeeping applies identically (D-10). No PII exposed. |
| T-32-02 | Tampering | _statusDataHandler | mitigate | No _csrfCheck needed (D-12, read-only). No mutations possible via this endpoint. |
| T-32-03 | Denial of Service | _statusDataHandler | accept | Polling at 5s interval is negligible load. No expensive operations — all data from in-memory vars. |
| T-32-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all LMS-built-in modules. |
</threat_model>

<verification>
prove t/05_perl_syntax.t t/13_status_page.t — all tests pass
prove t/ — full suite green, no regressions
curl http://localhost:9000/plugins/SpotOn/status/data returns valid JSON with keys: daemons, api, errors, tokens, system
</verification>

<success_criteria>
- Status.pm module exists and compiles cleanly
- JSON endpoint /plugins/SpotOn/status/data returns all 5 data categories
- API counters increment correctly during normal operation
- Ring-buffer caps at 30 entries
- All existing tests still pass (no regressions)
- t/13_status_page.t covers recordError, ring-buffer trimming, statusSnapshot, reset
</success_criteria>

<output>
Create `.planning/phases/32-status-page/32-01-SUMMARY.md` when done
</output>
