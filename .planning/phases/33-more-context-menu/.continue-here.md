---
context: phase
phase: 33-more-context-menu
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-06-26T08:41:35Z
---

<current_state>
Phase 33 (More Context Menu) — Plan 33-01 execution in progress. Task 1 partially done: Plugin.pm `_trackItem` cache extension (artistId, artistIds, albumId) is in the working tree but NOT committed. ProtocolHandler.pm and Connect.pm cache sites are untouched. Task 2 (trackInfoMenu/trackInfoURL rewrite) not started.

Uncommitted changes:
- `Plugins/SpotOn/Plugin.pm` — _trackItem cache fields added + `use JSON::XS qw(encode_json)` import
- `.planning/STATE.md` — phase begin marker
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Extend metadata cache — IN PROGRESS
  - Plugin.pm `_trackItem` (line 737): artistId, artistIds (JSON-encoded), albumId added to cache hash — DONE but uncommitted
  - ProtocolHandler.pm `_cacheExplodedTrack` — NOT done
  - ProtocolHandler.pm `_cacheExplodedEpisode` — NOT done
  - ProtocolHandler.pm `_asyncRefetch` — NOT done
  - Connect.pm `_fetchTrackMetadata` — NOT done
- Task 2: Rewrite trackInfoMenu and trackInfoURL — NOT started
</completed_work>

<remaining_work>

Task 1 remaining (cache extension):
- ProtocolHandler.pm `_cacheExplodedTrack` (line 786): Add artistId, albumId fields. Add $albumId parameter. Update both callers (lines 466, 494/542) to pass album ID.
- ProtocolHandler.pm `_cacheExplodedEpisode` (line 798): Add showId, showName fields.
- ProtocolHandler.pm `_asyncRefetch` (line 894 area): Add artistId, albumId for tracks; showId, showName for episodes to %new_meta hash.
- Connect.pm `_fetchTrackMetadata` (line 957): Add artistId, albumId to cache hash.

Task 2 (full rewrite):
- Plugin.pm line 167: Add `after => 'top'` to registerInfoProvider
- Plugin.pm `trackInfoMenu` (line 464): Rewrite to return arrayref — Artist View, Album View, Like/Unlike for tracks; View Show, Follow/Unfollow for episodes. Read cache for IDs.
- ProtocolHandler.pm `trackInfoURL` (line 758): Mirror same items with full package paths.
</remaining_work>

<decisions_made>

- Sonnet executor agent failed silently (reported success, made no commits). Recommend inline execution for remaining work.
- artistIds stored as JSON-encoded string (not Perl arrayref) — matches existing pattern and avoids Storable serialization issues in LMS cache.
</decisions_made>

<blockers>
None
</blockers>

## Required Reading (in order)
1. `.planning/phases/33-more-context-menu/33-01-PLAN.md` — Full plan with exact line numbers, interface details, and verification steps
2. `Plugins/SpotOn/Plugin.pm` — Primary file, partial changes already in working tree
3. `Plugins/SpotOn/ProtocolHandler.pm` — Cache functions and trackInfoURL to modify
4. `Plugins/SpotOn/Connect.pm` — _fetchTrackMetadata cache site

## Infrastructure State
- No running services or background processes
- Working tree has uncommitted changes in Plugin.pm and STATE.md

<context>
Phase 33 resolves GitHub Issue #29: More context menu for SpotOn tracks/episodes. The plan is very well-specified with exact function signatures, line numbers, and cache field details. The approach: extend all metadata cache write-sites with artist/album/show IDs (Task 1), then rewrite trackInfoMenu to return an arrayref of context items using those cached IDs (Task 2). Execute inline — the Sonnet subagent approach did not work reliably.
</context>

<next_action>
Start with: Commit the existing Plugin.pm _trackItem changes (Task 1 partial). Then continue Task 1: extend cache in ProtocolHandler.pm (_cacheExplodedTrack with new $albumId param, _cacheExplodedEpisode, _asyncRefetch) and Connect.pm (_fetchTrackMetadata). Run `perl -c` on each file after changes. Commit Task 1. Then proceed to Task 2.
</next_action>
