---
phase: 46-code-review-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Plugins/SpotOn/API/Client.pm
  - Plugins/SpotOn/API/TokenManager.pm
  - Plugins/SpotOn/Plugin.pm
  - Plugins/SpotOn/Connect.pm
  - Plugins/SpotOn/ProtocolHandler.pm
  - Plugins/SpotOn/DontStopTheMusic.pm
  - Plugins/SpotOn/Unified/Daemon.pm
  - Plugins/SpotOn/Unified/DaemonManager.pm
  - Plugins/SpotOn/Settings.pm
  - librespot-spoton/src/main.rs
  - librespot-spoton/src/unified.rs
  - librespot-spoton/src/connect.rs
  - t/07_token_manager.t
  - t/08_api_client.t
autonomous: true
requirements: [H1, H2, H3, H4, H5, H6, H7, H8, H9, H10, H11, H12, H13, M1, M2, M3, M5, M6, M7, M8, M9, M10, M11, M12, M15, M16, M18, M19, W1, W2]

must_haves:
  truths:
    - "API requests never deadlock: an exception after the inflight counter increments always decrements it again"
    - "A hanging librespot --get-token subprocess cannot freeze the LMS event loop (bounded timeout, async polling)"
    - "Concurrent token cache misses produce exactly one --get-token subprocess; all waiters get the result"
    - "Browsing without a player client (e.g. web UI without active player) does not crash _getAccountId"
    - "Connect change/seek/volume events from the binary are ignored for players not in an active Connect session (metadata bleed fixed)"
    - "Crash-loop discovery cooldown survives the 60s watchdog tick; health-restart rate limit survives stopHelper"
    - "LMS credentials are not visible in /proc/<pid>/cmdline of the daemon process"
    - "A superseded Browse request does not increment the failure counter, trigger session teardown, or corrupt ActiveMode"
    - "glob() call sites handle cache paths containing spaces (Windows)"
  artifacts:
    - "Plugins/SpotOn/API/Client.pm — uri_escape_utf8, eval-guarded dispatch, cache-before-token, bundled rate-limit gate on fallback"
    - "Plugins/SpotOn/API/TokenManager.pm — async Proc::Background token fetch with watchdog + in-flight coalescing, credentials dir removal, discoveryPid accessor"
    - "librespot-spoton/src/unified.rs — 409 supersession status, track-ID-guarded mode reset, effective browse_cancel, relay generation counter, bounded shutdown, reconnect backoff"
  key_links:
    - "Client.pm $userCb is the single decrement point for $inflightCount — every new error path must exit through it"
    - "Daemon.pm sets SPOTON_LMS_AUTH env var; main.rs must read it (env takes precedence over --lms-auth flag) or Connect notify auth breaks"
    - "Connect.pm seek handler pendingConnect deferred-seek path (CON-17) must survive the new isSpotifyConnect guard"
---

<objective>
Fix all High (H1-H13) and Medium (M1-M3, M5-M12, M15, M16, M18, M19) findings from the Fable full-project code review (2026-07-02), plus Windows compatibility fixes (W1, W2).

This is a bugfix-only plan: every change is minimal and targeted, no new features, no refactors beyond what a finding requires. Fixes are grouped by module into 7 tasks.

Purpose: eliminate a class of deadlocks, event-loop freezes, crashes, state-machine corruption, and credential exposure found by systematic review — before v2.3 library work builds on this code.
Output: patched Perl plugin modules, patched Rust daemon sources, green `prove t/` suite, clean `cargo check`.

**Finding → Task coverage (source audit):**

| Task | Findings |
|------|----------|
| 1 | H1, M1, M3 (Client.pm) |
| 2 | H2, H3, M2 (TokenManager.pm) |
| 3 | H4, M5, M6, M7, W2 (Plugin.pm + cache-version literal sites) |
| 4 | H6, H7, M8, M11 (Connect.pm + getMetadataFor) |
| 5 | H5, M9 (ProtocolHandler.pm) |
| 6 | H8, H9, H10, M10, M12, W1 (DaemonManager.pm, Daemon.pm, Settings.pm, main.rs) |
| 7 | H11, H12, H13, M15, M16, M18, M19 (unified.rs, connect.rs) |

All 30 findings from the review handoff are covered. M4, M13, M14, M17 were not in scope of the handoff (not listed); no other exclusions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client.pm — inflight-counter safety, UTF-8 escaping, cache-before-token, bundled rate-limit gate (H1, M1, M3)</name>
  <files>Plugins/SpotOn/API/Client.pm, t/08_api_client.t</files>
  <action>
Read Plugins/SpotOn/API/Client.pm fully before editing (focus: `_request` at ~555-593, `_doFlavouredRequest` at ~619-810).

**H1a — UTF-8-safe escaping:** Line 8 imports `uri_escape` only. Change to `use URI::Escape qw(uri_escape uri_escape_utf8);` and replace the `uri_escape($params->{$key})` call in the query-building loop (~line 669) with `uri_escape_utf8(...)`. `uri_escape` dies on characters > 255 (e.g. non-Latin search terms), and that die happens after `$inflightCount++`, permanently leaking the counter until MAX_CONCURRENT_REQUESTS is reached and all API traffic deadlocks.

**H1b — exception-safe dispatch:** In `_request` Step 5 (~line 592), wrap the `$class->_doFlavouredRequest(...)` call in `eval { ... }`; on `$@`: log the error and call `$userCb->(undef, { error => 'internal_error' })` — `$userCb` is the single decrement point for `$inflightCount` and has the double-call guard, so route ALL new error paths through it, never decrement directly.

**H1c — exception-safe callback body:** Inside `_doFlavouredRequest`, the URL/query/cache-key building block (~lines 665-687) runs inside the async `getToken` callback where a die would escape the eval added in H1b. Wrap that block in its own `eval`; on `$@`: log and `$userCb->(undef, { error => 'internal_error' }); return;`.

**M1 — cache check before token fetch:** Currently the response-cache lookup (~lines 676-687) happens inside the `getToken` callback, i.e. AFTER a token fetch that may spawn a blocking subprocess. The cache key is flavor-agnostic (comment at line 664) and built only from accountId/path/query/locale — none of which depend on the token. Restructure `_doFlavouredRequest`: build `$url`, `$queryStr`, and `$cacheKey` BEFORE calling `TokenManager->getToken`, do the `$cache->get($cacheKey)` lookup there, and on hit call `$userCb->($cached)` and return without ever fetching a token. Keep `$params->{_cacheKey}` assignment so the success path still writes the cache. Preserve the `_noCache` bypass.

**M3 — bundled fallback respects bundled rate limit:** There are two bundled-retry sites: the no-token fallback (~lines 635-649) and the 403/410/deprecated-404 fallback (~lines 787-797). At both, before recursing with flavor 'bundled', check `$cache->get('spoton_rate_limit_bundled')`; if set, do NOT retry — log a warning and call `$userCb->(undef, { error => 'rate_limited', code => 429 })`. Without this, an own-token failure storm bypasses the per-flavor rate-limit gate (checked only in `_request` Step 2 for the START flavor) and hammers the bundled client ID during a 429 window.

**Test:** Extend t/08_api_client.t with a regression test: build a query param containing a wide character (e.g. "Motörhead" with a codepoint > 255 such as "\x{263A}") and assert the request path does not die and the inflight counter returns to 0 (test via the existing stub harness pattern in that file — read it first and follow its stub conventions). If the harness cannot reach the counter directly, assert at minimum that `_request` with a wide-char param invokes the callback with a result or error rather than dying.
  </action>
  <verify>
    <automated>prove t/08_api_client.t t/05_perl_syntax.t</automated>
  </verify>
  <done>uri_escape_utf8 used for query params; every path after $inflightCount++ exits through $userCb (verified by reading all branches); cache hit short-circuits before getToken; both bundled fallback sites check spoton_rate_limit_bundled; prove t/ green.</done>
</task>

<task type="auto">
  <name>Task 2: TokenManager.pm — async token fetch with watchdog, in-flight coalescing, credentials cleanup (H2, H3, M2)</name>
  <files>Plugins/SpotOn/API/TokenManager.pm, t/07_token_manager.t</files>
  <action>
Read Plugins/SpotOn/API/TokenManager.pm fully before editing (focus: `getToken` ~46-66, `removeAccount` ~70-99, `_fetchKeymasterToken` ~362-475, `startDiscovery` ~168-230 for the existing Proc::Background pattern).

**H3 — in-flight coalescing (do this first, it frames H2):** Add a package-level `my %tokenFetchInflight;` keyed by `"${accountId}|${flavor}"`, each value an arrayref of pending callbacks. In `_fetchKeymasterToken`: if the key already has an entry, push `$cb` onto the queue and return immediately (log at info level: "coalescing token fetch"). Otherwise create the entry with `[$cb]` and start the fetch. When the fetch completes (success, failure, or timeout), drain ALL queued callbacks with the same result and delete the key — delete the key BEFORE invoking callbacks so a callback that re-triggers a fetch doesn't self-coalesce into a dead entry.

**H2 — replace blocking backtick with Proc::Background + async watchdog:** The current code (~lines 425-475) defers via a 0.1s timer, then runs `my $output = `$cmd`;` — a blocking backtick with NO timeout. If the librespot binary hangs (network stall, AP outage), the entire LMS event loop freezes indefinitely. Replace with:
1. Build the argument LIST (not a shell string): helper path, `--get-token`, `--cache`, dir, and `--client-id`, id as separate list elements. This removes the manual `$escFn`/`${q}` shell-quoting entirely and is Windows-safe. Delete the now-dead quoting code.
2. Create a tempfile (File::Temp, same pattern as Daemon.pm port capture: `DIR => catdir($serverPrefs->get('cachedir'), 'spoton')`) and spawn via `Proc::Background->new({ die_upon_destroy => 1, stdout => $tmpfile, stderr => $tmpfile }, @args)`. Merging stderr into the tempfile preserves the existing Keymaster error diagnostics (status_code / payload parsing of librespot's stderr output, ~lines 436-449) — keep that parsing logic, feeding it the tempfile content.
3. Poll with `Slim::Utils::Timers::setTimer` every 0.2s: when the process exits, read the tempfile, unlink it, and run the existing output-parsing logic (token extraction on success, diagnostic parsing on failure), then resolve the coalescing queue.
4. Watchdog: after 15 seconds of polling, call `$proc->die`, log `"TokenManager: --get-token timed out after 15s"`, unlink the tempfile, and resolve the queue with undef. This bounds the worst case at 15s of ASYNC waiting instead of an infinite SYNCHRONOUS freeze.
Note: `Proc::Background` is already used in this file for discovery — reuse the import.

**M2 — removeAccount deletes credentials from disk:** `removeAccount` (~70-99) clears prefs and cache keys but leaves the account's credentials.json on disk, so a "removed" account's Spotify credentials persist. Determine the account cache directory the same way `_fetchKeymasterToken` builds its `--cache` dir for this accountId, then `require File::Path;` and `File::Path::remove_tree($dir)` guarded by `-d $dir` and wrapped in eval (log a warning on failure, don't die). Only remove the per-ACCOUNT directory, never the shared spoton cache root — assert the path contains the accountId segment before removing.

**Test:** Update t/07_token_manager.t: the existing stubs likely mock the backtick flow — read the test first and adapt its stubs to the Proc::Background + tempfile + timer flow, preserving assertion intent. Add a coalescing regression test: call `getToken` twice for the same account/flavor while the (stubbed) fetch is pending, assert only one fetch is started and both callbacks fire. Stub `Slim::Utils::Timers` to execute callbacks synchronously if the harness doesn't already.
  </action>
  <verify>
    <automated>prove t/07_token_manager.t t/05_perl_syntax.t</automated>
  </verify>
  <done>No backtick execution remains in TokenManager.pm; token fetch is Proc::Background + timer polling with a 15s watchdog; concurrent getToken calls for the same key coalesce to one subprocess; removeAccount removes the account credentials directory; prove t/ green.</done>
</task>

<task type="auto">
  <name>Task 3: Plugin.pm — undef-client crash, cache-version constant, pgrep regex, conditional-my, discovery PID exclusion (H4, M5, M6, M7, W2)</name>
  <files>Plugins/SpotOn/Plugin.pm, Plugins/SpotOn/ProtocolHandler.pm, Plugins/SpotOn/Connect.pm, Plugins/SpotOn/DontStopTheMusic.pm, Plugins/SpotOn/API/TokenManager.pm, Plugins/SpotOn/API/Client.pm</files>
  <action>
**H4 — _getAccountId undef-client crash:** Plugin.pm `_getAccountId` (~520-525) calls `$prefs->client($client)` which crashes when `$client` is undef (e.g. web UI browse with no player selected). Add as the first line of the sub: `return $prefs->get('activeAccount') || '' unless $client;`

**M5 — centralize cache version constant:** Plugin.pm:23 already defines `use constant SPOTON_CACHE_VERSION => 4;` but five files duplicate the literal: ProtocolHandler.pm:18, DontStopTheMusic.pm:16, Connect.pm:44, API/TokenManager.pm:33, API/Client.pm:35 — each `Slim::Utils::Cache->new('spoton', 4)`. Replace the literal in all five with `Plugins::SpotOn::Plugin::SPOTON_CACHE_VERSION()` (explicit parens — fully-qualified constant call). Compile-order check: these `my $cache = ...` statements run at module load time, so Plugin.pm's constant must be defined before any of these modules compile. Verify in Plugin.pm that the `use constant SPOTON_CACHE_VERSION` line (line 23) precedes every `use Plugins::SpotOn::*` statement; if any submodule is `use`d earlier, move the constant definition above it. TokenManager and Client are loaded via runtime `require` — safe. If a load-order case exists where Plugin.pm is not yet compiled (e.g. a scanner process loading a module standalone), guard with `defined &Plugins::SpotOn::Plugin::SPOTON_CACHE_VERSION ? ... : 4` is NOT wanted — instead add `require Plugins::SpotOn::Plugin;` is also circular; the correct minimal approach: keep the fully-qualified call and confirm via `prove t/` that all module-load tests pass. If tests reveal a circular-load failure in a specific file, fall back for THAT file only to `use constant SPOTON_CACHE_VERSION => 4;` locally with a comment pointing at Plugin.pm — and note it in the summary.

**M6 — pgrep pattern regex-escape:** Plugin.pm `_killOrphanedProcesses` (~374-375): `pgrep -f '$safeHelper'` treats the helper path as an extended regex — dots and other metacharacters match too broadly and can kill unrelated processes. Apply `quotemeta` to the helper path FIRST (regex escaping), THEN the existing single-quote shell escaping on the result, and pass that to pgrep.

**M7 — conditional my trap:** Plugin.pm:338: `my $pid = $song->transcodeProcess() if $song->can('transcodeProcess');` — `my ... if` is undefined behavior in Perl (the variable may retain its previous value across loop iterations, polluting %activePids with stale PIDs). Split: `my $pid; $pid = $song->transcodeProcess() if $song->can('transcodeProcess');`. Also verify intent of the CR-01 `%activePids` block: check whether `transcodeProcess` exists as a method on LMS Song objects (grep slimserver knowledge / the codebase); if it provably never exists under the Unified daemon architecture, keep the guard as-is (it's harmless once the my-trap is fixed) but add a one-line comment noting it is a defensive no-op under Unified — do NOT remove logic in this bugfix phase.

**W2 — exclude discovery process from orphan kill:** TokenManager.pm holds the ZeroConf discovery process in package-level `my $discoveryProc` (line 36). `_killOrphanedProcesses` in Plugin.pm can kill it because it matches the helper binary name. Add a class method to TokenManager.pm: `sub discoveryPid { return ($discoveryProc && $discoveryProc->alive) ? $discoveryProc->pid : undef; }`. In Plugin.pm `_killOrphanedProcesses`, before the kill loops, collect it (guarded: `if ($INC{'Plugins/SpotOn/API/TokenManager.pm'})`) and add it to the exclusion checks in BOTH the Windows branch (~line 358) and the Unix branch (~line 377), alongside the existing `%activePids` / `%unifiedPids` checks.
  </action>
  <verify>
    <automated>prove t/</automated>
  </verify>
  <done>_getAccountId returns global activeAccount for undef client without crashing; zero occurrences of `Cache->new('spoton', 4)` remain (all use the Plugin.pm constant or a documented local fallback); pgrep pattern is quotemeta'd; conditional-my split; discovery PID excluded in both kill branches; prove t/ green.</done>
</task>

<task type="auto">
  <name>Task 4: Connect.pm — active-Connect guards, newTrack leak, metadata bleed, ready-after-pause (H6, H7, M8, M11)</name>
  <files>Plugins/SpotOn/Connect.pm, Plugins/SpotOn/ProtocolHandler.pm</files>
  <action>
Read Plugins/SpotOn/Connect.pm in full before editing — the event dispatcher is one large sub and the handlers interact (focus: `isSpotifyConnect` ~72-80, volume ~510, seek ~541, resume ~571+, stop ~707+, change ~780, ready ~877, `_fetchTrackMetadata` ~904+, newTrack sites at 634, 710, 740, 929, 1005-1006).

**H6 — active-Connect guard on change/seek/volume:** These handlers act on `$client` even when the player is NOT in an active Connect session, so a Spirc event for a player that switched back to Browse playback overwrites Browse metadata/position (confirmed known error: connect-metadata-bleed). Add an early return at the top of each of the three handlers (`volume` ~510, `seek` ~541, `change` ~780) mirroring the guard the stop handler already has, BUT preserve the CON-17 deferred-seek path: the seek handler must still accept events while `$client->pluginData('pendingConnect')` is set (seek can arrive before playlist play). Guard shape: `return unless __PACKAGE__->isSpotifyConnect($client) || $client->pluginData('pendingConnect');` for seek and change; for volume, `pendingConnect` is not needed — plain `return unless __PACKAGE__->isSpotifyConnect($client);`. Check the `change` handler's own re-play logic (~line 803 already calls isSpotifyConnect) and remove redundancy if the new top guard subsumes it. Log at debug level when dropping an event so field diagnostics can see suppressed events.

**H7 — newTrack flag leak:** `newTrack => 1` is set at ~634 and ~740 and cleared at ~929 and ~1005-1006 (metadata-fetch success paths). If the metadata API call fails, or a callback returns early, the flag stays set forever and ALL subsequent stop events are swallowed (~710), so LMS never learns Spotify stopped. Fix three-fold: (1) in `_fetchTrackMetadata`, ensure EVERY exit path of the API callback — success, error, and early-return branches — clears `newTrack` (audit each branch); (2) wherever `newTrack => 1` is set, also arm a fallback timer: `Slim::Utils::Timers::killTimers($client, \&_clearNewTrack); Slim::Utils::Timers::setTimer($client, Time::HiRes::time() + 10, \&_clearNewTrack);` with a new small sub `_clearNewTrack { my $client = shift; $client->pluginData(newTrack => 0); }`; (3) where the flag is cleared normally, kill the fallback timer. 10s is generous — the flag's purpose is only to suppress the transitional stop-before-play burst (~1-2s).

**M8 — getMetadataFor metadata bleed:** ProtocolHandler.pm `getMetadataFor` (~696-702) returns `$connectSong->pluginData('info')` for ANY `spoton://connect-` URL whenever a playingSong exists — including when the lookup URL is a DIFFERENT connect- URL (history entries, stale queue items). Add the URL identity check: only return `$info` when `$connectSong->track->url eq $url` (guard `$connectSong->track` for definedness). Non-matching URLs fall through to the existing cache-based lookup below (~707) which handles history correctly.

**M11 — ready handler forces playback after pause:** The `ready` handler (~877-895) re-issues `playlist play` unconditionally when Spirc reconnects, which force-starts playback even if the user had paused. Guard the play re-issue: skip (log at info: "Spirc ready but player is paused — not forcing playback") when `$client->isPaused()`. Keep the `$_activeConnectPlayer` check as-is.
  </action>
  <verify>
    <automated>prove t/</automated>
  </verify>
  <done>volume/seek/change handlers early-return for non-Connect players (with pendingConnect exception for seek/change); newTrack has a 10s fallback timer and is cleared on all metadata-callback exit paths; getMetadataFor only returns live Connect info when song URL matches the requested URL; ready handler does not force playback while paused; prove t/ green.</done>
</task>

<task type="auto">
  <name>Task 5: ProtocolHandler.pm — album pagination offset, prefetch skip-timer guard (H5, M9)</name>
  <files>Plugins/SpotOn/ProtocolHandler.pm</files>
  <action>
**H5 — album explodePlaylist pagination offset:** In the album branch of `explodePlaylist` (~450-514): tracks without an id are filtered out of `@allItems` (`next unless $track && $track->{id}`), but `scalar(@allItems)` is used both as the next API offset (line 511: `$fetchPage->(scalar(@allItems))`) and in the completion checks (lines 473, 500). One skipped track shifts every subsequent page back by one — re-fetching duplicates and, in the worst case, looping on the same page. Fix: introduce `my $fetched = 0;` alongside `@allItems`, increment it by the RAW page size (`scalar(@{ $data->{items} })`) for the first page and for every `$fetchPage` response, and use `$fetched` (raw count) for: the initial `$fetchPage->($fetched)` call, the recursive `$fetchPage->($fetched)` offset (replacing `$offset + scalar(@{$data->{items}})`), and both completion comparisons against `$total` (`$fetched >= $total` terminates). `@allItems` remains the filtered result list only. Read the first-page handling above line 470 to wire the initial raw count correctly. Check whether the playlist branch (~517+) has the same pattern; the finding scopes only the album branch — if the playlist branch already uses raw offsets, leave it; if it has the identical bug, apply the identical fix and note it in the summary.

**M9 — prefetch 404 skip timer fires after state change:** In `handleDirectError` (~203-212), when a prefetch 404 schedules `_skipUnavailable` for track-end, playback state can change before the timer fires (user skipped manually, new track, stop) and the late skip then kills the WRONG track. Read `_skipUnavailable` first. Fix: capture the failing `$url` and pass it to the timer callback (Slim::Utils::Timers passes extra args after the coderef); inside `_skipUnavailable`, guard: only perform the skip if the client's `streamingSong` still exists and its URL equals the captured URL (compare via the song's track URL) — otherwise log at debug and return. Keep the existing `killTimers` call so re-scheduling replaces stale timers.
  </action>
  <verify>
    <automated>prove t/ && perl -e 'exit 0'</automated>
  </verify>
  <done>Album pagination uses a raw-item $fetched counter for API offsets and completion checks (filtered @allItems no longer drives the offset); _skipUnavailable no-ops when the streaming song no longer matches the 404'd URL; prove t/ green.</done>
</task>

<task type="auto">
  <name>Task 6: Daemon lifecycle — crash-loop cooldown, health-restart rate limit, credentials via env, unsubscribe, async port wait, bsd_glob (H8, H9, H10, M10, M12, W1)</name>
  <files>Plugins/SpotOn/Unified/DaemonManager.pm, Plugins/SpotOn/Unified/Daemon.pm, Plugins/SpotOn/Settings.pm, librespot-spoton/src/main.rs</files>
  <action>
Read Plugins/SpotOn/Unified/DaemonManager.pm and Plugins/SpotOn/Unified/Daemon.pm in full before editing.

**H8 — crash-loop cooldown defeated by watchdog:** `initHelpers` (~156-176) resets `discoveryDisabledByCrashLoop` (per-client) and global `disableDiscovery` on EVERY invocation — and initHelpers is re-invoked by the 60s watchdog cadence, so the crash-loop protection is wiped 60s after it engages and the daemon crash-loops forever. Fix: add package-level `my $crashLoopFlagsWereReset = 0;`, wrap the entire reset block (lines ~161-176) in `unless ($crashLoopFlagsWereReset)` and set the flag to 1 after the first run. Intent preserved: flags reset once per LMS process start (fresh chance after restart), never again within the same process.

**H9 — health-restart rate limit stored on deleted object:** `_restartForHealth` (~488-515) stores `_lastHealthRestart` on the Daemon object, but `stopHelper` (line 522) DELETES that object, so the very restart being rate-limited erases the timestamp — the 5-minute window never engages and a dead session restarts every ~60s health tick. Fix: add package-level `my %lastHealthRestart;` in DaemonManager.pm keyed by `$helper->mac`; replace both the read (`$helper->_lastHealthRestart // 0`) and the write with the hash. Check Daemon.pm for the now-unused `_lastHealthRestart` accessor and remove it only if nothing else references it (grep first).

**H10 — credentials out of argv:** Daemon.pm (~176-181) pushes `--lms-auth <base64(user:pass)>` onto the argument list — visible to every local user via `/proc/<pid>/cmdline` and `ps`. Fix both ends: (1) Daemon.pm: remove the argv push; instead set `$ENV{SPOTON_LMS_AUTH} = encode_base64(sprintf("%s:%s", $serverPrefs->get('username'), $serverPrefs->get('password')), '')` in the same env-var block that sets `SPOTON_PORT_FILE` (~line 229), and `delete $ENV{SPOTON_LMS_AUTH}` immediately after the Proc::Background spawn alongside the existing deletes (~line 243) — keep the condition `$connectEnabled && $serverPrefs->get('authorize')`. Keep the T-29-07 comment discipline: the env var must never be logged. (2) main.rs: where args are parsed (~line 104/182), after the arg loop read `std::env::var("SPOTON_LMS_AUTH")` and let the env var take precedence over the `--lms-auth` flag value (flag retained for manual testing / backward compat). The `--check` capability manifest keeps reporting `"lms-auth": true` (main.rs ~273) since the capability is unchanged.

**M10 — unsubscribe at shutdown:** `init` (~102-153) registers two `Slim::Control::Request::subscribe` callbacks (client new/disconnect at ~106, sync at ~116) as anonymous subs that are never unsubscribed — after plugin shutdown/re-init they accumulate. Store both coderefs in package-level vars (e.g. `my ($clientSubRef, $syncSubRef);`), pass those refs to subscribe, and in `shutdown()` full mode (the `unless ($mode && $mode eq 'inactive-only')` block ~552-555) call `Slim::Control::Request::unsubscribe($_)` for each defined ref and undef them.

**M12 — async port wait:** Daemon.pm (~264-284) busy-waits up to 5s (50 × 100ms usleep) for the port announcement, blocking the LMS event loop per daemon start (multiple players = serialized multi-second freezes). Convert to async polling: extract everything from the poll loop through the port-parse/failure handling (~264 to the end of start-up handling, including the tempfile unlink, stale-file cleanup, `stream_port` parse, `_streamPort` set, and the failure path with `$self->_proc->die`) into a completion continuation driven by `Slim::Utils::Timers::setTimer` at 0.1s intervals with a 50-attempt cap. CRITICAL contract check: grep DaemonManager.pm (startHelper and callers) for how `start()`'s return value / post-conditions are consumed — if callers assume the port is known synchronously after `start()` returns (e.g. immediately reading `_streamPort`), adapt those call sites to tolerate deferred port availability (they already must handle the "no port" failure case; the crash-loop accounting in DaemonManager must still fire on async failure — wire the failure path to the same handling it triggers today). If the contract analysis shows the synchronous assumption is deeply load-bearing across many call sites, implement the poll with a REDUCED blocking profile instead (e.g. keep loop but drop usleep to 50ms and total cap to 2s) and document why in the summary — do not destabilize daemon startup for a Medium finding.

**W1 — bsd_glob for space-safe paths:** Perl's `glob()` splits its argument on whitespace, so a cachedir like `C:\Program Files\...` silently matches nothing (or wrong paths). Replace `glob(` with `bsd_glob(` at exactly: Daemon.pm:282 (stale port files), DaemonManager.pm:299 (orphaned logs), Settings.pm:456-457 (clearLogs, two calls). Add `use File::Glob qw(bsd_glob);` to Daemon.pm and DaemonManager.pm (Settings.pm already imports it at line 10). Grep each file afterwards for any remaining bare `glob(` calls and convert those too if they take interpolated paths.
  </action>
  <verify>
    <automated>prove t/ && cargo check --manifest-path librespot-spoton/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>Crash-loop flag reset runs once per LMS process; health-restart timestamps survive stopHelper (package hash keyed by MAC); no credentials in daemon argv — SPOTON_LMS_AUTH env var set/deleted around spawn and read by main.rs with env precedence; both subscriptions unsubscribed in full shutdown; port wait no longer blocks the event loop 5s (or documented reduced-blocking fallback); all three glob sites use bsd_glob; prove t/ green and cargo check clean.</done>
</task>

<task type="auto">
  <name>Task 7: Rust daemon — supersession status, mode guards, browse_cancel, relay takeover, timeouts, reconnect backoff (H11, H12, H13, M15, M16, M18, M19)</name>
  <files>librespot-spoton/src/unified.rs, librespot-spoton/src/connect.rs</files>
  <action>
Read the /track handler (~700-880), /stream relay (~540-670), server loop end (~995-1010), and browse-reconnect loop (~1700-1740) in unified.rs, plus `LMS::notify` in connect.rs (~278-333), before editing.

**H11 — superseded request must not count as browse failure:** The pre-spawn supersession check (~799-807) sends `StatusCode::NOT_FOUND` through `status_tx`, so the outer handler (~842-861) increments `consecutive_browse_fails` and after 2 supersessions (normal during rapid skipping!) tears down a healthy session via `browse_reconnect_signal`. Fix: the supersession path sends `StatusCode::CONFLICT` (409) instead; in the outer `early_status` match, add a CONFLICT arm that returns `empty_response(StatusCode::CONFLICT)` WITHOUT touching `consecutive_browse_fails` and WITHOUT the reconnect trigger (log at info: "superseded — 409, not counted as failure"). LMS side: the superseded response goes to a connection LMS has already abandoned, and Perl `handleDirectError` only special-cases 404, so 409 is safe.

**H12 — mode reset must check track ownership:** Two `ActiveMode::Idle` reset sites — post-serve (~826-832) and early-404 (~846-852) — reset the mode whenever it matches `ActiveMode::Browse(_)`, even when a NEWER request has already installed ITS track in the mode. A superseded/failed old request then corrupts the new request's mode to Idle, breaking subsequent Connect/Browse arbitration. Fix both sites: only reset when the stored track matches this task's track — `matches!(*mode, ActiveMode::Browse(ref t) if *t == track_id_for_task)` (use the appropriate track variable in each scope; the early-404 site uses `track_id`).

**H13 — browse_cancel is a no-op:** The cancel listener (~770-777) drops a CLONE of `pcm_tx`, but `serve_track_request` holds the original sender, so no EOF is produced and LMS keeps the dead stream. Fix: give the cancel listener the ability to abort the serve task itself. Restructure the spawn order: spawn the serve task first (it already consumes `pcm_tx` by move), capture its `AbortHandle`, then spawn the cancel listener which on `cancel.notified().await` calls that abort handle — aborting the serve task drops its `pcm_tx`, producing the EOF. Preserve the existing reverse path (`cancel_abort_for_task.abort()` at ~825 aborting the cancel listener when serve finishes) by keeping the cancel task's own abort handle; watch for the ordering constraint that `cancel_abort` is currently used before the serve task is defined — reorder declarations accordingly. Also ensure the mode-reset and status_tx logic still runs (task abort skips serve's tail code — the abort path should therefore also perform the guarded mode reset from H12, or accept that cancel is only fired when Spirc takes over and Spirc's takeover path already rewrites the mode; verify which holds by reading the browse_cancel notify site, and document the choice in a code comment).

**M15 — /stream relay takeover generation:** Two relays can poll the shared `pcm_rx` after a reconnect: the takeover (~546-550) logs but doesn't stop the old relay, whose `RelayGuard` later clears `relay_active` out from under the new relay. Fix: add a shared `Arc<AtomicU64>` relay generation counter next to `relay_active`; each new /stream connection does `fetch_add(1) + 1` and captures `my_relay_gen`; the relay loop breaks when `relay_gen.load(Ordering::Acquire) != my_relay_gen` (check alongside the `browse_preempting` check); the RelayGuard only clears `relay_active` if the generation still matches (pass gen + counter into the guard). Old relay exits within one loop iteration of a takeover.

**M16 — notify connect timeout:** connect.rs `LMS::notify` (~321): `TcpStream::connect` has no timeout — an unreachable LMS host stalls the notify caller for the OS TCP timeout (minutes). Wrap the entire connect+write+shutdown block in `tokio::time::timeout(Duration::from_secs(5), async { ... })`; on `Err(_)` (elapsed) log `log::debug!("[spoton] notify({cmd}): timed out after 5s")`. Import Duration if not in scope in that module.

**M18 — bounded graceful shutdown:** unified.rs ~1009: `graceful.shutdown().await` waits for ALL watched connections; an active /stream connection is long-lived, so shutdown hangs indefinitely (comment claims 5s but nothing enforces it). Wrap: `let _ = tokio::time::timeout(Duration::from_secs(5), graceful.shutdown()).await;` and log a warning on timeout.

**M19 — reconnect failure backoff:** The browse-reconnect arm (~1717-1730): on `ns.connect` failure it installs a brand-new UNCONNECTED fallback session and clears `browse_reconnect_pending` — the next browse failure immediately re-triggers reconnect, producing a tight fail loop against Spotify APs. Fix: retry `Session::new` + `connect` with exponential backoff inside the arm — attempts at 1s, 2s, 4s delays (3 retries after the initial failure), breaking on first success; only after all attempts fail install the fallback session, log an error noting the session is unhealthy, and keep the existing `browse_reconnect_pending.store(false, ...)` so a future failure can retry (by then the backoff has already spaced things out). Use `tokio::time::sleep` between attempts.

After the changes run `cargo check` and fix warnings introduced by the edits (unused imports etc.). Do NOT run `cargo build --release` — CI builds release binaries.
  </action>
  <verify>
    <automated>cargo check --manifest-path librespot-spoton/Cargo.toml 2>&1 | tail -5</automated>
  </verify>
  <done>Supersession returns 409 and never increments the failure counter or triggers reconnect; both Idle resets are guarded by track-ID match; browse_cancel aborts the serve task producing real EOF; relay loop exits on generation mismatch; notify and graceful shutdown are bounded at 5s; reconnect retries with 1s/2s/4s backoff before installing a fallback; cargo check clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local users → daemon argv | any local user can read /proc/pid/cmdline |
| Spotify Spirc events → LMS player state | remote-controlled events act on local players |
| Spotify API responses → Perl callbacks | untrusted response content drives control flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Information Disclosure | Daemon.pm --lms-auth argv | high | mitigate | H10: SPOTON_LMS_AUTH env var, deleted post-spawn, never logged (Task 6) |
| T-46-02 | Denial of Service | Client.pm inflight counter | high | mitigate | H1: uri_escape_utf8 + eval-guarded dispatch, all errors exit via $userCb (Task 1) |
| T-46-03 | Denial of Service | TokenManager blocking subprocess | high | mitigate | H2: Proc::Background + 15s watchdog, async polling (Task 2) |
| T-46-04 | Denial of Service | unified.rs session teardown via supersession | medium | mitigate | H11/M19: 409 status bypasses failure counter; reconnect backoff (Task 7) |
| T-46-05 | Tampering | pgrep -f regex over-match kills foreign processes | medium | mitigate | M6: quotemeta on helper path (Task 3) |
| T-46-06 | Information Disclosure | credentials.json persists after account removal | medium | mitigate | M2: remove_tree of account cache dir (Task 2) |
</threat_model>

<verification>
1. `prove t/` — full Perl suite green (syntax check across all modified .pm files plus TokenManager/Client regression tests).
2. `cargo check --manifest-path librespot-spoton/Cargo.toml` — Rust compiles clean.
3. `grep -rn "Cache->new('spoton', 4)" Plugins/` returns nothing (M5).
4. `grep -n "lms-auth" Plugins/SpotOn/Unified/Daemon.pm` shows no argv push of credentials (H10).
5. `grep -cn "bsd_glob" Plugins/SpotOn/Unified/Daemon.pm Plugins/SpotOn/Unified/DaemonManager.pm` ≥ 1 each (W1).

<human-check>
End-of-phase UAT on the dev LMS (per feedback_local_test_then_release):
- Browse an album with an unavailable/skipped track — full track listing, no duplicates (H5).
- Play via Browse while a phone controls Connect on another player — no metadata bleed (H6/M8, known-error-connect-metadata-bleed).
- Pause via Spotify app, wait for Spirc session expiry/reconnect — playback stays paused (M11).
- Rapid-skip 5+ tracks in Browse — no session teardown, playback recovers (H11, known-error-rapid-skip).
- `ps aux | grep spoton` — no base64 credentials visible (H10).
- Remove an account in settings — its credentials dir under the spoton cache is gone (M2).
</human-check>
</verification>

<success_criteria>
- All 30 findings (13 High, 15 Medium, 2 Windows) have a corresponding committed code change, traceable per task.
- `prove t/` passes; `cargo check` passes.
- No new features, no opportunistic refactors — each diff maps to a finding.
- One atomic commit per task (7 commits), message format `fix(46-01): <module> — <findings>`.
</success_criteria>

<output>
Create `.planning/phases/46-code-review-bugfixes/46-01-SUMMARY.md` when done. Document any fallback decisions taken (M5 circular-load fallback, M12 reduced-blocking fallback, H13 mode-reset choice) explicitly.
</output>
