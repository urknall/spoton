soc pcm * *
	# No capability flags — Connect path uses canDirectStream (direct HTTP stream
	# from the librespot PCM server). The identity transcoder (-) is a fallback
	# stub for synced-player proxy scenarios; the I (stdin) flag is not needed
	# because LMS opens the stream URL directly rather than piping via stdin.
	-
